# Paid Core v1 Gate 1 — Production Supabase readiness

## 2026-09-10 Post-PR828 execution-readiness repair

PR828 merged into Preview at32924b6. The clean existing worktree fast-forwarded to that exact merge. CI checks were not registered on the PR; merge is not Hosted execution evidence. This continuation performed no pause, resume, Production access, Recovery action or publication.

The first actual producer-byte comparison matched only4/7 files: Windows core.autocrlf=true had introduced CRLF in the watchdog runner, engine and transport. All three differed solely by line endings. A new root .gitattributes pins exactly the seven existing producer paths to text/eol=lf. After restoring their published LF bytes, all7 current files and a fresh checkout-index extraction with core.autocrlf=true matched the published blobs exactly. No source-verifier relaxation or behavioral change was used for that repair. The ignored post828-source-byte-check.json captures this state BEFORE the subsequent hint repair below; it must not be read as identity proof for later modified source.

The bounded Preview observer then used SQL extracted from the published transport and the newly accepted HTTP baseline. At07:16:34UTC, native PG17/read-only/TLS passed at the one resolved address, anon SELECT=false was independently confirmed, DNS address set stayed stable, and a deliberately incorrect hostname failed certificate-name verification. Auth returned200/GoTrue and a synthetic invalid key returned a separate gateway denial. However, the published REST predicate rejected the actual table401 response: its exact four fields, code42501, expected table-permission message and null details matched, but hint was a string instead of null. A separate single-request structural diagnostic confirmed only these field/type predicates; no raw hint, response body, key, address, account data or business row was emitted. The original post828-preview-baseline.json remains NOT_ACCEPTED.

The local correction accepts hint only as null or text, treating it as non-authoritative advisory metadata. The required HTTP status, exact field set, error code/table message, null details, independent SQL privilege result and all stopping conditions remain unchanged. A regression first rejected representative advisory text on the published behavior; the repair also rejects object/array/boolean/number/missing hints, wrong codes, wrong tables and unexpected details, with zero pause requests. This is compatibility with observed error metadata, not acceptance of a generic401 as health or stopping.

Local repair acceptance:38/38 watchdog tests, scoped ESLint, operator-contract and diff check PASS; the primary agent inspected the one-line predicate change, regression and seven exact LF attribute paths. The hint change is currently unpublished, so the prior merge is no longer an exact source identity for the modified transport. Do not invoke the native Production runner against that old source commit. Publish the narrow repair and refresh the read-only baseline before proposing another Hosted pause. No stop-success evidence, Storage preservation exception or capacity/deadline gate is waived. Existing branch/worktree are retained; no new task or cleanup is needed. Gate1 NO-GO.

## 2026-09-10 Oracle contract approved — local implementation

The user approved the preceding public-key/table-route and pinned-address design for local implementation. This supersedes the acceptance-pending wording below, not the independent Hosted gates. No remote stop, Recovery action or publication is authorized.

Implementation plan (primary agent, existing worktree): (1) extend native transport regression fixtures for a bound publishable key, exact backend response and independent anon privilege/TLS checks; observe failing tests; (2) implement bounded DNS capture, per-address native TLS validation and same-run refusal checks, with unknown results rejecting confirmation; (3) verify identity before/after endpoint probes, original cumulative deadline, cancellation and rejected-preflight zero-pause behavior; (4) inspect the final diff, run watchdog tests/scoped lint/operator contract, and record the result here and in task.md. The existing transport/test files own behavior; no new dependency or deployment is needed.

Implemented in the source watchdog transport: protected runner context now additionally requires publicProbe with exactly projectRef (matching the existing Production binding) and apiKey (an enabled modern sb_publishable key obtained for that project). Legacy anon and secret/service keys are not accepted by this input. The key is captured once, sent only to the bound Supabase HTTPS host, and never emitted in status/journal metadata. Existing management token/authorization remain separate. REST is fixed to the existing entitlement table with select=*&limit=0; the exact401/42501 table-denial body is required alongside Auth200/GoTrue and independent native anon SELECT=false. It is a dispatch baseline, not a successful business-data read. No caller-supplied route or IP is accepted.

Preflight captures a nonempty set of at most8 DNS address entries and validates every distinct address with native PG17/read-only/TLS, retaining PGHOST for verify-full while internally setting PGHOSTADDR. It repeats identity and DNS checks before readiness and must finish within the original T0+5-minute budget. Confirmation brackets endpoint checks with exact INACTIVE project and DNS readback; requires both HTTP503 responses and explicit address/port-matched refusal for every pinned address. ENOTFOUND permits checking existing pins but contributes no refusal evidence. Other DNS errors, timeout, resumed project, new address or incomplete checks return UNKNOWN. A new address permanently invalidates that run's coverage; re-preflight after pause is rejected. Pin evidence cannot outlive T0+20 minutes or be revived by clock rollback. The parent watchdog's existing3-second confirmation bound and one-shot pause remain unchanged.

Regression evidence includes an initial failing run before implementation, then explicit baseline/route/key/privilege/body/address checks, partial connectivity, transient DNS, DNS cancellation with late callback, project/address changes during checks, deadline expiry, rollback and captured-key immutability. Fixtures use synthetic addresses/credentials and are not Hosted evidence. Final local acceptance: watchdog37/37 tests, scoped ESLint, operator-contract and backup-recovery-contract (restore-order6/snapshot-dumps4) PASS; primary diff review and diff check completed. Local implementation is accepted; source publication and a separately approved Hosted rehearsal remain necessary. Do not run the native production runner from this uncommitted source: its selected-commit byte verification remains mandatory. No remote call or mutation was performed by this implementation turn. Gate1 NO-GO.

## 2026-09-10 Hosted oracle candidate — pinned-address diagnosis; acceptance change pending

The read-only continuation checked current official documentation and the exact Preview binding. The [OpenAPI breaking change](https://supabase.com/changelog/42949-breaking-change-removing-access-to-openapi-spec-via-the-anon-key) removes anon access to REST root schema discovery; it explicitly preserves ordinary table routes and does not affect self-hosted instances. Therefore the existing keyless REST-root200 baseline is a conservative blocker, not a supported Hosted health route. Do not obtain a service-role key or grant table access merely to make that check pass. The earlier API_KEY_REJECTED label was a broad message heuristic, not proof that the public key was invalid. The [pause API](https://supabase.com/docs/reference/api/v1-pause-a-project) documents an empty acknowledgement, not guaranteed DNS/HTTP failure signatures. [Service health](https://supabase.com/docs/reference/api/v1-get-services-health) is supplementary control-plane evidence, not independent proof that client routes are closed.

At06:32:26UTC, the exact Preview was ACTIVE_HEALTHY. A bounded public-key diagnostic observed Auth200/GoTrue; the existing entitlement table GET with select=*&limit=0 returned401/42501 with the expected table-permission error, while independent read-only has_table_privilege confirmed anon SELECT=false. A synthetic invalid key returned a separate gateway-key rejection. No business rows or user sessions were obtained. Root401 remained UNCLASSIFIED against the documented error text, including a separately narrowed check; do not attribute that particular body to the documented change as a proven fact or weaken the classifier to force acceptance. The original diagnostic receipt stays NOT_ACCEPTED because that root classification did not pass.

For Direct, one currently resolved address was tested using native PostgreSQL17.11 with PGHOSTADDR set to that address and PGHOST retained as the original bound hostname. All observed addresses (one) passed verify-full TLS/read-only SQL; the same address with a deliberately mismatched hostname failed certificate-name verification before SQL. This follows the documented [libpq host/hostaddr behavior](https://www.postgresql.org/docs/17/libpq-connect.html#LIBPQ-PARAMKEYWORDS). These are positive/negative connection checks, not post-pause refusal evidence. Raw addresses, keys, credentials and response bodies were not recorded in the summary. Ignored diagnostic scripts/receipt are under .tmp/gate1-evidence-20260910/stop-oracle-*. Production was not contacted or mutated; no pause/resume, provisioning, cost or publication occurred.

### Candidate comparison and proposed contract

| Candidate | Decision and limitation |
| --- | --- |
| Same public key on Auth + bounded table route; Direct checks use freshly validated addresses | Recommended for local implementation after acceptance of the contract change. Avoids REST-root schema discovery and can test Direct refusal without treating DNS failure as refusal. Actual Hosted post-pause results remain unknown. |
| Accept INACTIVE plus generic DNS error/401 | Rejected: does not distinguish resolver/gateway failure from successful containment. |
| Replace client probes with management health alone | Supplementary only: a control-plane response does not test independent external access or every service route. |

Proposed baseline: bind target/run/source, an enabled publishable key and the exact known-table route; keep keys in protected input only. Require Auth200 with validated GoTrue response, table401/code42501 with independently verified expected missing anon SELECT permission, native PG17 read-only TLS, and a bounded nonempty set of all resolved Direct addresses. The table result means BACKEND_REACHABLE_PERMISSION_DENIED, not permission success or full application health. Unexpected200, wrong table/error/schema, malformed/oversized body, redirects, unknown DNS/network result or incomplete address coverage reject readiness. This does not add tables, privileges, fixtures, login flows or secret keys.

Proposed stopped-state candidate: fresh exact-project INACTIVE before and after the probe group; same bound key and routes must both return503; every address captured and positively validated in this run must explicitly refuse TCP5432. Newly resolved addresses require coverage too; any new/unvalidated address, open socket, timeout or partial coverage prevents confirmation. If the bound hostname has no DNS answer, retain that fact as UNKNOWN_DNS and still require explicit refusal at every previously validated address; DNS absence itself never satisfies the refusal condition. Do not accept an HTTP permission error as stopped. Address evidence is scoped to the original uninterrupted run and cumulative deadline, cannot be loaded from a previous rehearsal, and does not prove all alternate database endpoints or all other services are closed. Pause once; uncertain confirmation remains failed, with the already-approved target-specific failure path and no same-target restore replay.

This changes the earlier REST-root200 baseline and hostname-only Direct probe, so the proposed production/recovery contract is NOT ADOPTED in this turn. Product watchdog source is unchanged from the prior guard patch. Before formal integration, local tests must cover key/target mismatch, each HTTP classification, stale or incomplete address sets, DNS failure with open/refused/timed-out pinned sockets, address changes, abort/deadline behavior and zero pause on rejected preflight. A later Hosted rehearsal requires its own concrete authorization and preservation/failure plan; no local result authorizes re-pausing Preview. Storage/Realtime/Functions, alternate routes, capacity and whole-procedure deadline remain separate open gates. Gate1 NO-GO.

## 2026-09-10 Stop-oracle review — reject unsupported baseline before pause

The production watchdog preflight checked project identity and native PostgreSQL but omitted the HTTP routes used for stopping confirmation. Keyless gateway401 is independent of backend readiness. Preflight now requires exact200 from BOTH bound REST/Auth routes using the same credential policy as confirmation, before readiness. Any non200 or transport failure leaves readiness false and blocks pause; failed repeated preflight clears earlier readiness. This deliberately blocks the observed Hosted configuration before mutation, rather than making it operationally supported.

Confirmation remains exact INACTIVE + Direct ECONNREFUSED + both HTTP503. DNS failure, timeout,401/403 and management acknowledgement remain UNKNOWN. The regression failed on the old code and covers independent REST/Auth204/302/401/403/404/500/503 responses with zero pause POSTs after rejection. Synthetic200-to503 transitions prove control flow only.

The next Hosted oracle needs a protected, target-bound nonprivileged application key and an established backend response on the same route/key before and after stopping. It must distinguish PostgREST permission denial from gateway key rejection, and establish independent Direct stopping evidence without promoting generic DNS failure or timeout. Auth health200 is an observed candidate; REST root200 is not established. Known-table limit0/42501 proves dispatch/denial only, not health or isolation. This patch adds no key-bearing probe or DNS fallback. Management INACTIVE alone does not establish Storage/Realtime/Functions or alternate-route closure. Do not initiate another pause rehearsal until these evidence gaps have an accepted resolution.

This continuation changes local source/tests/docs only; no Hosted operation, spending, Production mutation or publication. Source watchdog authorization remains production-specific and cannot be reused for Recovery. Gate1 NO-GO; the prior76/77 preservation result remains rejected.

## 2026-09-10 Hosted pause counterexample — Preview resumed, rehearsal not accepted

The approved exception reached one exact Preview pause request at05:45:43UTC and independently observed INACTIVE at05:47:24. At05:47:25 the bound Direct hostname returned DNS failure, while keyless REST/Auth probes both returned401, not the required Direct connection refusal and HTTP503. This is a real counterexample to the existing strict stopping oracle, not proof that generic DNS errors or401 establish isolation. No Recovery project or cost confirmation was created. Following the approved failure path, one original-Preview resume request was made at05:47:46; ACTIVE_HEALTHY returned at05:52:05. Final inventory showed only the original two projects, both ACTIVE_HEALTHY. Production was never paused or changed.

The native post-resume comparison succeeded over verify-full TLS/PG17 but correctly rejected complete preservation:76 of77 captured relation content/security records matched; only storage.migrations changed. Extraction of that table from the protected pre-pause archive showed65 original rows preserved exactly and3 added rows (65 objects-key-version-index,66 objects-current-version-index,67 objects-null-version-index). The corresponding3 indexes are ready/valid and match the official Storage v1.72.11 migrations [65](https://raw.githubusercontent.com/supabase/storage/v1.72.11/migrations/tenant/0065-objects-key-version-index.sql), [66](https://raw.githubusercontent.com/supabase/storage/v1.72.11/migrations/tenant/0066-objects-current-version-index.sql), [67](https://raw.githubusercontent.com/supabase/storage/v1.72.11/migrations/tenant/0067-objects-null-version-index.sql). This is consistent with platform-managed Storage migration during resume; no migration command was executed by this task. Business/Auth/Vault relation contents stayed equal, history remained56, Vault2, Storage objects0, active Cron0. Do not rewrite managed history, roll back provider indexes or relabel the original strict check as PASS. Complete database catalog equality and portable restore remain unproven.

Post-resume Direct TCP reopened and actual native read-only SQL succeeded. A current publishable key returned Auth health200. The REST root returned401 even with public keys, so it was not accepted as a health oracle. A known-table limit0 request reached PostgREST and returned401/code42501; an independent read-only privilege check confirmed anon lacks SELECT on that table, as intended. These HTTP probes returned no business rows and did not create or use a real-account session. This establishes the specified service responses, not a full application/real-account smoke. API-key-free401 must not be mistaken for stopped service; [API keys](https://supabase.com/docs/guides/getting-started/api-keys) are the application-level authentication layer.

Result: HOSTED_REHEARSAL_NOT_ACCEPTED / PREVIEW_RESUMED / STOP_ORACLE_UNSUPPORTED. Ignored receipts: hosted-preview-pause-characterization.json, preview-maintenance-preserve.json, preview-storage-resume-difference.json and public-key probe receipts under .tmp/gate1-evidence-20260910. Protected backup and before/after hashes remain retained. The next repair must design target-bound, known-valid endpoint controls and a supported independent stopped-state rule, with explicit treatment of DNS and managed migrations; it must not simply allow DNS/401, relax existing fingerprints or repeat the pause in this attempt. Recovery creation/configuration/stopping, additional service isolation and full timing acceptance remain untested. No commit/push/deploy, paid action, project deletion or support message occurred; Gate1 NO-GO.

## 2026-09-10 Hosted synthetic rehearsal exception approved — pre-pause checkpoint

The user approved the preceding concrete Preview-pause / fresh Recovery synthetic test / Recovery-pause / Preview-resume proposal at amount0. This authorizes that bounded exception to the earlier healthy-Preview no-pause rule, not a Production pause, retained-data restore, cutover, paid fallback or project deletion. Required prerequisites still apply. The proposal's NOT AUTHORIZED label below is now historical for this exact scope.

Pre-pause native verify-full/read-only Preview preservation passed:77 relation content/security records, history56, Vault2, Storage objects0, active Cron0 and no concurrent active client queries. A protected full custom archive of1122934bytes was captured and completely decoded without database execution; archive capture before/after relation digests matched. This is a supplemental maintenance backup, not a portable hosted restore proof; Preview Vault is not transferred into Recovery. Current Direct TCP is open and unauthenticated REST/Auth gateway probes both return401. The exact Preview ref/Direct host was independently matched to fresh connector inventory; the dedicated Supabase pause and restore tools provide the operation path, without modifying the Production-only watchdog. Preview has not yet been paused at this checkpoint.

## 2026-09-10 Recovery capacity and Hosted rehearsal decision packet

Fresh connected Supabase read-only observations in this continuation, completed by05:32UTC, returned exactly two projects: v-streamer-tools-prod and comment-translator-paid-preview, both ACTIVE_HEALTHY, same selected organization and ap-northeast-1. Organization plan=free; project cost estimate amount=0, recurrence=monthly (the tool did not return a currency). No recovery project appeared. This is a new live observation, not promotion of the September9 snapshot. Organization/project refs and database endpoints were kept out of reports. No cost confirmation, project creation, pause/resume, configuration mutation, restore or cutover was performed.

The current [billing rules](https://supabase.com/docs/guides/platform/billing-on-supabase) exclude paused projects from the two-active-Free-project allowance. The zero estimate does not reserve capacity or prove successful creation. With both observed projects healthy and active, the existing no-healthy-Production/Preview-pause and no-spending constraints leave no slot for an advance third-project Hosted rehearsal. More local fixture passes cannot close this live capacity/stop-oracle gap. The approved incident-only recipe remains unchanged unless the user explicitly approves an exception.

### Concrete optional exception — NOT AUTHORIZED

The least-cost Hosted rehearsal proposal would temporarily pause only comment-translator-paid-preview, provision one fresh project named ct-gate1-recovery-rehearsal in the already selected Free organization/ap-northeast-1, run empty/synthetic-only Hosted configuration and stopping checks, leave that rehearsal project paused, then resume Preview. Production remains active throughout; no Production backup, real-user data, migration, application cutover, provider execution or external message is included. The new ref must be captured from successful creation and independently bound to its GET result, organization, region, Direct host and actual PG17 before use. No reuse of a similarly named existing project, automatic duplicate creation, paid fallback or managed privilege widening is allowed.

Before any Preview pause, independently bind Preview from the current inventory, verify its preservation/backup and concurrent-use readiness, record the exact resume operation and protected settings references, and prepare target-specific status/Direct/REST/Auth observations. Any missing prerequisite stops before pause. The production-only watchdog transport explicitly requires target=production and source-bound authorization; it must not be retargeted or weakened for this rehearsal. Preview pause/resume and Recovery emergency pause need distinct recorded target identities and explicit exception authority.

| Step | Required result / failure path |
| --- | --- |
| Pause Preview | One exact-project pause request; independently observe stopped status and bound endpoints. An acknowledgement alone is not success. If uncertain, do not create Recovery; reconcile Preview state and use only the approved Preview resume path. |
| Check slot and create | Refresh active capacity and amount0 estimate, then perform the separately required cost confirmation. Create once in the bound organization/Tokyo. Timeout means inspect inventory for that attempt, never create again blindly. A nonzero quote or unexpected project/PG version stops the test. |
| Empty/synthetic Hosted checks | No retained backup restore. Verify configuration readbacks and actual Auth/Data API/Storage/Realtime/Functions routes with no external delivery. Keep actual observations distinct from local-service tests. Unknown capabilities or inability to close a route stops before adding retained data or opening business traffic. |
| Recovery failure containment | Request pause only for the newly bound rehearsal project, then independently observe status and exact endpoint behavior. Existing strict candidate requires fresh INACTIVE, Direct connection refusal and REST/Auth503; record differing responses as UNKNOWN, not a relaxed pass. Additional service paths must be independently evaluated. No repeat restore or automatic unpause of this project. |
| Return Preview | After Recovery is confirmed paused and the free slot is available, resume only the original Preview and verify ACTIVE_HEALTHY plus its accepted baseline/service state. Do not delete either project. If capacity or resume is uncertain, report the outage and exact state; no paid fallback or Production stop. |

This proposal temporarily makes Preview unavailable and has no proven completion/resume duration. It is an exception request, not a claim that Hosted execution is ready or that existing approval includes it. The stopped rehearsal project remains unused and is not a prepared incident restore destination. Any later retained-data rehearsal, Production DDL/watchdog arming, incident provisioning, cutover or reopening remains outside this proposal. The alternative is to retain current constraints and keep Hosted acceptance/Gate1 NO-GO; no support inquiry or paid service is required merely to make that decision.

## 2026-09-10 Additional Storage and Realtime route verification

The user authorized the next local step. storage-object-closure.mjs/.json in the ignored evidence directory extends the earlier cached Storage v1.72.11 fixture. A service-role control uploaded one real synthetic JSON object into a private bucket. For both anon and an actual fixture Auth access token, upload and upload-signing returned400/AccessDenied, authenticated download and download-signing returned400/NoSuchKey, and the public download route returned400/NoSuchBucket. All10 denied requests preserved the exact sorted full-row bucket/object digest. A service-issued signed URL successfully returned the fixture object with anon credentials, proving the service/object/signing path worked. The fixture object and bucket were removed; owned containers/volumes remaining0. Status: LOCAL_SYNTHETIC_STORAGE_OBJECT_CLOSURE_PASS.

This narrows the earlier object/signing gap; it does not prove arbitrary Storage isolation. Existing signed URLs can authorize object reads without a new RLS decision. The approved candidate therefore still requires an independently verified empty source/target Storage inventory and no unknown policies, external object content, public buckets or usable pre-existing signed capabilities. Do not reuse a previously exposed target or interpret policy0 alone as closing signed/public reads. S3/TUS, transformations, Hosted routing and exact current runtime parity remain untested.

The additional realtime-http-closure.mjs/.json verified both batch and single-message HTTP Broadcast in the cached v2.112.1 service. Each positive request returned202 and its message was received over the existing WebSocket. After suspend=true, both HTTP routes returned403; the existing socket closed (43ms in this run), reconnect returned403, and independent database readback confirmed suspend=true. After local resume, the same positive socket/batch/single delivery checks passed. Final readiness/reconnect transport-error arrays were empty and owned containers/volumes remaining0. Status: LOCAL_SYNTHETIC_REALTIME_HTTP_CLOSURE_PASS.

The fixture needed two corrections before acceptance: self-host seeding temporarily runs an API before the final server starts, so readiness now waits for the final server exec marker and a healthy response; the single-message endpoint uses the pinned [V2 binary user-broadcast framing](https://raw.githubusercontent.com/supabase/realtime/v2.112.1/lib/realtime_web/socket/v2_serializer.ex), which the fixture now decodes before asserting delivery. Earlier connection errors, transient startup responses and an undecoded-message timeout were not accepted as closure. The final run repeats the prior socket checks after final-server readiness, replacing the earlier startup-ambiguous fixture evidence for that scope. The pinned [batch](https://raw.githubusercontent.com/supabase/realtime/v2.112.1/lib/realtime/tenants/batch_broadcast.ex) and [single](https://raw.githubusercontent.com/supabase/realtime/v2.112.1/lib/realtime/tenants/single_broadcast.ex) implementations explicitly reject a suspended tenant; Hosted propagation and alternate gateways still require independent acceptance.

This local extension is complete. Remaining gaps are Hosted destination/capacity, actual Data API and complete alternate-route closure, current capability/inventory parity, recovery-target emergency stop confirmation and the full-procedure deadline. Explicit Presence/Postgres Changes and Storage alternate protocols were not exercised. No Hosted action, source pause, publication, real-account access or external message occurred; product source and original backup are unchanged. Gate1 NO-GO.

## 2026-09-10 P6 Auth-first reopening revision approved; containment evaluation

The user explicitly approved changing P6 to public Auth reopening, operator login smoke, then application/business-API reopening, and continuing local closure/failure analysis. Existing users may authenticate and modify their own Auth state during that declared interval. Signup and unused providers remain closed. This supersedes the approval-pending P6 text below, but does not authorize Hosted operations, support messages, spending, source pause, publication or waiving remaining service-closure and deadline evidence. Business account/Free-state readback before Data API reopening must use the authorized direct-DB readback; an application read that requires the disabled Data API is not yet an available smoke route.

The local synthetic auth-reopen-failure-containment.mjs/.json under .tmp/gate1-evidence-20260910 ran the accepted closed Auth/atomic restoration checks, reopened Email with signup still disabled, and confirmed three original fixture passwords/IDs. After an injected smoke failure, re-closing providers still allowed a newly issued access token, refresh token and recovery PKCE code. The same destination's restore retry was rejected at the persistent claim before SQL, without stopping its owner. The owning driver then stopped the exact Auth and database containers and disposed all owned containers/volumes; remaining0. The business fixture digest was unchanged, but no business API server was exposed in this test, so this is not a business-gateway isolation proof. Hosted stopping remains unobserved.

A separate empty-storage-closure.mjs/.json used cached Storage v1.72.11 with the reviewed initializer, one fake Auth user and no Storage policies. Anon and authenticated bucket-create requests both returned400/AccessDenied and buckets/objects stayed empty. A service-role positive control could create and read a private fixture bucket, while the authenticated list remained empty; the fixture bucket was then removed. This distinguishes a working API with policy denial from an unavailable service. Owned containers/volumes0. The retained source-security snapshot also has Storage policies0, but that historical count must be rechecked on the actual capture and target; no unknown policy/content is accepted. Object upload, signed URLs, alternate protocols, Hosted gateway behavior and runtime catalog equivalence were not fully tested by this bounded check.

For Hosted failure containment, [pause project](https://supabase.com/docs/reference/api/v1-pause-a-project) is a candidate recovery-target action; its200 response is an empty acknowledgement, not independent stopping evidence. [Project Pausing](https://supabase.com/docs/guides/platform/free-project-pausing) states paid projects cannot be paused. The current source watchdog is separately bound to the SOURCE and its strict hosted INACTIVE/Direct-refusal/Auth+REST503 oracle is not yet live-accepted. Do not point that watchdog at Recovery or reuse its source authorization. A recovery-target-specific stop/confirmation packet and pre-authorized failure response must be established before Auth is reopened on Hosted. No automatic unpause, same-target replay or provider-toggle-only containment is allowed.

Realtime has documented [suspend configuration](https://supabase.com/docs/reference/api/v1-update-realtime-config) and [shutdown](https://supabase.com/docs/reference/api/v1-shutdown-realtime) operations. Both need actual target readback and existing/new socket evidence; an API acknowledgement or empty publication list is insufficient. Its self-hosted tenant operations are not automatically equivalent to the Hosted management endpoints.

The isolated realtime-closure.mjs/.json used cached Realtime v2.112.1 and a fresh synthetic database, with no external network or host ports. The final IPv4-bound probe first confirmed HTTP101 and a working public Broadcast echo using a valid synthetic service-role JWT. Setting the local tenant's suspend=true returned200, the existing WebSocket closed in94ms in this one run, and a new handshake returned403. The pinned [tenant view](https://raw.githubusercontent.com/supabase/realtime/v2.112.1/lib/realtime_web/views/tenant_view.ex) omits suspend from its GET response, so a separate direct database read confirmed the stored true value while still suspended. After explicit local resume, a new connection and Broadcast echo succeeded. No shutdown/reload fallback was needed. Owned containers/volumes remaining0. Earlier fixture attempts failed before acceptance due to connection setup or the unsupported HTTP readback assertion; their destinations were disposed, with no failed result counted as closure evidence.

Status: LOCAL_SYNTHETIC_REALTIME_CLOSURE_PASS. The measured94ms is not an SLA or Hosted propagation bound. This is one local service instance, not multi-node Hosted evidence; HTTP Broadcast, explicit Presence operations, Postgres Changes, alternate hostnames/protocols and business-data preservation were not separately exercised. In the pinned [tenant update implementation](https://raw.githubusercontent.com/supabase/realtime/v2.112.1/lib/realtime/api.ex), suspend changes trigger socket disconnect and database-connection restart; that source mapping does not establish how Hosted deploys or propagates it.

### Remaining evidence before a Hosted execution proposal

| Boundary | Required evidence / current limit |
| --- | --- |
| Recovery identity and capacity | Exact approved destination, no additional cost and healthy-source availability. No Recovery provisioning is authorized by these local results. |
| Closed restoration | Fresh source/target capability inventories; target signing-trust independence; Data API disabled with independent setting readback and REST/RPC/GraphQL route denial; empty or explicitly controlled Storage/Realtime/Functions and alternate routes. No original grants/RLS relaxation. |
| Auth-first P6 | Preserve accepted atomic readback, then explicitly reopen only required Auth, permit the approved existing-user Auth interval, perform operator login and direct-DB business/Free-state smoke, then reopen application/business endpoints. Signup/unused providers remain closed. |
| Failed smoke | Pre-authorized exact Recovery stop action plus independent stopped-state and route evidence. Provider-off alone is insufficient. Preserve the persistent no-reuse claim; no automatic replay/unpause or unrelated/source stop. A management acknowledgement is not containment. |
| Overall acceptance | Hosted key/config propagation, service-route coverage, capture freshness and whole-procedure deadline remain unaccepted. Local restoration timing alone does not prove the deadline. |

The approved local continuation is recorded without further external actions. Product source, original backup and existing restoration acceptance are unchanged. Gate1 remains NO-GO; no support dependency is reintroduced.


## 2026-09-10 Combined closure PASS; operator-hook counterexample

The user authorized further local evaluation of the P2 alternative. Support remains withdrawn. Product executor and original backup are unchanged; no Hosted calls or public settings were changed. This checkpoint supersedes the next-local-test items in the evaluation below, but does not adopt P2b or open Gate1.

The new synthetic combination retained real Auth-generated fixture sessions/refresh rows and three PKCE flows in its synthetic dump. The existing ordinary-postgres atomic adapter restored them, verified source preservation, reset credentials and independently matched the committed expected state. The final run started Auth with independent target signing material and Email/phone/anonymous/signup closed BEFORE baseline inspection and atomic execution; Auth remained running during restoration. There was no deliberate request barrier inside the SQL transaction, so this is not an exhaustive concurrency/interleaving test.

After committed readback,15 requests rejected with the expected Auth error classes: password, recovery, magiclink, OTP, resend, signup, three old access tokens, three old refresh tokens and three old PKCE codes. All23 Auth table contents were hashed using sorted full-row multisets before and after the denied requests; every digest matched, including refresh rows and audit entries. The local mail collector had a positive baseline and zero message delta. Sessions/refresh rows were zero. Re-enabling Auth allowed all three original fixture passwords to return their original user IDs. All owned containers/volumes were disposed. Final ignored evidence: provider-closure-atomic-active-auth.json/.mjs under .tmp/gate1-evidence-20260910; earlier provider-closure-atomic-combination.json recorded the narrower after-restore-start variant and is preserved separately.

Status: LOCAL_SYNTHETIC_CLOSURE_ATOMIC_COMBINATION_PASS. This does not establish Hosted Data API/Storage/Realtime/Functions closure, real-account login, signing-key propagation, verify-link GET/POST coverage with the combined settings, or an operator-only reopening window. Earlier link/TOTP evidence is separate, not silently promoted to this combined acceptance.

### Operator-only HTTP hook experiment: rejected candidate

An isolated HTTP Custom Access Token hook used a fixed synthetic operator ID and verified the pinned Auth service's webhook HMAC/timestamp. It echoed original claims only for that operator and returned a rejection otherwise. A Send Email HTTP hook rejected delivery. This introduced no Postgres functions, schemas or ACL overlays, avoiding the previously identified database-fingerprint conflict. It was a disposable local test endpoint, not a deployed service or approved secret-handling design.

With providers enabled and signup closed, the non-operator's valid password rejected403 with all23 Auth table hashes unchanged; the operator's password succeeded. Hook unavailability also prevented token issuance. However, a non-operator recovery request with a PKCE challenge returned403 and sent no mail while changing auth.flow_state. The current implementation creates the pending flow before the transactional mail-hook rejection. Thus denying issuance/delivery does not prove absence of unauthorized Auth writes. Receipt: http-hook-smoke-characterization.json/.mjs in the same ignored directory, status COUNTEREXAMPLE_FOUND; owned containers/volumes0. Do not deploy this candidate as an accepted P2 replacement, accept the residue silently, or weaken preservation fingerprints to hide it.

### Decision prepared, not applied

Preserve the closed-restore condition and separate it from a deliberately declared public Auth reopening point. One possible revised P6 would keep Data API/application/providers for external business integrations closed, explicitly reopen only the required Auth provider after restoration acceptance, then perform the operator's login smoke before reopening business traffic. Signup and unused providers would remain closed. Existing users could nevertheless authenticate and mutate their own Auth state during that interval. This changes the current requirement for operator-only login BEFORE public Auth reopening; it needs explicit acceptance. It also needs independent confirmation that every business-data surface remains closed and a target-bound response to failed smoke. Turning Email off again does not invalidate tokens issued during the interval, so failure cannot be declared contained by toggling the provider alone; signing trust/session containment or target disposal must be independently supported and approved before Hosted execution.

The alternative is to retain operator-only pre-public Auth smoke as mandatory and continue searching for an enforceable mechanism; the tested HTTP hook combination is insufficient. No authentication bypass, admin impersonation link, all-user ban, password replacement, relaxed Auth-write allowance or new hosted architecture is selected. No support response or paid feature is a prerequisite for further local analysis.

Current Gate1 NO-GO. The next decision is whether to revise P6's Auth reopening/smoke order; it is not permission to create or modify a Hosted target, send email, publish changes or waive remaining service coverage and deadline checks.


## 2026-09-10 P2 alternative under evaluation (support path withdrawn)

The user rejected reliance on unanswered Free-account support inquiries and authorized evaluation of an alternative. No inquiry will be sent. The previous unsent support draft is replaced by this decision packet. Existing local replay acceptance is retained; all-HTTPS isolation remains the current requirement until a concrete revision is explicitly accepted. Gate1 NO-GO. This is design/source evidence, not a tested Hosted recipe.

### Proposed invariant and options

Candidate P2b would allow public HTTPS endpoints to remain reachable while preventing non-operator access to retained data, business-data writes, account creation, usable authentication issuance and recovery-triggered external messages before explicit reopening. Independent target signing material, protected credentials, existing atomic reset/preservation checks and source availability remain mandatory. Credential secrecy is not a substitute for denial controls: test with the target public key and a valid existing-user password. Invalid-request logs/rate counters may still change; this is a residual difference from total network isolation and requires an explicit bounded acceptance decision. No business/Auth preservation exception is implied.

| Option | Assessment |
| --- | --- |
| A: Recovery configuration closure + atomic reset | Preferred first experiment. Disable all enabled login providers, anonymous signup, signup, SSO/OAuth server/passkeys/third-party trust as applicable, and Data API; retain exact independent target keys. No temporary business RLS/ACL changes. Must test every alternative Auth path, outbound effect and the reopening transition; not yet sufficient for GO. |
| B: Add a Recovery-only Custom Access Token hook for operator smoke | Candidate only if A cannot preserve operator-only testing. Free plan offers this hook. It can return errors before issuance, but does not establish absence of earlier Auth writes, email sends or existing-token use. Hook failure/timeout must reject. Use an exact approved immutable operator ID, never user_metadata or a client-supplied role. Such an allowlist still relies on operator account credentials; it is not network isolation. |
| C: New self-hosted recovery service behind private ingress | Provides controllable network boundaries, but changes hosting/operations and potentially cost. Outside the current approved architecture; not selected. |

[Auth Hooks](https://supabase.com/docs/guides/auth/auth-hooks) lists Custom Access Token and Before User Created on Free/Pro; Password Verification Attempt is Teams/Enterprise and is excluded from the no-cost candidate. [Custom Access Token](https://supabase.com/docs/guides/auth/auth-hooks/custom-access-token-hook) is invoked before token issuance. These facts do not prove the complete candidate.

### Route coverage and unresolved evidence

| Surface | Proposed control / required test |
| --- | --- |
| Existing-user password and new signup | Email/phone provider off plus signup/anonymous off. Pinned [token.go](https://raw.githubusercontent.com/supabase/auth/v2.192.0/internal/api/token.go) checks provider state before password authentication. Prove valid-password denial and no account/session creation; signup-off alone is inadequate. |
| Magiclink, recovery, OTP, resend | Provider off. [magic_link.go](https://raw.githubusercontent.com/supabase/auth/v2.192.0/internal/api/magic_link.go) checks Email enabled; [api.go](https://raw.githubusercontent.com/supabase/auth/v2.192.0/internal/api/api.go) wraps recover with requireEmailProvider. Resend/OTP need separate native coverage, including no message or new token/flow residue. CAPTCHA/rate-limit rejection is not proof of provider denial. |
| Old access/refresh, verify GET/POST, PKCE, invite and email/phone changes | New target signing trust plus the already verified atomic invalidation. Provider-off is not a universal verify/PKCE guard. Validate rejection before/during/after commit with non-expired synthetic credentials and detect new sessions and unintended retained-row changes. Do not extract real-user tokens. |
| Social/SSO/passkey/Web3/third-party JWT and OAuth server | Independently enumerate actual target capabilities/trusted issuers and explicitly disable or prove absent. Unknown enabled capability stops execution; the historical email-only backup is not proof of current target configuration. |
| REST/GraphQL/RPC | Recovery Data API off with bound target readback and actual route probes. [API security](https://supabase.com/docs/guides/api/securing-your-api) documents Data API disablement independently of grants/RLS. Prove GraphQL and RPC coverage too; do not rewrite original grants to manufacture preservation. |
| Storage / Realtime / Functions | The earlier source inventory was empty, but refresh at the approved capture and assert empty target/restore inventories. Include public buckets/objects/signed URLs, publication tables, public Broadcast/Presence, Functions and existing sockets. Empty publication tables alone do not close Realtime. Any content or callable writer requires a separate control; Data API off does not cover these services. |
| App, callbacks, webhooks, scheduler, direct DB and background jobs | Recovery app stays operator-restricted across all hostnames; no provider/scheduler binding or success acknowledgements during recovery. Operator-only DB access is separate. Audit restored triggers/jobs/external connections and platform background activity. A denied Auth request can still produce logs; classify and verify instead of claiming all writes are impossible. |

### State transitions and preservation

EMPTY_CLOSED: independent target keys/trust and disabled entry points applied and independently checked before real data. No old target users, sessions or prior public use is acceptable. Capture the exact active-configuration field whitelist and inverse settings privately; never replay the raw242-field API response.

RESTORED_CLOSED: run the published atomic source-match/reset/delta/independent-readback path with the closure still active. Unknown capability, successful forbidden access, incomplete denial oracle, preservation mismatch or uncertain configuration fails closed; keep/dispose the owned target according to approved policy, never reopen on failure. Keep the original artifacts and cumulative incident deadlines.

OPERATOR_SMOKE: do not simply re-enable Email for everyone while the app proxy is closed; direct Auth remains reachable. A separately validated operator-only issuance boundary is required before enabling a provider for smoke. Option B must first prove non-operator denial and no forbidden side effects. A Postgres hook adds functions/ACLs covered by current source/security fingerprints; current restoration does NOT accept it automatically. Do not exclude the schema, weaken a hash or modify original backup files to pass. Resolve timing/declared target-only overlay and independently verify its removal in a separate reviewed change, or retain A as blocked at this transition.

REOPEN: only after closed restoration and authorized operator smoke pass, restore the exact approved settings and enable integrations in a reviewed order, record the public reopening point, and verify ordinary fresh login and intended account state. Fixture cleanup and expected smoke deltas must be explicit. No source shutdown or extra spending is added.

### Next bounded local evaluation

Use only cached Auth v2.192.0/PG images, a newly owned network=none synthetic target and local mail collector. Probe option A with valid fake passwords, old non-expired access/refresh/link/PKCE and direct resend/OTP calls while providers/signup are disabled, then after atomic reset. Assert exact error classes, session/token/flow/user preservation and local mail-count deltas. Include positive controls with providers enabled, otherwise a broken fixture could look safe. Stop and classify the first counterexample; no real backup or Hosted calls are needed. A positive result establishes local Auth closure only, not Hosted gateway/Storage/Realtime enforcement or operator smoke. Prototype B only after its fingerprint/timing design is reviewed.

A first bounded synthetic characterization used cached PG/Auth v2.192.0, a local mail collector and network=none. Positive-control password and recovery-PKCE generation succeeded before closure. With Email/phone/anonymous disabled and signup disabled, password422, recovery400, magiclink422, OTP422 and resend400 returned email_provider_disabled; signup422 returned signup_disabled. User/session/flow/one-time-token counts stayed unchanged for those six requests. This is count evidence only: row values, refresh-token rows, audit logs and mail-count deltas were not fully compared. With the same signing key and no reset, the old access token still returned200 and the old PKCE code issued a session200, proving provider closure alone is insufficient. The original successful atomic-reset evidence remains separate; this characterization did not combine the mechanisms. Owned containers/volumes0, no external delivery. Ignored receipt: .tmp/gate1-evidence-20260910/provider-closure-characterization.json. A timeout/error was not counted as a successful rejection.

No production implementation, support message, commit/push or Hosted operation occurred in this evaluation. Only the owned synthetic Auth configuration was changed and the target disposed. The user has approved investigation, not adoption of P2b or relaxation of Gate1. Remaining decision: validate option A locally; accept a precisely scoped P2 revision only once its closure and operator-smoke gaps are resolved.

## 2026-09-10 Post-PR826 retained atomic replay accepted locally

PR826 is merged into Preview at dd19e85b5eb0eb61d33bf76e9ec912879b689f14. The protected retained reader independently matched the historical13 capture producers, current20 published executor files and original six artifacts. A fresh owned network=none destination passed the complete approved empty-baseline comparison:595 objects,762 dependency edges with zero missing/extra edges,92 catalog-derived internal-trigger mappings and29 TOAST-parent mappings. Exact managed security and public defaults matched after the reviewed empty-target preparation; role attributes and memberships stayed unchanged. Auth users, public tables, Storage objects and Vault rows were empty; application migration history was absent.

The published runRetainedAtomicPostgres entry point executed the unchanged retained six-file backup once as non-superuser postgres. It returned ATOMIC_POSTGRES_RESTORE_OBSERVED with preResetSourceStateMatched=true, resetDeltaMatched=true and independentCommittedReadbackMatched=true. The call completed in118663ms within its600000ms bound, including provenance checks. No source guard, artifact rewrite or test seam was substituted. The result was RESTORED_QUARANTINED and targetReusable=false; the rehearsal destination was then disposed, with owned containers0 and owned volumes0. Persistent attempt claims and unrelated volumes remain preserved. Earlier failed attempts retain their separate receipts.

Status: LOCAL_RETAINED_ATOMIC_REPLAY_PASS. This closes the published-source, full local managed-baseline and real six-file atomic restore/reset/readback acceptance for this pinned local target. It does not prove real-account Auth login or Hosted signing-key behavior, whole-Recovery HTTPS isolation/reopening, current incident freshness, a complete supervised recovery within the overall5/10/20-minute budgets, or Hosted restoration. No new capture, Production connection, external setting change or deployment occurred. Gate1 remains NO-GO and stageAuthority=false.

Retained ignored evidence: .tmp/gate1-evidence-20260910/atomic-post826-preflight.json, atomic-post826-real-replay.json and atomic-post826-stable-managed-catalog.json; the bounded driver is atomic-post826-real-replay.mjs. Primary-agent execution and acceptance only. Next: establish supported P2 whole-Recovery isolation and its target-specific verification/reopening procedure before any Hosted real-data operation. Existing healthy-source availability and no-cost constraints remain.


## 2026-09-10 Post-PR825 real attempt and email-PKCE provider repair

This checkpoint supersedes earlier zero-real-native-execution statuses below. PR825 is merged at 8c07fc74ba24eae7f0ef0f46b2442e2d15a63c55. Before editing, the actual protected retained reader passed capture13/executor20/original6-file checks. The empty standalone PG17.6.1.140 destination used network=none, cached Auth v2.192.0 and the previously pinned Storage initializer. Managed catalog and Storage history matched the pinned accepted inputs. Two Storage owner-ACL representation differences were reconciled using the already reviewed owner action. Standalone-image default privileges were broader than the earlier CLI baseline, so future-table CRUD and future-function EXECUTE grants were revoked for anon/authenticated/service_role, alongside the existing sequence-default restriction. Exact managed ACLs and public defaults then matched the retained source, role attributes/memberships were unchanged, Auth users/public tables/Storage objects/Vault rows were empty and migration history was absent. No role escalation or source change occurred.

A separate fresh empty-baseline comparison subsequently checked all catalog components. Raw internal-trigger/TOAST names differed across instances; the existing reviewed catalog-derived mapping resolved those identities while preserving every edge and duplicate. All595 objects,762 dependency rows,92 internal-trigger mappings and29 TOAST-parent mappings matched the accepted local baseline with zero missing/extra edges. Unmapped identities reject; no global numeric-name stripping is used. This closes the local empty-baseline comparison, not the rejected real restoration or the Hosted baseline. Retained evidence: atomic-post825-stable-baseline-probe.json and atomic-post825-stable-managed-catalog.json. Future real-attempt preparation must apply this full comparison as well as the exact source-ACL/default checks.

The first two fresh empty-target preparation runs stopped before any retained SQL because of the default-privilege mismatch. The third driver passed the complete target-profile assertions and called runRetainedAtomicPostgres once with unchanged protected inputs and a600000ms budget. It failed during restore with ATOMIC_GUARD_REJECTED after59926ms, confirmed target stopping and required disposal. Owned containers and observed volumes were removed; persistent attempt claims remain. This proves one rejected real native execution, not successful pre-reset preservation or restoration. No same-target retry or replacement budget reset was performed. Receipts are ignored atomic-post825-real-replay*.json under .tmp/gate1-evidence-20260910.

Read-only inspection of the original data identified a sufficient validation blocker:20 flow_state rows comprise provider=email with method=email/signup10, magiclink/magiclink2 and recovery/recovery8. The old code accepted only provider_type=email for the reviewed email methods. The public transport exposed only the generic guard class, so the precise first guard error in the real attempt is not independently attributed. All original hashes were reverified after that attempt.

The pinned Auth implementation explicitly generates method-named email providers in [magic_link.go](https://raw.githubusercontent.com/supabase/auth/v2.192.0/internal/api/magic_link.go), [recover.go](https://raw.githubusercontent.com/supabase/auth/v2.192.0/internal/api/recover.go) and [user.go](https://raw.githubusercontent.com/supabase/auth/v2.192.0/internal/api/user.go). The local repair adds only the exact magiclink/magiclink, recovery/recovery and email_change/email_change pairs to the existing four email-provider methods. One predicate is shared by unsupported-flow validation, expected retained-row projection and deletion. Unknown providers, mismatched pairs, NULLs and non-email OAuth are still rejected; the existing synthetic unrelated control remains preserved. This corrects coverage of the already approved email reset; it does not introduce a general OAuth purge or alter users/passwords/identities/factors/business data.

The new native contract obtains all three variants through actual cached Auth APIs and a mail collector sharing the isolated network namespace; no external mail/provider traffic occurs. It reproduced the old guard rejection, then passed atomic normal-postgres restore/reset/independent readback after the repair. All three old API-generated PKCE codes reject404, fresh API-generated codes work and original passwords return the same user IDs. Eight unsupported provider/method pairs reject in the actual generated validation block. Related24 Node tests, existing strict native rollback/failure checks, Auth/TOTP native regression, scoped two-file ESLint and operator contract pass. Fixture resources are removed; older unowned volumes remain preserved.

Current status: LOCAL_EMAIL_PKCE_REPAIR_VERIFIED / REAL_RESTORE_UNACCEPTED. This changed executor must be published and independently rebound before another retained-data native attempt; no unpublished-code bypass is permitted. No new capture, Hosted mutation, source pause, commit/push/PR or deployment occurred in this continuation. Hosted HTTPS isolation/reopening and real restoration acceptance remain open. Gate1 NO-GO.


## 2026-09-10 Post-PR824 empty managed baseline repair

PR824 is merged into the Preview integration branch at 805fadee3c3038582d544750faf735870255d549. Before editing, the actual protected retained-data reader verified the historical13 capture producers, all20 published executor files and all six original backup files. No source guard was bypassed; native executions of the retained files remain zero.

An empty, owned network=none PostgreSQL17.6.1.140 destination initialized with cached Auth v2.192.0 and the previously selected Storage initializer matched the retained initialized managed catalog and Storage migration history. Auth users were zero and the application migration-history table did not exist. The published baseline query failed with42P01 because it reused the strict post-restore source-state query, which necessarily reads supabase_migrations.schema_migrations. The synthetic fixture had previously hidden this gap by creating history before baseline inspection.

The local repair separates baseline security collection from post-restore source/history verification. Both paths retain full ordinary-table/sequence fingerprints and definition/role/membership fingerprints. Baseline security explicitly covers schema/relation/column/function ACLs and owners, RLS/policies, default privileges and extensions without fabricating history. Strict post-restore fingerprint SQL is byte-identical to the pre-repair export; original backup bytes, fixed reset, whole-state delta and independent readback remain unchanged. Full target/source compatibility is still required before real-data execution; a matching managed catalog alone does not establish that authority.

The new native regression accepts an empty image without history, checks stable repeated readback, detects eight row/catalog/ACL/RLS/history changes and rejects missing Auth. The full cached managed initializer now passes the baseline query. Related24 Node tests, three-file ESLint and operator contract pass. All four ordinary-postgres native adapter scenarios also pass on the repair (SQL failure, connection loss, committed-readback mismatch and success), with no owned container/volume remaining. These are local checks of the unpublished repair, not retained-data execution or Hosted isolation evidence. Only owned fixture resources are removed; older unowned volumes and persistent attempt claims remain preserved.

Remaining: publish this repair before any operational retained-data call; attest the complete target profile and perform real six-file atomic restore/reset/readback within the existing budget. Hosted HTTPS isolation, reopening and target-specific external authority remain open. No new backup, production mutation, commit/push/PR or deployment was performed in this continuation. Gate1 NO-GO.


## 2026-09-10 Approved disposable-target policy implemented locally

Status: LOCAL_DISPOSABLE_TARGET_VERIFIED / REAL_REPLAY_AND_HOSTED_UNACCEPTED. The user approved the design revision after the sequence-ownership finding. The earlier approval-pending and managed-sequence-blocked paragraphs below are historical checkpoints. No ownership grant, SET ROLE expansion, original-backup rewrite, remote deletion or production change was made.

The new explicit discard-target-v1 policy leaves the original Auth refresh sequence setter unchanged and omits its transactional RESTART guard only for auth.refresh_tokens_id_seq. All other sequence guards and the default strict path remain intact. Successful restoration still compares source state, applies the fixed credential reset and compares the full expected delta plus independent committed readback. This policy explicitly does not guarantee whole-state rollback: sequence residue after a failed transaction is expected, and that destination must never be reused or reopened.

Before the adapter sends SQL, an append-only ledger claims the immutable container ID and every mounted data-volume identity. Records are synced before execution and shared under Git common metadata across this repository's worktrees. Success, failure, normal resource removal and process death do not clear claims. Incomplete/corrupt records deny reuse. A replacement container cannot reuse a claimed volume; a fresh destination needs fresh volumes and a newly verified baseline. The ledger stores hashes/operational metadata, not SQL or credentials. Do not delete these records as temporary cleanup.

The ordinary-postgres adapter requires an owned exact image/baseline, network=none including actual attached networks, no published ports/host binds and a non-superuser session. Failures after acquiring the attempt report disposalRequired and stopConfirmed separately; stopping is not deletion. Unknown stopping remains unconfirmed and never accepts recovery. A losing/repeated claimant has no attempt ownership and must not stop or dispose another writer's target. Successful results remain RESTORED_QUARANTINED and targetReusable=false. External destruction still requires the exact target and separate authority; this API does not perform Hosted deletion or reopen services.

Verification: 45 related Node tests PASS; the ledger also survives a child-process SIGKILL and rejects reuse from a fresh process. Four native ordinary-postgres scenarios each use a fresh isolated container/volume: SQL failure, connection loss, committed-readback mismatch and successful Auth-setter restore/reset/readback. Failed cases demonstrate Auth sequence residue and retry rejection; the final fresh destination succeeds. All observed native fixture containers/volumes were removed, while claims remain. The existing strict native failure suite and cached Auth/TOTP regression also PASS, including old link/access/refresh rejection, original password/user ID login and same-factor fresh AAL2. Scoped lint covers all11 changed/new JavaScript files. The primary agent implements/reviews alone.

Remaining gates: publish the new executor and ledger source before retained-data native use; establish and attest the complete managed baseline and real six-file replay; prove P2 whole-Recovery HTTPS isolation and reopening before Hosted use. The local network=none proof is not a Hosted access-control proof. Existing overall5/10/20-minute budgets are not restarted by a replacement attempt, and healthy source availability/no-cost constraints remain. Real backup native executions=0, commit/push/PR=0 in this continuation, Gate1 NO-GO. The older13 unattributed anonymous volumes remain preserved.


## 2026-09-10 Stage3 adapter implemented; managed sequence blocks real acceptance

Status: LOCAL_ADAPTER_CONTRACT_VERIFIED / REAL_BACKUP_BLOCKED_ON_MANAGED_SEQUENCE. The primary agent added atomic-provenance.mjs, atomic-retained.mjs and atomic-postgres-process.mjs plus four test/native-contract files. Historical producer hashes are checked against the capture commit; loaded/current executor bytes and ancestry are separately checked against the accepted preview commit. Protected process/acquisition/manifest records are joined without rewriting their source/run/snapshot/native completion identities. The operational retained-data entry exposes no injected verifier or caller PASS flag. Its total deadline includes preparation and postcheck; cancellation, uncertain exit and any failed readback reject acceptance.

The adapter binds an immutable local container ID, unique owner label, exact image and expected baseline fingerprint. It requires network=none, no host bind/port publication, postgres/non-superuser, PG17 and an empty Auth user set. It executes the unchanged six artifacts plus fixed reset in one transaction and independently reads committed state. The original synthetic and six-transaction paths remain unchanged. This does not initialize or certify a Hosted baseline, reopen Auth, or produce stage authority.

Final checks: 40 related Node tests PASS; all seven new JavaScript files pass ESLint; operator-contract PASS; direct single-agent review. The native cached PG17.6.1.140/Auth v2.192.0 fixture verifies ordinary-postgres COPY/public-owned-sequence/reset/readback success and injected SQL-error rollback. Managed fixture schema scaffolding uses its platform actor only during empty synthetic setup; the restore process stays postgres. Owned containers and observed volumes were removed. The historical13 unattributed volumes remain retained.

The same native test establishes that postgres neither inherits ownership of the Auth refresh sequence nor can SET ROLE to its owner. Transactional ALTER SEQUENCE RESTART is rejected with SEQUENCE_OWNERSHIP_REQUIRED. The test verifies rejection and unchanged users/sequence values after rollback; this negative case is not successful managed-sequence recovery. Read-only classification of the retained real backup confirms one Auth sequence setter and no public/other sequence setters; no original bytes were changed. The new operational reader validates the real record join and then rejects at provenance because these new executor files are not published. Real backup executions=0. Publishing this patch alone will not close the managed-sequence blocker.

### Decision required before changing failure semantics

The current accepted design requires sequence state as well as row data to roll back. Keep that requirement unless the user approves a material revision. A concrete alternative is to preserve all original successful restore/reset/preservation checks and execute the original managed setter unchanged, while treating any failed or uncertain restore as a permanently unusable recovery destination: keep it isolated, never retry or reopen it, destroy it only under target-specific cleanup authority, and initialize a new destination before another attempt. Do not compensate by granting ownership or manually patching the failed sequence. This would replace complete sequence rollback with disposal of the failed destination; it is not yet implemented or approved.

That alternative requires proven P2 isolation before loading real data, explicit destination ownership/cleanup authority, and the existing overall5/10/20-minute budget without restarting its clock. Hosted isolation and target creation remain unresolved; healthy source pause, paid capacity and Hosted deletion are not authorized by this proposal. The other path is an independently supported owner-level sequence execution mechanism with reviewed permissions; none has been established. Until a choice is approved and verified, keep the existing failure rejection, do not run the real backup, and retain Gate1 NO-GO. No commit/push/PR or external mutation was performed in this continuation.


## 2026-09-10 Post-PR823 stages 3 and 4 preflight

PR823 is merged to preview at 8680e45df97c27d5c1bdcf63448d56a2b720ab3f; this worktree was fast-forwarded to it. The user requested continuation with real-backup rehearsal and recovery isolation. Published acquisition producer verification passes for all13 files. The retained backup was acquired at f1959bee; the existing real-backup runner requires that capture commit to match its current loaded producers, and that verification now rejects the shared SQL-parser difference. STOPPED_BEFORE_REAL_RESTORE, sourceGuardBypassed=false, realBackupExecutions=0. Do not rewrite old receipts or simply substitute the current commit into them.

The published atomic process remains synthetic-only and uses supabase_admin. Stage3 needs a reviewed ordinary-postgres execution adapter with explicit capture-provenance versus current-executor provenance, the existing managed baseline and watchdog, one-transaction execution and independent readback. Publishing the SQL builder alone does not complete that connection. A disposable cached PG17.6.1.140 check established that normal postgres is non-superuser but can read pg_authid/pg_auth_members; this is only a preliminary permission check, not full reset/sequence/managed-schema acceptance. Its owned container and observed volumes were removed.

Stage4 remains unproven. The current official Network Restrictions documentation still explicitly excludes HTTPS Auth/PostgREST/Storage. A DB allowlist or application edge rule alone is insufficient. No complete recovery-only HTTPS control or exact disposable Hosted target has been established. Preserve the no-cost and healthy-source-online constraints; do not pause healthy projects or change production settings to manufacture a rehearsal target. Next concrete work is the Stage3 adapter/provenance contract, followed by a supported Stage4 isolation design and target-specific execution packet. No new capture, real-data restore, external mutation or credential probe occurred during this preflight. Gate1 remains NO-GO.

Sanitized ignored receipts: atomic-stage3-source-preflight.json and atomic-normal-role-preflight.json under .tmp/gate1-evidence-20260910. The older13 unattributed volumes remain retained.


## 2026-09-10 Atomic COPY/setval compatibility and PR acceptance

Status: LOCAL_SYNTHETIC_VERIFIED / REAL_BACKUP_STATIC_COMPATIBLE. The user authorized continuation through commit/push/PR creation to the preview integration branch; merge and Hosted operations remain excluded. This section supersedes the earlier compatibility blockers and publication-pending checkpoints below.

The opt-in SQL reader accepts only qualified pg_dump COPY-text headers, treats data lines as opaque bytes, and rejects unsupported modes or missing terminators. Comment-only Auth/Storage review artifacts are accepted only at their assigned position. Default parser behavior and the original six-transaction restore path remain unchanged. Exact pg_dump sequence setters are preceded by a guard that captures last_value/is_called, uses transactional ALTER SEQUENCE RESTART and restores the captured state before executing the original data artifact. PG17 native tests demonstrate committed source values and rollback on failure/connection loss; ownership is required and is not widened. This is a trusted-artifact construction seam, not an arbitrary-SQL sandbox or published-source execution authority.

A read-only check of the retained six original files (467983 bytes) passed artifact-store integrity checks and compilation: 46 COPY spans and one sequence setter. All original hashes remained equal. Evidence: ignored atomic-real-backup-static-compatibility.json. Native executions of these real files: zero. Source provenance guards remain enforced; the changed shared producer must be published and separately integrated before operational acceptance.

Final implementation checks: 102 related Node tests PASS; native PG17 COPY/setval commit, Unicode/escape preservation, rollback/failure/timeout/cancel and ownership rejection PASS; cached Auth v2.192.0 with native Auth pg_dump COPY/setval PASS. Auth remained online during the independently observed uncommitted restore, old link/access/refresh were rejected, original password/user ID login succeeded, and the same enrolled TOTP factor completed fresh AAL2. Scoped ESLint passed for seven JavaScript files; backup-recovery-contract, operator-contract and diff checks passed. The primary agent reviewed the actual implementation directly without subagents. Current native runs removed their observed owned containers/volumes; the earlier 13 unattributed volume candidates remain retained.

Remaining acceptance: published producer integration, native real six-file replay with the managed baseline/normal postgres role, complete catalog acceptance, and P2 whole-Recovery HTTPS isolation. No Production connection, backup reacquisition, source pause, external setting change, deploy or merge occurred. Gate1 remains NO-GO.

## 2026-09-10 Approved atomic local implementation (historical checkpoint)

Status: LOCAL_SYNTHETIC_VERIFIED / REAL_BACKUP_BLOCKED. Final primary-agent checks passed: 25 focused atomic/original-restore/SQL-parser tests, 10 native database scenarios, the cached Auth/TOTP contract with a separate pg_stat_activity observation confirming the restored transaction was still uncommitted during the old-link probe, scoped ESLint for all five new JavaScript files, operator-contract and diff checks. Final native/Auth runs exited0; their observed containers and volumes were removed. The earlier 13 unattributed volume candidates below remain outside that cleanup claim.

The user approved the five-step implementation below with `進めて`. The primary agent implemented the additive local path directly; the earlier proposal/approval-pending paragraphs below are historical checkpoints. No subagent, runtime/model change, publication or hosted operation was performed. Runtime reasoning effort was not independently attested.

`scripts/lib/comment-translator-paid-core-v1-gate1-atomic-restore.mjs` validates the six original artifact names, order, byte lengths and SHA256 values without changing their contents. One explicit transaction restores them, compares the original sourceState on the same connection, checks the managed Auth shape and unsupported dependencies, computes the exact allowed post-reset projection, executes the fixed reset, compares every retained ordinary-table row and sequence value plus security/definition fingerprints, and commits. The original six-transaction rehearsal module is unchanged. The sourceState comparison retains the producer's `pg_catalog, public` search path; its historical row-count evidence is not relabeled as full source-row hashing. Additional full-row comparisons establish preservation across the intentional reset.

Reset is limited to sessions/refresh tokens and their known MFA authentication-claim cascade, the six reviewed one-time-token types and corresponding nonempty user token fields, and the four reviewed email PKCE methods. NULL/empty token fields, all other user fields/password hashes, identities, enrolled factors, business data and migration history are retained. Unknown Auth shapes, email flow methods, non-synthetic non-email flows, triggers/rules, unsupported incoming foreign keys, nonempty SAML and foreign/materialized relations stop before reset. Shape pins cover the minimal synthetic schema and the observed cached Auth v2.192.0 schema; they are not a Hosted version attestation.

`atomic-local-process.mjs` binds an explicitly local Docker socket and uniquely labeled synthetic container. It owns ON_ERROR_STOP, the 10-minute total process/readback budget, bounded output and cancellation. Errors or uncertain exit never create an accepted result; stopping the owned synthetic database is the cleanup path. Successful process closure is followed by a separate connection comparing the committed fingerprint. Its result is explicitly LOCAL_SYNTHETIC_ONLY / stageAuthority=false / NO-GO. This synthetic runner does not accept a hosted URL, replace the production watchdog, bypass immutable producer guards or authorize real-backup use.

Verification entry points are `scripts/comment-translator-paid-core-v1-gate1-atomic-restore.test.mjs`, `scripts/comment-translator-paid-core-v1-gate1-atomic-native-contract.mjs`, and `scripts/comment-translator-paid-core-v1-gate1-atomic-auth-native-contract.mjs`. The native fixture covers success, source mismatch, mid-restore failure, connection loss, unexpected rule, a deliberately corrupted reset delta, timeout, cancellation, unknown flow and unknown managed column. The actual cached Auth fixture keeps Auth running while the restored rows remain uncommitted, observes old magiclink rejection before/after commit, rejects old access/refresh credentials, logs in with the original password/user ID, and verifies a fresh AAL2 challenge with the same enrolled TOTP factor. Credential values never appear in the result.

Limits remain explicit: real six-file backup replay and normal managed-postgres permissions are not accepted by these fixtures. COPY input and nontransactional sequence setters are rejected rather than rewritten; their compatibility with the real artifact set remains unresolved. No new source publication, original-backup modification, Production connection, restore, pause, migration, SMTP/SMS send or external setting change occurred. Published-producer integration, the full real-backup/catalog acceptance and P2 whole-Recovery HTTPS isolation remain open. Healthy Free stays online during final backup; the existing 20-minute RPO is unchanged. Gate1 remains NO-GO.

Cleanup correction: early minimal native-test runs removed their containers without the anonymous-volume flag. The runner now removes and independently checks each volume it observed on its owned container. Thirteen dangling volumes from the same time window could not be independently attributed after Docker's event history rolled over; they were retained, not deleted by age. Their private local inventory is `.tmp/gate1-evidence-20260910/atomic-unattributed-volume-candidates.json`. Do not claim global volume cleanup or delete these candidates without establishing ownership.

## 2026-09-10 Atomic restore/reset visibility prototype and next implementation decision

- 2026-09-10 Atomic Auth visibility LOCAL_PROTOTYPE_PASS: 正式restoreは6artifact個別transaction後readbackであり、最後にresetを追加するだけでは旧credentialのcommit済み窓が残る。Auth稼働中の架空5表を単一transactionで復元→同接続で元全行一致→限定reset→commitするprototypeを実行。別接続user0/旧magiclink403を途中2地点、commit後旧link403・元password/ID login成功、故意SQL失敗で復元user0へrollback確認。6file正式経路/通常postgres/全token/Hosted隔離の受入れではない。正式transaction/readback契約変更は未実装・承認対象、外部変更0/Gate1 NO-GO。

The current source scripts/lib/comment-translator-paid-core-v1-gate1-rehearsal-restore.mjs executes roles.sql, schema.sql, auth_storage_changes.sql, data.sql, history_schema.sql and history_data.sql through six individual execute calls, then calls readState after all succeed. Each receipt requires transaction=true. Its existing acceptance preserves the complete source state and does not execute a credential reset. Appending reset after these commits would not by itself prevent a running Auth service from seeing restored credentials between data commit and reset. Prior local tests stopped Auth; that service-control assumption has not been established for Hosted recovery.

The new standalone ignored synthetic-atomic-auth-visibility.mjs uses an owned local database, cached Auth, one fake account and a no-send admin-generated magiclink. It snapshots five Auth tables into a private synthetic schema, empties the target fixture, starts Auth with new signing material and keeps it online during the probe. Inside one transaction it restores users/identities/sessions/refresh_tokens/one_time_tokens and compares every row to the private snapshot before changing anything. A separate database connection sees no target users, and concurrent Auth redemption returns403. It then removes only the fixture's session/refresh/recovery state, checks the precise retained-user delta, and commits. Outside readers see the account only after commit; the old link still returns403 and original password login returns the same user ID. A separate injected division-by-zero case exits3 and leaves0 target users, establishing rollback of this fixture. Final receipt synthetic-atomic-auth-visibility-result.json starts2026-09-09T16:12:11Z; PASS is a visibility/rollback prototype result only.

This local experiment does not authorize or validate replacement of the formal six-file restore. It used local supabase_admin, a single magiclink type and private CTAS fixtures, not real backups, normal postgres permissions, complete managed/catalog comparison, full credential coverage or the production process supervisor. Keeping old credentials uncommitted addresses this visibility window; it is not a network deny rule and does not close P2 whole-Recovery HTTPS isolation, authorized-operator access, concurrent new signup/write paths, public callbacks or background jobs.

Concrete proposed next local implementation (not yet started):

1. Add a separately versioned atomic candidate path while preserving the accepted six-transaction pure-restore path and its historical receipts. Keep original six-file bytes, ordering, hashes and immutable-source checks; never rewrite the accepted backups or treat old receipts as atomic evidence.
2. Bind one owned native transaction/session for all six artifacts, verify full sourceState in that same transaction before any intentional delta, apply a fixed reviewed credential-reset allowlist, verify the expected delta, then commit. Check every artifact for transaction-incompatible operations before execution; unsupported SQL is a stop, not permission to drop statements or relax comparison. Do not accept arbitrary caller-supplied reset SQL.
3. Preserve exact user non-token columns, password hashes, identities, enrolled factors, business/history and complete approved security/catalog checks. Reject unexpected managed versions, token/flow types, dependencies/triggers, nonempty unsupported SAML and undeclared changes. Combine the currently tested six OTP types/four email flow methods only after the formal dataset establishes applicability.
4. Retain the existing bounded process/watchdog, timeout/cancel/unknown-exit and immutable-producer rules. Failed comparison/reset/commit or uncertain termination cannot produce accepted evidence. After commit, perform independent readback of the expected post-reset state; capture pre-reset equality separately inside the transaction because another connection cannot see uncommitted restored rows.
5. First verify synthetic success, mid-restore failure, reset/delta failure, connection loss and outside-reader/old-link behavior while Auth is running. Formal native real-backup acceptance needs the newly published source path and separate existing publication authority; no unpublished-source bypass. No hosted changes, commit/push/PR, backup reacquisition or source pause follows from this local implementation proposal.

This is a material change to the transaction boundary and the location/timing of pre-reset preservation evidence. The user's decision is needed before adopting it in the formal restore contract. Healthy Free remains online during final backup and the existing20-minute incident RPO is unchanged. The unresolved HTTPS restriction requirement is not waived by this proposal. Current tracked changes remain the three readiness/task documents only; operator-contract and diff checks pass, and owned synthetic containers/network are absent after cleanup.

## 2026-09-10 Remaining one-time-token types and nonempty TOTP preservation

- 2026-09-10 Transient credentials/TOTP LOCAL_SYNTHETIC_PASS: 一時token6種8行・user token列8値とemail系PKCE4方式を限定失効。旧メール12経路403、電話変更OTP403、再認証nonce422（password不変）、PKCE4方式404、旧access/refresh7組拒否。新メール6経路/電話OTP/再認証/PKCE4方式成功。TOTP実登録1件の全行をreset/restart後保持、同じfactorで新challenge→AAL2成功。user非token列/identity/business/tableACL/RLSflags・対象外flow1行を厳密保持。SQL合成電話/再認証/PKCEを含みSMTP/SMS/外部provider発行は未実証、Hosted隔離/復旧は未完了。外部変更0、cleanup残存0、Gate1 NO-GO。

Primary-agent continuation extended the local candidate and did not modify a hosted project or adopt this as the incident execution recipe. Final receipt: ignored synthetic-mfa-preservation-result.json, started 2026-09-09T15:05:06Z (2026-09-10 JST), standalone synthetic-mfa-preservation.mjs. Earlier no-MFA acceptance synthetic-remaining-token-reset-result.json at15:02:59Z is separate. Its initial phone-fixture failure is retained as synthetic-remaining-token-reset-attempt1.json: a plus-prefixed internal phone value disagreed with Auth normalization; the corrected digits-only fixture passed old and fresh OTP checks. No failures are treated as credential-rejection success.

The single transaction, with local Auth stopped, deletes sessions/refresh tokens; removes only the six recognized one_time_tokens types (confirmation_token, recovery_token, email_change_token_current, email_change_token_new, reauthentication_token, phone_change_token); empties only nonempty values of those six matching users columns; and removes flow_state only where provider_type=email and authentication_method is magiclink/recovery/email/signup/email_change. Eight OTP rows/eight user token values/four flow rows are the declared deltas. NULL/empty values and all other user columns are preserved, including pending new email/phone values, confirmed timestamps and password hashes. A non-email flow fixture remains exactly equal. Five mutation targets have no noninternal triggers locally. Known incoming cascades are refresh_tokens/mfa_amr_claims from sessions and saml_relay_states from flow_state; SAML relay states are empty, so nonempty SAML is not claimed verified. No schema/grant/policy or trigger-disable operation is used in reset.

Exact retained-state comparison covers all user rows, identities, the nonempty verified TOTP factor, synthetic business data, table ACL/RLS flags, OTP residuals, retained flow and empty SAML state. It passes immediately after reset, after Auth restarts with independent signing material, and after unauthenticated old-credential probes; session count stays0 during those probes. Local Postgres/Auth images remain17.6.1.140/v2.192.0. This is not a restore from the six production backup files and is not a hosted-role/managed-version check.

| Case | Old credential after reset | Fresh credential / preservation control |
| --- | --- | --- |
| Magiclink, recovery, signup, invite, email-change current/new | Twelve token-hash and email-OTP calls return403/otp_expired | Six reissued links return200; exact user IDs and /user success |
| Phone-change OTP |403/otp_expired | New SQL-seeded OTP returns200 and updates the intended synthetic phone |
| Reauthentication nonce |422/reauthentication_not_valid; password hash stays identical | A new SQL-seeded nonce authorizes password change, and login with the new password succeeds |
| Email PKCE: magiclink/recovery/email/signup/email_change | Four code/verifier exchanges return404/flow_state_not_found | Each synthetic flow has a pre-reset positive control and a fresh post-reset200 exchange for the same user |
| Access/refresh | Seven observed token pairs rejected (including an MFA-issued pair) | Original passwords for three unaffected users still work |
| Enrolled TOTP | Verified factor's full row remains equal through reset/restart | Same factor ID/secret with fresh challenge reaches aal2 and accepted /user |

Email action links come from the actual local admin generate_link endpoint (no email is sent). Phone/re-auth OTP hashes follow the pinned Auth SHA224(address+OTP) algorithm and are seeded directly, not issued through an SMS/email provider. Reauthentication-required mode is enabled only on the synthetic Auth instance; its test session is aged locally by25 hours to force the nonce check before attempting a password change. Fresh successful authentication can legitimately mutate users, identities, passwords, factors and sessions; reset preservation is measured before these positive controls. TOTP was enrolled/challenged/verified through actual Auth APIs; its secret/QR/token bytes never appear in the receipt. Existing MFA challenge invalidation, recovery codes, WebAuthn and nonempty SAML are outside this test.

Source checks: [reauthentication](https://raw.githubusercontent.com/supabase/auth/v2.192.0/internal/api/reauthenticate.go), [password update](https://raw.githubusercontent.com/supabase/auth/v2.192.0/internal/api/user.go), [phone normalization](https://raw.githubusercontent.com/supabase/auth/v2.192.0/internal/api/phone.go), [TOTP APIs](https://raw.githubusercontent.com/supabase/auth/v2.192.0/internal/api/mfa.go). No dependency installation, real SMTP/SMS/third-party provider, hosted connection/mutation, commit/push or deployment. Owned synthetic containers and network are independently absent after cleanup. The local baseline's table flags/ACL checks are not an exhaustive security-catalog or full backup-restore equivalence proof. Complete Hosted HTTPS isolation, target-side permissions/dependencies, preservation-before-intentional-delta and incident restore/cutover remain unclosed; Gate1 NO-GO.

## 2026-09-09 Confirmation and email-magiclink PKCE local extension

- 2026-09-09 Confirmation/email-PKCE LOCAL_SYNTHETIC_PASS: 前回reset後も旧signup/inviteリンクとSQL合成PKCE codeからsession発行を再現。確認token2行/ユーザーconfirmation_token2値＋email/magiclink flow_state1行を限定失効する候補で旧signup/invite403/otp_expired・旧PKCE404/flow_state_not_found、新規確認リンク2/合成PKCE1成功。既存旧access4/refresh6/メール4経路拒否、新loginもPASS。reset前後のuser全列は指定token列のみdelta、ID/password/確認済み状態/identity/business/ACL/RLSflags保持、対象外token1保持、SAML空集合不変。実OAuth/PKCE発行全工程・他OTP/MFA非空・Hosted隔離は未検証。外部変更/送信0、cleanup残存0、Gate1 NO-GO。

Continuation stays in synthetic local validation; this does not adopt a hosted recipe or change the production preservation specification. Counterexample receipt synthetic-transient-credential-probe-result.json (14:53:05Z) proves that the prior recovery-only reset leaves signup/invite links and a synthetic email/magiclink flow_state credential usable. Candidate receipt synthetic-confirmation-pkce-reset-result.json (14:55:39Z) proves bounded rejection and new-credential success. Both files and their standalone mjs harnesses are ignored under .tmp/gate1-evidence-20260909; previous results remain separate. These standalone executed harnesses are the evidence source; prepare-* generation helpers are construction history, not authoritative replay commands.

The version-pinned [PKCE exchange implementation](https://raw.githubusercontent.com/supabase/auth/v2.192.0/internal/api/token.go) reads flow_state independently of sessions/one_time_tokens. The [admin link generator](https://raw.githubusercontent.com/supabase/auth/v2.192.0/internal/api/mail.go) produces signup/invite confirmation credentials without SMTP. PKCE fixtures are inserted directly into the owned local database and exchanged through actual Auth HTTP. A pre-reset positive control succeeds, so the later rejection is not inferred from a malformed credential. Initial fixture attempts returned500 because the synthetic shape used NULL for string-valued provider-token fields; changing those to empty strings, consistent with the model, made the control succeed. This tests exchange semantics, not an end-to-end browser/provider issuance flow or Hosted gateway.

The candidate adds only one_time_tokens confirmation_token rows, nonempty users.confirmation_token values, and flow_state WHERE provider_type='email' AND authentication_method='magiclink' to the prior local reset. NULL/empty recovery_token values are preserved rather than normalized on unrelated new users. Before/after equality allows only the declared2 confirmation fields/2 token rows/1 flow row plus the existing recovery/session changes. Other user columns (including confirmed timestamps and password hashes), identities, business and one unrelated reauthentication token are equal. SAML relay states are empty and remain empty; the flow-state FK cascade is explicitly recognized, and no noninternal trigger exists on the local reset targets. Nonempty SAML preservation is untested, not proven by the empty case.

Old signup/invite token hashes are rejected403/otp_expired; the retained old PKCE code/verifier is rejected404/flow_state_not_found. These probes do not change users or add sessions. Fresh signup/invite links reissued for the same users work and /user accepts their sessions; a fresh SQL-synthetic PKCE state also exchanges successfully for the original user. Fresh confirmation legitimately changes confirmed timestamps (and an invited account without a password may receive an Auth-generated password); those post-login changes are not asserted to match the reset baseline. Earlier magiclink/recovery token-hash+email-OTP rejection4, access4/refresh6 rejection, fresh email links2 and password logins2 remain passing.

Uncovered: signup/recovery/other-provider PKCE flow subtypes, email-change/phone-change/reauthentication credentials, nonempty MFA/SAML, external provider issuance, Hosted permissions/signing material/network isolation and final recovery cutover. This local candidate must not be described as complete all-credential revocation. Single primary, cached images, no install/real SMTP/hosted access/mutation/deploy/commit/push. Owned synthetic containers and network were independently absent after cleanup; operator-contract and diff checks pass.

## 2026-09-09 Approved bounded email-token reset: local acceptance

- 2026-09-09 Bounded email-token reset LOCAL_SYNTHETIC_PASS (user approved): Auth v2.192.0でrecovery_token種別2行削除＋users.recovery_token2列値を空文字へ変更（session/refresh削除も継続）。旧magiclink/recovery各token_hash・email_otp計4経路403/otp_expired、新session0。旧access4/refresh6拒否、新magiclink/recovery2件と元password login2件は同一user IDで成功。全user列の差分はrecovery_tokenのみ、password hash/identities/business/table ACL/RLS flags・対象外reauthentication token1行保持。未知trigger0/one_time_tokens被参照FK0、独立cleanup残存0。他OTP/PKCE/MFA非空/Hosted隔離は未検証、本番接続・送信・設定変更0、Gate1 NO-GO。

The user's approval extends only this synthetic/local investigation to the previously proposed recovery-token rows/column. The prior counterexample remains preserved. Evidence: ignored synthetic-email-reset.mjs and synthetic-email-reset-result.json, run started 2026-09-09T14:45:39Z, cached Postgres17.6.1.140/Auth v2.192.0. No production data, provider credentials or SMTP were used; admin generate_link created test credentials without sending email. No subagent.

With Auth stopped, one transaction deletes refresh_tokens and sessions, deletes one_time_tokens WHERE token_type='recovery_token', and clears users.recovery_token. A full before snapshot is transformed in memory by only the declared recovery-token deltas; exact equality of all retained user columns (including password hashes), identities, MFA factors (empty), synthetic business rows, table ACL/RLS flags and unrelated one-time-token rows is required after reset, after Auth restarts with independent signing material, and after every old-credential probe has finished. Two nonempty recovery fields and two corresponding token rows existed before reset; one unrelated reauthentication-token row is retained exactly. The candidate's session incoming FKs remain refresh_tokens/mfa_amr_claims cascade; one_time_tokens has no incoming FK and the four mutation targets have no noninternal triggers. This is local target evidence, not a hosted permission/dependency check.

Both token_hash and token+email requests for each old magiclink/recovery credential return403 with otp_expired and no access token; session count remains0. Four old access tokens and six refresh-token fixtures are also rejected. Two newly generated email links return200 with tokens accepted by /user and original user IDs, and password sign-in succeeds for both original users. Expected legitimate new-login Auth timestamps/sessions are not compared to the pre-login baseline. The receipt PASS means the bounded local behavior passed, not global credential revocation or network isolation.

No confirmation/invite/email-change/phone-change/reauthentication token, PKCE flow_state, nonempty MFA or hosted signing-key behavior is accepted by this test. The retained unrelated-token fixture intentionally demonstrates why this is not an all-token reset. Whole-Recovery HTTPS isolation and full hosted restore/cutover remain separate unclosed gates. Existing healthy-source online/RPO20-minute requirements are unchanged. The local containers/volumes/network were cleaned up; no dependency installation, hosted mutation, commit, push or deployment occurred.

## 2026-09-09 Old email-link counterexample and bounded next candidate

- 2026-09-09 Old email-link gap LOCAL_REPRODUCED: Auth v2.192.0の架空user向けgenerate_link（送信なし）で作った旧magiclinkはsession/refresh全削除＋別署名secret後もPOST /verify=200、同一userの新session発行。旧access4/refresh6拒否と元user/business保持は引き続きPASS。従来session-resetのみでは全旧認証情報失効にならない。次はone_time_tokensのrecovery_token種別＋users.recovery_tokenの限定失効を別local synthetic検証として提案（未実装、users全行保持条件への意図的delta）。PKCE/他OTP種別/Hosted経路隔離は別未検証。本番接続/設定変更/送信0、検証container/network残存0、Gate1 NO-GO。

Evidence: ignored synthetic-email-link-probe.mjs and synthetic-email-link-probe-result.json. The probe reuses cached Postgres17.6.1.140/Auth v2.192.0, creates only synthetic users and a locally signed admin credential, generates one magiclink without sending mail, records preservation before redemption, and submits token_hash to /verify without an API key after the reset/restart. HTTP200, access_token presence and exact user-ID equality establish the counterexample; no token, password or user identifier is retained in the result. PASS in this characterization means the gap was reproduced, not that isolation passed. The original session-reset result remains intact.

The version-pinned [Auth verify implementation](https://raw.githubusercontent.com/supabase/auth/v2.192.0/internal/api/verify.go) handles magiclink/recovery redemption and issues a session. Its [one-time-token model](https://raw.githubusercontent.com/supabase/auth/v2.192.0/internal/models/one_time_token.go) looks up a separate token table. This explains why deleting sessions/refresh tokens does not remove the old link. This probe called local Auth directly; it did not emulate or prove Hosted API-gateway behavior, source-to-target URL rewriting, or actual production outstanding links.

The next proposed local-only mutation allowlist adds auth.one_time_tokens rows WHERE token_type='recovery_token' and clears only auth.users.recovery_token, after original-state preservation is established and Auth is stopped. Retain sessions/refresh reset and distinct signing secret. Preserve all other user columns, IDs/password hashes, identities, MFA factors, business/history, schema/ACL/RLS; compare the exact declared delta separately. Fixtures must include separate old magiclink and recovery links, a token+email OTP path if available, and an unrelated token type to prove bounded deletion. Require old links to fail, fresh password login to retain IDs, and newly generated links to work. Unknown dependencies/triggers, undeclared row/column changes or unexpected successful old credentials stop acceptance. This candidate is not executed and is not a complete transient-credential reset: confirmation/email-change/reauthentication/phone tokens and PKCE flow_state require their own coverage. It changes the earlier all-user-columns-preserved invariant, so adoption requires the user's scope decision.

For network isolation, [PrivateLink](https://supabase.com/docs/guides/platform/privatelink) explicitly excludes Auth/API/Storage/Realtime; paying for that DB feature is not a resolution. The [API-key guide](https://supabase.com/docs/guides/getting-started/api-keys) separates legacy-key disablement from creating new keys. The official optional [Kong configuration](https://raw.githubusercontent.com/supabase/supabase/master/docker/volumes/api/kong.yml) contains unkeyed Auth verify/callback routes; it is self-hosted reference only, not Hosted proof. The [gateway changelog](https://supabase.com/changelog/48048-self-hosted-supabase-envoy-becomes-the-default-api-gateway-b) announces Envoy as the self-hosted default, so do not identify Kong as the current universal gateway. Whole-Recovery HTTPS isolation remains UNPROVEN. No network/key/provider configuration changed; healthy-source online and incident RPO20-minute requirements remain.

## 2026-09-09 Local synthetic session reset

- 2026-09-09 Session-reset LOCAL SYNTHETIC PASS (user approved): cached Postgres17.6.1.140/Auth v2.192.0、架空2user/4sessionで旧access4拒否・旧refresh4拒否＋未紐付/失効済fixture2拒否、同一user ID新login2成功。Auth停止後にtransaction内DELETE refresh_tokens→sessions、別synthetic JWT secretで再起動。削除直後/再起動後のusers（password hash含む）/identities/business全行とtable ACL/RLS flagsが完全一致。MFA factorsは空集合のみ、MFA実利用・実backup復元・hosted signing keys/全HTTPS隔離は未検証。専用container/network削除、外部変更0。local検証承認はhosted実行承認ではなくGate1 NO-GOを維持。証跡: `.tmp/gate1-evidence-20260909/synthetic-session-reset-result.json`（ignored、秘密値なし）。

The test uses localhost-only Auth and a dedicated Docker bridge, auto-confirmed example.test users, no SMTP/provider credentials, and no hosted connection. The bridge itself does not enforce outbound isolation. The observed incoming session foreign keys are refresh_tokens and mfa_amr_claims (cascade); no TRUNCATE, schema change, grant widening or trigger disabling is used for reset. Original-state comparison precedes fresh login because successful login legitimately updates Auth records. Business coverage is a synthetic row, not production history; policies and nonempty MFA behavior are not exhaustively tested. Initial harness startup/port and regclass-name assertion failures were corrected before the passing run. This demonstrates the combined reset plus changed signing secret, not session DELETE alone rejecting existing access tokens. Remote adoption remains pending and must retain the separately required restoration-preservation proof before an intentional session delta.

## 2026-09-09 Post-PR822: published repair passed native local revalidation

PR822 is merged into codex/comment-translator-paid-v1-preview at f1959bee32d42da2ed89902b41c8e71447e7d681 (head8c3858bdd1a7d89026913e9808ad29c3cf7009fb). The existing clean worktree was fast-forwarded and all13 published producer files matched in the executing processes. The user requested continuing the approved verification after merge. No main promotion or hosted write was performed.

Fresh capture attempt5 used the existing approved read-only acquisition recipe and restricted artifact store: six files,467983 bytes, checksum74672ms after T0, unchanged before/after managed catalog and Production writes0. Original backups/receipts were preserved. A new owned A12 local target used the previously approved Auth/Storage initialization and target-baseline preparation. Its driver was regenerated from the current tracked implementation so the shared4MiB reader was used; it did not retain the earlier copied1MiB function.

Native result: all six restore transactions and strict source-state comparison passed; the CLI applied34 pending migrations with exit0 and confirmed termination, yielding history22→56. Original49 tables/500 rows retained OIDs, row counts and data digests; the original22 history rows retained their complete-row digest. Auth/Storage catalog and security matched their recorded baselines; source-era structure and dependency edges were unchanged. The current reader captured a1134034-byte post-apply artifact and canonical/archive/shape checks passed as VALID_LOCAL_ONLY. No diagnostic fallback or separate recovery readback was needed. Active local cron jobs were0.

Both initialization candidates and the owned CLI project were cleaned successfully (two CLI cleanup actions). Independent enumeration confirmed owned containers, volumes, networks and scratch directories each0. The native execution receipts are separate from older observations: goal2-capture-attempt5.json, goal2-a12-restore-receipt.json, post822-independent-cleanup.json and post822-native-local-summary.json are retained under the ignored local evidence directory. The first reporting-only summary attempt failed without a captured classification; reinspection of the same immutable backup records and successful execution/cleanup receipts passed. This did not rerun capture or restoration, alter their receipts, or replace a failed native execution with a fabricated pass.

The published capacity repair is now verified against a real local PostgreSQL restoration/replay, in addition to the existing29 local test checks and ESLint acceptance of the exact merged patch. The primary agent directly inspected the native results and independently checked cleanup; no subagents or source edits. Operator contract and diff checks passed for this documentation update. Local-only artifact annotations and evidence remain insufficient for formal hosted Gate1 authority: hosted recovery capability is still unproven and Gate1 remains NO-GO. Further hosted restore/apply/deploy and main promotion retain their separate authorization boundaries. The prior Goal3 unpublished and Goal2 diagnostic sections below describe earlier checkpoints.


## 2026-09-09 Goal 3: bounded post-apply capture repair locally accepted, unpublished

Scope and design: the user authorized setting and executing the next local repair goal after Goal2. The measured post-apply query produced 1133757 bytes, so the former 1 MiB limit could not capture a complete catalog. Inspection also found the same limit in native psql normalization, the JSON parser and final artifact-reference validation. Use one exported POSTAPPLY_MAX_OUTPUT_BYTES = 4 MiB allowance across those boundaries. This gives bounded headroom above the measured catalog without introducing streaming/storage changes. An unrestricted increase was rejected; changing only the first reader would leave downstream failures.

The local Docker reader uses the shared limit for both spawn capture and UTF8 byte validation. The fixed collector opts into the same allowance through createPsqlTransport, whose optional numeric limit must be a positive safe integer no greater than 4 MiB. Its version probe and ordinary preflight default/parser stay at 1 MiB. Evidence-index validation and actual file reads allow 4 MiB only for catalog/catalog-expectations roles in previewReadback, rehearsalBackup, productionReadback and canonicalReadback. Other roles/stages, receipts and authority documents retain 1 MiB; the aggregate JSON allowance remains 32 MiB. Artifact hashes, metadata/path checks, exact JSON shape, target bindings, SQL and ACL/RLS/dependency comparisons are unchanged.

Verification: the initial four regression files reproduced the old capture/reference failures and invalid-limit handling. Final acceptance passed nine verification files (29 TAP checks across the final successful runs): postapply acquire, postapply query, preflight readonly, evidence, native postapply capture, runtime cleanup, postapply catalog, stage evidence and backup acquisition. All nine changed/new mjs files passed ESLint. The tests cover the observed size and exact 4 MiB acceptance; 4 MiB+1, multibyte UTF8 overflow, incomplete captures, ENOBUFS, signals, stderr and invalid bounds are refused without partial artifacts or automatic retry. Evidence tests exercise real bounded file reads for all four eligible stages and both roles, as well as wrong-stage/role rejection and the unchanged 32 MiB aggregate limit. Existing unknown-termination cleanup guards remain closed and their tests passed.

Native compatibility evidence is local only: real Node child-process pipes exercise the production psql transport's OS capture options at the observed size and boundary; they are not PostgreSQL execution. The retained Goal2 catalog was also emitted through a local Node pipe and accepted by the production transport, actual row parser and shape inspector: 1133756 bytes without its query newline, 1134034-byte stored artifact, history56, VALID_LOCAL_ONLY. Its original file hash is unchanged. The sanitized receipt is retained at the ignored local goal3-retained-catalog-compatibility.json. No DB connection, new backup, restore, migration, dependency installation or deployment occurred in Goal3.

The primary agent implemented, reviewed and verified directly; no subagents or model changes were requested. Repository configuration remains Astra Light; runtime model/effort was not independently observed. The producer set remains13 files and its public-source guard is not bypassed. The patch is locally accepted and unpublished; publication and formal DB execution remain separate steps. Prior Goal2 failure/recovery/cleanup receipts remain historical observations and are not promoted to formal Gate1 evidence. Goal3 is complete within its local scope, hosted Production recovery remains unproven and Gate1 remains NO-GO. The earlier Goal2 section's uncorrected 1 MiB limitation describes that earlier revision.


## 2026-09-09 Goal 2: full local restore and replay accepted; Gate 1 remains NO-GO

PR821 is merged at 28cd0b9c18ef4306f66fce6e718bfa12016e78c3. The published producer guard matched all13 files before fresh capture. Native capture attempt4 produced six artifacts totaling467983 bytes, including four gzip snapshot dumps. The checksum was observed75942ms after T0, the exporter closed successfully, the before/after Production baseline was identical, and Production writes were0. Original backups and receipts remain unchanged in their restricted local artifact stores; private data and paths are not embedded here.

The owned isolated A11 target restored all six transactions and passed strict source-state comparison before replay. Native CLI applied all34 pending migrations successfully with confirmed process termination. Readback matched the exact repository history56 rows (from22), canonical structure and archive structure. All original49 tables/500 rows retained their object identities, row counts and data digests; all original22 migration-history rows retained their full-row digest. Auth/Storage595 catalog objects and managed ACL/RLS/default privileges matched their recorded baseline. Source-era structure and native dependency edges matched before/after. Active cron jobs were0. This establishes the approved local full-restoration objective, including the repaired Windows binary transport.

Evidence qualification: the ordinary post-replay reader stopped at its1MiB output limit (POSTAPPLY_QUERY_CAPTURE_INVALID). The original failed restore receipt is retained. With the prior database query confirmed closed, an independent fixed read-only query read the same restored target using an explicit4MiB diagnostic bound and default_transaction_read_only=on. Its1133757-byte output passed all comparisons and the shape inspector as VALID_LOCAL_ONLY. The successful recovery receipt does not rewrite or convert the original failed receipt into a formal collector pass. The source collector remains capped at1MiB and must be repaired and verified separately before this route can supply formal Gate1 evidence.

Evidence chain retained under the ignored local auth-storage-review directory: goal2-a11-restore-receipt.json (original failure); goal2-a11-r2-recovery-receipt.json (successful readback, cleanup pending at that observation); goal2-a11-recovered-cleanup.json (subsequent cleanup PASS); goal2-a11-independent-cleanup.json (owned containers/volumes/networks/scratch each0). Both initialization candidates were removed after exact ownership/image checks, and native CLI stop confirmed termination before scratch removal. Unrelated resources and original backup evidence were preserved.

The primary agent performed verification and review directly; no subagents or model changes. This post-merge cycle changes only these operational documents, with no new commit/push/PR, deployment, hosted restore or remote migration apply. Goal2 is LOCAL_FULL_RESTORE_ACCEPTED; hosted Production recovery capability remains unproven and Gate1 remains NO-GO. The older checkpoints below are historical, including the pre-publication state.


## 2026-09-09 Goal 2: binary dump transport fix locally accepted, publication pending

The approved local source repair uses --compress=gzip for all four snapshot pg_dump commands. A shared decoder bounds both compressed input and decompressed output to32MiB, checks UTF8 without replacement, and preserves the original SQL bytes, including LF, CRLF and bare CR inside bodies/literals. Invalid gzip checksums, truncation, trailing padding/garbage, empty/NUL/invalid UTF8 and decompression overflow fail closed. No post-capture newline normalization is permitted. The existing reviewed pg_dumpall role recipe stays plain; this fix changes the four pg_dump streams that carry schema/data/history. The six-artifact format and existing transformation/restore transaction contract remain unchanged.

Every dump now records transport.encoding, stdoutBytes, stdoutSha256 and decodedBytes. rawSha256 remains the decoded raw SQL hash before restore transformations; the compressed native hash is kept separately. Acquisition, native process receipt and final stage validation require the exact transport shape, gzip for the four snapshot dumps, and matching plain byte/hash identity for roles. Older missing/plain snapshot transport observations cannot be used as proof of this repaired path. The decoder is included in the published producer guard (now13 files); the current uncommitted producer set correctly rejects the previous published110bc03 baseline. No source guard was bypassed.

Verification:104 tests passed across dump transport, capture, snapshot, acquisition, native process receipt, stage evidence, artifact store, SQL transforms and rehearsal restore. All11 changed/new mjs files passed ESLint. The local native contract (node scripts/comment-translator-paid-core-v1-gate1-backup-dump-native-contract.mjs --local-only) used the existing Windows pg_dump17.11 and one owned PG17 container: non-compressed stdout reproduced definition drift for3/3 synthetic LF/CRLF/bare-CR functions; gzip dump→production decoder→native restore matched both definition and body hashes for3/3. Its labelled tmpfs container cleanup passed. Native synthetic roundtrip evidence is separate from full Production-backup restoration.

Primary agent implemented, reviewed the actual diff and performed acceptance directly; no subagents or runtime changes. No dependencies, original backup bytes/receipts, hosted DB state, migrations or bridge hash rules changed. Commit/push/PR were not performed. The next boundary is publication approval; after an accepted published source is verified, a fresh capture and full restore/replay remain necessary to establish Goal2. Current state: LOCAL_IMPLEMENTATION_ACCEPTED / UNPUBLISHED, Goal2 incomplete, Gate1 NO-GO. The previous diagnosis below records the failed plain-text path and is not a claim about the repaired producer.


## 2026-09-09 Goal 2: target baseline resolved; exact restore blocked on dump line endings

The user approved the isolated target adjustment. The local adapter verified the complete CLI Webhook dependency closure (25 objects, no external dependents) before removing that initialization, removed only the extra sequence-default grants, and reproduced the existing Storage owner's grant options. Role attributes and memberships remained unchanged. The original six artifacts and their hashes/receipts were preserved. All six transactions ran as normal postgres; the captured sourceState fields now match exactly, including48 relations, history22, ACL/RLS digests and Auth FK state. This sourceState comparison does **not** prove every function definition matches.

A8 native CLI replay applied the four pending pre-bridge migrations, then stopped at20260811000000 with SQLSTATE P0001; history readback contains26 rows. A9's inline diagnostic failed to locate a query boundary because the working SQL uses CRLF; its independent read-only observer still captured the exact bridge state. A10 corrected only that diagnostic query handling and confirmed the same three mismatches inline. The bridge's own catalog query differs from its expected legacy state only at the definitionMd5 of the three legacy Paid functions; tables, triggers, ACLs and dependency counts match, and canonical scope is empty.

Read-only native function diagnostics in A10 found33/81/52 carriage returns respectively. All three raw definition hashes mismatch; replacing CRLF with LF **for comparison only** yields the expected MD5 for all three. No function body, backup artifact or bridge comparison was changed. The captured schema.sql contains3077 CRLF sequences and15 bare LF sequences (the producer also appends generated SQL). This is an exact-definition restore failure, not accepted recovery. PostgreSQL17's [official archiver implementation](https://github.com/postgres/postgres/blob/REL_17_STABLE/src/bin/pg_dump/pg_backup_archiver.c#L2285-L2298) leaves uncompressed plain-text stdout in Windows text mode; archive/compressed stdout uses binary mode. This explains the observed carriage-return insertion.

Proposed next source repair: use a binary-safe native dump transport, such as gzip-compressed stdout with bounded decoding in the producer, preserving original CR/LF bytes rather than normalizing SQL after capture. Keep compressed/native byte observation, decoded/transform hashes, snapshot binding, output bounds, child termination, deadline and durable receipt lineage explicit. Local regression must include original LF, CRLF and bare CR in function bodies/literals, corrupt/truncated/oversized gzip rejection, and native round-trip function hashes. The sourceState check must not substitute for definition readback. Publication and a necessary fresh capture remain subsequent steps; the current original artifacts remain immutable evidence of this failed path. No producer source repair or fresh recapture was performed in this target-adjustment slice.

A10 deliberately stopped before replay on the known native mismatch. Candidate cleanup2 and CLI cleanup2actions passed; independent final inventory confirms containers0/volumes0/networks0/scratch0. Production writes0; no hosted restore, deployment or activation. Goal2 remains incomplete, final history56/canonical/archive/source-era/data preservation after replay are NOT_VERIFIED, and Gate1 remains NO-GO. Primary agent alone performed implementation and diagnosis; no subagents or runtime setting changes.

Restricted evidence: auth-storage-review/goal2-a8-restore-differences.json, goal2-a8-failed-history.json, goal2-a9-observer-receipt.json, goal2-a10-target-profile.json, goal2-a10-native-bridge-diff.json, goal2-a10-function-line-ending-diagnosis.json, goal2-a10-restore-receipt.json and goal2-a10-independent-cleanup.json under .tmp/gate1-evidence-20260909/. This section supersedes the target-adjustment approval wait below.


## 2026-09-09 Recovery session reset candidate: design approval required

The official Auth OpenAPI inspected in this step exposes user-scoped /logout with global/local/others scope; it does not establish a project-wide revoke-all operation. Global means all sessions for that user, not all users. The retained candidate managed schema shows auth.refresh_tokens.session_id is nullable and references auth.sessions with ON DELETE CASCADE. auth.mfa_amr_claims also references sessions with ON DELETE CASCADE. Therefore deleting sessions alone cannot be assumed to remove every legacy/unlinked refresh token. This is static candidate-schema evidence, not hosted operational behavior or approval to modify Auth tables.

Concrete candidate for the next isolated verification: restore synthetic users and linked/unlinked/revoked refresh credentials into a local Auth-compatible target; prove the unchanged restoration first; then evaluate a separately bounded session-reset transaction against ONLY auth.sessions and auth.refresh_tokens, with the known cascade into auth.mfa_amr_claims. Preserve user IDs, password hashes, identities, enrolled MFA factors, business data, migration history, grants/RLS and all schema objects. Do not use TRUNCATE CASCADE or disable triggers. Verify exact expected row deltas and zero residual restored sessions/refresh credentials, retain pre-reset preservation evidence, and test old access/refresh rejection plus successful new login against a real local Auth service with independent signing material. Orphans, unknown dependencies/triggers, concurrent session creation or failed rejection tests must stop acceptance. Synthetic tests may not call SMTP/Stripe/YouTube or use Production tokens/data.

This candidate is NOT yet an accepted hosted recipe. It introduces an intentional Auth-data delta after strict restore and therefore needs explicit specification approval before implementation as a recovery operation. A real hosted target's exact managed version, permissions and complete dependency graph must be checked independently; the retained candidate schema is insufficient. No session rows were deleted, no tokens minted/revoked, no SQL reset script executed and no new test service started in this step.

Recovery isolation remains a separate blocker: key inventory/disablement is a potential API-gateway control but not an established complete isolation method for existing sessions, public API-key exceptions, direct DB and background jobs. Creating new keys does not revoke legacy keys. The current service-role consumers prevent silently disabling legacy keys or converting credentials as an incidental step. No key/configuration changes were made. Source healthy operation and the20-minute incident pause policy remain unchanged. Gate1 NO-GO.

Decision needed is limited to approving the synthetic/local session-reset investigation and its explicit expected-delta design; it does not authorize Production/Preview Auth mutation, hosted reset, healthy-source pause or public cutover. Owner remains the user; old Resend key remains retained. Preparation review only, primary agent, external mutations0.

## 2026-09-09 SMTP rotation reported; saved input and Turnstile recovery reference checked

The user reports creating a new Resend key, saving it in Production Supabase SMTP, receiving the Production signup email and logging in through that email. Delivery/login are USER_OBSERVED, not independent agent delivery tests. Last-used attribution was explained but no new-key log was inspected. The user explicitly retains the old key for now: do not delete, revoke or rotate it automatically. The signup screen displayed an account-creation failure despite email delivery/login; retain this as a separate unresolved registration-flow issue, not a confirmed cosmetic bug or SMTP failure. No repair, test-account creation or email sending was performed in this step.

The exact operator-designated file C:/Users/taka/KEYS/resend-streamer-tools.txt was read without displaying or using its content. It is a regular non-reparse file, readable, with one Resend-shaped key and no other non-whitespace content. ACL inheritance is enabled; no Allow entry resolves to Everyone, Authenticated Users or builtin Users, and no Allow identity translation failed. This proves local input availability and the stated narrow ACL check, not token validity, exact equality to Supabase's masked password, encryption at rest or portable disaster-backup availability. No copy or permission change was made. The earlier resend-api.txt was withdrawn by the user and must not be used as a trusted input.

Fresh Cloudflare Turnstile inventory identified one widget matching the previously observed Production NEXT_PUBLIC_TURNSTILE_SITE_KEY binding. It has two allowed hostnames and managed mode; the configured Supabase Site URL hostname is covered. The widget detail response exposes its existing secret through the authenticated read-only configuration view, and a private equality check matched the earlier Supabase SECURITY_CAPTCHA_SECRET observation. The comparison is cross-observation, not a simultaneous refresh of all systems. No challenge was solved, secret printed/persisted, or widget setting changed. Sanitized receipt: ignored turnstile-recovery-readonly.json.

Turnstile recovery method is now identified: use the same existing widget/sitekey/secret while preserving the already-allowed application hostname; retrieve through the owner's authenticated Cloudflare widget settings when executing an approved restore. SMTP needs no further key creation at this checkpoint, and CAPTCHA needs no rotation merely for preservation. A different recovery application hostname requires its own reviewed compatibility/configuration action before smoke; never assume an unknown preview hostname is covered. Availability of the owner's Cloudflare account at incident time and actual Recovery configuration/readback remain separate requirements.

Next unresolved Gate1 work is Recovery-only HTTPS protection, complete session invalidation/reauthentication and supervisor/pause-oracle/hosted execution evidence, plus the private non-secret Auth supplement. The saved SMTP input and retrievable matching Turnstile reference narrow the credential-preservation gap but do not constitute hosted recovery acceptance. Gate1 NO-GO; primary agent only, external mutations0, existing three documents only.

## 2026-09-09 Resend SMTP recovery input identified

Fresh Supabase Dashboard observation confirmed custom SMTP enabled with smtp.resend.com. Read-only Resend connector inventory returned two API key metadata entries, including Kuro Streamer Tools, and one verified sending-enabled domain. The privately compared Supabase sender domain matches that verified domain. No key values, email contents or recipients were requested. The list is metadata only: it cannot establish which key is currently configured behind Supabase's masked SMTP password, or whether a usable copy exists in the operator's secret store.

[Resend SMTP](https://resend.com/docs/send-with-smtp) uses a Resend API key as the SMTP password. [API key documentation](https://resend.com/docs/dashboard/api-keys/introduction) states that the token is shown once at creation and supports sending-only permission scoped to a domain. Consequently, do not keep attempting to reveal the old value from Resend or Supabase.

Concrete next decision: if the operator has a saved original key, retain its protected reference and validate the applicable scope through the provider without printing it. If no usable copy exists, propose a separate key named Gate1 Recovery SMTP with sending_access restricted to the existing verified sender domain. It would be used only by the future approved recovery project; leave both existing keys and current Production SMTP unchanged. Creating a credential is a separately approved external security action, not implied by this read-only investigation. Before creation, agree on the protected storage destination; preserve the one-time token there without chat/log output. The existing create-key connector's display instruction must not override the user's no-secret-output boundary. No key was created, changed or deleted and no SMTP authentication or test email was attempted.

The remaining SMTP input is therefore a saved-key reference or authorization and protected handling for this exact recovery-only key. Domain verification and service identification are closed; credential availability and delivery validation remain unproven. Test delivery later requires explicit recipient/purpose authorization. This does not close recovery isolation/session/supervisor gates; Gate1 NO-GO remains.

## 2026-09-09 Preservation and reauthentication decision packet

Preparation goal: define the next executable input and test packet, not perform export, revoke sessions, configure isolation or prove hosted recovery. The242 field names in the already-observed Auth response are inventoried in ignored recovery-auth-field-manifest.json:204 private-configuration fields and38 secret-reference fields, exact unique coverage242. Values are absent. Credential-key/token fields are included in the secret-reference classification; password-policy booleans are ordinary configuration. Private configuration may still contain email addresses, callback URLs and mail contents and must never be printed or committed as values. The inventory is not a writable API schema or a backup; never replay all242 fields as a PATCH. Inactive provider/hook/SMS credentials are not collection targets. Match applicability and the writable field schema for the actual source/target before creating the supplement.

The next preservation operation is restricted local output only: preserve original18-field export unchanged; create a separate dated supplement under the already-used protected backup boundary only after confirming the exact allowed directory, inherited ACL and no broad read grants. Include source observation time/binding digest, selected field names, private values for active configuration and a checksum receipt. It must cover SMTP host/port/sender/rate; applicable MAILER_SUBJECTS and MAILER_TEMPLATES entries; CAPTCHA provider with secret reference and matching application site-key/hostname; session/JWT/refresh/MFA/rate settings; active provider/site/redirect selections. Mark defaults/inactive fields explicitly, and stop on an unknown field instead of silently omitting it. SMTP_PASS is masked and must point to the user's retrievable original secret-store item. Do not copy masked placeholders, generate replacements, collect unrelated disabled-provider secrets or export the source signing key. No new private-values artifact has been written in this goal; the exact directory/secret-reference retrieval input remains unresolved.

### Publication and reauthentication acceptance tests

| Test / order | Input and expected result | Limit / stop condition |
| --- | --- | --- |
| P1: recovery target binding | Approved new target, independent project credentials/signing material, reviewed app source and protected supplement; all source/target bindings checked before restoration/cutover | No project exists yet. Source healthy-state operations remain unchanged |
| P2: restriction coverage | Operator-only app access across all actual hostnames and direct-service restrictions verified separately; no public callbacks, webhook success acknowledgement or schedulers during setup | Worker Access does not prove Supabase HTTPS isolation; no supported complete mechanism established. This remains a design blocker, not waived by unpublished keys |
| P3: source preservation before session changes | Exact restored history/data/Auth/security assertions before any approved session invalidation; retain immutable backup and comparison receipts | Do not delete/truncate auth tables to manufacture reauthentication. Any approved session mutation needs a separately declared expected delta |
| P4: old access token rejection | Use a specifically authorized test account's old access token through a private test fixture against bound Recovery; require definite authentication rejection, with no token values in logs | Old source signing secret must not be reused. Timeout/403 from an unrelated edge gate/absence of browser cookies is not evidence that Recovery rejected the token |
| P5: old refresh token rejection | Same authorized test account's pre-recovery refresh credential; require definite refusal to issue a new session after the approved invalidation mechanism | A refresh attempt may mutate session state or rotate a token if accepted. No production-user token extraction, no live attempt in preparation; accepted refresh is a hard failure |
| P6: new login and reopen | Operator creates a new session with preserved account identity through CAPTCHA and approved email flow as applicable; reads expected account/Free state on Recovery, then rechecks writer/scheduler disposition before explicit reopening | Login/email/provider/session tests are writes or communications; each needs its approved fixture. One-account success does not establish all restored sessions were revoked |

[Supabase sign-out semantics](https://supabase.com/docs/guides/auth/signout) revoke refresh credentials for affected sessions but leave access tokens valid until expiry under their trusted signing key. The installed auth-js admin signOut accepts a JWT, not a user ID or an unconditional project-wide revoke flag; the application signOutAction only operates on its current session. Neither is a demonstrated all-user Recovery invalidation tool. Do not create impersonation tokens or ask users to supply credentials to make it work. The reviewed supported project-wide/session invalidation mechanism and evidence for all affected sessions remain unselected; if a targeted Auth-data mutation is ultimately required, it is a material design/approval change with exact preserved-user/security invariants.

Execution decision: preparation packet complete, EXECUTION_NOT_READY. Required decisions/inputs are a supported recovery-only HTTPS protection method, supported complete session invalidation with approved test fixture, and retrievable SMTP/Turnstile references plus exact protected supplement destination. No further healthy-source freeze, plan upgrade or key migration is implicitly required or authorized. Direct primary review, field-manifest check, operator-contract and diff checks pass; no source code changes or external mutations. Gate1 remains NO-GO.

## 2026-09-09 Solo-operator ownership accepted; source/recovery boundaries clarified

User-confirmed operating model: the user is the sole incident decision maker, Supabase/Cloudflare operator, secret custodian and reopening owner. No additional external writer/job is known to the operator. This is a bounded operator declaration, not an independently proven absence of all external writers. Known application integrations (Stripe/YouTube), Cloudflare builds, database Cron and manual management interfaces remain in the inventory. During the incident the operator must avoid unrelated dashboard edits, SQL/script executions and deployments, and keep other Codex tasks from writing the target. No second person or separate team assignment is required. This supersedes previous owner-awaiting-response entries.

Correction to scope: the approved specification explicitly keeps Free online during final backup and accepts writes after T0 up to source pause within20 minutes. A global healthy-source write freeze before DDL is NOT a new acceptance prerequisite. Keep the existing5/10/20-minute watchdog contract; do not pause healthy Production to satisfy a newly inferred isolation requirement. Source emergency inaccessibility and recovery pre-publication access are distinct checks. The prior writer matrix is an inventory for incident coordination and recovery reopening, not permission to impose a zero-loss maintenance window.

Control assessment: [Data API security](https://supabase.com/docs/guides/api/securing-your-api) offers Data API disablement and PostgREST pre-request checks, but the latter does not cover Realtime/Storage/other products. These are not evidence of a single Auth/REST/Storage kill switch. Disabling Data API would also interrupt the application's Supabase data clients and cannot be used during a normal Free smoke without staged re-enablement. No such setting, grant or policy was changed. The documented DB IP restriction and Worker Access limits in the following proposal still apply. No supported whole-recovery HTTPS isolation mechanism has yet been established in this account; do not claim it is impossible on every plan or silently mark the requirement satisfied.

Reauthentication also requires care: the official [Auth migration guidance](https://supabase.com/docs/guides/troubleshooting/migrating-auth-users-between-projects) distinguishes restoring user rows from JWT signing-key continuity. Do not import the old signing secret into Recovery. New access-token signing keys alone do not prove all restored refresh sessions unusable; preserve the approved Auth data and require an explicit reviewed session invalidation/reauthentication procedure and old-session test before acceptance. No Auth table deletion, global logout or key rotation is authorized by this note.

Next execution packet is now concrete: (1) the user retains SMTP credential retrieval ownership; its masked Dashboard value cannot be a backup; prepare references through the existing secret store without asking for values in chat; (2) privately preserve the non-secret Auth supplement and matching Turnstile references once the exact protected output/field handling is reviewed; (3) resolve the recovery-only HTTPS access and old-session validation method without changing normal source availability; (4) then request only the exact necessary configuration/incident actions, preserving no-cost and healthy-source constraints. Source-state preservation, hosted pause oracle, supervisor readiness and hosted restore/cutover are still independent gates. Single primary agent, documentation only, no external mutation; Gate1 NO-GO.

## 2026-09-09 Concrete preservation and restriction proposal (not applied)

Additional-secret readiness: the previously observed SMTP_PASS is masked, so saving the Dashboard response cannot preserve a usable SMTP password. The CAPTCHA provider is Turnstile; login/signup/reset-password pages consume NEXT_PUBLIC_TURNSTILE_SITE_KEY and account actions submit the CAPTCHA token to Supabase Auth. The provider secret, application site key and permitted hostname must remain compatible; do not rotate or disable either side as a workaround. Raw secrets and template content were not exported in this step.

| Proposed packet | Exact content / handling | Closure condition |
| --- | --- | --- |
| Non-secret Auth supplement | Selected mail host/port/sender/rate settings, subjects/templates, session/JWT/refresh/MFA/rate settings, enabled-feature selections, source/version/timestamp and field manifest. Keep private URLs/email/template content in the existing restricted backup boundary, separate from Git and sanitized receipts | Review the field manifest and preserve privately without overwriting the original18-field file. Hash/count evidence only in repository output; actual target readback still required |
| Independent secret references | SMTP credential's existing secret-manager item reference and version, Turnstile secret reference plus matching site-key/hostname configuration, existing YouTube encryption references | Named operator can retrieve the original value through its normal approved secret store. A masked field, mere presence, or an inaccessible reference is not PASS. Do not ask for secret values in chat, scrape broad credential stores or mint replacements |
| Recovery-project credentials | New project-scoped public/server credentials, issued only for the approved recovery project, retained through the reviewed protected-input path | Cannot be completed before provisioning; never copy old project-scoped keys or assume service-role and secret-key consumers are interchangeable |

Proposed application restriction: one Worker-scoped Cloudflare Access application for v-streamer-tools, covering production and previews, with only the specifically designated incident operator allowed. Do not use account-wide protection or allow all account members/domain members by default. Inventory pre-existing hostname/path rules first because they take precedence over Worker-level rules. Prerequisites are Zero Trust availability, required permissions, cost acceptance, exact Worker identity and the named operator; none is inferred from a logged-in Dashboard. No Access application was created or changed.

[Cloudflare's documented scope](https://developers.cloudflare.com/workers/configuration/cloudflare-access/) covers routes, Custom Domains, workers.dev and previews. Worker-level protection rejects WebSocket upgrades; include this limitation in smoke acceptance. It does not establish a pause for already-running requests, scheduled handlers, service-binding callers or independently connecting Supabase clients. Existing source has no matching WebSocket/EventSource/Upgrade/DurableObject references in the bounded app/lib/workers scan, but that is not deployed-runtime proof.

[Supabase Network Restrictions](https://supabase.com/docs/guides/platform/network-restrictions) cover Direct Postgres and pooler connections, not HTTPS Auth/PostgREST/Storage. Therefore this proposal does NOT combine Access plus a DB IP allowlist and label the result a global freeze. DB restrictions, if later approved, must include the actual operator's IPv4/IPv6 paths and retain TLS. The dedicated control for recovery HTTPS services remains unresolved. Keeping new credentials/URLs unpublished reduces accidental exposure but is not a verified isolation mechanism. Disabling signup alone leaves signin/token refresh and other services; changing RLS/grants or CAPTCHA would change approved restore/security semantics and is not authorized.

Owner matrix to complete before any activation: incident decision/reopen owner; Cloudflare Access/deploy operator; Supabase pause/provision/restore operator; SMTP/Turnstile/YouTube secret custodian; external webhook/job/DB-writer owner; supervisor host operator. The user was asked to identify the operator and external writers. No role/account assignment was inferred. Neither current checkout nor provider-linked Production source contains .github workflow definitions; this is not proof that external CI, Cloudflare builds, dashboard jobs or external credential holders are absent.

Acceptance remains PREPARED_NOT_APPLIED. Concrete next inputs are operator/writer ownership and retrievable SMTP/Turnstile references (references only), followed by review of the private supplement manifest. Global recovery HTTPS isolation still needs a supported mechanism or an explicitly reviewed design revision. There is no approval request to pause healthy source, provision, send email, create Access rules, rotate credentials, deploy or reopen writes in this step. Existing5/10/20-minute policy and Gate1 NO-GO remain unchanged.

## 2026-09-09 Recovery coverage goal: preparation accepted, execution remains closed

Scope: enumerate applicable recovery configuration and writer entry points, and define exact preservation/restriction/reopening checks without applying them. Source scope is the current f1959be checkout plus the provider-linked Production source5d91202. Route and Server Action inventories were checked in both revisions; paid-maintenance and the scheduled wrapper belong to the newer source and must not be assumed deployed. The goal is preparation acceptance, not complete hosted recovery or a claim that every external credential holder is known.

### Additional configuration inventory

The authenticated Auth response observed at2026-09-09T12:26:26.661Z was classified without printing or persisting its raw values. SMTP host/user/secret are present; CAPTCHA is enabled;13 mail-template fields contain content. Nonempty templates are not automatically custom: compare against the matching platform default before deciding what can be omitted. Enabled Auth hooks0, SAML disabled, session timebox/inactivity limits absent, single-session restriction false and refresh-token rotation enabled. These observations extend the selected18-field export; they do not extend that protected export's contents. No additional secret export was performed.

A fresh read-only Production inventory returned Edge Functions0, Storage buckets0 and Realtime publication tables0; public noninternal triggers1. The trigger remains part of the existing strict catalog/dependency restore checks; do not disable it to claim write isolation. Realtime publication count0 does not prove that Broadcast/Presence or every Realtime project setting is unused.

| Configuration family | Required preservation and acceptance | Current evidence / remaining input |
| --- | --- | --- |
| Auth selected settings | Preserve original export/checksum and exact provider/site/redirect values; private target readback before login |18 selected fields semantically match current source; already preserved |
| SMTP and email delivery | Preserve secure credential references, sender/host/port/rate settings and all relevant mail subjects/templates; compare target values privately; separately authorized single-recipient delivery test | SMTP configured;13 template fields populated; protected export/reference completeness and default-template comparison missing. Do not send test email in this preparation |
| CAPTCHA | Preserve provider and secure key reference; verify allowed recovery application hostname with provider; rebuild matching public site-key configuration where consumed | Enabled; provider/key/hostname compatibility not yet accepted. Do not disable CAPTCHA to obtain a passing login |
| Sessions, security, limits | Inventory JWT/session/refresh/MFA/rate settings and exact applicable values in restricted recovery material; classify defaults against target platform | Partial observed configuration only. Restored Auth rows do not establish equivalence of these project settings |
| Hooks / SAML / external providers | Recheck active selections before final backup; preserve any newly applicable endpoints and secret references | Hooks0/SAML disabled; selected provider inventory Email-only. Newly enabled entries reopen preparation |
| Storage / Realtime / functions | Recheck zero-object/blob prerequisite; capture applicable project settings and publication/trigger catalog; identify any external producer | Buckets0, objects0 from preceding read, Edge Functions0, publication tables0. Broadcast/Presence/settings coverage unproven; no blob migration authorized |
| Application and independent secrets | Use provider-linked Production source, captured build/runtime configuration and referenced independent encryption-key versions; replace only recovery-project scoped credentials | Build linkage observed, artifact bytes not verified. SMTP/CAPTCHA/YouTube encryption and OAuth/Stripe references must not be blindly rotated with Supabase credentials |

### Writer coverage and boundary matrix

A HTTP verb filter or Translator allowlist is insufficient. Before opening a recovery endpoint, the incident operator must provide the exact deployed rule identifiers and coverage for all application hostnames including workers.dev/preview aliases, plus the independent Supabase endpoints and non-HTTP writers. No new access rule or database privilege change is approved by this checklist.

| Writer entry / source anchor | Possible effect | Required incident restriction and proof |
| --- | --- | --- |
| Account Server Actions: app/account/actions.ts and billing/actions.ts | Auth signup/signin/password/reset/logout, user_preferences upsert, YouTube connect/disconnect, Checkout/Portal | Cover Server Action POST requests on page URLs, not just /api. Block public application traffic before middleware/action execution; no Checkout or email test during isolation |
| Translator Server Actions: app/tools/comment-translator/actions.ts and session/feed/account/retention-waitlist-actions.ts | Start/stop/heartbeat, usage/feed persistence, retention/deletion, waitlist, credential refresh | Restrict every action entry and existing session poller; verify already-open tabs and in-flight/background work cannot continue writing after the accepted boundary |
| Session/harness APIs: app/api/comment-translator/session/route.ts and free-beta/route-api-harness/route.ts | Command dispatch and persistence | Include both routes and all accepted methods in edge coverage; keep harness/runtime gates closed |
| GET callbacks and credential-status: app/auth/confirm/route.ts, youtube/oauth/callback/route.ts, youtube/credential-status/route.ts; middleware.ts | OTP/code exchange, token persistence/refresh and Auth session work | Treat GET as potentially state-changing. Block callbacks and relevant page requests before application Auth middleware; ordinary page GET is not a safe isolation probe |
| Stripe webhook: billing/webhook/route.ts | Signed entitlement/lifecycle reconciliation | Block or use an approved provider-compatible deferred-delivery procedure; never acknowledge discarded events as success. Verify replay/idempotency and resulting entitlement state before reopening; provider changes/replay need their own authority |
| Maintenance/schedulers: paid-maintenance/route.ts and workers/comment-translator-paid-open-next-wrapper.mjs | GET-triggered reconciliation, outbound calls, scheduled writes | Keep database Cron and Cloudflare fallback inactive; inventory independent scheduler/manual invocation. Current Production has no fallback selector/Cron triggers, but current repository configuration differs |
| Direct Supabase clients: /auth/v1, /rest/v1 including RPC, /storage/v1, /realtime/v1 | Bypass application edge; Auth token refresh/signups, policy-authorized writes, service-role operations | Application hostname restrictions do not cover these endpoints. Require an approved supported isolation mechanism or keep recovery inaccessible/unpublished until ready. RLS does not block service-role holders. Do not widen/change grants to simulate a freeze |
| Direct DB/pooler, dashboard/CLI/SQL, CI/scripts and external service-role holders | Bypass HTTP edge and app gates; triggers execute with initiating writes | Inventory executing operators/jobs and credential references, quiesce approved writers and verify no in-flight writes. DB source pause remains the bounded-RPO source boundary; local tests cannot prove it |

### Ordered acceptance before incident execution

1. Finalize protected configuration/reference completeness, exact Production rebuild inputs, operator identity and hostname inventory. Confirm no unclassified writer or setting remains; a reference without retrievability/target compatibility is insufficient.
2. Review exact application-edge restrictions and independent Supabase/direct-DB isolation. Record owner, target, rule IDs, allowed operator, bypass-host coverage and rollback/reopening action. If the Free plan cannot support the required isolation, stop and revise the incident design under separate authority; do not assume an app allowlist covers Supabase.
3. Accept supervisor host/lifetime, immutable capture/executor receipt binding and actual paused-source oracle behavior. Keep the source healthy during preparation; no pause rehearsal is implied.
4. In an authorized incident, follow existing final-backup/arm/5-10-20-minute policy. Confirm source inaccessible before recovery provisioning; keep schedulers inactive, recovery unexposed and public callbacks/webhooks withheld. Restore and compare exact catalog/history/data/security, then privately read back all applicable configuration.
5. Under separately approved cutover/smoke authority, allow only the designated operator through every applicable boundary. Verify login/confirmation/CAPTCHA/email as specifically authorized, restored account identity, rejection of old credentials, retained independent decryption keys and route behavior. No real provider/session/Checkout exercise without its separate fixture/authority.
6. Recheck source remains paused, target state/canonical assertions, no unintended scheduler selection, and webhook/reconciliation disposition. Only then apply the reviewed reopening action with explicit authority. Observe new writes on Recovery, retain receipt evidence, and do not auto-unpause/delete the source.

Verification for this preparation is source/observation reconciliation plus operator-contract and diff checks; no live deny probe, login, webhook delivery or mutation was performed. Such probes can themselves create Auth/usage writes and require the approved incident test fixture. Additional sanitized observation is retained in ignored hosted-extra-auth-inventory.json. Unclosed execution gates: secure additional-settings preservation, exact enforceable isolation rules and writer/operator ownership, supervisor and actual pause oracle, recovery provision/restore/cutover. Preparation goal accepted; Gate1 remains NO-GO.

## 2026-09-09 Live Auth comparison and Production Worker observation

Using the authenticated Dashboard read-only session, the preserved18-field Auth selection was compared with current Production:14 configuration fields match semantically, Email is the sole enabled provider,24 provider buttons are disabled, custom providers0 and third-party API list0. Site URL matches exactly; the four redirects match as a set with order-only difference. API empty password-required-characters corresponds to the UI's saved No required characters (default) label. No settings or saved export were rewritten. This closes fresh comparison of the selected export, not complete Auth/SMTP/template/hook/session configuration coverage.

Wrangler4.95.0 deployment-list read failed on authentication; it was not retried or used as evidence. The authenticated Cloudflare Dashboard then showed Production Worker v-streamer-tools active version f45136b2 at100% traffic, deployment timestamp2026-08-12T19:03:10.040427Z. Its displayed build link identifies main commit5d91202c11a18dbd748bcba14a3b7b3a9dde738c (PR768). This is provider-reported build linkage, not independent downloaded artifact byte identity. Do not substitute current Preview/Gate1 code for the emergency rebuild source without review.

The Production environment bindings API returned24 bindings with no COMMENT_TRANSLATOR_PAID_SCHEDULER_AUTHORITY; the Dashboard explicitly showed no Cron triggers. Thus the observed deployed configuration does not select cloudflare-cron-fallback. This is a point-in-time configuration observation, not an invocation test or proof of future state. Repository wrangler.jsonc currently contains a five-minute trigger and must not be treated as the deployed configuration or blindly deployed during recovery.

Sanitized receipt: ignored hosted-auth-worker-readonly.json; no credentials, redirect values or raw API payloads are persisted there. Remaining operational gaps include actual hosted pause-oracle behavior, accepted supervisor host/lifetime and native receipt bindings, complete applicable configuration coverage and edge restriction/writer coverage, recovery provisioning/restore and cutover acceptance. No healthy project was paused, watchdog armed, function invoked or external setting changed. Gate1 NO-GO remains. Primary agent only; existing documentation updated, no source edits.

## 2026-09-09 Recovery input reconciliation

The existing protected Auth configuration artifact was read at its previously approved exact location. Its706 bytes and SHA256 match the saved original; all18 selected fields parse. It contains Email as the sole enabled provider,24 disabled providers, custom/third-party counts0, one valid Site URL and four nonempty redirect entries. Current ACL inspection found no Allow entries for Everyone, builtin Users or Authenticated Users; inheritance remains enabled. Values were not printed or rewritten. This proves preservation of the selected export, not complete project configuration coverage or a fresh comparison with hosted Auth settings. The older September8 statement that URL values were not exported describes an earlier checkpoint and is superseded for these selected fields.

A fresh Production read-only transaction returned Storage objects0, Vault records0 and active database Cron jobs0. Storage blob migration is outside the approved scope: the design requires zero objects, and any nonzero result at final-backup preflight is a hard stop. Likewise nonzero Vault records require a revised root-key recovery design. These point-in-time counts must be rechecked before final backup; they do not prove Cloudflare fallback is inactive.

Source inspection confirms the existing cutover consumer names and the wrapper's cloudflare-cron-fallback selector. The native paused-source oracle still requires exact INACTIVE status, Direct port5432 connection refusal, and REST/Auth HTTP503. No actual paused-project observation was performed. Normal healthy-state checks cannot validate that oracle. The published source commit is f1959bee32d42da2ed89902b41c8e71447e7d681; the older runner paragraph's unpublished label is historical, while hosted acceptance remains unproven.

Remaining evidence is now narrower: fresh private comparison of selected Auth settings (plus coverage of any additional applicable recovery settings), actual Production application identity and fallback state, approved supervisor lifetime/native receipt bindings, tested hosted pause oracle, and complete edge restriction/writer coverage. Recovery credentials cannot be verified before a recovery project exists. Preserve the incident-only provisioning and separate cutover/reopening approvals. Do not request a new Auth export merely because an older checkpoint predates the preserved file. No hosted mutation, watchdog arming, credential changes or source edits occurred. Gate1 remains NO-GO.

## 2026-09-09 Hosted recovery preparation: live inventory

Read-only Management API observations confirm the selected organization is Free and both Production and Preview are ACTIVE_HEALTHY in Tokyo (ap-northeast-1). No dedicated recovery project appeared in the returned inventory. Project cost estimate is USD0/month; it proves neither a reserved slot nor successful provisioning. No cost confirmation, creation, pause, restore, configuration mutation or cutover occurred.

Official [billing guidance](https://supabase.com/docs/guides/platform/billing-on-supabase) allows two active Free projects and excludes paused projects from that limit. The observed two healthy projects leave no third active Free slot. Retain the approved incident-only sequence: confirm the failed source is paused/inaccessible, then provision in the same organization and Tokyo region. Do not pause healthy Production or Preview for rehearsal or buy capacity. Actual provisioning, PG17 compatibility and recovery time remain UNPROVEN.

Concrete remaining checklist:

1. Bind the source and fresh final backup to the restricted six-file checksums. Accept the final backup within the approved five-minute T0 window before first DDL; prior local replay does not replace this checkpoint.
2. Prepare the watchdog and source-inaccessibility checks; arm immediately before first DDL after backup acceptance. On failure or T0+10 minutes request pause, confirm inaccessibility by T0+20 minutes. Request and confirmed pause are separate evidence. Never automatically unpause.
3. After the incident-only prerequisite and action-specific authority, recheck cost/capacity, provision and record actual PG17 version, region, extensions and managed Auth/Storage baseline. Stop on unexplained differences; local baseline preparation does not authorize hosted managed-schema/privilege changes.
4. Securely inventory Auth providers/redirects, API credentials, Realtime settings and applicable Edge Functions. Require Storage object count zero; stop on nonzero objects instead of expanding into blob migration. Record completeness or evidenced non-applicability without secret values. The official [restore guide](https://supabase.com/docs/guides/platform/migrating-within-supabase/dashboard-restore) requires separate project settings and explains that Storage metadata does not restore object bytes. This turn did not export or validate completeness of these settings/objects.
5. Restore only to the approved recovery target with the bounded fail-closed recipe; verify data, history, security and source-era dependencies. Validate endpoint/credential cutover, rotation and client reauthentication before reopening writes. Provisioning, restore, configuration/cutover, reopening and later source unpause/deletion retain separate authority.

Acceptance: LIVE_FREE_PLAN_AND_COST_CONFIRMED; HOSTED_RECOVERY_UNPROVEN; Gate1 NO-GO. Actual target provisioning/compatibility, complete settings/object inventory, live watchdog timing, restore duration and end-to-end cutover remain unproven. Primary agent only, no subagents. Existing documents only; no source changes.

## 2026-09-09 Goal 2: fresh backup captured; isolated restore blocked on target baseline

PR820 is merged at `110bc03149d4904abd0293fb203fa0f48ae3a836`; the existing worktree was fast-forwarded and all12 producer files matched the published source. Fresh native capture produced six artifacts (471938bytes), four identical-snapshot dumps, and durable acquisition/process records. Checksums completed79459ms after T0; the outer receipt was created118694ms after T0. Managed baseline before/after matched. Production writes0; subsequent read-only diagnosis matched the captured sourceState and observed idle transactions0.

All six artifact transactions completed as normal postgres in an owned isolated target. Strict readback stopped before replay on `rowCounts` and `grantsRlsSha256`. A second diagnostic run with the same immutable backup identified the entire difference:

- Source48 relations are present. Target adds three empty CLI Webhook tables: `net._http_response`, `net.http_request_queue`, `supabase_functions.hooks`.
- Security rows: source662/target713, source-only0. The51 target-only rows comprise net33, supabase_functions15 and its supabase_admin default ACLs3. CLI2.109.0 initializes this Webhook schema and pg_net unconditionally in its normal database startup template ([official template](https://github.com/supabase/cli/blob/v2.109.0/apps/cli-go/internal/db/start/templates/webhook.sql)).
- Three shared rows differ: postgres/public sequence defaults retain UPDATE for anon/authenticated/service_role on the target, while source has only owner privileges; storage.buckets/objects have the already explained owner-only grant-option representation difference. This is not accepted as an exact hash match.

Both runs cleaned up their two candidate containers and two CLI cleanup actions. Independent final inventory confirmed containers0/volumes0/networks0/scratch0 (goal2-a4-independent-cleanup.json). Replay22→56 remains NOT_EXECUTED. No backup recapture, mutation of original artifacts, comparison relaxation, hosted restore, deploy or Production DDL was performed.

Next proposed target-only adjustment is reviewable in ignored `goal2-target-baseline-proposal.sql`: before importing data into a newly owned empty target, inspect the complete dependency closure of the CLI-only Webhook objects, remove only that initialization (schema removal needs specific approval), reset the three extra sequence-default grants, and reproduce the existing owner's grant-option representation without adding non-owner privileges or role membership. Recheck raw security/catalog and all six original artifacts strictly before replay. No proposed DROP or GRANT has been executed. This supersedes the previous publication/clock wait checkpoint; Goal2 remains incomplete and Gate1 NO-GO.

Evidence is restricted under `.tmp/gate1-evidence-20260909/`: `goal2-capture-attempt3.json`, `auth-storage-review/goal2-a3-restore-receipt.json`, `goal2-a4-restore-receipt.json`, `goal2-a4-security-differences.json`, `goal2-source-security.json`. Raw SQL/data and connection values are not copied into this document. Primary agent implemented and inspected the operational drivers directly; no subagents.

## 2026-09-09 Goal 2: clock fix locally accepted, publication pending

ユーザー承認の時計修正をローカル実装・検証した。DB T0が受信したPC時刻より最大1000ms未来でも許容し、それを超える値はmicrosecond精度で拒否する。DB T0原文を変更しない。受信時の残り時間は `300000 - (hostNow - Date.parse(T0)) - 1000` msで固定し、以後は単調時計で減算する。許容差を5分枠へ加算しない。dump timeout・held persistence前後・exporter closeも同じ期限を使用する。`elapsedMs` はこの保守的な消費予算を表す。PC観測時刻は実時刻のまま保存し、逆行・非数値は失敗としてcleanupする。

取得record・native process receipt・stage validatorはDB/PCをまたぐ順序のみ1秒を許容し、PC内のdump順序・checksum/close/persistence順序は厳密に保つ。完了上限は `T0 + 299000ms`。78関連tests PASS、最終microsecond境界追補後のsnapshot/capture32tests PASS、変更9mjsのESLint/syntax・actual diff review・diff check PASS。primary単独で実装・検証。今回remote操作なし。この状態はUNPUBLISHEDであり、公開済producer照合を迂回して取得しない。ソースのcommit/push/PRと公開が済んだ後に、既存Goal2のnative取得・full restore/replayを再開する。Goal2未完了、Gate1 NO-GO。以下のWindows同期待ちは旧checkpointであり、現在の次条件は修正ソースの公開。

## 2026-09-09 Goal 2: 時計同期待ち

2026-09-09 Goal 2 started / WAITING_FOR_LOCAL_CLOCK_SYNC: 最新Production6file取得＋隔離full restore/replay22→56をユーザー承認で開始。公開済source12producer照合・Goal1 baseline前確認PASS。保存先ACL初期設定は子Windows PowerShellのmodule path競合で停止し、空directoryを保護した後に再開。native captureはSNAPSHOT_METADATA_INVALIDで停止。固定RR/RO診断はPG17/read-only/isolation/sourceState22/defaultACL12/metadata形式が正常、DB T0が受信時369ms未来。別clock観測でもserver ahead516–899msを確認。時計検証を緩和せずWindows同期待ち。新規6file/取得record/process receiptは各0、残存idle transaction0。本番書込み0、復元/replay未実施、Goal2未完了、Gate1 NO-GO。

証跡はignored `goal2-capture-attempt2.json` / `goal2-capture-diagnostic.json` / `goal2-metadata-diagnosis.json` / `goal2-clock-cleanup.json`。OS時刻変更は未実施。再確認でもserver ahead538–969ms、idle transaction0、保存先各0を観測。ignored復元driverを準備し構文確認済みだが未実行。native取得receipt/source/manifestを照合してから所有local環境を作成し、Auth/Storage公式初期化・全local service停止・6transaction復元・sourceState厳密一致を確認する設計。synthetic26用のsource-era欠落を許すチェッカーは本番22の受け入れに流用しない。同期後に時刻条件を再確認し、同じ承認範囲でnative取得・完全復元・migration replayを続行する。失敗時の保存先は空で保護済み、既存バックアップは変更していない。

## 2026-09-09 Goal 1: 管理baseline差の説明と復元先条件

Goal 1は `MANAGED_BASELINE_DIFFERENCES_EXPLAINED`。以下が現在のAuth/Storage判定であり、後続の過去checkpointの `BLOCKED_ON_BASELINE` / user delta未確定を置き換える。実際のhosted復元先は未観測、本番復旧可能性は未証明、Gate1は **NO-GO** のまま。

固定CLI2.109.0/PG17.6.1.140で新規の所有隔離環境を作成し、生成configの `[storage].enabled=false` でCLIの事前Storage migrationを止めた。`--exclude storage-api`だけでは `initSchema15` の初期化jobを除外できない。空のStorage履歴から、承認済みStorage v1.72.11 imageの公式 `runMigrationsOnTenant` を `MULTI_TENANT=true`、`DB_INSTALL_ROLES=false`、migration refresh/vector migration無効で実行。Auth v2.192.0は公式 `GOTRUE_INDEX_WORKER_ENSURE_USER_SEARCH_INDEXES_EXIST=true` で起動した。管理tableの手動DROP、手動管理DDL、role権限拡大なし。公式根拠: [CLI初期化](https://github.com/supabase/cli/blob/v2.109.0/apps/cli-go/internal/db/start/start.go)、[Storage migration実装](https://github.com/supabase/storage/blob/518862dff5b2e4f3386ad51e4a172e91d0ce2ad2/src/internal/database/migrations/migrate.ts)。これはlocal再現条件であり、Production service設定の直接観測ではない。

| 比較対象 | 確定した結果 |
| --- | --- |
| 履歴・構造 | Auth77、Storage68/name/hash全件一致。Auth4索引あり、Iceberg追加2tableなし。native catalog595件中593件が完全一致、共通function21定義も完全一致 |
| ACL2件 | storage.buckets/objectsの所有者自身のgrant option表記のみ。他のACL、owner、RLS属性は完全一致。所有者は常にgrant optionを持つため実効権限差ではなく補正GRANT不要（[PostgreSQL 17権限仕様](https://www.postgresql.org/docs/17/ddl-priv.html)）。生ACL差は保持 |
| 内部依存 | source/local各RI trigger92行とTOAST29行。pg_trigger/pg_constraint/親relationのnative情報で意味上のidentityを対応付け、raw identityと重複行数を保持。単純なOID除去・重複排除なし |
| 外部依存 | source795/local762行の差33行はpublic policy→auth.uid()の13行とpublic FK→auth index/columnの20行。アプリschema復元に属し、管理schema差分SQLには含めない。未説明の比較差0 |

**利用者差分:** 観測した管理baselineへの追加利用者DDLは不要。空ファイルを作って取得済みbackupとは扱わず、この判断と比較artifactのhashを保持する。アプリpolicy/FKの省略は認めない。次のfresh captureでsource状態の整合を再確認し、変化があれば再レビューする。今回のread-only再取得は以前のsource595objects/795dependenciesと完全一致したが、将来snapshotの保証にはしない。

**実際の復元先の必須条件:**

1. PG17と互換な公式Auth/Storage baseline・owner/role・RLS/ACL・関数/索引・履歴name/hashを復元先自身で確認する。不足はplatform/serviceの正式経路で整え、利用者postgresの権限拡大や管理table DROPで合わせない。local環境変数をhosted操作手順へそのまま転用しない。
2. public schemaのdefault ACLを含む既存restore recipeを守り、public→Authの33依存行をschema復元後に確認する。
3. Authデータとprovider/redirect等の設定、Storage実体、6fileのsnapshot/hash、通常postgresによるrestoreと22→56 replayはGoal 2以降の実測条件。この比較で成立したとは扱わない。
4. 実hosted destinationの適合・作成可能性・復旧時間・切替は別証拠と承認が必要。未観測のままGOにしない。

restricted evidence: `.tmp/gate1-evidence-20260909/auth-storage-review/goal1-acceptance.json`、同directoryのraw catalog/identity/外部依存一覧・初期化receipt。最終candidate cleanup2件、CLI cleanup2actions PASS。primary agentが実測・比較・受け入れを担当、サブエージェントなし。本番はTLS verify-full/read-only管理catalog再取得のみで書込み0、fresh6file取得・deploy・activationなし。

2026-09-09 Auth/Storage delta review BLOCKED_ON_BASELINE: merged source1509dabの12producer照合PASS後、承認済みProduction Directをnative PG17.11/verify-full/read-onlyで確認。history22、legacy/Vault/Storage/vector0。Auth/Storage schema-onlyをrestricted artifactへ取得し、既存固定CLI2.109.0/PG17.6の所有ローカル隔離環境と比較。Auth migration77/latest20260625000000は一致、StorageはProduction68/latest67とlocal61/latest60で不一致。初期SQL token集計は本文保持不足のため無効（後続native catalog比較で置換）。これは管理baseline差を含み、review済みuser deltaや空auth_storage_changes.sqlの根拠にしない。schema dumpとversion queryは別snapshot。native CLIの既知TLS問題を回避するためhosted CLI diffは未実行。local cleanup2actions PASS、独立container/network/volume/scratch各0。本番書込み0、fresh6file capture未実施。次は復元先自身の管理baseline/依存関係を満たす経路を確定し、user-executable差分を再レビューする。Gate1 NO-GO。

2026-09-09 pre-publication review / lint LOCAL_REVIEWED・UNPUBLISHED: primary agentが実diffと新規moduleを直接レビューし、重大な指摘なし。既存lock固定のnpm ci --ignore-scripts --no-audit --no-fundを作業worktree内cacheで実行（691 packages）、manifest/lockfile SHA256不変。変更23 MJSのESLint --max-warnings=0 PASS、コード修正なし。直前の46 tests/native6file復元＋history56 replayの対象source/log SHA256不変を照合し、受理済み実行は反復しない。過去entryのESLint SETUP_BLOCKEDは今回解消。commit/push/PR・新本番backup・本番rehearsal・9-stage受理は未実施、Gate1 NO-GO。次は明示承認されたsource公開。

2026-09-09 Production rehearsal preparation / default ACL capture LOCAL_VERIFIED・UNPUBLISHED: 検証済みdefault-table-ACL SQL/生成器をbackup-default-acl.mjsへ共通化。Production captureのRR/RO exporterにtableDefaultsを必須追加し、不足/不正/重複/許可外scope-role-privilegeを拒否、freezeしてschema artifactへ結合。対象はpostgresのglobal/public table defaultsとanon/authenticated/service_role。26件ローカル検証と同じ処理を使うがProduction source-stateは22固定を維持。source producer12filesに追加し未公開HEADを拒否することを確認。関連46 tests・5 syntax PASS、共通化後native6file復元state完全一致→history56/canonical/archive PASS、独立cleanup全0。ESLint setup-blocked。旧backupには新しい同snapshot default ACL/正式producer証跡がないため最終T0証拠には昇格させない。順序は公開前レビュー/必要チェック→明示承認によるcommit/push/PRとsource確定→別途承認された最新backup等の取得→本番22history/実データの隔離rehearsal→9-stage判定。本番変更/deployはその後の別承認。今回hosted接続/再取得/変更0、Gate1 NO-GO。

2026-09-09 six-file restore + migration replay LOCAL_VERIFIED: --phase=local-restore-replay --local-cli=pinned-goを固定PG17/CLI2.109.0の所有隔離環境で完遂。ローカル26historyから同snapshot4dumpとrolesを取得し、6fileをhash確認してsingle-transaction/ON_ERROR_STOPで復元。復元前後source-state SHA256完全一致、その後history56/canonical構造/archive3tables・3functions PASS。空targetではhistory schema自体がない場合を受理。native診断でpublic12relationのtarget既定ACL混入を検出し、ローカルschema artifactに同snapshot取得のglobal/public既定table ACLの一時解除・正確復元を含めて修正。比較条件は維持し、Production validatorは22固定、local26専用validatorを分離。関連40 tests・8 syntax PASS、ESLintは依存未配置でsetup-blocked。独立inventoryでGate1 container/network/volume各0・scratch0。失敗attempt1–4も保持。これは本番22history/実データ/全source-eraの再現ではなく、Production6fileの完全rehearsal・9-stage受理・source公開は未完了。hosted接続/変更0、Gate1 NO-GO。

2026-09-09 isolated local migration replay LOCAL_VERIFIED: --phase=local-replay --local-cli=pinned-goで既存postapply経路を実行。過去hosted catalogは読まず、fixture一致の旧SQL4件と現在migrationで検証。固定CLI2.109.0/PG17・既存cacheを使用し、privateなproject内scratchで起動。実SQL適用、履歴56件、canonical構造、archive3 tables/3 functionsがPASS。source-era0のためProduction shapeはsource-era-mismatchで拒否されることを確認し、hostedEvidence=falseを維持。関連3 tests/2 syntax PASS。終了後独立read-only inventoryでGate1 container/network/volume各0・scratch directory0。full6file backup restoreとmigration replayの一体実行/9-stage受理は未実施。hosted接続/変更0、Gate1 NO-GO。

2026-09-09 legacy SQL recovery LOCAL_VERIFIED: 明示承認された保存backupのhistory_data.sqlのみread-onlyで解析。22history rowsから対象4件を抽出し、statements.join("\n")/末尾改行追加なしで既存fixtureのbytes/MD5全件一致。既存validateLegacyInputs PASS、parsed statement数17/25/17/25。合計32988 bytesをprivate ACLのignored recovered-legacy-sqlへ保存しreadback照合、secret scan PASS。元backup変更なし、SQL実行/hosted接続なし。CLIと元SQLの準備は解消したが、過去catalog証跡・隔離migration replay/完全rehearsal受理は未解決。Gate1 NO-GO。

2026-09-09 pinned CLI runner接続 LOCAL_VERIFIED: --local-cli=pinned-goの明示選択で固定Go経路を利用し、npm配置がなければ承認済みignored tools配置へ解決。毎回のSHA256/symlink拒否、local transport、bounded runner、既存npm既定を維持。CLI存在preflightをprofile対応。実runner経由native2.109.0 PASS、runtime-cleanup/cli-atomicity-order/postapply-queryの3 tests PASS。元SQL4件のGit履歴8revisionを照合したがbyte/MD5一致0。元SQL・過去catalog証跡不足のためreplay未実施。次は外部保存backupのhistory_dataから4件のみのローカル抽出と既存fixture照合が必要（プロジェクト外読取の範囲確認後）。hosted操作0、Gate1 NO-GO。

2026-09-09 CLI限定setup完了: 承認済み範囲でlock固定platform packageのSHA512とGo exe SHA256を照合し、ignored .tmp/tools/supabase-2.109.0へ配置。native --version=2.109.0 PASS、必要Docker image8種は既存cacheに存在。manifest/lock/node_modules変更・npm lifecycle・image pullなし。既存runnerはnode_modules entry前提のため未接続。旧履歴replayに必要な元SQL4件が未配置で、migration replay/完全rehearsalは未実施。hosted操作なし、Gate1 NO-GO。

2026-09-09 合成データの6file復元・source-state照合・失敗transaction rollbackを隔離PG17で実証。22 tests PASS、所有container残存0。CLI限定setupは下記のとおり完了。元SQL4件が未配置でmigration replay/完全rehearsal証跡はinput-blocked。hosted操作なし、Gate1 NO-GO。

2026-09-09 same-snapshot source-stateとfinalBackup観測adapterのローカル接続を検証済み。関連56 tests PASS、隔離PG17で同snapshot不変/新接続増分、RLS・function/sequence/default privileges変化を実測。通常captureで状態取得を必須化。rehearsal restore・9-stage全体受理・hosted backupは未実施、Gate1 NO-GO。詳細はR6。

## 現在の判断
2026-09-09 外側process記録のローカル実装: 固定取得CLIの実終了・stdout bytes/hashと保存済取得record/6file/source10files/runを照合し、別protected directoryへbackup-process.jsonを排他的保存する。秘密はstdinのみ。timeout時の所有child tree停止と終了済PID非操作を検証。ローカルprocess証拠であり、本番backup・9-stage authorityではない。同snapshot source-stateとstage adapterは未完了、Gate1 NO-GO。詳細はR6。
2026-09-09 保存/source-run結合のローカル実装: snapshot保持中に6file＋manifestの保存/fsync/readbackを完了し、close後に別の保護先へsource8files・run・target・T0・dump・manifestの記録を保存するnative APIを追加。caller入力変更/原本上書きを防止し、source前後不一致・保存失敗・5分期限超過を拒否。現HEADでは新producer未公開につきnative source guardで停止を実測。外部backup/復元未実施、outer native-process receipt/stage adapterと実証は未完了、Gate1 NO-GO。詳細はR6。
2026-09-09 native証跡結合のローカル実装: capture自身がnative T0・binding/snapshot digest・dump時刻/終了結果/hash・exporter close観測を返す。時計異常は成功にしない。保存前の観測追加であり、旧原本復元・信頼済みreceipt・arm/GO成立ではない。次はrestricted保存とsource/run receiptの結合。外部backup/復元未実施、Gate1 NO-GO。詳細はR6。
2026-09-09 Auth/Cron再照合完了: Production Auth18項目は保存exportと現在UI/設定GET応答で差分0。native Directの10分24秒後観測でProduction0job/0run、Preview同一inactive1job/27run、両方run増分0。Previewのjob ID・command・5分周期・postgres・最大run ID保持を確認。連続監視や将来のProduction Cron設定完了とは扱わない。Cloudflareログイン後のDashboard実測でProduction/PreviewともCronトリガー未設定、Preview authority=supabase-cron、Productionはauthority名なし。Cloudflare Cron fallback absentを観測時点で確認。稼働version表示はProduction f45136b2/Preview 9e1a72be（各100%）。配備annotationはartifact独立照合ではなくARTIFACT_IDENTITY=UNKNOWNを維持。DB/設定変更・backup/復元なし、Gate1 NO-GO。以下のAuth/10分差分未確認は過去checkpoint。
2026-09-09 Direct接続前提の復旧完了: 保存済み接続設定/CA/credentialと既存PG17.11 clientを再利用し、両環境のverify-full/TLS1.3/PG17/読み取り専用RRを実測PASS。外部保存先の6-file backupは旧受け入れmanifestと同一bytesでnative検査PASS、Auth exportも残存している。旧worktree内証跡の原本欠落と区別する。Auth exportの旧hash/現在設定一致、backup取得provenanceと9-stage結合は未回復。旧backupは最終T0に使わない。新規backup/復元/DB変更なし。以下のDirect入力不在・backup所在未確認は前段のcheckpoint。詳細はR6。
2026-09-09 新規read-only baseline: 承認済み既存Production/Previewを接続済みconnectorで観測。RR/ROの完全history/catalogはProduction22/pending34、Preview56/pending0、未知履歴0。Preview canonical33表/81関数/依存1443行。Production Vault0/Cron0、Preview予約Vault2件とinactive5分postgres Cron1件を確認。各観測の日時・完全結果・hashをrestricted保存し、旧原本の代替や旧時刻の復元とは扱わない。Direct verify-full設定は不在でnative証跡結合は未成立。Advisorは今回baselineのみ、Auth設定export/fallback/10分run delta/backup/復元は未確認。DB変更0。詳細はR6。

2026-09-09 証跡再取得方針（ユーザー承認）: Gitに残る実装・手順・過去の結果報告は保持する。復旧後に存在しない旧 `.tmp/gate1-evidence-20260908` 配下の証跡は「原本欠落」とし、hash・PASS報告だけから原本を再構成しない。ローカルテストは新しい日時で再取得する。Production backup・DB観測・復元試験は必要範囲と個別承認の確認後に新規実行し、旧観測時刻へ遡及させない。旧6-file backupの保存先原本は別途所在確認が必要で、作業フォルダー欠落だけから消失を断定しない。再取得は最終T0 backupの代用にならない。詳細はR6の再取得表。

2026-09-09: PR #818のPreview merge/source包含/完全tree一致は前回確認済み（task.md参照）。復旧した同じ2d79 worktreeで元HEAD・既存branchへ復帰した。ただし参照先のprivate phase5 local acceptance証跡が存在せず、復旧後の証跡保持・hash連続性は未確認。以下の未公開・watchdog未実装案内は過去checkpoint。次の証跡結合と実運用入力はR6を参照。Gate1 NO-GO。

`gate1_status=NO-GO` / `source_artifact=UNCOMMITTED/UNKNOWN` / `source_publication=PR818_MERGED_PREVIEW` / `trusted_nine_stage_authority=UNKNOWN` / `activation-closed`

trusted_nine_stage_authorityのUNKNOWNはGate 1で要求する承認済みimmutable authorityが未確立であることを示す。ソースPRのcommit作成とは別に確認する。

2026-09-08 PR #817 merge確認: source `ae544a55443ed90daf71d8975d2fcb2e45393b57` / Preview統合 `5a110ae110fe24689132440dc470b1105b08aa8e` は完全tree一致。native source collectorで56 migrationを再照合し、公開source8ファイル・既存証跡13件のhashを検証した。工程4のlocal6-file復元/bridge受け入れは保持。次は本番適用前の実watchdog・復旧/cutover手順・native authority取得経路の準備。既存State modeは状態判定のみで、期限を監視してpause/停止確認する実行経路の準備完了とは扱わない。古いbackupをfinal T0に流用せず、Preview applyも繰り返さない。新規hosted操作0、追加費用0、サブエージェントなし。Gate1 NO-GO。

2026-09-08 工程4 LOCAL_RESTORE_AND_REPLAY_REVIEWED。修正済みrecipeでfresh6-file backupを取得（同一snapshot4dump/13112ms、前後50表一致、保存bytes/hash/ACL検証済み）。空の復元先でpostgres自身のpublic既定権限を解除してから通常postgresで6段階復元し、50表data/Auth/history一致、34 migration/bridge再実行、履歴56件と既存22件の完全保持を確認。最終canonical33表/81関数は独立受け入れ済みPreviewと構造・権限一致。archive3表/3関数は0行・application ACL拒否、source-eraはfresh Productionと一致して保持、Vault/Cron/jobs/runs0。Storage multi-tenant8表、Auth77/Storage68履歴一致、owner ACL80項目同等。Auth非一意4索引は明示した管理性能差分で性能同等は未主張。親が実測・独立比較しサブエージェントなし。最終3コンテナ削除済み。source repair未公開・hosted Recovery未実施・9-stage authority未結合のためGate1 NO-GO。詳細と必須の復元前設定は計画R5。

復元先の初期既定権限を残した最初の試行では、legacy3表のACLが本番各16項目に対し各32項目へ増え、bridgeが正しく停止した。既存表の権限を後から変更して通すのではなく、新規空DBでpublicの既定権限を整えてから元の6ファイルを復元する。計画R5の必須準備を省略しない。以下は過去のcheckpoint。

2026-09-08 管理baseline追加診断: cached公式Storage imageの実装を確認し、MULTI_TENANT=trueが移行接続のstorage.multitenant=trueへ渡されることを特定した。新規隔離DBで公式tenant migratorと履歴hash検証済み追加7 migrationを実行するとStorage8表・iceberg内部表0となり、Auth77/Storage68の完全移行履歴がProduction観測と一致した。従来の内部2表DROPはsingle-tenant初期化によるlocal差分であり、hosted復元SQLへ含める根拠にならない。作成3/削除3/残存0、hosted接続0、host port0、データ投入0。別途Production read-only catalogでAuth4索引は全て有効な非unique・非primary・非exclusion索引、関連constraint/dependent0と確認したが、性能影響と復元先での存在は未検証。owner ACLの文字列表現差についても有効権限の同等性はまだ未証明。全431文の文字列一致を元仕様の必須条件とは扱わないが、未知差分を無視したり失敗SQLを削除して復元成功と扱うこともしない。次は4索引・owner ACLの互換性判定を確定し、正式な利用者差分とfresh6-file復元へ進む。元backup保持、source変更/公開・Production変更・サブエージェントなし。Gate1 NO-GO。

2026-09-08 最新工程4・バックアップ修正local受け入れ: 親タスクで実装・レビューし、サブエージェントは未使用。同一RR/RO exporter内でStorage管理2表の件数を直接取得し、両方0の場合だけdata dumpから除外する。欠落・非空・不正な応答・RLSで隠れた行はdump前に停止し、呼び出し元の0件申告や無効化flagでは迂回できない。GO検証器にもsnapshot一致とsource/restored stateの2表0件を必須化した。19 snapshot/capture tests、12 stage groups、backup contract、変更7JSのlint/syntax、PowerShell parseがPASS。実local PG17で4dump/1959ms・6段階復元・synthetic data/Auth/history/RLS/grants一致、snapshot後の同時書き込み分離、非空2ケース・表欠落・RLS非表示時の停止/接続終了を確認。最終2コンテナは削除済み、独立確認で残存0。実装は未公開で、Production接続・変更・実backup再取得はなし。元のbackupも保持。残るのは復元先の管理baselineと利用者差分の確定、およびfresh Production backupによる正式復元/security/bridge検証。Gate1 NO-GO。以下は過去のcheckpoint。

2026-09-08 最新工程4・原因確定: Productionのread-only設定とlocal検証から、管理表DROP2件・Auth索引作成4件は42501、session_replication_role設定とowner ACL4文は実行可能と判明。別途、data.sqlに含まれるstorage.buckets_vectors/vector_indexesへの空COPYも両方42501となり、ProductionのINSERT権限なし・両表0行を確認した。以前検証したlocal platform-admin baseline上で、この空COPY2ブロックのみをメモリ内で除外する追加診断を実施し、非superuser postgresによるデータ/履歴復元後の全50表件数・digest、Auth/Storage全431文、Cron0件が一致した。元の6ファイルは保持し、通常の6段階復元が成立したとは扱わない。次は同一snapshotでの空判定を伴う正式backup recipe修正と、復元先のplatform管理baselineと利用者差分の責任分離が必要。source/migration変更は未実施。各4回の隔離診断で作成3/削除3/残存0、Production変更・サブエージェントなし。Gate1 NO-GO。以下は過去のcheckpoint。

2026-09-08 最新工程4・時刻同期後: 承認済みWindows再同期が管理者承認経路でexit0となり、native DBとの時刻検証も54msでPASS。Productionの6ファイルbackupを保存し、同一snapshot4dumpは10697ms、exporter正常閉鎖、前後50表の件数/digest一致、独立した保存済みbytes/hash/ACL検証はPASS。実際の非superuser postgresによるlocal復元はroles/schemaの2段階後、auth_storage_changes.sqlで42501となり、データ投入前に停止した。作成3コンテナは削除済み。以前の全431文一致はlocal platform administratorでの比較元整備であり、通常復元roleでの手順成立を証明しない。保存済みbackupはbytes検証済みだが復元未受け入れで、Production最終適用には使わない。次はplatform管理スキーマの初期状態と利用者が実行可能なAuth/Storage差分を区別して手順を解決する。時刻検証緩和・DB権限昇格・Production変更なし。Gate1 NO-GO。以下の時刻blockerは解消済みの過去記録。

2026-09-08 最新工程4: Auth設定の実値を保護されたバックアップ先へ保存し、hash/ACLを確認済み。native verify-fullのAuth/Storageスキーマ取得と、Productionと同じAuth77/Storage68管理migrationを用いる隔離環境での差分replay後、全431文と関数本文21件が一致した。これはlocal rehearsal baselineの証拠であり、差分SQLのhosted実行許可ではない。Productionバックアップはスナップショット開始時刻がPCより約1.3秒先行するため、厳格な時刻検証で停止した。exporterは閉鎖済み、保存先は空、6ファイルbackup/restoreは未成立。時刻条件を緩和せず、Windows時刻同期の確認・必要時再同期に対するOS範囲の許可を得てから再検証する。詳細は計画R5。以下のAuth未保存・差分未取得の記載は過去のcheckpointである。

2026-09-08 工程3最終受け入れ完了: PR #816の統合 `c1bcd3dc4cebe8a748322ab04081ac56a03ff673` とsource `53c49f74197116f71b5ffc9c0a0d5b7260432e73` を完全tree照合し、native source observationで56 migrationを確認した。公開済み検証器でPreviewの履歴56/pending0、完全canonical33表/81関数/依存1,443行、運用行とCron/Vault metadata保持、run delta0がPASS。Advisorは適用直後から追加変化なし。migration再適用なし。工程4は復旧準備に移行し、同一Free organization・東京の2 healthy projects、PG17、6ツール、Production Vault0/Storage0/Cron0/履歴22をread-only確認済み。無償枠は障害sourceの期限内停止確認後に確保する既存設計を維持し、常時空き枠を要求しない。復旧先はユーザー確認済みの同一Free organization・東京、project作成見積りは月額0。見積りと障害時の実provision/期限内復旧能力は別証拠である。Auth/provider/redirect/cutover設定、Auth/Storage差分reviewとrehearsalは未成立。ユーザーログイン後のDashboard read-only確認でEmailのみ有効、他24無効、custom/third-party0、Site URL1/redirect4、最小password長8、OTP3600秒/8桁を確認した。制限領域に設定digestと集計を保持したが、URL実値の永続exportは未実施であり、復旧用設定保存完了とは扱わない。Auth/Storage diffは未実行。CLI代替pg-schema経路のURL変換では検証済みTLS構成を保持できず、process-wide verify-fullはlocal shadowにも及ぶため、この経路やTLS緩和は試していない。実pause/provision/backup/Production適用は未実施。詳細は既存計画R8/R5を参照する。工程3受け入れと最終9-stage authority/GOは別であり、Gate1 NO-GOを維持する。

2026-09-08 最新工程3記録: PR #815のmerge後、fresh完全入口確認を経てPreviewへexact26を1回適用済み。独立したCLI一覧/dry-runは履歴56・pending0。33テーブル/81関数の完全定義・ACL/RLS、依存関係1,443行、既存運用行とCron/Vault metadataの前後一致、Cron run delta0を確認した。証跡検証器が適用前からあるpg_depend重複3行を拒否したため、重複を削除せず期待値との出現回数まで照合するR8限定修正をローカル実装・root受け入れ済み。実native collectorと関連契約はPASSだが、R8の公開/mergeとsource binding整合後の最終read-only受け入れが残る。工程3はR8_PUBLICATION_PENDING、Gate1 NO-GOを維持する。Advisor新規Paid範囲0/high-critical0、範囲外性能指摘13件は記録済み。Production変更なし。Previewの再適用は不要かつ実施しない。詳細は既存計画R8を参照し、以下の記録は以前のcheckpointとして扱う。

2026-09-08 R7 source修正はローカル受け入れ済み: 実測Preview専用の厳密な入口判定と78定義のA3収束を実装した。実適用用と同一hashのGo CLI2.109.0 / PostgreSQL17で、実測before81関数・履歴30の再現、旧source RED、pending26→履歴56、最終81関数の全metadata/raw MD5/security完全一致、異常系9件、失敗migrationの履歴非追加、運用行とCron/Vault metadata保全、Cron run delta0を確認した。旧構造経路の退避3テーブル/3関数・最終canonicalもPASS。source/fixture/検証差分はrootがレビューし、サブエージェント未使用。追加のPreview read-only確認は予約対象114・未知relation0・履歴30で、外部DB変更なし。最終期待値とユーザー編集2ファイルは変更していない。詳細と制限は既存計画R7を参照する。R7のcommit/push/PR/merge、修正sourceでの新bindingと完全preapply確認、Preview実適用・適用後受け入れは未実施であり、工程3は未完了、Gate1 NO-GOを維持する。

2026-09-08 工程3の読み取り記録: PR #814はPreview統合commit `4d346a8a78d3d5c1136ae75c635b7ccb18850f9a`へmerge済みで、source `a7532540c5a4d3998b36eaf1dec45d741348a810`と完全tree一致。native source collectorの56 migration照合、両環境の完全履歴照合、同梱Go CLI2.109.0による各1回のlist/dry-runが成功した。exact pendingはPreview26/Production34。Previewのfresh完全catalogは保存済みv2 baselineと全項目一致し、Advisorも取得時刻以外の全項目一致で新規/削除0。Production Advisorは今回のbaselineとして保持する。古いProduction診断fixtureのwrapper不足と、検証済みv2取得artifactを混同しない。詳細・digestはtask.mdとrestrictedなphase3記録を参照する。これは適用前の実測であり、適用後の完全catalog/security期待値、scheduler/Vaultの前後保存確認、最終9-stage authority、Preview/Production適用はまだ成立していない。CIは登録結果なし。Gate 1 NO-GOを維持する。

この文書は実行順序と停止条件の運用入口であり、実環境のPASS証跡ではない。仕様は[承認済みdesign](../superpowers/specs/2026-08-31-comment-translator-paid-v1-gate1-production-supabase-design.md)、作業と最終allowlistは[承認済みplan](../superpowers/plans/2026-09-01-comment-translator-paid-v1-gate1-production-supabase-readiness.md)、最新の実行記録は[task.md](../../task.md)を参照する。矛盾があれば実行せず、具体的な差を解消する。

2026-09-06の包括承認は、Gate 1 GOに必要な**費用を発生させない**操作に限る。承認の存在は、target照合・review・backup/recovery・実測条件の代わりにならない。各操作のscopeと前提は引き続き独立して記録する。Paid公開、Provider/Checkout実通信、通常deploy、Cron activationはGate 1に含まれない。

| 証拠状態 | 現在の扱い |
| --- | --- |
| repository-implemented | 工程1の9-stage verifierとnative GO接続を含むソースを実装。工程2でPR公開対象77ファイルを確定。全hosted planの完了ではない |
| locally-verified | 工程2で68 source checks・29 Node syntax・3 PowerShell syntax・4 entry guards、TypeScript/lint/Next/Cloudflare buildがPASS。レビュー修正後の11 stage groupsと関連テストもPASS。既存actual-local Vault/R4証跡は実行経路を保持。新たな独立レビューは行わずparentが確認。実証跡取得・hosted検証は未完了で、fixtureは運用GOの証拠ではない。詳細とexact hashはplan/task記録を参照 |
| 実tool | Docker Client/Server 29.7.2、Supabase CLI 2.109.0、PG client 17.11の確認済み記録あり |
| Preview-not-applied / production-not-applied | このGate 1実行では未適用。保存済みreadbackを現在状態と呼ばない |
| backup-not-run | 実backup、restore rehearsal、final snapshotは未実施 |
| Vault-not-written / Cron-not-configured | このGate 1実行ではhosted書込みなし。既存Preview状態とは区別する |
| hosted readiness | 実証跡producer・semantic verifier・接続入力が未完備 |
| 無償recovery capacity | 2026-09-07 Management API再確認で同一Free organizationの2件がACTIVE_HEALTHY、同一region。現時点の空き枠は未確保。復旧先確保・incident後の期限内provision/cutover手順は未実証 |

現在のユーザー指示はサブエージェントの呼出し禁止。ユーザーが設定した現在の親モデルが実装・確認・受入れを直接担当する。以前のAstra/Luna分担は現在の実行指示ではない。runtime identityを独立確認したとは主張しない。単一writerを維持し、未コミット変更を保持する。

無償枠の制度根拠は[Supabase billing](https://supabase.com/docs/guides/platform/billing-on-supabase): Free project上限はOwner/Administratorの所属organizationを横断して2件、paused projectは算入しない。ただし健康なsourceを枠確保のため停止してはならない。APIで確認したplan/statusと、復旧先の無償確保・期限内復旧能力は別証拠である。

## Source / inventory / artifact境界

- 作業branchは `codex/comment-translator-paid-v1-gate1-source-readiness`。開始HEADは `2775fba1f5ca687d23b25c88286ec5125d1663ec`。公開前にactual diff全体をreviewし、commit/treeを実測値へ更新する。merge先は `codex/comment-translator-paid-v1-preview`。
- [environment inventories](../../scripts/fixtures/comment-translator-paid-core-v1-gate1-environment-inventories.json)の完全version/name行をauthorityとする。最終は56件、保存済み履歴からのpendingはPreview 26・Production 34。実行直前のCLI一覧・dry-runは完全行で再照合し、countだけでは進めない。
- `final55`はforward適用前の定義drift診断baseline。最終56件との差は承認済み `20260904000000_comment_translator_paid_gate1_a3_canonical_convergence` の1件だけ。欠落・重複・未知migrationは拒否する。
- desired canonicalはrepositoryのLF source bytesと安全なsecurity contractから生成する。[bridge states](../../scripts/fixtures/comment-translator-paid-core-v1-gate1-bridge-states.json)と[canonical authority](../../scripts/fixtures/comment-translator-paid-core-v1-gate1-canonical-authority.json)を完全構造で検証する。Previewはobserved driftであり、canonicalへコピーしない。
- canonicalのrowCountは観測値として分離する。legacyの0行条件、owner/ACL/RLS、完全FunctionRow、raw definitionMd5、dependencyは厳密比較を維持する。PUBLIC/anon/authenticatedのPaid RPC EXECUTEは0、service_roleは明示最小権限のみ。
- 81件は直接宣言されたRPC identityの件数。identity-only SHA-256を完全FunctionRow manifestのMD5へ代用しない。旧82件要求・旧MD5はretired。
- 保存済みProduction SHA-256は `569c325d2e09274f1b24b8352b482e8e38fcd3c94478b45824bfc7f8254bd3db`、Preview r13は `489b36c13953b9fa1f508ea7e8bdb9a6ca5cb0aa0998de07cbbbe4189d174a16`。r13は `C:/ProgramData/Codex/Gate1/catalog-readback-20260903-r13` に保持する。
- legacy SQL入力は `C:/ProgramData/Codex/Gate1/legacy-sql-20260902-r2` の4件のみ。本文を出力、repositoryへコピー、変更、削除しない。取得artifactとr4〜r12も保持する。digest再確認は新しいremote readbackではない。

## 実行順序と共通preflight

1. branch/HEAD/actual diff、適用するdesign/plan、writer、必要toolとinputを実行する同一processで確認する。
2. source契約・実DB/Vault検証・reviewを完了する。未実装・未実行・SETUP_BLOCKEDをPASSに変換しない。対象外差分、package/lock変更、秘密値・private identifier混入を検査する。
3. review済みsourceのcommit/push/PR、CI、Preview統合lineへのmergeを確認する。作業branchの存在やlocal commitだけではsourceCommit要件を満たさない。
4. Previewのfresh preflight、exact pending、適用、final56とcanonicalの完全readbackを確認する。既存inactive schedulerとVault値を変更しない。失敗中はProduction backup/DDLへ進まない。
5. DB/TLS、無償recovery capacity、rehearsal backup/restore、watchdogとcutover準備を実証する。
6. final snapshot backupとchecksumを完了し、arm後にProductionのexact pendingを適用する。完全history/catalog/security readbackが成功するまでVault/Cronへ進まない。
7. Vaultの2名を原子的に作成・count readbackし、その後にCronを原子的にinactiveで作成する。10分観測とfallback確認を完了する。
8. 全GO条件をactual evidenceで再監査する。単なるJSONのPASSラベル、synthetic結果、保存fixture、未実装verifierはGO根拠にならない。

Direct接続はprivateなtarget bindingのSHA-256、CAファイルのSHA-256、host/user/database/portの完全一致を必要とする。`verify-full`、PostgreSQL 17、`PGGSSENCMODE=disable`を維持する。接続値はsecure inputからtask process内へ渡し、chat・history・argv・stdoutへ出さない。CA未取得、binding不一致、秘密値表示が必要な経路では停止する。

read-only取得は固定query、REPEATABLE READ / READ ONLY、default transaction read-only、bounded timeout/output、最後のROLLBACKを使用する。適用前の22/30件契約と適用後56件契約を混同しない。Productionのcanonical wrapper欠落を空配列で補完しない。完全metadataはrestricted artifact内で検証し、外へはsanitized scalarだけを出す。

## 操作scope登録票

各行の実行記録には、private target binding、review済みsource revision、入力artifact digest、preflight結果、実施時刻、結果、停止時対応を結び付ける。値自体は公開しない。以下はcopy-readyなscope欄であり、空の証拠欄を自動的に承認済み結果へ変換しない。

| Scope | 許される操作と実行前条件 |
| --- | --- |
| source-implementation | plan allowlist内のsource・契約・docs。単一writer、既存差分保持、独立reviewが必要 |
| tooling-setup | 既存tool/imageの再利用を優先。追加費用は禁止。導入をPASSと推定せずversion/digest確認 |
| preview-apply | fresh一覧がexact pendingと一致し、source review/merge後のみ。scheduler/Vault/activation除外 |
| recovery-cost-capacity | plan・枠・region・PG17復旧先の無償確保を実証。有料project/PITR/IPv4/add-onは禁止 |
| recovery-project-provisioning | 確定済み無償枠とexact対象でのみ新規復旧先を作成。枠を空けるため本番を停止しない |
| recovery-region-extensions-auth | 復旧先のregion/extensions/Auth provider・redirectをreview済み構成と照合 |
| rehearsal-backup-restore | Preview gate後、Production read-only backupから新規隔離local Supabase PG17へrestore |
| final-backup | 同一exported snapshotに結び付くschema/data/historyと別rolesをrestricted保存 |
| bounded-rpo-watchdog | RPO最大20分・緊急停止/cutoverの条件を記録。実pause完了を証明できなければarm禁止 |
| production-migration-apply | backup/checksum、rehearsal、無償復旧能力、exact pendingが全部揃ってからarm・適用 |
| vault-write | schema/security readback後、予約2名だけ。rotation/update/decrypted read除外 |
| inactive-cron-configuration | Vault確認後、予約1jobだけをcommit前にinactive。activation除外 |
| incident-source-pause | arm後の失敗・deadlineに対するexact source project pauseだけ。健康なsourceの事前停止は禁止 |
| pause-confirmation | APIの要求成功だけではなく、deadline内の実際のinaccessibilityを確認 |
| recovery-restore | pause確認がT0+20分以内の場合だけ、新規復旧先へfinal backupをrestore。source上書き禁止 |
| endpoint-credential-cutover | 検証済み復旧先へ必要最小限の緊急endpoint/credential切替。通常deployと区別 |
| client-reauthentication | emergency cutoverに伴う再認証。秘密値を証拠へ含めない |
| reopening-writes | 復旧readbackとsanitized Free smoke成功後だけ。schedulerはinactiveを維持 |
| source-unpause-deletion | GO達成に必須ではなく、暗黙承認しない。原sourceはforensics用に保持 |
| activation | Gate 1外。Paid/Provider/Checkout/Cron activationを行わない |

commit/push/PR/mergeはreview済みsourceの統合のための独立した記録を持つ。CI未確認や必須local検証未完了のままsource readinessを受け入れない。backup/artifact削除はGate 1の前提ではなく、保持する。local試験の生成物だけは、そのrunのID・label・digest・mount・path所有確認後にcleanupする。

## Backup / restore / watchdog

### 固定版CLIのargument recipe

以下は2026-09-06にCLI 2.109.0とPG17.11の各`--help`でflagを確認した**非実行レシピ**。private linked targetの照合、Direct/TLS強制、sanitized captureを備えた実行producerは未完備であり、その完成前には実行しない。接続値を引数へ埋め込まず、raw stdout/stderrをterminalへ流さない。

| 段階 | CLI引数 | readback / stop |
| --- | --- | --- |
| 適用前一覧 | `migration list --linked` | bindingとlinked targetをprivateに照合し、全version/name行をfixtureと比較 |
| pending確認 | `db push --linked --dry-run --include-all` | Preview26 / Production34の期待完全一覧と一致。未知・重複・欠落は停止 |
| exact apply | `db push --linked --include-all` | source統合、一覧一致、環境別backup gate後に1回。seed/roles追加は禁止。失敗時は自動再試行しない |
| 適用後一覧 | `migration list --linked` | final56完全一致とpending空。別途fresh完全catalog/security readback必須 |
| filtering discovery | `db dump --linked --dry-run` | scriptはrestricted capture内で引数として検証し、実行可能textとして評価しない |
| Auth/Storage差分 | `db diff --linked --schema auth,storage --file <restricted-output-name>` | `--file`は新migration出力機能。repositoryで実行せず、専用restricted copied work directory内の生成先を確認。差分はreview後だけ利用 |
| roles | `db dump --linked --role-only --file <restricted>/roles.sql` | snapshot外。exit0、bytes、SHA-256を確認 |

`--include-all`は承認済み過去versionを含むexact集合のためだけであり、未知migrationを許可しない。`<restricted...>`は解決済み保存先の説明記号。`--linked`だけではDirect/TLSを証明できない。`--db-url`、password引数、`--debug`は禁止。`db diff`のshadow runtimeも専用生成物に限定し、既存runtime/volumeを流用しない。

snapshot dump本体はネイティブ`pg_dump`を使う。snapshot値は非表示pipeでrunnerへ渡す。

| artifact | PG17引数（非実行レシピ） |
| --- | --- |
| schema.sql | `--schema-only --snapshot <private-snapshot> --file <restricted>/schema.sql` + reviewed filters |
| data.sql | `--data-only --snapshot <private-snapshot> --file <restricted>/data.sql` + reviewed filters |
| history_schema.sql | `--schema-only --schema=supabase_migrations --snapshot <private-snapshot> --file <restricted>/history_schema.sql` |
| history_data.sql | `--data-only --schema=supabase_migrations --snapshot <private-snapshot> --file <restricted>/history_data.sql` |

COPYはPG17 `pg_dump`の既定。`--use-copy`はSupabase CLI専用でありネイティブへ渡さない。`--inserts`、`--column-inserts`、`--rows-per-insert`を拒否してCOPYを維持する。command-planのflag混入修正と直接回帰テストは親検証・再レビューで受入済み。これは実backup/restoreの成功ではなく、実行producer・接続・snapshot・復旧証拠は別途必要。

plain SQL restoreは下記の順に、新規専用targetで`psql --no-psqlrc --set=ON_ERROR_STOP=1 --single-transaction --file <restricted-artifact>`を配列呼出しし、各fileのexit0を要求する。失敗targetをreset/reuseして続行しない。`pg_restore --version`確認をplain SQLへの`pg_restore`実行に置き換えない。このレシピを実行済み証拠やhosted GOに昇格させない。

実行toolはPG major 17とCLI 2.109.0をpinし、CLIのdry-runから得たSupabase filtering argumentsをreviewする。command plan出力はbackup実行証拠ではない。[backup preflight](../../scripts/comment-translator-paid-core-v1-gate1-backup-recovery-preflight.ps1)の既定動作は非実行である。

- backup前にVault total/required-name rows=0、Storage objects=0をfresh確認する。非zeroなら既存設計では進めない。
- Auth/Storage差分はexact CLIの機能でread-only確認する。差分ありならreview済み `auth_storage_changes.sql` をrestricted artifactとして扱う。未reviewの差分は停止する。
- 必須artifactは `roles.sql`、`schema.sql`、COPY形式の`data.sql`、`history_schema.sql`、`history_data.sql`。本文、接続値、snapshot IDは表示・commitしない。filename/bytes/SHA-256/exit/timestampsのみ報告する。
- `T0`はsnapshot export元の同一REPEATABLE READ / READ ONLY transactionのserver時刻。schema/data/historyのdump完了までexport元transactionを保持する。rolesはsnapshot外の別取得として記録する。
- restore順は roles → schema → review済みAuth/Storage差分（ある場合）→ data → history schema → history data。対応toolでON_ERROR_STOPとtransactionを使用し、非対応modeまで単一transactionと主張しない。
- 新規local targetでhistory完全一致、legacy 3表0行、非Paid aggregate counts、Auth/FK/schema、bridge/canonical rehearsalを確認する。local restoreはhosted復旧枠の証明ではない。

| State / deadline | 必須動作 |
| --- | --- |
| PRE_DDL_UNARMED | backup/checksum中。pauseは禁止 |
| T0+5分まで未完了 | ABORTED_NO_DDL_NO_PAUSE。候補を復旧に使用せず、DDL/pauseなし。次回は新snapshot/T0 |
| ARMED_BEFORE_FIRST_DDL | backup/checksum完了後、最初のDDL直前にarm |
| T0+10分より前にmigration＋決定的readback成功 | SUCCESS_DISARMED。pause latchがあれば成功へ戻さない |
| arm後の決定的失敗／T0+10分まで未成功 | 即時、遅くともT0+10分にPAUSE_REQUESTED |
| T0+20分以内 | exact sourceのinaccessibilityを確認してPAUSE_CONFIRMED |
| deadline内のpause未確認 | T0 backupによるrestore/cutoverは禁止。NO-GO、証跡保持、review済みforward対応へ |

emergency recoveryはsource停止確認 → 無償復旧先のprovision/config → 所定順restore → history/data/Auth/Storage/grants/RLS/catalog/Vault readback → endpoint/credential cutover → client再認証 → Free smoke → writes再開。原sourceを自動unpause/deleteしない。Freeでのzero-loss rollbackは主張しない。

## Watchdog runner and recovery handoff (2026-09-08)

Local implementation: [watchdog runner](../../scripts/comment-translator-paid-core-v1-gate1-watchdog-runner.mjs), [deadline supervisor](../../scripts/lib/comment-translator-paid-core-v1-gate1-watchdog.mjs), [native transport](../../scripts/lib/comment-translator-paid-core-v1-gate1-watchdog-transport.mjs), [focused tests](../../scripts/comment-translator-paid-core-v1-gate1-watchdog.test.mjs). This implements execution, not operational GO. The CLI admits only native dependencies and rejects producer files whose actual bytes differ from its explicitly selected source commit. The new source is currently unpublished. The exported dependency seams are for local tests; results obtained through them are not native hosted authority.

Run the runner as a separate process using Node and its script path, with no credentials or JSON in argv. The trusted operator supplies bounded UTF8 NDJSON through private stdin. Sequence starts at0 and increases by exactly1. `init` carries policy (schemaVersion1, real snapshot-server T0, runId, sourceCommit, Production binding SHA), exact private binding, minimal libpq environment, Management API token, target/run/source-bound read/pre-authorized pause/confirmation permissions and an existing ignored journal directory below this checkout's `.tmp`. No secret or project identifier is emitted. The journal contains only sanitized metadata and a sequential hash chain, opened exclusively and fsynced. It is separate from the immutable six backup files.

Wait for `ready`, send `backup` with the exact six-file manifest/directory and producer completion/checksum timestamps, then wait for `backup-accepted`. The runner performs native persisted-byte/ACL inspection and requires completion, checksums, inspection and durable acknowledgement within T0+5 minutes. The supervising operator must independently bind that manifest and those timestamps to the native same-snapshot capture receipt; the runner's byte inspection alone cannot establish acquisition provenance or make an old rehearsal backup current. Send `arm` and wait for the durable `armed` acknowledgement immediately before starting DDL. No DDL may run after absent/failed acknowledgement. After actual migration and decisive catalog/security verification, send `success` with same-run/source/target identity and the actual ordered completion timestamps. These are reports from the trusted native executor, not a replacement for full readback evidence. On decisive failure send `failure`; retain the process/channel until its terminal receipt. EOF, malformed or out-of-order input after arm latches pause. Do not terminate the watchdog to cancel an armed run.

The separate process uses monotonic elapsed time plus wall-clock progress, with a one-second request scheduling margin before the10-minute deadline. Requests are one-shot and individually bounded. Request rejection/timeout does not imply a stopped project; confirmation continues independently. Lost time, failed durable records, late results and20-minute expiry cannot establish recovery eligibility. A process or machine outage is not proven safe by the local timer tests: the actual execution host, supervisor lifetime, native input/receipt bindings and emergency authority must be accepted before arming. A wake after expiry may finish its one bounded pause attempt, but cannot authorize restore. Even `SUCCESS_DISARMED` returns whole-Gate `NO-GO`; the separate nine-stage verifier remains mandatory.

Native pause uses the fixed [pause endpoint](https://supabase.com/docs/reference/api/v1-pause-a-project); project identity/status uses the fixed [project endpoint](https://supabase.com/docs/reference/api/v1-get-project). TLS is mandatory, redirects are rejected, and the token goes only to the Management API origin. Preflight requires exact ACTIVE_HEALTHY identity and a successful read-only PostgreSQL17 probe. The currently implemented confirmation oracle requires exact fresh INACTIVE identity, explicit connection refusal on the bound Direct database endpoint, and HTTP503 from both the bound REST and Auth endpoints. DNS errors, TLS errors, timeouts, pending statuses, generic non-success and authorization failures remain UNKNOWN. This oracle has local transport-contract coverage; its behavior on an actual paused hosted project has not been observed. Do not claim emergency readiness or relax it merely because the normal-state preflight succeeds.

The incident checklist below maps all ten required configuration entries to concrete consumers and stop conditions. Values remain in protected operator artifacts; a row's specification is not proof that the hosted setting has been applied.

| Entry | Exact preparation / incident action / verification |
| --- | --- |
| region | Preserve the user-selected same Free organization and `ap-northeast-1`. Recheck the zero-cost plan/quote and active limit before arm. Provision only after exact source inaccessibility is confirmed within20 minutes; healthy Production is never paused to create a rehearsal slot. Verify the new project's actual identity/region/PG17 before restore. |
| extensions | Compare the complete captured extension/version inventory and require the design's pg_net, pg_cron and Vault availability. Check managed Auth77/Storage68 compatibility and the explicitly bounded index/ACL differences. Perform the R5 empty-public default-privilege preparation as ordinary postgres, then restore the unchanged six files. Stop on an unknown platform baseline or permissions difference. |
| authProviders | Restore the protected Auth export's selected providers and settings; the saved selection is Email enabled with other providers disabled. Independently read back configuration. Auth table restoration is separate from provider configuration; do not create replacement user IDs. |
| authRedirects | Restore the exact protected Site URL/redirect list, including the application confirmation flow implemented in `app/auth/confirm/route.ts`. Compare saved values privately; never infer new callbacks or expose URL/private input lists in logs. |
| endpointCutover | Identify the actually deployed Production application artifact first; `wrangler.jsonc` names `v-streamer-tools`, but source configuration is not deployed identity. Prepare an emergency rebuild of that accepted production application revision with the Recovery public URL/key and server credentials. `lib/supabase/env.ts`, `server.ts`, `browser.ts`, billing runtime and durable stores consume these settings. Rebuild/redeploy is required for browser-bundled NEXT_PUBLIC values; a server-secret update alone is insufficient. Keep normal feature deployment/Paid activation outside this operation. |
| secretRotation | Replace the recovered project's `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` and actually consumed `SUPABASE_SERVICE_ROLE_KEY` together. `SUPABASE_SECRET_KEY` is a declared server name, not a substitute for consumers that explicitly read service-role. Obtain new project credentials through protected input; do not copy a paused project's project-scoped credentials. Preserve the existing application-level YouTube credential-reference/token-store key references and versions needed to read restored records; `lib/comment-translator-youtube-oauth-token-store-persistence.ts` identifies `YOUTUBE_OAUTH_TOKEN_STORE_KEY_REF` / `_VERSION`. Do not blindly rotate these independent keys or OAuth/Stripe secrets. |
| clientReauthentication | Keep old project tokens/cookies out of the new session. Exercise the new-project login and `/auth/confirm` path with the same restored account identity. `app/account/actions.ts` supplies signOutAction, but remote sign-out against an unavailable old project is not proof of global invalidation. Verify old tokens fail and the new authenticated session is bound to Recovery; never inspect or log browser token values. |
| freeSmoke | With only the approved operator able to reach the recovery application, verify login/account, `/tools/comment-translator`, saved Free settings/quota projection and the approved bounded session Start/Stop path. `app/api/comment-translator/session/route.ts` is the route boundary. A live translation/provider/OAuth exercise requires its specific incident authorization and fixture; otherwise report that portion unverified. Confirm no Paid entitlement promotion, Checkout or maintenance execution. |
| reopenWrites | Keep an approved edge-level maintenance/access restriction until readback and operator smoke pass. The existing `COMMENT_TRANSLATOR_FREE_BETA_RUNTIME_ACCESS` / `COMMENT_TRANSLATOR_PRIVATE_LAUNCH_ALLOWED_USER_HASHES` gate in `lib/comment-translator-private-launch-access-gate.ts` can restrict Translator access, but does not cover every Auth/webhook/direct writer. Do not use it as a global write freeze. Exact edge rule/allowed operator, all writer coverage and reopening authority are still required; cutover remains blocked if they are absent. Restore the reviewed access configuration only after explicit writes-reopen approval. |
| exactSourcePause | Bind the source ref privately to the reviewed Production Direct binding. Retain the watchdog pause/confirmation receipt and independent complete history/data/Auth/grants/RLS/Vault/Storage readback. Keep the source paused; no automatic unpause or deletion. In `workers/comment-translator-paid-open-next-wrapper.mjs`, prevent `COMMENT_TRANSLATOR_PAID_SCHEDULER_AUTHORITY` from selecting `cloudflare-cron-fallback`; verify actual fallback state separately from the source guard and preserve inactive database Cron. |

Remaining operational inputs are explicit: trusted native backup/executor receipt binding, actual paused-project oracle support, accepted supervisor host/lifetime, deployed Production application identity, protected recovery credential references, complete edge restriction/writer coverage and the individual incident/cutover/reopening approvals. No existing local PASS or checklist hash fills these gaps. No source pause, project provisioning, Auth change, credential rewrite, deploy, restore or cutover was performed in this local cycle.

## Vault / inactive Cron

2026-09-06 PG17拒否境界訂正: `\quit 4/5`は終了コード指定として機能しないため使用禁止。reserved名の既存/partial拒否は固定SQLSTATE `PGT01`、commit前postcondition失敗は`PGT02`のSQL例外で現在transactionをabortし、`ON_ERROR_STOP=1`のpsql exit3で停止する。bound runner/local adapterには`VERBOSITY=sqlstate`を指定し、本文・直接メッセージを表示しない。local harnessはexit3と唯一の期待SQLSTATE ERROR recordを組で検証し、stdout0、有限capture、既存行保持または独立zero-row readbackを要求する。generic nonzero、別SQLSTATE、context内のトークン、複数/欠落ERROR recordは期待拒否として受理しない。second-createのtest-only注入例外は`PGT03`として同じ厳密判定を行い、故障タイミングは変えない。advisory lock、既存predicate、bound input、暗号化、成功時だけのCOMMIT、hosted readinessによる接続前blockは維持する。変更対象はVault runnerとlocal integration contractの2ファイルだけ。

2026-09-07 bound値忠実性訂正: PG17の同一adapter・新規隔離環境で、非秘密stress値と予約名の独立MD5比較を各形式1回実施した。SQLリテラル用の変数展開は両値不一致、raw変数展開は両値一致（各exit0/stderr0）だった。runnerとpartial fixtureのbind引数はraw変数展開へ統一し、SQL文字列連結やshell再評価へ置換しない。source回帰は旧形式を拒否する。actual-local成功caseでは実runnerから取り出した同じbind行と既存adapterを用い、引用符・改行・バックスラッシュ・空白を含む非秘密値の独立期待digestとのサーバー側一致を最初に要求する。結果は固定一致フラグのみ検証し、値・digest・decrypted secretは表示/取得しない。不一致時は書込み前停止する。既存4ケース・厳密出力/SQLSTATE・readback・cleanup・hosted接続前blockを維持し、success出力の外部形式は変更しない。

Vault予約名は `comment_translator_paid_maintenance_url` と `comment_translator_paid_cron_token` の2つ。deterministic transaction advisory lock下で両名の不在を再確認し、bound/session inputから原子的に作成する。commit前にcount=distinct count=2、encrypted record non-null、duplicate=0を確認する。作成・assertion失敗は両件rollback。decrypted値は取得しない。現在のhosted Writeはreadiness未検証で接続前blockを維持する。

actual-local試験は実runnerのtransaction生成を再利用し、成功2件、rerun拒否、partial-existingのID保持、second-create失敗とpost-create assertion失敗の独立rollback readbackを確認する。syntheticだけでは完了しない。既存DB/volumeを使わず、cached digest、network none、tmpfs、log-driver none、秘密非表示、所有確認cleanupを必要とする。

2026-09-06 local harness訂正: cached image既定の`postgres -D /etc/postgresql`を保持し、その後に既存のlogging保護設定を付ける。所有検証も同じ完全CMDを照合する。bare case DBへのextension導入ではimage primaryとACL初期状態が異なるため、4ケースを順次別々の新規owned containerのimage-initialized primary DBで実行する。第1環境は成功とrerun、第2はpartial-existing保持、第3はsecond-create rollback、第4はpost-create assertion rollback。各環境の開始前にreserved名不在と既存のmetadata/権限条件を確認し、終了ごとに所有確認cleanupとresidue確認を行う。失敗したら後続ケースを開始しない。既存runtimeのreuse/reset、DB clone、GRANT/ALTER ROLE/owner変更、adminによる通常secret transaction代替は禁止。runner SQL、stress input、期待値、security比較は変更しない。primary単独成功は全matrix成功へ昇格させない。

隔離local case DBのschema scaffoldと通常検証は`postgres`を維持する。superuserが必要なextension導入だけは、所有確認済み新規container内で既存`supabase_admin`の存在・login・superuser能力を確認して実行する。ロールへのgrant、ALTER ROLE、owner変更はしない。導入後のmetadata/readback/secret transactionは必ず`postgres`へ戻す。bootstrap能力不足ならSETUP_BLOCKEDとし、hosted実行の権限や接続方式へ流用しない。

Cronは予約名 `comment-translator-paid-maintenance`、cadence `*/5 * * * *`、承認済みprivate maintenance-from-Vault呼出しだけ。database/usernameはpostgres。1transaction内で不在確認 → schedule → commit前にactive=false → exact row確認を行う。直接cron.job更新、二重scheduler、activationは禁止。

commit前baselineと新job identityをprivateに束ね、commit後10分（2 cadence）の開始・終了でinactive、同じjobのbaseline後run delta=0を確認する。無関係な過去job履歴を混ぜない。matching=1、active=0、scheduler process=1、承認済みcommand digestとcadence一致、Cloudflare fallback absent/inactiveを実測する。

## Rollback / stop / GO監査

bridge commit前はtransaction失敗に伴うcatalog/historyの原子性を確認する。commit後にarchiveだけを戻してhistoryを残す操作は禁止。review済みforward修正か、期限内pauseが確認された復旧先restore/cutoverのみを使用する。Vault/Cronの値や業務行を直接書き換えてrollbackを模擬しない。

target/commit/pending/fingerprintの不一致、legacy非zero、source-era変更、完全manifest/ACL/RLS/raw digest/dependency不一致、backup/recovery未完備、advisorの新規in-scopeまたはhigh/critical問題、Vault重複、Cron active/run発生、fallback二重化、秘密表示の必要性は停止条件。失敗したmutationを自動再試行しない。原因と変更するscopeを確定してから実行条件を更新する。

GOには、review済みsourceのPreview統合merge、両環境final56と完全canonical/security一致、archive/source-era保持、実backup/rehearsal/final snapshot/watchdog/無償復旧能力、extensions利用可能、Vault exact2名、inactive Cron exact1件と10分run0、fallback無効、phase別rollback証拠がすべて必要。通常deployとactivationは未実施のままとする。未取得、未実装、stale、間接証拠、矛盾が1つでもあればNO-GO。

2026-09-09 Storage baseline release identified: native verify-full/read-onlyでProduction storage.migrationsの68件を取得。公式storage v1.72.11 commit518862dff5b2e4f3386ad51e4a172e91d0ce2ad2のtenant SQL1-67は全件Git blob整合・本番name/hash一致。bootstrap0もpostgres-migrations v5.3.0公式SQLのname/SHA1(filename+SQL)一致、合計68/68。固定local storage v1.61.7は0060までで、差分0061-0067は公式platform更新と確認。v1.72.11は比較候補であり、本番稼働imageや復元先catalog適合の証明ではない。新image取得/新version起動/管理SQL適用は未実施。次は承認済みの使い捨てlocalサービスとして候補版を起動し、完全Auth/Storage catalog・ACL・依存関係を再比較する。本番書込み0、user delta未確定、Gate1 NO-GO。

2026-09-09 Storage v1.72.11 local candidate comparison: 承認済みimageを取得しsha256:d189b8072865b5d8289af9435dee18eb6eef82f465e1d7d092fa9665bbd4ce54へ固定。既存所有local CLI環境内のStorageのみ候補serviceへ切替、local DB alias/network一致を確認しhosted credentialは渡さず実行。Auth77/Storage68とStorage name/hash全68一致。しかしnative RR/RO catalogはsource595/candidate621 objects、dependencies795/802で不一致。共通function21の定義は完全一致、source-only Auth index4、candidate-only Iceberg2tablesと関連18columns/5constraints/5indexes、共通storage.buckets/objectsのowner grant-option表記2差が残る。Auth4indexは公式Auth v2.192.0 indexworkerの定義に一致し、Storage0038/0047/0048はstorage.multitenantでIceberg table作成を分岐する。これは初期化設定差の説明であり、本番service設定の直接観測ではない。既存SQL token比較は文字列/dollar本文を十分保持していなかったため旧14/23・8/20件は判定根拠から除外。生本文を保持するとCRLF/LF由来の関数差も含むため、最終判断はnative catalog比較を使用する。2回目は不足していたnative定義/ACL/依存関係取得のため実施。両runの候補container cleanupとCLI cleanup2actions PASS、独立CLI container/network/volume・候補container・scratch各0。次は管理サービスの正式な初期化設定（Auth indexworkerとStorage multitenant分岐）を空の隔離環境で再現する設計確認。管理tableの手動DROP、role権限拡大、本番書込み、6file本番backupは未実施。user delta未確定、Gate1 NO-GO。
