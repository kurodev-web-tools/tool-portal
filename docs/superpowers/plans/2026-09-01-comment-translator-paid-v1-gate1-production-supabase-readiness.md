# Comment Translator Paid Core v1 Gate 1 Production Supabase Readiness Implementation Plan

## 2026-09-10 Post-PR826 local retained replay acceptance

- [x] Verify PR826 merge dd19e85 and protected capture13/executor20/original6 binding.
- [x] Compare full pinned empty managed catalog/dependencies/identity mappings, history, exact ACL/defaults and empty-data conditions on a fresh owned isolated target.
- [x] Run the published ordinary-postgres retained adapter once: pre-reset source, fixed-reset delta and independent committed readback all PASS (118663ms). Dispose rehearsal target; owned containers/volumes0, permanent claims preserved.
- [ ] Establish supported Hosted P2 whole-Recovery HTTPS isolation/reopening and target-specific execution authority. Local restoration success does not establish Hosted acceptance, incident freshness or overall recovery deadlines; Gate1 NO-GO.


## 2026-09-10 Post-PR825 email-PKCE continuation

- [x] Verify published capture/executor/original inputs and attest the isolated empty target profile without widening privileges.
- [x] Execute one retained six-file atomic attempt; retain its generic guard refusal, stop/disposal evidence and original-hash verification without accepting restoration.
- [x] Identify official method-named email-provider variants, reproduce the rejection with actual Auth API output, and correct the shared validation/projection/reset predicate locally.
- [x] Verify old/fresh API codes, original-password/ID preservation, eight unsupported pairs and existing strict/Auth-TOTP regressions.
- [ ] Publish the repair and rebind the executor before further retained-data restoration. Real restore and Hosted acceptance remain unproven; do not bypass publication or reset an in-flight replacement budget.


## 2026-09-10 Post-PR824 baseline continuation

- [x] Verify merged805fade published capture/executor binding and protected six-file input without native restoration.
- [x] Reproduce missing migration-history42P01 on an empty official managed baseline; do not create fake history to pass inspection.
- [x] Separate baseline fingerprint from strict post-restore source/history checks and verify empty baseline plus security/data drift locally.
- [ ] Publish the repair, attest the complete target profile and run the retained six-file atomic restoration. No unpublished-code bypass or Hosted operation is authorized by the local repair.


## 2026-09-10 Approved disposable-target failure policy

The user approved replacing complete sequence rollback with permanent quarantine and disposal/recreation of a failed recovery target. Implement and verify locally; Hosted deletion still needs an exact target and separate authority. Keep source publication, original bytes, successful-state/reset checks, P2 before real data and the existing overall deadline.

- [x] Add an explicit discard-target-v1 builder policy. Keep the default strict sequence guard; only the known Auth refresh sequence may execute its original setter without transactional guarding under the new policy.
- [x] Persist a one-attempt claim for each immutable container and mounted data volume before SQL. Share the ledger across this repository's worktrees. Never clear it on failure, success or ordinary resource cleanup; refuse reuse in another process or a replacement container using old volumes.
- [x] Use the new policy only in the isolated ordinary-postgres adapter. Failure/uncertain outcome requires disposal and reports stopping separately from deletion; successful restoration remains quarantined. Preserve original source checks and total deadline.
- [x] Verify native Auth setter success, intentional row rollback with possible sequence residue on failure, persistent retry rejection and a fresh-destination attempt. Retain synthetic/strict regression coverage and update readiness. Do not claim Hosted isolation or execute the real retained backup with unpublished code.


## 2026-09-10 Approved Stage3 adapter continuation

Local implementation/contract checks completed. Real retained replay remains blocked both by unpublished executor files and by the Auth sequence ownership requirement. See active readiness for the unapproved failed-destination disposal alternative; do not relax rollback semantics or execute real data under this implementation approval.

Goal: connect the retained capture to an ordinary-postgres atomic local rehearsal without relabeling the acquisition commit or bypassing executor publication. The user approved continuation after the Stage3/4 preflight. Existing single-agent, no external mutation and publication gates apply.

- [x] Add separate historical producer validation (record hashes against the acquisition commit, ancestry under the accepted preview commit) and loaded/current executor validation against the accepted commit. Preserve original receipts and hashes; do not execute historical JavaScript.
- [x] Join restricted persisted process/acquisition/manifest inputs before loading SQL. Check run/source/binding/native completion/snapshot and file agreement. Return a one-use in-process verified input, not a caller PASS flag.
- [x] Add an owned local Docker adapter using only postgres, immutable container ID, exact image/baseline, non-superuser check and no externally exposed target. Use one bounded restore transaction and independent committed readback; retain failure/cancel/unknown-exit rejection. Keep the synthetic adapter and original six-transaction path intact.
- [x] Test wrong historical hash, current-source changes, unrelated commit, record/file mixing, unpublished executor, wrong role/target, rollback and committed readback. Complete scoped lint/contracts and direct review. Real backup execution waits for publication of the changed executor; Gate1/Hosted isolation remain unaccepted.


## 2026-09-10 Approved continuation through PR creation

The user requested a goal ending at PR creation (not merge). Reuse this worktree and the approved atomic-restore design. The verified base is `codex/comment-translator-paid-v1-preview` at the same f1959bee tree as the current branch. Commit/push/PR of the cohesive implementation, tests and Gate1 records are authorized; Hosted operations and merge are excluded.

- [x] Add an opt-in lexical COPY-text span reader with strict pg_dump headers and opaque row payloads. Preserve original UTF-8 bytes, ordering and hashes; keep the default parser and six-transaction path unchanged. Reject malformed terminators, non-text modes and unsupported commands.
- [x] Recognize only exact pg_dump sequence setters. Before the containing data artifact, capture the current sequence state, use transactional ALTER SEQUENCE RESTART, and restore that state inside the transaction; then execute the original artifact unchanged. The initial PG17 native probe confirmed that raw setval leaks across rollback while this guarded form preserves original values/is_called after rollback and injected failure. Verify false/true states, permissions, ordinary commit and connection-loss rollback; never waive ownership requirements.
- [x] Exercise native pg_dump COPY/setval fixtures plus the existing atomic failure/Auth/TOTP checks. Related 102 tests and scoped ESLint passed; primary-agent implementation review completed and operational limits updated.
- [ ] Commit/push and create a PR to the verified preview integration branch. Published-source real-backup acceptance and Hosted P2 isolation remain later stages. Retain the 13 unattributed anonymous-volume candidates; do not delete by age.

- 2026-09-10 Approved atomic local implementation: ユーザー承認後、単独で追加経路を実装。元6artifactのbyte/hash/orderを維持し、同一transactionでsourceState照合→固定credential reset→全行/sequence/security定義の意図的delta照合→commit、別native接続で再照合。既存6分割経路は不変。合成異常系とcached Auth稼働中の旧credential拒否・元password/ID login・同一TOTP要素のAAL2を検証するtracked runnerを追加。real backup/COPY/setval互換・通常postgres・published producer接続・Hosted P2隔離は未受入れ、外部変更0/Gate1 NO-GO。初期試験の匿名volume削除漏れを修正し、新規試験はobserved mountの消滅も検証。同時間帯の未帰属dangling volume13件は保全、全volume cleanup完了とはしない。詳細と検証コマンドはactive readiness先頭。

- 2026-09-10 Atomic Auth visibility LOCAL_PROTOTYPE_PASS: 正式restoreは6artifact個別transaction後readbackであり、最後にresetを追加するだけでは旧credentialのcommit済み窓が残る。Auth稼働中の架空5表を単一transactionで復元→同接続で元全行一致→限定reset→commitするprototypeを実行。別接続user0/旧magiclink403を途中2地点、commit後旧link403・元password/ID login成功、故意SQL失敗で復元user0へrollback確認。6file正式経路/通常postgres/全token/Hosted隔離の受入れではない。正式transaction/readback契約変更は未実装・承認対象、外部変更0/Gate1 NO-GO。

- 2026-09-10 Transient credentials/TOTP LOCAL_SYNTHETIC_PASS: 一時token6種8行・user token列8値とemail系PKCE4方式を限定失効。旧メール12経路403、電話変更OTP403、再認証nonce422（password不変）、PKCE4方式404、旧access/refresh7組拒否。新メール6経路/電話OTP/再認証/PKCE4方式成功。TOTP実登録1件の全行をreset/restart後保持、同じfactorで新challenge→AAL2成功。user非token列/identity/business/tableACL/RLSflags・対象外flow1行を厳密保持。SQL合成電話/再認証/PKCEを含みSMTP/SMS/外部provider発行は未実証、Hosted隔離/復旧は未完了。外部変更0、cleanup残存0、Gate1 NO-GO。

- 2026-09-09 Confirmation/email-PKCE LOCAL_SYNTHETIC_PASS: 前回reset後も旧signup/inviteリンクとSQL合成PKCE codeからsession発行を再現。確認token2行/ユーザーconfirmation_token2値＋email/magiclink flow_state1行を限定失効する候補で旧signup/invite403/otp_expired・旧PKCE404/flow_state_not_found、新規確認リンク2/合成PKCE1成功。既存旧access4/refresh6/メール4経路拒否、新loginもPASS。reset前後のuser全列は指定token列のみdelta、ID/password/確認済み状態/identity/business/ACL/RLSflags保持、対象外token1保持、SAML空集合不変。実OAuth/PKCE発行全工程・他OTP/MFA非空・Hosted隔離は未検証。外部変更/送信0、cleanup残存0、Gate1 NO-GO。

- 2026-09-09 Bounded email-token reset LOCAL_SYNTHETIC_PASS (user approved): Auth v2.192.0でrecovery_token種別2行削除＋users.recovery_token2列値を空文字へ変更（session/refresh削除も継続）。旧magiclink/recovery各token_hash・email_otp計4経路403/otp_expired、新session0。旧access4/refresh6拒否、新magiclink/recovery2件と元password login2件は同一user IDで成功。全user列の差分はrecovery_tokenのみ、password hash/identities/business/table ACL/RLS flags・対象外reauthentication token1行保持。未知trigger0/one_time_tokens被参照FK0、独立cleanup残存0。他OTP/PKCE/MFA非空/Hosted隔離は未検証、本番接続・送信・設定変更0、Gate1 NO-GO。

- 2026-09-09 Old email-link gap LOCAL_REPRODUCED: Auth v2.192.0の架空user向けgenerate_link（送信なし）で作った旧magiclinkはsession/refresh全削除＋別署名secret後もPOST /verify=200、同一userの新session発行。旧access4/refresh6拒否と元user/business保持は引き続きPASS。従来session-resetのみでは全旧認証情報失効にならない。次はone_time_tokensのrecovery_token種別＋users.recovery_tokenの限定失効を別local synthetic検証として提案（未実装、users全行保持条件への意図的delta）。PKCE/他OTP種別/Hosted経路隔離は別未検証。本番接続/設定変更/送信0、検証container/network残存0、Gate1 NO-GO。

- 2026-09-09 Session-reset LOCAL SYNTHETIC PASS (user approved): cached Postgres17.6.1.140/Auth v2.192.0、架空2user/4sessionで旧access4拒否・旧refresh4拒否＋未紐付/失効済fixture2拒否、同一user ID新login2成功。Auth停止後にtransaction内DELETE refresh_tokens→sessions、別synthetic JWT secretで再起動。削除直後/再起動後のusers（password hash含む）/identities/business全行とtable ACL/RLS flagsが完全一致。MFA factorsは空集合のみ、MFA実利用・実backup復元・hosted signing keys/全HTTPS隔離は未検証。専用container/network削除、外部変更0。local検証承認はhosted実行承認ではなくGate1 NO-GOを維持。証跡: `.tmp/gate1-evidence-20260909/synthetic-session-reset-result.json`（ignored、秘密値なし）。

- 2026-09-09 SMTP/Turnstile checkpoint: user reports本番SMTP新key保存・メール到着/login成功、旧key保持を明示。指定resend-streamer-tools.txtは単一key形式/読取可/広範Allow0（実key使用なし）。旧resend-api.txtは撤回済。Turnstileは本番sitekey一致widget1・許可host2/現在host包含・secret再参照可/以前のSupabase観測と非公開一致、rotation不要。登録失敗表示は別件未解決。独立delivery/log照合・Recovery実証なし、Gate1 NO-GO。

- 2026-09-09 Resend identified: Supabase SMTP host確認、Resend read-onlyでkey metadata2件（Kuro Streamer Toolsを含む）、verified/sending enabled domain1件・送信元一致。キー本体は作成時のみ表示、使用中key identity/保管済原本は未確認。原本がなければGate1 Recovery SMTP/sending_access/既存送信domain限定の別keyを提案（未作成、既存key/Production設定不変）。保存先と作成の個別承認が次条件。メール送信0、Gate1 NO-GO。

- 2026-09-09 Preservation/reauth packet prepared: Auth242field名を204private-config/38secret-referenceに完全分類（値0、raw PATCH不可、無効連携secret取得不要）。別補足保存の対象/停止条件と公開前P1-P6（binding/遮断/復元保持/旧access拒否/旧refresh拒否/新login再開）を確定。SMTP原本参照・保護保存先・全HTTPS隔離・全session失効手段/fixtureは未確定。準備ゴール完了、EXECUTION_NOT_READY、外部変更/秘密値新規保存なし、Gate1 NO-GO。

- 2026-09-09 Solo-operator ownership accepted: ユーザーが復旧/設定保管/再開を担当、追加external writerは把握範囲でなし。既知Stripe/YouTube等は別途inventory保持。承認specどおりfinal backup中Free online/RPO20分を維持し、正常source全面freezeを追加要件にしない。復旧先公開前保護は別条件。Data API停止のみで全HTTPS遮断とは扱わず、旧refresh sessionの再認証確認も未成立。担当回答待ちは解消、外部変更なし/Gate1 NO-GO。

- 2026-09-09 Preservation/restriction proposal: SMTP_PASSはmaskedでresponse保存では復元不能、元secret参照/取得担当が必要。Turnstile site-key/secret/hostname整合を明記。単一Worker Access(all traffic/operator限定)案と優先rule/WebSocket制約を公式仕様で確認。Supabase DB IP制限はHTTPS Auth/REST/Storageを覆わずglobal freeze不可、直接HTTPS隔離は未解決。非secret補足/独立secret参照/新project credentialを分離しowner6役を定義、担当/外部writer回答待ち。外部変更なし、PREPARED_NOT_APPLIED/Gate1 NO-GO。

- 2026-09-09 Post-PR822 / NATIVE_LOCAL_REVALIDATION_ACCEPTED: Preview mergef1959beを確認し同worktreeをfast-forward、公開13producer照合PASS。新しい6file467983bytesを承認済read-only手順で取得（checksum74672ms、管理baseline前後一致、本番書込み0）。最新tracked readerから再生成したA12で6transaction復元・厳密sourceState一致・migration22→56・元49table/500row/元履歴22件・Auth/Storage/ACL/RLS/source-era依存保持を確認。4MiB正式local readerで1134034-byte artifactを取得しcanonical/archive/shape PASS、診断用代替読込なし。候補2とCLI cleanup2actions PASS、独立inventoryで所有container/volume/network/scratch各0、active cron0。旧失敗・回復receiptと新capture/restore receiptは別々に保持。公開済修正のlocal実機再検証完了、hosted復旧可能性は未証明、Gate1 NO-GO。今回追加source変更/main反映/hosted restore/apply/deployなし、結果3文書のみ未コミット。

- 2026-09-09 Goal 3 / POSTAPPLY_CAPTURE_LOCALLY_ACCEPTED・UNPUBLISHED: ユーザーの「ゴール定めて進めてください」で1MiB制限修正を実施。POSTAPPLY_MAX_OUTPUT_BYTES=4MiBを共有し、local Docker reader・正式psql query受信/正規化・厳密JSON parser・指定4stageのcatalog/catalog-expectations読込を整合。通常preflight/バージョン/他role/receiptは1MiB、総JSON32MiBを維持。実測1133757bytes・4MiB丁度を受入れ、+1byte/UTF8超過/ENOBUFS/signal/stderr/不正上限を拒否。9検証ファイルPASS（最終状態で29 TAP checks）、変更/追加9mjs ESLint PASS、primary差分レビュー。保存済catalog1133756bytes（改行除外）を実Node pipe→正式transport/実parser/shape inspectorで照合し原本hash不変、DB接続0。SQL/権限/終了不明時cleanup停止条件は変更なし。Goal3ローカル修正完了、未公開・正式DB再実行なし、既存receipt昇格なし、Gate1 NO-GO。次は明示承認によるcommit/push/PRと公開後の正式検証。

- 2026-09-09 Goal 2 / LOCAL_FULL_RESTORE_ACCEPTED: PR821 merge28cd0b9・公開13producer一致を確認し、fresh Production6file467983bytesを承認済手順で取得（同snapshot4gzip dump、checksum75942ms、前後baseline一致、本番書込み0）。所有隔離localで6transaction完全復元・厳密sourceState一致後、CLI migration34件を実行し履歴22→56を確認。元49table/500rowのOID・件数・digest、元履歴22件の全行digest、Auth/Storage595objects・ACL/RLS、source-era構造/依存を保持、canonical/archive PASS、active cron0。通常readbackは1MiB上限で停止したため、その失敗receiptを保存し、同じ復元済DBを4MiB上限の固定read-only診断で再読込（1133757bytes、VALID_LOCAL_ONLY）。正式collectorの1MiB制限は未修正でGate1証拠への昇格不可。候補container2・CLI cleanup2actions PASS、独立inventoryで所有container/volume/network/scratch各0。Goal2のlocal復元検証完了、hosted復旧可能性は未証明、Gate1 NO-GO。primary単独、今回追加source変更/commit/push/PRなし。

- 2026-09-09 Recovery coverage preparation accepted: SMTP/CAPTCHA有効・mail template13内容ありを既存Auth観測から分類、hook0/SAML無効。fresh read-onlyでEdge Functions0/Storage bucket0/Realtime publication table0/public trigger1。本番source5d91202と現sourceのAPI/Server Action・GET callback・scheduler・直接Supabase/DB writerを8分類し、設定7分類と保全/遮断/再開6段階をactive readinessへ具体化。追加秘密設定の保全・enforceable isolation/所有者・supervisor/停止oracle・hosted実証は未成立。準備ゴール完了、外部変更なし、Gate1 NO-GO。

- 2026-09-09 Live Auth/Worker: selected Auth18fields semantic一致（redirect順序/password表示差のみ）、追加連携0。Cloudflare Dashboardで本番f45136b2/traffic100%、main5d91202(PR768) build linkage、24bindings中fallback selectorなし・Cron trigger0を確認。CLI認証失敗はDashboard観測と分離。artifact bytes未照合、hosted pause/supervisor/設定全範囲/writer coverageは未成立。外部変更なし、Gate1 NO-GO。

- 2026-09-09 Recovery input reconciliation: preserved selected Auth export706bytes/hash一致、Emailのみ/redirect4、広範Allowなしを再確認（継承有効、live設定比較は未実施）。Production read-onlyでStorage0/Vault0/active DB Cron0。Storage移行はscope外・非zeroなら停止。既存cutover consumer/停止oracleをsource確認し、hosted oracle・稼働app/fallback・supervisor/writer coverageは未証明。外部変更/source変更なし、Gate1 NO-GO。

- 2026-09-09 Hosted recovery preparation / LIVE_FREE_PLAN_AND_COST_CONFIRMED: read-only API confirmed Free plan, two healthy Tokyo projects and USD0/month project estimate. No third active Free slot; price does not prove provisioning. Preserve incident-only failed-source pause then recovery creation. Active readiness now records concrete backup/watchdog, PG17/baseline, Auth/Storage and cutover prerequisites. No external mutation; hosted recovery UNPROVEN; Gate1 NO-GO.

- 2026-09-09 Goal 2 / BACKUP_CAPTURED・RESTORE_BLOCKED_ON_TARGET_BASELINE: PR820 merge110bc03を確認し同worktreeをfast-forward。公開済12producer照合PASS。最新6file471938bytes、同snapshot4dump、checksum79459ms・native receipt118694ms、取得前後baseline一致、本番書込み0、idle transaction0。通常postgresで6transaction復元後、履歴22/Auth FK等は一致したがrowCountsとgrantsRlsSha256で停止。診断付きlocal再現で元48relationの欠落なし、local追加空3table（net._http_response/net.http_request_queue/supabase_functions.hooks）。security source662/local713行、source-only0、local-only51行はWebhook初期化、共通差3行はpublic sequence既定ACLの追加UPDATE3roleとStorage所有者grant-option表記2件。両local試行はcandidate2・CLI2actions cleanup PASS。replay22→56未実行。次は所有隔離先のWebhook初期状態除去・sequence既定ACL・owner表記合わせの限定承認と厳密再検証。backup原本/receipt/比較条件を維持し、管理schema DROPや権限変更を未承認実行しない。Goal2未完了、Gate1 NO-GO。

2026-09-09 Goal 2 clock prerequisite locally accepted / UNPUBLISHED: approved bounded future T0 tolerance1000ms, monotonic held-snapshot deadline, and no five-minute extension are implemented. Initial remaining budget is300000-(hostNow-DB T0)-1000ms; all held capture/persistence/close checks consume it monotonically. Preserve native T0 and real host observations; same-clock reversal still fails closed. Acquisition/native receipt/stage comparisons allow only bounded cross-clock ordering, with completion ceilingT0+299000ms. Related78tests PASS; final microsecond-boundary update affected32tests PASS; nine changed mjs ESLint/syntax and diff check PASS. Primary implemented/reviewed directly, no subagents. No fresh hosted capture/restore or OS changes in this slice. Publication approval is required before the source-bound native capture can resume; do not bypass the producer guard. Goal2 remains incomplete and Gate1 NO-GO.

2026-09-09 Goal 1 accepted: managed baselineの公式初期化を所有隔離環境で再現。Auth77/Storage68、595objects中593完全一致、残る2件はowner grant-option表記のみで実効差なし。内部RI trigger92/TOAST29行をnative identityで対応付け、795/762依存行の差33はpublic policy13/FK20で説明済み。重複保持、未説明差0。観測baselineの追加利用者DDL不要、空backup artifactは作らない。実復元先自身のbaseline適合、public依存再確認、fresh captureと通常postgresによる本番データrestore/replay、hosted復旧時間・切替は別条件。詳細はactive readinessの「2026-09-09 Goal 1」。これは以下の旧baseline未確定checkpointを置き換えるが、Goal 2/3と本番復旧は未成立、Gate1 NO-GO。単一primary agent、subagentなし。過去の自動delegation指定は現行AGENTS.mdで上書きされる。

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking. The Astra parent owns acceptance and delegates implementation to `luna-implementer` (`gpt-5.6-luna / max`) in the existing worktree; no new top-level task is required. Named `sol-reviewer` and `sol-repairer` use configured `gpt-6-astra / medium`; runtime evidence must be reported separately. Source implementation must not perform Preview or production migration apply, history repair, backup, project pause/provisioning, Vault write, Cron mutation, function invocation, deploy, activation, push, or PR creation without the matching later approval.

**Goal:** Add the source-controlled migrations, exact sanitized manifests, fail-closed contracts, and operator procedures required to move Gate 1 item 1 safely from source `NO-GO` toward separately approved Preview and production execution.

**Architecture:** Reconcile the 18 production-only history labels with inert markers, then use one pre-base bridge that recognizes exact legacy, canonical, or clean states and mutates only the three zero-row Paid-legacy tables and three Paid-legacy functions. Keep production-only overlay/moderator-share/dictionary/history objects outside the mutation allowlist, add `pg_net` before the Task 9 HTTP RPC, and encode every external operation as an explicit state transition backed by sanitized readback. Source implementation ends at locally verified operator readiness; every remote or destructive phase remains a separate approval.

**Tech Stack:** PostgreSQL 17 / PL/pgSQL, Supabase CLI `2.109.0`, Node.js 22 ESM contracts, PowerShell operator preflight, JSON manifests, existing Paid Core v1 migrations and contracts.

**Approved specification:** `docs/superpowers/specs/2026-08-31-comment-translator-paid-v1-gate1-production-supabase-design.md`

## R8 — Preserve native dependency multiplicities during post-apply verification

**Final engineering phase3 acceptance (2026-09-08):** PR #816 is merged at Preview integration `c1bcd3dc4cebe8a748322ab04081ac56a03ff673`, with the same complete tree as source `53c49f74197116f71b5ffc9c0a0d5b7260432e73`. Fresh native source observation checks all56 migration blobs/corpus. The published R8 native collector and complete desired-catalog/dependency-multiset comparator pass; independent linked list/dry-run confirms56/pending0, original operational rows and Cron/Vault metadata are unchanged, and run delta remains0. Advisor is unchanged from the postapply observation, including the13 recorded out-of-scope performance findings. No migration was reapplied. Restricted `phase3-final-acceptance.json` records engineering phase3 ACCEPTED while explicitly retaining full nine-stage authority UNESTABLISHED and Gate1 NO-GO. The paragraphs below describe the earlier local/publication checkpoint.

**Status (2026-09-08): implemented and root-accepted locally; publication/merge pending.** PR #815 merged R7 into Preview integration `05d6ff1c0afbffbc72be312f0bf0d60609133023`, with the same tree as source `ceedefdaf92f51251e2ab5196160587b2135c61d`. Under the user's engineering phase3 continuation approval, the hash-pinned Go CLI applied Preview's exact26 once. Independent linked list/dry-run then confirmed final56 and no pending migrations. Production was not mutated. Never retry the already completed Preview apply.

**Observed verifier defect:** full before/after readbacks match all33 canonical tables,81 complete desired FunctionRows/raw MD5/security, absent archive/source-era objects, and all1,443 dependency identity rows. Operational row count/digests and existing Cron/Vault metadata are unchanged, with Cron run delta0. However, the strict shape inspector rejects3 repeated dependency rows that were already present before apply. A separate read-only native query confirms that these are repeated complete `pg_depend` addresses on three existing partial indexes, not duplicate JSON keys or a collector join artifact. The failure is in the verifier's uniqueness assumption; migration reapplication or remote catalog edits are not remedies.

**Bounded repair and acceptance:** retain every dependency row and require nondecreasing ordering of complete identity keys. Compare the exact arrays, including multiplicity, against independently bound expectations. Do not deduplicate, normalize away fields, relax history/object identity uniqueness, change schema versions or public interfaces, or modify any migration. Ownership is limited to the postapply catalog validator and its existing contract, this plan, active authority and task.md. The root continues under the user's no-subagent instruction. Regression tests first reproduced the refusal, then passed identical repeated rows for Preview and Production while rejecting extra/missing occurrences on either side, same-count multiplicity substitutions, identity/type changes and ordering drift. Acquisition, eleven stage-evidence tests and operator contracts pass. The actual native read-only collector now accepts the hosted artifact with the local R8 validator; complete comparison against the desired canonical fixture and preserved before-state dependency multiset also passes. This local validator hash is explicitly recorded as uncommitted, not published source authority.

**Advisor and remaining boundary:** security35 before/after unchanged; performance42→55, adding10 `auth_rls_initplan` and3 `unused_index` findings outside the Paid scope after the approved historical migrations. New Paid-scope findings0 and new high/critical findings0; these out-of-scope findings are retained, not repaired or hidden. Restricted evidence is held under `r7-merged-*`, `r7-postapply-cli-preview`, `r7-dependency-duplicate-diagnosis.json`, `r8-preview-native-postapply.json` and `r8-preview-readback-acceptance.json`. Engineering phase3 remains at R8 publication/merge pending. After publication, reconcile the validator/source binding and perform final read-only acceptance; do not apply the26 again. Nine-stage trusted authority, Production backup/recovery/apply, Vault/Cron configuration and activation remain later work. Gate1 stays NO-GO.

## R7 approved repair — make engineering phase3 executable against the actual Preview state

**Status (2026-09-08, updated after PR #815): R7 is merged and Preview's exact26 apply completed once; R8 above records the remaining verifier/publication boundary.** Engineering phase3 means Preview migration apply followed by exact56 history, canonical schema/RPC/security and scheduler/Vault preservation acceptance. Local verification is not phase3 completion. Reuse the current worktree and branch; root directly implemented under the no-subagent instruction. The following R7 local acceptance paragraphs are historical pre-publication evidence.

**R7 local acceptance:** the same bundled Go CLI2.109.0 used for the approved linked list/dry-run was independently hash-checked (`59cd06ac674fdf5d6add75206408ada0a24b1dcb796d099c13b1f2aaf3f463f0`). `--phase=preview-convergence` passed in a newly owned PostgreSQL17 context: exact observed-before81/history30, original-source RED, exact pending26 to final56, all81 complete desired FunctionRows/raw MD5 and RPC security,9 negative cases, failed-bridge history absence, unchanged operational rows/Cron/Vault metadata and Cron run delta0. Clean/canonical compatibility and refusal of generic canonical-plus-Cron passed. The existing legacy `--phase=postapply-query` also passed with archive3 tables/3 functions, final56 and full desired canonical structure; it explicitly does not establish Production's source-era shape. Focused source/inventory/catalog/cleanup/query contracts and syntax checks passed. A separate native Preview read-only query observed114 reserved objects,0 unexpected reserved relation types and unchanged history30; repeat the complete entry and this supplemental check against the new published source before apply. Desired canonical fixtures/manifests and the user's two files remain unchanged. Independent Docker readback found0 generated containers/networks/volumes. One initial-run temporary directory remains because automatic approval review rejected its manual deletion; it contains no active runtime and is not reused. No hosted mutation, commit, push, PR or merge was performed for R7.

**Observed failure, independently checked before mutation:** Source `a7532540c5a4d3998b36eaf1dec45d741348a810` and Preview integration `4d346a8a78d3d5c1136ae75c635b7ccb18850f9a` have the same tree. Native CLI2.109.0 list/dry-run and Management API exact version/name rows confirm Preview30/pending26 and Production22/pending34, including the unapplied bridge and A3 in both targets. The fresh Preview complete catalog equals the previously validated v2 observation in all fields. Nevertheless, the bridge compares that pre-convergence catalog to the desired final canonical fixture, and rejects any existing Paid Cron job unconditionally. Preview has one required inactive job. Against the desired fixture, the observed catalog has3 table ACL differences,1 function ACL difference and78 raw function-definition MD5 differences; all other compared structural fields and dependency counts agree. A separate fixed read-only PostgreSQL query proves76 of the78 raw differences disappear after CRLF-to-LF normalization; only `ct_paid_azure_direct_fallback` and `ct_paid_record_provider_hourly_detail` remain semantically different under that diagnostic. The committed A3 contains those two definitions and four revokes, so it cannot remove the other76 raw definition differences. Normalized-hash comparison is diagnosis only and must never replace the final raw MD5 contract. No Preview apply was attempted.

**Recommended approach:** repair the two existing, independently confirmed unapplied migration files. Keep the exact56 migration inventory and Preview26/Production34 pending sets. Add a distinct exact observed-Preview entry classification to the bridge; do not change the desired canonical fixture or treat the observed Preview definition/ACL drift as the desired state. The alternative is additional migrations and a newly approved history/pending-count design; that is broader and is not proposed here. Removing/recreating the Preview Cron, editing remote history, skipping the bridge, normalizing the final comparison, or granting broader privileges are excluded.

**Write boundaries after approval:** `supabase/migrations/20260811000000_comment_translator_paid_v1_legacy_schema_bridge.sql`, `supabase/migrations/20260904000000_comment_translator_paid_gate1_a3_canonical_convergence.sql`, a distinct sanitized observed-Preview fixture `scripts/fixtures/comment-translator-paid-core-v1-gate1-preview-entry-observation.json`, the existing Gate1 migration/inventory/catalog contracts affected by these exact boundaries, the existing database integration harness and one focused Preview-convergence regression entry, plus this plan, the linked design, active authority and task.md. No application code, dependencies, other migrations, AGENTS or Codex configuration. Reuse the existing2d79 worktree, preserve the user's two files and all private artifacts, and keep one root writer under the current no-subagent instruction. A source revision change invalidates the old migration-corpus binding for subsequent apply; generate and review the new exact source binding after publication.

**Bridge acceptance:** retain exact-clean/exact-legacy/exact-canonical behavior and all zero-row, dependency, owner, ACL, source-era and archive guards. The new observed-Preview entry must match the complete separately named before-state (structural metadata, raw definition hashes and ACLs), have no Paid legacy/archive/source-era objects, and require exactly the already observed inactive job with the reviewed schedule/command/database/user metadata. Preserve the job and the two reserved Vault-name records without reading secret values or changing either subsystem. Known observed drift is an entry condition only; unknown objects, same-count substitutions, partial/mixed states, active/duplicate/wrong-command jobs and incompatible ACLs still reject. A generic canonical-plus-Cron exemption is forbidden. Bind the caller to the already approved private Preview target before invoking SQL.

**Forward convergence acceptance:** retain the two existing semantic reinstalls and the four exact revokes. Add only the76 separately enumerated newline-drift function definitions, selected from their final authoritative repository declarations and written as LF. Include final CREATE-only declarations after historical DROP statements, changing only that leading keyword to CREATE OR REPLACE for a non-destructive reinstall. Preserve the existing source-controlled DO rewrite of the OpenAI ordinary-429 retry guard; selecting direct declarations alone would undo it. Thus78 definitions are approved, with no changed signatures, new RPC identities, new business behavior, invocation, Vault/Cron operation, or broad grant. Validate exact source bytes after these explicit source-derived transformations and the complete81-row final function manifest including owner, security-definer, search_path/config, ACL and raw definition MD5. The desired canonical fixture remains unchanged. Reinstalling definitions must preserve current operational rows and all unchanged interfaces.

**Verification before publication or hosted apply:** reproduce the actual Preview before-state in one isolated cached local PostgreSQL17/Supabase context, using no production data. Independently verify its complete before-catalog and its30-row history. Run the current source against that reproduction and capture the expected bridge refusal as RED, then run the repaired source through the exact CLI pending26 path. Require56 unique exact version/name rows, full desired canonical structure and all81 full FunctionRows, archive/source-era absence, pg_net installed, operational row preservation, and unchanged one inactive Cron/two Vault metadata records with no new job run or secret-value reads. Include active/duplicate/wrong-command Cron, unknown catalog/ACL/hash changes, failed migration rollback and no-new-history negatives. Rerun affected clean/legacy/canonical contracts once for the new state; do not repeat unrelated broad passing suites. Parent reviews the actual source diff and independently accepts the final checks. Publish/merge the reviewed repair under explicit authorization before recomputing bindings and attempting Preview exactly once, followed by fresh complete readback and Advisor comparison. Production, backup/recovery and activation remain later engineering phases.

**Evidence retained:** restricted `phase3-cli-preview/result.json`, `phase3-cli-production/result.json`, `phase3-source-observation.json`, `phase3-preview-preapply-catalog.json`, `phase3-preview-bridge-comparison.json`, `phase3-preview-definition-diagnosis.json` and Advisor comparison artifacts. These are parent observations, not a completed final nine-stage authority bundle. Engineering phase3 remains incomplete and Gate1 remains NO-GO.

**Approved amendment (2026-09-03):** The user approved this written design/plan following independent `sol-reviewer / medium` review. Resume amendment checkpoints A1-A7 in the existing verified `gpt-5.6-luna / max` task, preserving existing dirty work and acquired artifacts. The written-approval conditions below are now satisfied for this bounded local source/test scope. Keep overall `NO-GO`; this amendment does not add remote, cleanup, tooling-repair/install, commit/push/PR, deploy or activation authority.

---

## Chunk R: 2026-09-07 next-task correction proposals and execution order

This section is the current continuation entrypoint. It does not replace the approved design or reduce Gate1 GO to source readiness. The user requested a new Astra parent task using the SAME worktree, with Astra designing every correction and Luna Max implementing it, including later review repairs. No implementation is performed while preparing this handoff. Alternatives below are proposals: Astra must settle their proof equivalence and exact interfaces before issuing a writer packet. Do not repeat approval requests for unchanged authorized scope; escalate material weakening, new cost, target changes or actions outside inherited authorization.

### R0 — Workspace, routing and evidence checkpoint

- Exact worktree: `C:/Users/taka/.codex/worktrees/2d79/V_streamer_tools`; branch `codex/comment-translator-paid-v1-gate1-source-readiness`.
- `git fetch origin --prune` succeeded on 2026-09-07. HEAD and fetched `origin/codex/comment-translator-paid-v1-preview` both equal `2775fba1f5ca687d23b25c88286ec5125d1663ec`. This is not publication of the dirty patch. Preserve all56 modified/untracked paths, especially user-owned `AGENTS.md` and `.codex/config.toml`; no reset, new worktree, branch cleanup or root-checkout substitution.
- Baseline runner SHA-256: `4521ed4611b7ba2930e76c2de08c609ab601c8c3bb8f8a18e0052671a436a4f5`; Vault harness: `a2477ac49d9bfd52efb9a1edee7d81024af281d63392b32d3381a5eddcbb1267`. Capture the full dirty-file hash inventory before the next writer; Git diff alone omits untracked contents.
- Existing evidence: parent TypeScript PASS, lint errors0/new warnings0 (2 baseline warnings), Next and Cloudflare production builds PASS. Vault parent synthetic/default/syntax and scoped semantic review passed; actual byte fidelity, success/rerun identity and partial preservation passed. Second-create fault installation failed42501; both rollback cases remain unverified. Build evidence does not prove hosted readiness.
- Source regression accounting: 41 PASS records, Vault default is SETUP_BLOCKED rather than PASS, one pre-existing default-privileges failure,9 later entries unexecuted; independent DB integration and saved-artifact diagnostic remain separate. Do not blindly replay passing suites. The Production saved canonical wrapper is incomplete; Preview r13 is preserved, not fresh remote evidence.
- Parent `gpt-6-astra / medium`: design, packets, documentation, Git, final acceptance. All code implementation and follow-up code repair: exact `luna-implementer / gpt-5.6-luna / max`. The user's latest instruction overrides the optional `sol-repairer` implementation exception. `sol-reviewer / Astra medium` is read-only; `sol-debugger / Astra medium` is bounded diagnosis. Never substitute a generic worker. Independently exposed runtime metadata is required for observed identity; otherwise report configured and unverified separately.
- One active writer. Each packet contains OBJECTIVE, APPROVED SPECIFICATION, ACCEPTANCE CRITERIA, FILES AND OWNERSHIP, INTERFACES, CONSTRAINTS, VERIFICATION, RETURN. Reuse a suitable child for related slices. Parent inspects the actual final delta and runs relevant checks once per final state. Review findings return through Astra's written repair proposal to Luna, never reviewer self-repair.

### R1 — Repair Vault fault injection without expanding privileges

**Files:** `scripts/comment-translator-paid-core-v1-gate1-vault-binding-integration-contract.mjs` only by default. The production runner `scripts/comment-translator-paid-core-v1-gate1-vault-write.ps1` must remain byte-identical unless Astra separately proves a production defect and assigns a new bounded packet.

**Known cause:** original fault installation under postgres returned exit3/stdout0/stderr14/one SQLSTATE42501. Schema, function and trigger creation are in that installation; the rejected individual DDL has not been identified. Do not call the transaction rollback broken or assume trigger creation was conclusively isolated.

**Approaches:** (a) preferred: instrument an in-memory copy of the exact generated transaction at its two fault sites; (b) keep trigger injection only if it can be proven to work under existing allowed privileges; (c) GRANT, ownership changes or replacing normal execution with an administrator are excluded. An unconditional error before any first write is not an equivalent substitute.

- [x] Astra maps both original fault sites and confirms the alternative preserves first/second-create ordering, the real transaction boundary and the original commit postcondition. If this cannot be proved, stop with the exact missing proof; do not implement an easier test.
- [x] For the second-create variant, replace only the exact second create expression with a test-only failure call in a disposable schema the normal role can already create. The injected call must first verify the first reserved row exists and the second does not, then raise PGT03. A failed injection precondition must produce a different state, never PGT03. Do not require a trigger or change Vault table privileges. Fail closed if the required existing schema/function capability is unavailable.
- [x] For the postcondition variant, execute both original creates, verify the real precommit state is exactly the expected two rows, then force only the original postcondition-failure branch to raise PGT02. A test injected before both creates is not equivalent. Do not weaken or modify the production assertion.
- [x] Prove each transformation has exactly one intended anchor and unchanged surrounding transaction text. Add RED tests for missing/duplicate anchors, early injection, wrong state, skipped first/second create, bypassed postcondition, output leakage and cleanup failure. Test-injection setup errors are not successful fault evidence.
- [x] Luna implements the settled transformation and regression only. Preserve raw bind fidelity, fixed PGT01/02/03 classification, exit3/strict capture, four sequential fresh primary environments, owned cleanup and all current readback assertions.
- [x] Parent and reviewer verify source first. Parent then runs one actual matrix at the accepted new hash: `node scripts/comment-translator-paid-core-v1-gate1-vault-binding-integration-contract.mjs --phase integration`, with the already approved minimal environment/cached-image guards. Both failure cases must independently read back zero rows in a new transaction. A failure stops later cases and prohibits an unchanged full retry; diagnose the new evidence before another packet.

**R1 settled correction (2026-09-07, current parent):** replace only the second create expression with a run-owned VOLATILE SECURITY INVOKER helper under postgres. Its own query must establish first reserved row/id/encrypted counts1, second0 and total1 before PGT03; any different state uses PGT04 and cannot count as expected failure. This proves the transaction failure boundary after the first write, not an internal Vault INSERT fault. The postcondition variant keeps both real creates and the original complete metadata query, requires its real result true, then forces only the original PGT02 branch. Preserve all surrounding transaction bytes, permissions and the production runner. Initial implementation84e4b8160ef2fb1c052627df94cc0d9ca79a581ec62a5c5762f25380a4fe1d5f passed child source checks, but independent review found mandatory wrong-state negative coverage missing; actual integration is held until Luna repair and re-review. The repair must execute the emitted helper predicate over valid/invalid synthetic states, detect first-count1-to0 and second-count0-to1 mutants, and retain the distinct PGT04 rejection boundary.

### R2 — Correct the historical default-privileges inventory contract

**Files:** `scripts/comment-translator-supabase-default-privileges-guard-contract.mjs`; read-only references are its existing guard migration and `docs/active/COMMENT_TRANSLATOR_SUPABASE_DB_AUTH_RLS_SECURITY_AUDIT.md`. Any new inventory fixture requires an explicit path and reviewed complete names in Astra's packet. Do not edit migrations, grants or the security policy merely to pass the contract.

- [x] Astra separates the documented historical9-table scope from the present full repository inventory. The observed42-vs9 failure already exists at HEAD; this is not a Gate1 migration regression. Include this required blocker explicitly in the correction packet, not as an unrelated opportunistic refactor.
- [x] Preferred proposal: preserve exact historical9 identity checks against their historical authority, and separately validate the complete current table identity set against a reviewed explicit inventory. Reuse the existing migration parsing approach where adequate; do not equate a regex count with a complete catalog.
- [x] Add RED cases for missing historical tables, extra/unknown current tables, wrong identities and disabled grant/ACL guards. Merely changing9 to42 or asserting a lower bound is forbidden.
- [x] Luna implements; parent runs `node scripts/comment-translator-supabase-default-privileges-guard-contract.mjs` and focused new regressions. Then execute the9 previously stopped entries from Task10's exact list with all remote opt-ins removed. Do not silently remove the failed contract from the list.

**R2 settled scope (2026-09-07, current parent):** preserve the audit table rows' exact historical9 identities and separately compare the declarations at or before the default-privileges guard migration cutoff. Validate the current direct public CREATE TABLE declaration inventory against the reviewed explicit42 names in the guard contract. Unsupported top-level declaration forms fail closed; comments, literals and function bodies are not catalog evidence. This is static source-inventory verification, not current live RLS/catalog acceptance. All four revoke clauses and existing deny guards remain mandatory. Initial implementation7255988c53691c008e0a34683407456fe460f6ce965f287a7499b9d4825e0bf2 has child source PASS only; independent review and parent checks remain pending.

**2026-09-07 verified checkpoint:** R1 final harness7ee8b314fb6ba965fc84a1c4ef6327981f4b033080dfb2ef97216857b028e24b passed independent review, parent source checks and one actual-local matrix, including both independent rollback-zero readbacks. Parent source/r13 preservation and cleanup0/0 confirmed. R2 final guard4c96bf350a7e6b9a9a81784a8cea86b71b039675eb0a7ecf4674b0c63046fac3 passed review and parent checks; later contracts7PASS, historical risk-acceptance FAIL, support-pending unexecuted. Earlier pending descriptions above are retained as implementation history, not current results.

**R2 subsequent risk-acceptance reconciliation (2026-09-07, settled):** the existing Step11 contract incorrectly applies its historical release-no and five-file scope to current Gate1 work. Preserve all original historical markers, deny assertions and secret patterns, reading fixed introduction commit ad94b34ac4c5c3149b880fdbd8e0bdc5d99b055e blobs; verify exact parent4c06e4ecffd769c74632bcf00b74d2535b7ce5cc and ancestor of HEAD. Validate its exact five-file parent-to-commit diff, never a moving branch or current-worktree fallback. Separately preserve current risk/support record constraints, unique default-privileges status rows in the current board section, decision-only/remediation-not-run and new-public-object review boundaries, and secret scans of all four current documents. Current later PL-G6 release-yes is valid and must remain. Add negative tests for Git/artifact failure, historical marker/state/scope changes and current acceptance expansion, contradictory/duplicate status rows, remediation-complete claims or secrets; add current-release-yes positive case. Write only the existing risk-acceptance contract. This is historical/source evidence, not remote posture or GO. Support-pending stays unexecuted until this required entry passes.

### R3 — Complete actual-evidence acquisition and final decision verification

**R3b completion scope (2026-09-08):** Under the user's current no-subagent instruction, the parent owns the remaining eight semantic stage validators, their tests and native operator integration. Register exact new paths `scripts/lib/comment-translator-paid-core-v1-gate1-stage-evidence.mjs` and `scripts/comment-translator-paid-core-v1-gate1-stage-evidence.test.mjs` in Task10. Public `verifyGate1Evidence({repositoryRoot,evidenceRoot,policy,sourcePolicy,stagePolicy})` calls the native authority/source verifiers itself; no result or adapter injection. The independently parent-selected stagePolicy pins freshness, full expected security/catalog artifacts and backup directory/manifest pairs. Require exact stage roles/targets, source/corpus equality, chronological stages and all existing R3b companion constraints. Postapply stages compare actual full catalogs with independently pinned replay/source expectations and security identities. Backups require native six-file reinspection, five successful dump outcomes with four sharing the server snapshot, reviewed Auth changes, zero Vault/Storage, full source/restore count/history/Auth-FK/grants-RLS equality and rehearsal bridge/canonical replay. Final T0/checksum/arm/DDL/readback/disarm enforce5/10-minute success bounds. Vault requires exactly two unique encrypted reserved records and no browser access; Cron requires one inactive five-minute postgres job, exact command/ID, zero runs over10 minutes and absent/inactive fallback. Recovery proof retains either an available slot or incident source-pause-confirmed-then-provision route, zero additional cost, exact configuration/cutover checklist and RPO20 with5/10/20 deadlines; successful operation does not require an actual incident pause. Final operational acceptance still requires the parent-confirmed policy/execution receipt pair. Source tests never establish hosted GO. Engineering phase2 contains final broad checks, review and separately authorized publication.

Rehearsal also supplies full production-shaped catalog, independently pinned catalog-expectations and security-identities artifacts bound to the isolated rehearsal target. Every catalog's exact56 history rows must equal the native source observation's migration filenames. Rehearsal/Production/decisive canonical structures and Preview canonical structure must agree; canonical row counts alone may vary while Free remains online. The existing evidence contract's historical permanent-stub assertion is replaced by behavioral rejection of caller PASS labels and fabricated verifier results; this is the only additional contract edit. Unknown field/role, duplicate identity, unpinned expected bytes, stale or mixed-target observations fail closed. The policy remains invalid after any unreviewed source/target change; parent acceptance must independently establish this before running or accepting a decision. No arbitrary caller policy or synthetic test output is operational GO.

**Engineering phase1 source checkpoint (2026-09-08):** All8 remaining semantic validators and the native9-stage/operator connection are implemented. Parent inspected actual source and ran4 syntax checks,10 focused test groups (32.72s), evidence contract and operator contract successfully. Final stage library SHA256 `9c4c26a815b5e0c4bace17a5da0147c0cb992d1edb51fcf94d9da1cd75cefe2a`; test `704f87044e098aedb89beb04c84e7e4cadbdb514ec05850564a0fba5f9c256da`; operator `c0cb2396883f3de32e46d508b0c73ab31963c575e73563664fac37358c04743b`; evidence contract `eb5545420b4b01d78d8d2cb1231b856b3a19b4957f70cc00b292f9e51b4c80a1`. Tests use actual catalog/stage/operator code with isolated native module-link fixtures; exported native missing/malformed-policy calls remain unavailable. No positive operational GO, actual stage acquisition, source publication or hosted action is claimed. No agents used, per current instruction. Engineering phase2 starts with final all-task scope reconciliation/checks/review: the preliminary HEAD comparison reports79 dirty paths against78 unique planned paths, including protected AGENTS/config and earlier gate0a2 deltas and two planned paths unchanged at HEAD; this is not the final comparison against the verified handoff commit. Preserve those existing changes and resolve the publication scope without silently including or reverting them. Whole-Gate1 remains NO-GO.

**2026-09-08 bounded preflight correction:** For the approved pre-apply Production state, the private maintenance function may not exist. Guard the exact function with to_regprocedure before privilege hashing; absence returns SQL NULL, never a zero privilege count or a digest of null-valued privileges. When present, resolve the OID for all three existing role checks. Scope is the existing preflight and contract only; target/TLS/read-only/capture/output contracts and GO conditions stay fixed. Parent directly implements and verifies under the user instruction forbidding subagents. Native changed-state validation covers absent Production and present Preview once each; no hosted apply or maintenance invocation. Source and native checks passed; remaining companion evidence still blocks overall preflight acceptance.

**R3 trusted authority boundary (2026-09-07, settled and independently reviewed):** the parent independently verifies native acquisition and explicitly fixes `expectedAuthorityCommit` in a trusted invocation policy. Never derive that value from untrusted evidence, its index or mutable worktree documentation; missing policy is NO-GO. Read the exact approved authority commit with Git and prove its actual diff contains only allowed evidence documentation. Bind producer entry/hash, native outcome, target, source revision, observation interval and artifact digest/size to that authority. Stage semantics, freshness, ordering and invalidation after target changes remain mandatory. Accepted GO evidence is the pair of verifier result and the parent's independently confirmed execution receipt showing the approved policy; a standalone GO output under arbitrary caller policy is not acceptance. This establishes the approval boundary without new signing keys. The nine-stage interfaces and implementation remain incomplete.

**R3a common authority loader (settled after independent design review, 2026-09-07):** create only the evidence library and evidence contract listed below. This slice returns `AUTHORITY_VALID` or a fixed unavailable reason; it never returns GO, changes `actualEvidence()` or claims stage semantics/freshness complete. R3b must invoke the native loader itself, then verify every stage; it must not accept a caller-constructed loader result as proof. Preserve the parent-confirmed policy/execution-receipt pair as the final acceptance boundary.

- API: `verifyAuthorityBundle({ repositoryRoot, evidenceRoot, policy })`; native public entry has no injected filesystem/Git/process/clock adapter. Pure internal validation may be tested separately using fixtures; synthetic tests never establish actual provenance. Imports are inert, there is no network or mutation path, and default/missing policy fails before Git or artifact access.
- Fixed stages are `sourceCommit`, `previewReadback`, `rehearsalBackup`, `finalBackup`, `productionReadback`, `canonicalReadback`, `vaultReadback`, `cronReadback`, `rollbackEvidence`, exactly once. Strict policy keys are `schemaVersion:1`, `expectedAuthorityCommit`, `expectedSourceCommit`, `expectedMigrationCorpusSha256`, `stages`. Each policy stage has exactly `producer:{entry,sha256}`, `targetBindings`, `observationWindow:{notBefore,notAfter}`. All values are required and independently root-selected; no manifest, environment or mutable document fallback. Target-binding maps are nonempty, at most6 entries, with keys only `repository|preview|production|rehearsal|recovery|cloudflare` and SHA256 values. R3b will require the stage-specific exact target sets. Producer entries are safe repository-relative paths under scripts/ or the fixed authority document, and their expected hashes are explicit policy inputs. Commit IDs are lowercase40 hex, digests lowercase64 hex. Source and authority commits must differ.
- Fixed authority path: `docs/active/COMMENT_TRANSLATOR_PAID_V1_GATE1_PRODUCTION_SUPABASE_READINESS.md`. Read this path from `expectedAuthorityCommit`, never the working file. Require a single-parent authority commit whose actual nonempty parent diff changes only that path and no rename/delete/type change; its blob mode must be100644. Prove expected source is an ancestor. For each producer, require a regular100644/100755 blob at expected source and compare its raw SHA256 with policy. Fixed bounded Git argv only, no shell, network, checkout or dynamic command text; disable replace refs, external diff/textconv and optional locks, use a minimal environment without repository redirection overrides. Each command has10s timeout and8MiB combined capture cap; error/signal/nonzero/unexpected stderr/incomplete output fails closed. Ancestry exit1 is a mismatch, not success.
- Extract exactly one ordered pair of `<!-- gate1-actual-evidence-index:start -->` / `<!-- gate1-actual-evidence-index:end -->` containing one JSON code fence and no other text. Index keys: `schemaVersion:1`, `sourceCommit`, `migrationCorpusSha256`, `stages`. Each stage descriptor has exactly `producer`, `targetBindings`, `startedAt`, `completedAt`, `receipt`, `artifacts`. Producer/targets must exactly match the corresponding trusted policy stage. Source/corpus must match policy. UTC times use Date.toISOString form; start<=completion, both inside that stage's independently approved observation window, and neither the window end nor observations may be in the future. This window check is not a replacement for R3b phase ordering, freshness and invalidation after later target changes.
- Receipt reference has exactly `{path,sha256,bytes}`. Each data artifact reference has exactly `{role,path,sha256,bytes}`;1..16 per stage, unique roles and paths, and no receipt/data path overlap. Role is lowercase alphanumeric/hyphen starting with a letter, max64. References are positive safe byte lengths <=1MiB and regular JSON files under the trusted evidence root. No absolute paths, backslashes, empty/dot/dotdot components, symlinks/junctions/reparse traversal or nonregular files. Resolve/check root and every ancestor, then open/read/check file identity and exact length/hash; fail on replacement or changed metadata. Across stages a reused path must have identical size/hash. Bulk backup payload hashing/restore remains the later backup-stage verifier's responsibility, not bypassed by this metadata limit.
- Native receipt keys: `schemaVersion:1`, `stage`, `sourceCommit`, `migrationCorpusSha256`, `producer`, `targetBindings`, `startedAt`, `completedAt`, `native`, `artifacts`. All shared fields and the ordered artifact references must exactly match the committed descriptor. Native keys are `kind:"process"`, `exitCode:0`, `signal:null`, `error:null`, `captureComplete:true`, nonnegative safe integer `stdoutBytes`, and `stderrBytes:0`. These are the audited outer producer's result; underlying expected-failure/DDL diagnostics remain inside data artifacts for R3b semantics. Unknown fields, including synthetic/injected labels, fail. Parent approval requires independently observed native execution and restricted artifact-directory ACLs; a receipt's labels alone never satisfy that approval boundary.
- Index and receipt JSON must be losslessly equivalent to JSON.stringify(parsed) after removing only insignificant whitespace outside strings. This deliberately rejects duplicate keys, ambiguous/noncanonical numeric/string encodings and malformed JSON without adding a generic parser dependency. Exact allowed-key checks apply recursively to these envelopes. Data artifact bytes remain opaque in R3a for existing/stage-specific parsers. Authority document is capped at1MiB; total referenced JSON bytes are capped at32MiB, with fail-closed checks before reading oversized input.
- Return a sanitized summary with status/reason/stage count and zero remote-call/mutation counters; verified raw artifact bytes may be returned only as an internal bundle for R3b, never printed by the contract/CLI. No raw paths, Git output, receipt contents, SQL, secrets or private identifiers in errors. Fixed unavailable reason classes cover policy, Git/authority, index, producer, binding/time and artifact/receipt failures. No actual-evidence or GO promotion in this slice.
- Windows clarification (settled before writer completion): the fixed read-only Windows PowerShell 5.1 GetAttributes probe is the sole exception to the native-Git-only process boundary. Pass original unresolved paths from volume root through every root ancestor and artifact descendant via bounded JSON stdin, never interpolated command text. Use the fixed SystemRoot/System32/WindowsPowerShell/v1.0/powershell.exe with NoProfile/NonInteractive, 10s timeout and bounded capture; accept only complete status0/error-null/signal-null/stderr-empty output with exactly checkedCount and reparseCount integers, exact input count and zero reparse points. Attribute exceptions, missing/unknown paths, count mismatch and capture failure reject. Probe before opening and after identity checks; retain Node handle/stat/realpath/hash checks and independently confirmed restricted-root ACLs. This detects reparse traversal but does not claim atomic protection against path races.
- Tests cover valid pure authority bundle and missing/unknown/duplicate stages; missing policy with zero I/O; wrong authority/source/diff/ancestry/blob/producer; target/corpus mismatches; time ordering/window/future failures; duplicate/unknown envelope fields; traversal/symlink/nonregular/changed-file/oversize/hash/size failures; mismatched native receipt and incomplete/error/signal/stderr capture. Exercise actual extracted native boundary functions with controlled lower-level fixtures without adding adapters to the public API. Confirm the existing operator remains fail-closed. Parent runs2 syntax checks, new evidence contract and operator contract; no repeated R4 database matrix because its files/interfaces are unchanged.

**R3b sourceCommit stage (settled 2026-09-07):** add only the source-evidence library and contract listed below. This completes the source-stage content/native-observation mechanism; it never enables operator GO or treats local refs as remote freshness. Final parent acceptance still requires independent current remote merge, review and CI evidence bound to the exact intended remote, Preview branch and source/integration commits, plus the existing independently observed policy/native-receipt/restricted-root ACL boundary.

- APIs: `collectSourceCommitObservation({repositoryRoot,sourcePolicy})` and `verifySourceCommitStage({repositoryRoot,evidenceRoot,policy,sourcePolicy})`. Imports are inert. Neither public entry accepts injected filesystem/Git/process/clock/loader/result adapters. The verifier must invoke R3a `verifyAuthorityBundle` itself, then the native collector; it cannot accept a caller-constructed authority bundle. Missing or malformed inputs fail before native I/O.
- Strict sourcePolicy keys: `schemaVersion:1`, `expectedSourceCommit`, `expectedIntegrationCommit`, `expectedMigrationCorpusSha256`, `expectedMigrationPaths`, `expectedRepositoryBindingSha256`. Commit and digest formats match R3a. Paths are exactly56 unique lexically sorted direct-child `supabase/migrations/<14digits>_<lowercase-alphanumeric-or-underscore-name>.sql` paths, independently selected by the parent from the approved final56 inventory. There are no manifest/environment/default-policy fallbacks. In verifier mode source/corpus must equal R3a policy; sourceCommit targetBindings must contain exactly repository with the same expected binding digest.
- Resolve the fixed local ref `refs/remotes/origin/codex/comment-translator-paid-v1-preview` and require its exact expectedIntegrationCommit. Require both commit objects and source ancestry into integration. Enumerate the entire recursive `supabase/migrations` tree at both commits; each must contain exactly the56 expected paths, regular100644 blobs only. Unknown, missing, duplicate, nested and non-SQL entries reject. Ancestry alone never proves preservation after merge.
- Before each blob read, strictly parse its Git size and require1..1MiB; enforce at most32MiB per complete tree. Read exact raw Git blob bytes and match observed length. Sort rows by ASCII path and create each object with keys in the fixed insertion order `{path,bytes,sha256}`. Corpus SHA256 is over UTF-8 `JSON.stringify(rows)`, with no whitespace insertion or line-ending normalization. Both source and integration rows and their digests must match each other and the independently supplied expected corpus digest.
- Git uses fixed bounded argv, minimal environment excluding Git redirection/replace refs, no shell, external diff/textconv, network, checkout or mutations. Apply10s per command,8MiB combined capture and60s overall collector deadline; lower the remaining command timeout to the remaining deadline. Require status0/error-null/signal-null/stderr-empty/complete capture. Ancestry exit1 rejects. Return only fixed sanitized unavailable reasons on all failures, never Git output, source bytes, paths or private identifiers.
- Strict observation keys are `schemaVersion:1`, `sourceCommit`, `integrationCommit`, `integrationRef`, `migrationCorpusSha256`, `repositoryBindingSha256`, `migrations` (the exact ordered rows). Collector success is `SOURCE_OBSERVATION_VALID` with remoteCalls/mutations0, migrationCount56 and a nonenumerable internal observation. Stage verifier requires exactly one sourceCommit artifact with role `source-observation`, parses its strict JSON and compares every field/row with freshly native-derived observation. Success is `SOURCE_STAGE_VALID` for sourceCommit, never GO. Observation timing remains bound by the existing descriptor/receipt and parent-approved observation window; current remote state/review/CI cannot be inferred from these local Git operations.
- Tests exercise actual extracted native boundary functions with controlled lower-level fixtures, without public adapters or Git init/commits/network/DB/new worktrees. Cover complete valid trees and strict policy, wrong integration ref/ancestry, source/integration tree drift and unknown/missing/type entries, pre-read/aggregate limits and incomplete/error/signal/stderr capture, raw-byte hashing, strict artifact shape/mismatch, binding mismatch, native loader ordering and caller-bundle rejection. Every negative must isolate its intended condition. Parent runs2 syntax checks, the source-evidence contract and operator contract once on the final reviewed state; unchanged R3a and R4 suites are not repeated.

**R3b sourceCommit source acceptance (2026-09-07):** library487243172e64ac52511037d33c3f816560e809d3f3161f2279e56c4d30b11a1b / contracta76321e16a440b729e4d6dbc50cfb671d7140c733f9844ad0d3a54f9a9795d6c accepted after independent semantic re-review and parent2 syntax/source-evidence/operator checks PASS. Both P2 findings are resolved: deadline is checked after spawn and final hashing/comparison, and actual extracted stage-verifier success/order/forwarding/refusal paths are exercised with independent fixtures. Existing60 files remained unchanged through the writer. This accepts the source-stage mechanism only; actual source publication, current remote merge/review/CI and the parent policy/native receipt boundary remain unverified. Other stage semantics, post-apply acquisition and hosted evidence remain mandatory; operator GO stays closed.

**R3b post-apply catalog comparison primitive (settled 2026-09-07):** add only the postapply-catalog library and contract listed below. This is a pure comparison primitive for later trusted stage verifiers/acquirers, not a native acquisition, provenance proof or completed stage. A matching caller-supplied expected value cannot establish actual evidence. Full stage acceptance must later load both observation and independently selected source/replay/baseline expectations through the native authority boundary.

- API: `comparePostApplyCatalog({artifact,expectations})`. Imports and invocation perform no filesystem/process/network/DB I/O or mutation. Return only `POSTAPPLY_CATALOG_MATCH` or `POSTAPPLY_CATALOG_MISMATCH` with fixed sanitized reason, target/count summaries and remoteCalls/mutations0. Keep per-table canonical rowCount observations in a nonenumerable internal property. No raw identifiers/paths/SQL/data in errors or contract output; never return GO or a stage-valid/provenance label.
- Strict artifact keys: `schemaVersion:2`, `target`, `sourceCommit`, `migrationCorpusSha256`, `targetBindingSha256`, `readOnly`, `history`, `canonical`, `archive`, `sourceEra`, `edgeScopeVersion:1`, `pgDependEdges`. Expectations have the same exact keys except `readOnly`; canonical expectations use the structural type without rowCount. Targets are preview or production; SHA1/SHA256 formats match R3a. All binding fields must match. readOnly has exactly serverVersionMajor:"17", transactionReadOnly:"on", defaultTransactionReadOnly:"on", transactionIsolation:"repeatable read". History is exactly56 unique version/name rows, sorted by version, with14-digit versions and nonempty lowercase alphanumeric/underscore names, and full equality to independently selected expected history.
- Reuse accepted catalog exports for strict full-state validation, canonical projection/structural validation and Paid RPC browser EXECUTE rejection, without editing that module. Require canonical33 tables with the exact imported CANONICAL_TABLE_NAMES,81 public ct_paid_* functions, postgres owners and table RLS. Validate full acquired canonical TableRows including safe nonnegative rowCount, then remove exactly canonical rowCount for comparison. Retain raw definitionMd5, ACL, policies, columns, indexes, constraints, trigger and dependencyCounts equality. Preserve arrays, recursively sort object keys for comparison; top-level table/function/trigger lists must be sorted and unique by schema.name, schema.name(identityArguments), and tableSchema.tableName.name respectively. Do not normalize raw definition hashes or silently drop unknown fields.
- Production archive is a full state under comment_translator_paid_legacy_archive with exactly3 tables,3 functions and1 disabled trigger; tables are zero-row/RLS-enabled, table/function owners are postgres, and no ACL privilege is granted to PUBLIC/anon/authenticated/service_role. Production sourceEra is full public state with6 tables and7 functions, postgres owners, RLS and zero-row tables. Both states, including every rowCount and dependencyCounts field, must equal their independently obtained complete expectations. Preview archive/sourceEra states and their pgDependEdges are empty with all dependencyCounts zero. Schema existence/absence and effective privileges are separate mandatory companions; empty contents do not establish schema absence. Require all counts to be safe nonnegative integers.
- pgDependEdges has exactly canonical/archive/sourceEra arrays. edgeScopeVersion1 defines seeds as the scope's exact table/function/trigger object addresses and every scoped table column; recursively add dependents of scoped addresses through incoming automatic/internal pg_depend ownership edges (deptype a/i). Collect every pg_depend row whose dependent OR referenced address is in that closure, including internal edges and all deptypes. Use the same scope rule for expected and observed acquisition. This rule is an acquisition obligation; this pure comparator does not prove that acquisition executed.
- Each edge has exactly classid,refclassid,deptype,dependent,referenced. classid/refclassid are qualified pg_catalog.pg_* catalog names, never numeric OIDs; deptype is one ASCII letter retained without filtering. Endpoints have exactly type,schema,name,identity from pg_identify_object: type/identity nonempty strings, schema/name null or nonempty strings. Build a fixed-field-order JSON key (edge keys in the order just stated; endpoint type/schema/name/identity), require arrays sorted lexically by that key and no duplicate key, and compare all complete rows. Do not replace identities with group counts. Canonical edges must be nonempty; production archive/sourceEra edges must be nonempty; Preview excluded-scope edges must be empty.
- pg_depend does not cover outsideFunctionSourceReferences and other separately counted non-catalog references. Preserve their existing dependencyCounts equality, but full identity companion evidence remains mandatory later; same-count reference substitution cannot be ruled out by this primitive. Namespace/schema ACL, effective browser/service privileges, extensions usability, advisors, Preview Vault/Cron preservation and all other stage conditions remain required companion evidence. No maintenance RPC is invoked in production during this Gate.
- Tests use isolated synthetic copies of existing public fixtures or explicit typed synthetic data; never read restricted external artifacts or treat a synthetic expected archive as actual replay. Exercise the actual exported comparator: valid Preview/Production, canonical nonzero counts allowed and preserved internally, every other catalog field/count/type/binding/readOnly/history drift refused, malformed expectations, unknown/missing/duplicate identities, same-count edge identity/kind/deptype replacement, empty-required-edge sets and input immutability/no I/O. Parent runs2 syntax checks, the postapply-catalog contract and operator contract once after actual diff review; unchanged R3a/sourceCommit/R4 suites are not repeated.

**R3b post-apply comparison source acceptance (2026-09-07):** librarya9ba23e0151f5c188cc982ba9f930f7851d1e0347fb00c2cb779d30dfd932028 / contract2f53284f5db173f957379fa29937258b8375235f9c9c665c0d179f6933cefacc accepted after independent semantic re-review and parent2 syntax/postapply/operator checks PASS. Both P2 findings are resolved: all six SHA input values require strings before regex validation; Production shape and forbidden ACL cases execute, with independent catalog/readOnly/edge negatives. Earlier input-property-order and unsafe-count findings are also resolved. Existing62 files remained unchanged through implementation and repair. This is acceptance of a pure comparison primitive only: native acquisition, independently selected source/replay expectations and all specified companion/stage evidence remain mandatory. Operator GO stays closed.

**R3c bounded transport specification (2026-09-07):** preserve the fixed read-only SQL, argument/environment/TLS binding and public output schema. Bound client-version process to10 seconds and query process to60 seconds, preserving server statement30s/lock5s limits. Accept native capture only with status0, no error/signal, string stdout/stderr, aggregate UTF-8 bytes at most1MiB and stderr empty; validate PG17 from stdout only. Any incomplete/error/timeout/capture failure returns the existing sanitized connection failure with empty captures and rows0. Infer actual query row count from nonempty output lines, never hard-code1. Parser rejects explicitly signaled incomplete capture/error/signal metadata; omitted synthetic metadata remains compatible and does not establish actual provenance. Tests use injected processes only, prove failures cannot advance version to query or produce readonly evidence, and retain existing invocation/security assertions. No actualEvidence promotion, hosted calls or new files in this slice.

**R3c post-apply native catalog acquisition (settled 2026-09-07):** implement a bounded native collector for the accepted comparison primitive. Add only the postapply-acquire library and contract below; modify only the postapply-catalog library/contract to expose its existing strict artifact-shape validation. This does not supply independent expectations, persist authority artifacts/receipts, complete a stage or enable GO.

- Export pure `inspectPostApplyCatalogArtifact(artifact)` from the catalog library. Reuse the existing strict full artifact metadata/readOnly/history/catalog/count/edge validators, without requiring or fabricating expectations. Return sanitized `POSTAPPLY_CATALOG_SHAPE_VALID` or `POSTAPPLY_CATALOG_SHAPE_INVALID`, fixed reason/counts and nonenumerable canonical row observations; never MATCH/provenance/GO. Existing comparator behavior remains unchanged. Both imports and the inspector perform no I/O or input mutation.
- New acquisition module exports a fixed `POSTAPPLY_CATALOG_SQL` and async `collectPostApplyCatalogObservation(request)`. Request has exactly target/sourceCommit/migrationCorpusSha256/targetBindingSha256: preview or production and strict lowercase SHA1/SHA256 strings. No public environment/filesystem/process/clock/transport/SQL/result adapters. Invalid request fails before dynamic import or native I/O. Importing this new module is inert.
- After request validation, dynamically import the existing preflight module to reuse parseTargetBinding, computeBindingSha256, buildPsqlInvocation and createPsqlTransport. Its repository-inventory reads during import are permitted; they are not remote observation or source-publication proof. Validate process.env GATE1_TARGET_BINDING_JSON/SHA256 against the request digest/target before CA reads or native process execution. Retain native Direct db.<projectRef>.supabase.co:5432/postgres/postgres, pinned CA/verify-full, password XOR password-file, minimal child environment and all existing connection exclusions. Replace only the validated invocation input with the fixed SQL; no caller SQL or connection-string argv.
- Use a private fixed wrapper around real spawnSync to count actual query-process start attempts only (version probe counts0). Public remoteCalls is this attempt count, not successful connections; it is0 before query start and at most1. Pass that private wrapper to the existing transport; never accept one from callers. Preserve client-version10s/query60s limits, client PG17, complete exit0/no error/no signal/stderr0, aggregate1MiB capture, shell:false/windowsHide:true and no retries. Failures retain the actual attempt count and sanitized fixed reasons; never expose captures, credentials or identifiers.
- Fixed SQL uses one repeatable-read read-only transaction, lock5s/statement30s, and ends with ROLLBACK. Return exactly one JSON row with exactly readOnly/history/canonical/archive/sourceEra/edgeScopeVersion/pgDependEdges. readOnly matches the accepted four fields, with server major derived from server_version_num /10000 as text; history is the full ordered version/name rows, never a filtered pending list. No DDL/DML/maintenance RPC, secret-value read or function body output. Exact table counts may use the existing quoted-identifier count/query_to_xml pattern inside this transaction.
- Observe all three scopes on BOTH targets. Canonical captures public ordinary/partitioned tables with the literal comment_translator_paid_ prefix (including unknown additional names) and every public function with literal ct_paid_ prefix; archive captures all ordinary/partitioned tables/functions in comment_translator_paid_legacy_archive; sourceEra captures the existing exact6 public table names and7 public function names with every overload. Derive these fixed source-era names from the existing public fixture/preflight inventory, not caller input. Never force Preview archive/sourceEra states empty. Later strict shape checks reject unexpected/missing cardinality/identities.
- Produce complete accepted TableRow/FunctionRow/TriggerRow values and dependencyCounts. Preserve existing full catalog field semantics and normalizations, raw pg_get_functiondef MD5, all ACL rows and all noninternal table triggers. Keep each existing dependencyCounts aggregate formula scoped to the same actual tables/functions; do not substitute ownership-closure edge counts. Canonical table/column children and all arrays retain deterministic ordering under C collation where SQL text order is used.
- pgDependEdges follows edgeScopeVersion1 exactly: seed each scope's actual tables/functions/noninternal triggers and all non-dropped table columns; recursively add incoming automatic/internal dependents through deptype a/i. Collect every pg_depend row with either address in that closure, all deptypes and both objsubid values. Resolve both endpoints with pg_identify_object(classid,objid,objsubid) and both catalog class names as qualified pg_catalog.pg_* names. Never omit invalid/unsupported identities, deduplicate observed rows or replace them with counts. PostgreSQL reference: [pg_depend](https://www.postgresql.org/docs/17/catalog-pg-depend.html).
- Strictly parse the sole complete JSON row with the accepted duplicate-key-rejecting parser. Add request metadata to form schemaVersion2 artifact; these source/corpus fields are annotations, not independently verified provenance. Sort only full top-level table/function/trigger lists by the accepted identity key and full edge rows by the accepted fixed-field-order JSON key; preserve every row and nested array. Validate through the new artifact inspector. Return sanitized `POSTAPPLY_CATALOG_OBSERVED` or unavailable with fixed reason/counts, query-attempt remoteCalls and mutations0; retain the full artifact only in a nonenumerable internal property. No artifact/receipt filesystem writes in this slice.
- Native producer/revision/time/target authority, independent source/replay expectations and all non-pg_depend identity, namespace/effective-access, extensions/advisors and Preview Vault/Cron companions remain mandatory. An observed catalog or valid shape cannot establish any stage or GO; source annotations and self-comparison must never substitute for this evidence.
- Tests exercise the actual inspector/collector plus extracted native boundary functions with controlled lower-level fixtures, without public adapters or actual remote/DB/Docker calls. Cover invalid request before import, binding rejection before CA/process/query, exact forwarded argv/minimal environment/fixed SQL, version-before-query and attempt counts, all native capture failures/limits, duplicate/multiple/truncated JSON, strict shape refusal, sanitized outputs and inert imports. SQL scope/transaction/full-field/ownership-closure checks are source evidence only; actual PostgreSQL17 query execution is required later before stage acceptance. Parent runs4 syntax checks plus catalog/acquire/operator contracts once on the final reviewed change; unchanged R3a/source/R4 suites are not repeated.

**R3c native post-apply acquisition source accepted (2026-09-07): Luna implemented the fixed read-only collector and pure artifact inspector. Independent Astra semantic review found no issues; parent inspected the actual change and passed four syntax checks plus catalog/acquire/operator contracts on the reviewed state. Only one of the previous64 files changed; the other63 were preserved, with two new acquisition files added. Acquire SHA2564999867910c41e12b4c7a246290972a3b0bc2c3ea58f3b1f6dc038bcd5d2be47; acquire contractc6895ef01787d8d62737fa99b88f58f34d7d795dfe567293312bbd39cd41a90b; catalogbb90b47efff974eeee188bd379b390b10da3b06d6fdfbed65eef31f167ef7eea. Actual PostgreSQL17 execution, independent replay expectations, protected artifact/receipt authority and all companion/stage evidence remain mandatory. This source acceptance does not establish hosted evidence; full Gate1 remains NO-GO. Configured Luna Max/Astra Medium; runtime identity unverified.**

**R3c actual local post-apply query probe (settled 2026-09-07):** add `--phase=postapply-query` to the existing database integration harness and only one new `scripts/comment-translator-paid-core-v1-gate1-postapply-query.test.mjs`. Preserve all existing R4 phases and assertions. This probe closes actual PG17 execution uncertainty for the new fixed SQL; it does not replace restored-database or hosted evidence, independent full expected artifacts, any companions or stage decisions.

- Use a separate runCliPostapplyQueryCase with the existing cached-image/local transport/project configuration and owned-resource cleanup. Reproduce the accepted CLI prebridge canonical migrations plus four recovered Paid legacy bodies, initial local reset without seed, actual pg_cron extension prerequisite without jobs, original bridge and remaining canonical migrations, and one successful migration up. Do not run the injected failure/rollback or full negative matrix. Revalidate actual PG17 runtime, full migration history56 and the existing independent canonical structure comparison.
- Immediately before native exec, resolve and verify the exact container ID using both accepted project labels, cached PostgreSQL image ID/reference and inspected container identity; a name substring alone is insufficient. Query only that verified disposable container, postgres database/role, with psql --no-psqlrc --no-password --quiet --tuples-only --no-align --set=ON_ERROR_STOP=1. Only this exec receives PGOPTIONS=-c default_transaction_read_only=on; preserve setup session behavior. Input is the unchanged imported POSTAPPLY_CATALOG_SQL, never a replacement query.
- Add only a validated internal captureByteLimit option to runNativeDocker: default remains64MiB, supplied value must be a positive safe integer no larger than that default and reject before spawn otherwise. Query passes1MiB; retain60s timeout and original transport/termination semantics. Accept only exit0, clean complete capture, no error/signal/terminationUnknown/captureFailure, stderr empty and aggregate UTF-8 bytes<=1MiB. Require exactly one nonempty line and accepted parseStrictJson, including duplicate-key rejection. Emit no SQL, captures, credentials, object names or raw errors.
- Normalize only the same top-level lists and fixed-field-order full edge rows as the accepted collector. A small local pure helper is allowed, with actual-function equivalence tests against the collector helper; do not expand collector public APIs. Preserve all rows and nested arrays. Compare observed canonical structure against the independently selected existing bridge-state authority; compare full history version/name against exact repository migration filenames. Validate readOnly fields and retain real archive3/3/1 and sourceEra observations.
- Existing replay does not recreate sourceEra6/7: old source-era files are marker migrations. Never add synthetic source-era objects, fabricated rows, suppress source-era observations or relabel this replay as full Production. Invoke the actual production artifact inspector using explicitly local-only annotations; require and report its existing source-era-mismatch refusal for this known local baseline, alongside sourceEra table/function counts0/0. A different refusal is a failed probe. Public probe status can be PASS only for SQL execution, independent canonical/history checks and the expected fail-closed refusal; productionShape=NOT_ESTABLISHED and hostedEvidence=false remain explicit. No stage PASS, receipt/artifact authority, full comparator self-comparison or GO claim.
- Always use original owned-only cleanup, including on setup/query/assertion failures; preserve pre-existing identities and bytes. Tests execute actual new lifecycle and capture helpers through controlled lower fixtures, including query arguments, byte limits, all capture failures, parser failures, normalization equivalence, independent catalog/history mismatch, expected versus unexpected shape rejection and cleanup on failure. No actual Docker/DB/remote calls by the writer. Parent checks harness/new-test syntax and new-test plus affected runtime-cleanup/cli-atomicity-order/operator contracts once final state, then separately performs one actual changed-state probe after source/input/resource readiness. Any actual failure stops unchanged retries for diagnosis. No dependencies, image downloads, source-era reconstruction, hosted writes, deploy, cost or activation.


**R3c capture serialization correction (2026-09-07):** actual full result1054690 bytes exceeded the existing1MiB bound. Preserve that bound and every JSON value/row. Serialize the existing final JSON with a string-token-preserving regexp that removes only whitespace outside quoted strings; use explicit PostgreSQL escape-string literals. Native read-only diagnostic proved compact1001008 bytes and JSON equality. Acquisition library/contract are additionally in scope for this correction, with escaped quotes/backslashes/whitespace/Unicode coverage. The local query phase reuses the existing atomicity temporary-directory prefix so the unchanged cleanup helper accepts it; test the actual removal helper through lower fixtures. Preserve query capture diagnostics even when cleanup fails. No source-era fabrication, GO relaxation or hosted evidence claim. Direct parent ownership replaces delegation at the user's explicit request; subagents must not be invoked.

**Gate 1 R3c actual compact local query PASS (2026-09-07): one changed-state probe at harnessfedaf12b3ca90f48e2ba53e971ad1e3a70305b57d0dc554b56d042578d7988e7 / acquire2dca6062b27e68a2f5c22da6781862486027c5b3d1eaaa1e7654baeca6c228ce completed exit0, no error/signal, complete capture and stderr0. Actual PostgreSQL17 executed the unchanged imported compact fixed SQL; independent canonical structure and exact56 version/name history PASS. Archive3 tables/3 functions observed; sourceEra0/0 correctly causes source-era-mismatch and productionShape=NOT_ESTABLISHED, hostedEvidence=false. Query SHA25660fd8c9555ccc45437e62c7a4ffda90d1091bb6a6135f62c0e3bbe8058cba7ab; local artifact SHA25604995488646ba1b1883fdd574b653ec0640f19a11fb056193529d04864272910 is an execution observation, not a persisted trusted artifact. Original owned cleanup PASS (2 actions/0 failures). Independent postchecks: owned Docker0, all prior Docker identities preserved, new temp0, all19 pre-existing temp directory contents preserved, source changes0, saved/legacy bytes preserved. The one earlier policy-blocked temp deletion remains pending and was not retried; no other pre-existing temp is in cleanup scope. Full Gate1 remains NO-GO: independent full replay/source expectations, companion identities/security, remaining8 trusted stages, source publication, no-cost recovery and hosted sequence remain required. No subagents; do not repeat this unchanged successful probe.**

**Existing ownership candidates:** `scripts/comment-translator-paid-core-v1-gate1-operator-contract.mjs`, `scripts/comment-translator-paid-core-v1-gate1-preflight-readonly.mjs` and its contract, `scripts/comment-translator-paid-core-v1-gate1-catalog-acquire-readonly.ps1`, `scripts/comment-translator-paid-core-v1-gate1-catalog-readback-contract.mjs`, `scripts/comment-translator-paid-core-v1-gate1-backup-recovery-preflight.ps1` and its contract. Do not assign all files to one giant writer packet. Separate evidence validation, stage adapters and recovery execution.

**Proposed new paths only where existing responsibilities cannot safely contain the work:** `scripts/lib/comment-translator-paid-core-v1-gate1-evidence.mjs`, `scripts/comment-translator-paid-core-v1-gate1-evidence-contract.mjs`, `scripts/lib/comment-translator-paid-core-v1-gate1-source-evidence.mjs`, `scripts/comment-translator-paid-core-v1-gate1-source-evidence-contract.mjs`, `scripts/lib/comment-translator-paid-core-v1-gate1-postapply-catalog.mjs`, `scripts/comment-translator-paid-core-v1-gate1-postapply-catalog-contract.mjs`, `scripts/lib/comment-translator-paid-core-v1-gate1-postapply-acquire.mjs`, `scripts/comment-translator-paid-core-v1-gate1-postapply-acquire-contract.mjs`, `scripts/comment-translator-paid-core-v1-gate1-backup-recovery-execute.ps1`, `scripts/comment-translator-paid-core-v1-gate1-backup-recovery-execute-contract.mjs`. Before creation, Astra settles their exact interfaces, updates the final allowlist and reconciles Task10's exact file inventory; an unknown extra contract must fail rather than be ignored.

- [ ] R3a: settle a versioned evidence envelope with source revision, exact target-binding digest, stage, observation interval, artifact digest/size, command outcome and producer provenance. Require actual producer output and trusted restricted artifact validation; a caller-supplied JSON label, timestamp or digest is not trusted provenance. Reject synthetic, incomplete, stale, wrong-target/revision/stage, tampered and contradictory evidence. Do not expose private fields in public summaries.
- [ ] R3b: implement stage-specific semantic verifiers for the nine existing `GO_EVIDENCE_KEYS`: sourceCommit, previewReadback, rehearsalBackup, finalBackup, productionReadback, canonicalReadback, vaultReadback, cronReadback and rollbackEvidence. Cross-check shared target/revision/time bindings. Replace `actualEvidence()`'s fail-closed stub only after those verifiers and their trusted input authority exist; never return true on presence or approvals alone.
- [ ] R3c: complete safe real acquisition/execution paths around the existing preflight recipes. Native tool calls use argument arrays, restricted outputs, bounded capture/timeouts and exact target/TLS validation. No execution of retrieved shell text. Keep dry-run plans and actual artifacts distinguishable. Exact audited operator instructions may be used where the approved design allows them; do not build a new general orchestration framework solely for this task.
- [ ] Recovery execution must hold the exported snapshot through schema/data/history dumps, collect roles separately, enforce the5/10/20-minute state machine and preserve the pause latch. A mocked pause response cannot prove actual source inaccessibility. No automatic reuse/reset of failed restore targets.
- [ ] Add local RED/GREEN tests for provenance spoofing, cross-target/revision mixing, missing catalog fields, stale evidence, partial captures, deadline races and late success after pause. Real integration remains a separate mandatory stage. Each sub-slice returns through Astra review and Luna repair.

**2026-09-07 source acceptance:** R3c bounded capture/timeouts is accepted at runner0ecc45aca813cbc89248faf7cd2dd00a59120d6acf2a3d9070f652bc0911086e / contract3ecd79787f3c10429415b9669145cc084744803254acd829095991e73d46e76c, independent review and parent checks PASS. R2 risk reconciliation and all nine later contracts PASS. R3a common authority-loader source is accepted at libraryd81dfd0216f6f24d812e68c1782d192c25eae5ddaa06e8657f9a158683dad7bc / contract83612dd607834cbb3c690951cc6e615c1f13c54db10f034ab0974981e96cf51f: both semantic-review P2 findings resolved, independent re-review has no new findings, parent2 syntax/evidence/operator checks PASS, prior58 files preserved through the writer, and native symlink readiness confirmed with zero probe residue. This does not establish actual stage provenance or GO. R3b stage verifiers, remaining R3c acquisition, independently observed policy/receipt/ACL authority and hosted evidence stay unfinished; actualEvidence remains fail-closed. Post-apply catalog needs a separate v2 validator/acquisition path preserving preapply v1/r13; full dependency identity rows and replay-backed expected authority remain to be acquired, not inferred from counts.

### R4 — Finish local acceptance and publish the exact source

**2026-09-07 pre-execution findings:** full DB harness has unresolved local Docker target/env pinning, generated-password argv, incomplete CLI service/cache coverage, and cleanup ownership/failed-start/residue handling. No full matrix was run. Settle a bounded correction packet before execution; full harness/runtime-cleanup helper/focused contracts are candidates, no blanket file ownership yet. Do not extend the Vault-only log-driver/startup-logging requirement to CLI by assumption: CLI uses private recovered SQL with sanitized capture and no log retrieval, and the existing authority does not require a particular CLI log driver. Preserve source/r13/legacy bytes and owned-resource isolation; no daemon settings changes or image pulls.

**Files:** final implementation allowlist in Task10; parent owns `task.md` and current authority documents. Preserve unrelated user changes without staging them by accident.

- [ ] Complete Task10's exact inventory, syntax, security/scope checks and the separately gated restored-database integration. Prove legacy bridge/canonical convergence and source-era preservation; saved fixtures alone do not prove live DB behavior.
- [ ] Inspect all untracked file contents as well as tracked diff. Retain existing build/type/lint PASS evidence when relevant files are unchanged; rerun only checks invalidated by new files, interfaces or other evidence.
- [ ] Independent final semantic review of the complete patch, then Astra's acceptance checks. Publish only intended reviewed files through commit/push/PR/CI/merge into the Preview integration line under the inherited source-publication scope. No main push, no cleanup. Record actual merged commit/tree, not the handoff HEAD as the new artifact.

**R4A settled native-target correction (2026-09-07):** write only the full database-integration harness and its existing runtime-cleanup source test. Keep actual SQL/migration/fixture/rollback semantics unchanged. Before any daemon operation or CLI invocation, reject nonempty inherited Docker host/context/config/TLS/cert and Supabase binary override, construct a Windows-essential-only environment, and inspect the fixed desktop-linux context endpoint. Require exactly npipe:////./pipe/dockerDesktopLinuxEngine; malformed/missing/nonlocal context fails before daemon calls. Pin subsequent direct calls and CLI child environment to that verified local endpoint, avoiding a later mutable selected-context fallback. Route all direct Docker calls, including cleanup, through the verified transport. Invoke the existing installed Supabase Node wrapper through process.execPath and argument arrays with shell=false; no shell command strings or inherited hosted/PG/Supabase tokens. Preserve existing sanitized output/result interfaces, add bounded Docker and outer CLI process timeouts, and test the emitted actual transport with injected processes for wrong endpoint/overrides/capture failure and for all call paths sharing the bound environment. Source-only tests; actual full matrix remains held for separate password/cache/ownership corrections. No edits to the already accepted Vault harness, native CLI runner, cleanup helper, migrations, fixtures or dependency files in R4A.

**R4A review correction (2026-09-07):** diagnostic tail-ring truncation is not native process failure. Preserve successful init/start/reset/migration-up results when ordinary diagnostic logs exceed the ring, while retaining scanned failure diagnostics. When `preserveStdout` is required for query/status/migration-list output, reject truncated stdout as incomplete data. Keep native runner error/signal/nonzero exit and actual child error/signal fail-closed. Add focused injected regressions for both cases without starting Docker or changing SQL/fixtures. Parent initial syntax/transport/atomicity/diagnostics checks passed; acceptance remains pending this review repair and final checks.

**R4A second review finding (2026-09-07):** the added outer runner timeout does not establish child-process termination on Windows. Both CLI and streamed-Docker runners spawn descendants; cleanup must not race a still-running start/reset/migration/psql operation. Before another writer starts, settle explicit process-tree ownership, bounded termination and termination verification, and the fail-closed cleanup behavior when termination cannot be established. The existing CLI runner is currently outside R4A writer ownership; any required expansion must be explicit. Do not run the actual matrix with either unresolved review finding.

**R4A settled child-termination repair (2026-09-07):** extend writer ownership only to the existing CLI runner and CLI diagnostics/atomicity-order tests, in addition to the full harness and runtime-cleanup test. The runner owns the spawned process tree and an internal timeout (default/max300s). On Windows, terminate only its numeric child PID and descendants using absolute System32 taskkill with `/PID`, `/T`, `/F`, no shell and a10s process bound; allow at most10s more for child close. Emit one receipt with `terminationConfirmed` and `timedOut`. Normal close confirms completion; timeout confirms termination only after both successful tree termination and child close. Clear timers and input/stdio streams appropriately. Remove the outer runner timeout that could orphan the tree. If termination is unknown, destroy streams/unref handles and return bounded sanitized failure; no claim of successful termination. Missing/malformed receipt, abnormal runner exit/signal or capture failure also latches termination unknown unless a pre-spawn no-child failure is established. The harness must then block all later Docker/CLI operations and all generated-resource/work-directory cleanup; cleanup becoming unknown during CLI stop must also prevent directory removal. Timeout or runner/input error is always failure, checked before any expected SQL-failure/atomicity-marker PASS classification. Keep normal complete child nonzero SQL failures eligible for the original negative assertions. Preserve the earlier diagnostic-ring correction, native target/env pinning, SQL/fixture behavior and CLI output sanitization. Verify injected unknown/error/race paths and a bounded owned Windows child-plus-grandchild timeout test without Docker. No new module/framework, runtime fetch, dependency edit or full DB execution is included in this repair.

**R4A test repair (2026-09-07):** independent review found the runtime correction consistent with the two original findings, but required behavior coverage and the Windows test fixture remain incomplete. Repair only the existing CLI diagnostics, runtime-cleanup and CLI atomicity-order tests. Exercise the real runner with injected process/timer/stream dependencies for failed tree termination, successful termination without close, exit-before-close (no taskkill), and input error. Exercise both real harness transport paths and expected-failure consumers, proving timeout/error cannot be accepted through a SQL marker and that UNKNOWN arising during stop prevents later directory deletion. Regex presence/order alone is insufficient. The owned live child/grandchild fixture must confirm readiness, use a practical timeout instead of50ms, give both processes bounded self-exit fallbacks independent of the runner, and confirm both gone on success and failure. Remove PID records/temp directory only after termination is established; retain them on uncertainty. No Docker, real Supabase CLI or DB runs, and no new modules. If these tests expose a runtime defect, return it to Astra before editing runtime source.

**R4A signal correction (2026-09-07):** the test work exposed a real false-PASS path: a signaled streamed child could retain errorCode NONE and reach the isolated bridge marker assertion. Astra expanded Luna ownership to only two harness sites: normalize failed-child NONE to CLI_RUNNER_ERROR and reject a non-NONE signal before the bridge marker. Preserve normal nonzero SQL-marker acceptance and keep the native CLI runner unchanged. Final harness98212aa37e765013a139adeb64a7338eec448d09a0dfe55a5fa2cccb27439170 contains exactly these two line changes from729689815ad7a5c821505f68cbcd1ea10d05a3b006ad0db6efece32f43203020. Child focused checks passed; parent acceptance and final test-coverage review remain pending.

**R4A final verification (2026-09-07):** parent inspected the exact runtime/test deltas, confirmed the final filesystem-spy repair is one line only, and passed five syntax checks plus runtime-cleanup, CLI diagnostics, CLI atomicity-order and isolated bridge replay (all exit0/stderr0). Harness98212aa37e765013a139adeb64a7338eec448d09a0dfe55a5fa2cccb27439170 and runner626f79280c4ca7e173afd98475c1fac675808d55d595f7047de542101f831350 are unchanged by the final test repairs. Kill-success/no-close and real stop UNKNOWN-to-latch-to-no-delete tests close the two coverage gaps. The final spy records and throws without performing a real deletion. This source verification does not authorize actual matrix execution before R4B/C.

**R4B/C settled correction (2026-09-07):** direct Docker resources use a cryptographic nonce and exact run label, with absence verified before creation and attempts registered before every create. Read back success and failed-create resources against planned identity, labels, cached image ID/digest and configured mount/network; use observed IDs for container/network deletion and verified volume name. Revalidate ownership before deletion and prove zero residue afterward; unknown ownership/capture retains resources and fails closed. CLI uses a nonce project namespace, proves all three resource types absent before setting startAttempted, and inventories both Supabase/Compose project labels and cached image IDs/digests (including randomly named helpers) before explicit project-scoped no-backup stop. Precheck failure never stops an existing namespace. Preserve the R4A termination-unknown latch across resource and directory cleanup. Cleanup callbacks require explicit success and verified absence, never undefined or a statusless object. No foreign-resource deletion or runtime image pull.

Pass the generated Postgres password only through the specific Docker child environment and argv `--env POSTGRES_PASSWORD`, with no parent environment mutation or secret value in argv/log/file. Before start/reset require native CLI2.109.0, generated config DB major17/pooler off and experimental.pgdelta.enabled=false, and complete local image ID/RepoDigest inventory for postgres17.6.1.140, gotruev2.192.0, realtimev2.112.1, storage-apiv1.61.7, mailpitv1.30.2, postgrestv14.14, postgres-metav0.96.6 and logflare1.45.6. Keep the existing service exclusions and bootstrap/SQL semantics. Missing version/config/cache is SETUP_BLOCKED before resource creation. Ownership is only the full integration harness, existing runtime-cleanup helper, runtime-cleanup test and atomicity-order test. Verify actual emitted functions with injected outcomes for collision, failed create, ownership mismatch, incomplete inspection, false cleanup success/residue, stop UNKNOWN, password isolation and all cache/config guards. The native CLI runner, SQL, fixtures, Vault paths and dependencies stay unchanged. Parent actual matrix follows independent review and final focused acceptance.

**R4B/C review repair (2026-09-07):** initial source is not accepted. A collision resource was registered before absence validation and could be removed without a create attempt (P1). Register cleanup candidates only after verified absence and immediately before create; require an explicit attempted flag in cleanup. Replace generic stderr not-found matching with successful, complete, shape-validated resource listings and exact planned-name absence; daemon/client/capture failures are UNKNOWN, never absence. JSON-lines inventory must reject every non-object/null/array/missing-identity row, rather than silently converting invalid input to an empty inventory. Initial generated-config validation accepts the existing pgdelta boolean while requiring DB17/pooler-off and unique keys/sections; only after setting pgdelta=false does the strict start/reset validation require false. Retain the original image-config guard from the writer packet: direct PG image Config.Volumes must be present and empty, so unexpected anonymous volumes block before create. These corrections stay within the existing four-file ownership. Add real-function injected coverage for collision-to-cleanup/no-remove, failed-create registration/recovery, wrong label/ID/image/mount/network, incomplete listing/inspection, CLI three-type labels/random helpers/cache identity changes, precheck-failure/no-stop, password child-environment isolation and all version/image/config guards. Existing regex checks alone do not satisfy those criteria. Parent actual matrix stays held pending review and focused checks of the repaired final state.

**R4 final stop-condition repair (2026-09-07):** parent inspected the four-file repair and passed four syntax checks and four focused contracts, all exit0/stderr0; independent review confirmed the original implementation findings fixed. Two acceptance gaps remain before actual matrix execution. Run negative cases sequentially and stop on the first result whose status is not PASS, preserving executed results and explicitly listing unexecuted case IDs. The outer matrix must return FAIL before CLI atomicity whenever the negative matrix is not PASS, while still running owned-resource cleanup in finally. Preserve every case, expected SQL failure assertion and successful full-matrix behavior. Add injected tests executing the real negative loop and outer run path, proving first/middle failures invoke no later case or CLI atomicity and all-PASS executes the complete list. Also exercise the real stopGeneratedCliProject wrapper: cache/inventory/image rejection emits no stop; successful stop with residue or unknown post-inventory fails; confirmed stop and complete empty inventory passes with exact project-scoped no-backup arguments. Source ownership is only the full integration harness and existing atomicity-order test; no Docker, real CLI, DB, SQL/fixture edits or external operations in the writer lane.

**R4 image-shape correction and final review (2026-09-07):** a parent read-only inspection of the cached PG image confirmed a complete Config object with no Volumes property and zero declarations. The OCI image specification defines Volumes as optional and null as equivalent to absence (https://github.com/opencontainers/image-spec/blob/main/config.md). This supersedes the earlier present-empty shape assumption: require complete inspection and a plain Config object; absent/null/empty plain-object Volumes means no declared volumes, while invalid Config, invalid/nonempty Volumes and incomplete capture remain UNKNOWN. Independent review accepts this correction and confirms the sequential stop, outer source connection and real CLI stop-wrapper tests. Before acceptance, remove the newly added negative-failure report's generic all-zero mutationCounts because local DDL/DML has already run; retain truthful remote-zero fields. Add actual run() injection tests for negative FAIL and SETUP_BLOCKED, proving atomicity call count0, final FAIL with unverified atomicity, executed/unexecuted negative details preserved and finally cleanup call count1. Helper-only tests are insufficient. No new runtime orchestration or operation-counting mechanism is needed.

**R4 accepted source state (2026-09-07):** final independent review closes both report-shape and actual-outer-run coverage findings. Parent actual-diff inspection, three syntax checks and four focused contracts pass at harness3918d206fa715393d09169abb989ea9734a3060aa49d5a7361c6b7037ca94d65 / runtime-test0edf764523a10d846e632b3a03aea22a0e1983d2aa6ecb1bce9f3fcb7b16eed9 / atomicity-testbbade9b1240810d24b0811478f0bfdd1e8a39f68debec7a1b17894e24477f895; helper668d87fb3169d3da12feac82e94c760c509b0ac7cf3d31856d4d092ac889084b and native runner remain unchanged. Checks exit0/stderr0, source hashes preserved, no unexpected protected-file changes and diff-check passes. Proceed to the single actual local full matrix after current input, tool and resource inventory checks. This is not actual DB acceptance or full Gate1 GO.

**R4 actual startup finding and correction (2026-09-07):** the single full actual matrix stopped with an opaque LOCAL_VERIFICATION_EXCEPTION. One clean-prefix diagnostic located CLEAN_BOOTSTRAP/CONNECTION_ERROR during CLEAN_DATABASE_SETUP. A separate runtime-only observation found socket pg_isready exit0 while 127.0.0.1 TCP readiness exited2, with complete captures. This proves the current socket-only readiness condition can advance before TCP service is available; a transient initialization server is a supported hypothesis, not yet an independently confirmed cause of the bootstrap disconnect. Require pg_isready with explicit --host=127.0.0.1 and --port=5432 in the existing bounded startup loop, retaining the same local target, timeout, ownership and cleanup rules. Do not add broad retries or change database roles, SQL, mounts or authentication. Inject actual runtime-function tests proving TCP args, not-ready-to-ready progression and bounded never-ready rejection. Also make future outer exceptions diagnosable using a fixed local phase enum and an allowlisted failure class (assertion, known PSQL classification, or UNKNOWN_ERROR); never render raw exception messages, SQL, credentials or identifiers. Preserve LOCAL_VERIFICATION_EXCEPTION compatibility and remote-zero fields, and omit generic all-zero mutationCounts from that executed-runtime catch. Add actual run-path tests for a classified setup failure and an unknown secret-bearing exception, both with cleanup1 and safe output. Ownership is the existing harness, runtime-cleanup test and atomicity-order test only. Parent source review/checks precede the next actual matrix at the changed revision. All three actual observations independently preserved source/input hashes and existing resources with generated residue0/0.

**R4 TCP source acceptance (2026-09-07):** independent review reports no findings and parent actual-diff inspection plus three syntax/four focused checks pass at harness195f35d14d465da9e302645b5e701098a5f6e994a4f54063a8e377468f98b0d5. Source/protected hashes and diff-check pass. The next single full local matrix must prove actual recovery; source acceptance alone does not close the connection failure or Gate1.

**R4 native CLI compatibility correction (2026-09-07, settled):** actual TCP matrix passed clean/legacy/canonical/synthetic and24 negatives, then failed CLI query shape; independent post-inventory found10 generated resources despite cleanup PASS. CLI v2.109.0 [config source](https://github.com/supabase/cli/blob/v2.109.0/apps/cli-go/pkg/config/config.go) sanitizes IDs to40 characters; [query source](https://github.com/supabase/cli/blob/v2.109.0/apps/cli-go/internal/db/query/query.go) emits ordinary row arrays and optional agent envelopes. Luna owns only harness/runtime-cleanup test/atomicity-order test. Keep the existing project prefix and generatedNonce contract; bound the CLI project suffix to20 random hex characters (80 bits), ensuring total40, exact config/start/inventory/stop identity and existing collision preflight. Reject overlength IDs at read/ownership inventory boundaries; no silent normalizing existing cleanup targets. Parse the ordinary one-row array and existing rows envelope with unchanged strict one-column/object-value validation; reject empty/multiple/malformed rows and scalar values. Execute actual function tests for full24-character nonce to40-character ID, exact lifecycle identity, overlength rejection and both JSON forms/invalid forms. Preserve SQL, role/password transport, timeout, runner, cleanup helper, image/cache constraints and all GO criteria. Parent first verifies exact ownership of existing residue and cleans only those recorded resources; no new runtime until independent residue0. Then actual diff review and three syntax/four focused tests precede one new-state matrix attempt. No unchanged retry, source publication or hosted mutation from this correction alone.

**R4 CLI compatibility source acceptance:** final harness7c2cd6d3868b557ab0a81aa21f14311ac08c4cd4f1a40d182e9adf0012ea0301 passed independent scoped review with no findings, parent actual3-file delta inspection,3 syntax and4 focused contracts, source scope/preservation and diff-check. Prior10 residues were independently ownership-verified and removed: owned0, all10 foreign resource identities preserved. This is source acceptance and recovery cleanup evidence only. One new-state actual local matrix remains required before accepting CLI lifecycle recovery.

**R4 actual compatibility result:** the single7c2-state full run passes the same five case groups but still fails RUNTIME_IDENTITY_CLI_QUERY_SHAPE with native query status0/stdout739. Parent confirms cleanup0/0, existing resource identities and all source/saved/legacy hashes preserved. ID cleanup is actual-verified; parser recovery remains unverified. Before any further repair, run one bounded original CLI start/status/runtime-identity-query diagnostic with sanitized JSON shape/type/count metadata and original owned cleanup. Stop before any migration/reset/later case. No unchanged full retry or source edits during this diagnostic.

**R4 query output selector correction (settled):** the bounded actual query probe captured non-JSON stdout, with owned/temp0/0 and all protected identities/bytes preserved. Native fixed CLI help and [pinned query command source](https://github.com/supabase/cli/blob/v2.109.0/apps/cli-go/cmd/db.go) establish that query result selection uses --output/-o and otherwise defaults to table outside agent mode; global --output-format is separate. Luna changes only runCliQuery to pass -o json and adds actual-function tests in the existing CLI atomicity-order test asserting exact local/file args, structuredStdout selection, and successful strict parsing through runCliQuery. Keep parser strict, other command behavior, SQL, environment, runner, timeout and cleanup unchanged. Parent diff/review and2 syntax/4 focused checks precede one identity-only changed-state diagnostic; accept only parsed actual identity postgres/5432/PG17 and independent cleanup/source preservation. Only then allow a new-state full matrix. No guessed output stripping or unchanged full retry.

**R4 query selector source acceptance:** harnessd3160ff4b061c76f9e82350613301b85ed593ba987955dbd26d2f322c5c165ed and atomicity-test149a39e8386bc07aaab7fb737a50d3047d05126d95b43ef5896da3c79484a105 have no scoped review findings. Parent inspected full delta and passed2 syntax/4 focused checks plus source scope/preservation/diff-check. Actual identity-only success and cleanup remain the next gate; source tests do not prove recovery.

**R4 actual query recovery:** one d316-state identity-only probe passes original strict parser and postgres/5432/PG17 assertions. Native capture complete/stderr0; original cleanup PASS and independent postchecks owned/temp0/0, existing identities and source/saved/legacy bytes preserved. The query selector fix now has actual evidence. One full local matrix at this state is the next check; this probe does not claim migration/atomicity success or Gate1 GO.

**R4 accepted actual full matrix (2026-09-07):** the one full d316-state run completed exit0/error-null/signal-null, complete capture/stderr0. All clean/legacy/canonical56/synthetic preservation/24 negative cases pass. Actual CLI failure injection, rollback state, restored migration-up, success state, final catalog, canonical structure and migration-list56 pass. Parent postchecks confirm owned/temp0/0, all pre-existing identities and source/saved/legacy bytes preserved; remote connections/mutations0. R4 is accepted local evidence. Do not rerun without relevant changes or invalidating evidence. This does not complete R3 actual provenance/nine-stage verifiers, R5 recovery readiness, source publication, hosted verification or full Gate1 GO.

### R5 — Establish connection and zero-cost recovery prerequisites

**Actual Production backup / local restore and replay accepted (2026-09-08):** The fresh persisted set uses the accepted unpublished empty-vector capture source and the independently reviewed empty application-owned Auth/Storage delta. Four held-snapshot dumps completed in 13112ms; the exporter confirmed both vector tables empty, source50-table counts/digests were stable, and independent native persisted-byte/hash/ACL inspection passed. Manifest SHA-256: `288801029ad10e855bfa5edacaf49032902920e74642aae7b3693e18485cac1a`. Original backups are unchanged. Managed compatibility is bounded: exact Auth77/Storage68 histories, multi-tenant Storage8 tables, all410 common non-session schema statements, no unknown extra statements, all80 effective privilege/grant-option observations equal, and only four independently classified valid nonunique/nonconstraint/nondependent Auth indexes absent. Index performance equivalence and the initial state of a future hosted Recovery project are not claimed.

**Mandatory empty-destination privilege preparation:** New Supabase databases can add default grants while `schema.sql` creates tables. The first actual restore preserved data but doubled each legacy table ACL from16 to32 by inheriting anon/authenticated grants; the unchanged bridge correctly rejected that state. Before restoring any application schema, verify the approved destination binding, its non-superuser `postgres` profile and empty `public` application tables. Execute the following as `postgres`, in one transaction. A nonempty target stops; this is a fresh Recovery/rehearsal prerequisite, never an operation against healthy Production. It is not a seventh backup artifact and does not edit the six captured files. Managed Auth/Storage schemas and existing object privileges are not changed.

```sql
BEGIN;
DO $$
BEGIN
  IF EXISTS (
    SELECT 1 FROM pg_class c JOIN pg_namespace n ON n.oid = c.relnamespace
    WHERE n.nspname = 'public' AND c.relkind IN ('r', 'p')
  ) THEN
    RAISE EXCEPTION 'TARGET_NOT_EMPTY';
  END IF;
END
$$;
ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public
  REVOKE ALL ON TABLES FROM PUBLIC, anon, authenticated, service_role;
ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public
  REVOKE ALL ON SEQUENCES FROM PUBLIC, anon, authenticated, service_role;
ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public
  REVOKE ALL ON FUNCTIONS FROM PUBLIC, anon, authenticated, service_role;
COMMIT;
```

Then restore the original six files in their existing order, each with `ON_ERROR_STOP` and a transaction. Restore-time explicit ACL/default-ACL statements remain authoritative; this preparation removes destination additions rather than replacing source grants. Per-schema REVOKE does not remove global defaults, so do not infer global ACL equivalence from command success. Verify actual resulting catalog ACLs. References: [Supabase CLI privilege-migration note](https://supabase.com/docs/reference/cli/supabase-db-dump), [PostgreSQL17 default privileges](https://www.postgresql.org/docs/17/sql-alterdefaultprivileges.html).

**Final measured result:** With this prerequisite, the actual non-superuser postgres restored all6 files without SQL-content edits, matched all50 source table counts/digests including Auth and history, and reproduced the native Production legacy/clean bridge fingerprint including ACLs. It applied all34 pending migrations from pinned source Git blobs, each atomically with its history entry; final sorted version/name history is exactly56 and the original22 full history rows are unchanged. Canonical33 tables/81 functions match the independently accepted Preview structure and pass the canonical RPC security boundary. Archive3 tables/3 functions retain zero rows, RLS and denied application ACLs. The six source-era tables and their functions match fresh native Production observations and remain unchanged through bridge/forward replay. Vault secrets/Cron jobs/active jobs/runs are0 and the local launcher remains off. No hosted migration, pause, provision, deploy, activation, source publication or subagent occurred. Final owned resources3 created/3 removed/0 remaining.

The retained diagnostics distinguish product and harness issues: the initial default-grant drift was a real restore-preparation defect; later failures were JSON object-key-order comparison and catalog sampling as the bootstrap administrator (its search_path omitted the `auth.` qualification), not new schema defects. Final history comparison is key-order independent and final catalogs are sampled as postgres. Final local evidence is retained under the restricted ignored `phase4-portable-*` artifacts, including final-review, v5 execution/catalog and source/preparation bindings. It is LOCAL_RESTORE_AND_REPLAY_REVIEWED, not hosted recovery or final nine-stage authority. The vector repair remains unpublished; source/authority publication and the remaining actual operator stages are still required. Gate1 stays NO-GO.

**Managed baseline mode diagnosis:** 2026-09-08 管理baseline追加診断: cached公式Storage imageの実装を確認し、MULTI_TENANT=trueが移行接続のstorage.multitenant=trueへ渡されることを特定した。新規隔離DBで公式tenant migratorと履歴hash検証済み追加7 migrationを実行するとStorage8表・iceberg内部表0となり、Auth77/Storage68の完全移行履歴がProduction観測と一致した。従来の内部2表DROPはsingle-tenant初期化によるlocal差分であり、hosted復元SQLへ含める根拠にならない。作成3/削除3/残存0、hosted接続0、host port0、データ投入0。別途Production read-only catalogでAuth4索引は全て有効な非unique・非primary・非exclusion索引、関連constraint/dependent0と確認したが、性能影響と復元先での存在は未検証。owner ACLの文字列表現差についても有効権限の同等性はまだ未証明。全431文の文字列一致を元仕様の必須条件とは扱わないが、未知差分を無視したり失敗SQLを削除して復元成功と扱うこともしない。次は4索引・owner ACLの互換性判定を確定し、正式な利用者差分とfresh6-file復元へ進む。元backup保持、source変更/公開・Production変更・サブエージェントなし。Gate1 NO-GO。

**Empty-vector source repair accepted locally:** 2026-09-08 vector backup repair LOCAL_ACCEPTED / UNPUBLISHED. Root implemented and reviewed the approved source repair without subagents. createBackupCapture unconditionally requests native empty-vector guards in the held repeatable-read/read-only exporter transaction; both exact counts must be numeric0 before any of4 snapshot dumps. Missing/partial/nonzero/malformed observations fail closed, and caller counts or a caller false guard flag cannot bypass the guard. SET LOCAL row_security=off rejects filtered reads instead of misclassifying hidden rows as empty; it does not change table RLS policies. data.sql alone excludes storage.buckets_vectors and storage.vector_indexes. The snapshot hash and both counts are retained in vectorExclusion; nine-stage semantics require this exporter evidence plus explicit zero vectorCounts in source/restored state. The PowerShell actual/public arrays and plan shape require each exact exclusion once. Root19 snapshot/capture tests,12 stage groups,full backup recovery contract,7 scoped ESLint files,7 Node syntax checks and1 PowerShell parse pass. Actual final local PG17 roundtrip restores6 stages with4 held-snapshot dumps in1959ms, preserves synthetic data/Auth/history/RLS/grants, and proves post-snapshot concurrent vector inserts are excluded consistently. Each nonempty vector table, a missing table and real RLS-hidden rows independently stop with0 dumps/confirmed exporter cleanup. Earlier native RLS test established a false-empty acceptance and the final test rejects it; the initial attempt to demote the bootstrap superuser was a test setup error, diagnosed and replaced with an explicitly limited synthetic role. Final owned containers2/2/0 and independent remaining0. No Production connection/mutation, new real backup, original backup overwrite, migration, commit/push/PR or agent. This accepts the source repair and synthetic local protocol only. Destination managed baseline/user-delta portability and fresh full Production6-file recovery/security/bridge evidence remain unresolved; Gate1 NO-GO.

**Current interface requirements:** The capture receipt carries vectorExclusion={snapshotSha256,counts}, with the exact keys storage.buckets_vectors and storage.vector_indexes, both0, obtained by the native exporter. Backup stage observations carry that receipt in exporter.vectorExclusion and explicit vectorCounts in sourceState and restoredState. Old observations missing these fields are intentionally rejected; never synthesize them from earlier aggregate counts. The standalone snapshot transport retains an unguarded default for its generic holding-session use, but createBackupCapture always requests requireEmptyVectorTables=true. For the guard, row_security=off is a session-local error-on-filter safeguard, not a policy bypass. The local actual-source probe uses a fixed Docker adapter and synthetic bindings; it is not hosted/TLS/native authority evidence.

**Managed-schema and empty-vector COPY diagnosis:** 2026-09-08 phase4 root cause confirmed. Native Production read-only settings and role membership match the relevant local supautils privileged role/configuration: postgres has privileged-role membership but no Auth/Storage admin membership. Eleven local transaction/rollback probes establish session_replication_role is executable, both managed-table DROPs and all4 Auth CREATE INDEX statements reject with42501, and all4 owner-ACL statements execute. Official managed-schema restrictions independently corroborate the DDL boundary. Supplemental restoration on the previously matched local platform-admin baseline then exposed a separate data.sql42501: native Production INSERT privileges are false for storage.buckets_vectors and storage.vector_indexes, and two empty-COPY local probes each reproduce42501. Both source tables have0 rows. A new disposable diagnostic removes exactly those2 provably empty COPY blocks in memory (rejecting absent/duplicate/nonempty blocks), retains all original backup files, and restores the other5 stages as non-superuser postgres. All50 source table count/digests match, including Auth data and history; complete Auth/Storage exported SQL token multisets match431/431; Cron jobs/active/runs0 and launcher off. This is conditional local data/history evidence, NOT accepted6-file recovery: the managed baseline was prepared by the local platform administrator and the data was transformed for diagnosis. Each of4 runs created/removed3 containers, with remaining0; no source/migration edit, backup overwrite, Production mutation, agent, publication, or GO promotion. Next: repair the source-backed data recipe with same-snapshot native empty-vector guards and exact2 exclusions; separately establish a destination-owned managed baseline/user-delta boundary before a fresh6-file capture and full recovery/security/bridge authority. Existing byte-verified backup remains restore-unaccepted. Gate1 NO-GO.

**Repair specification (vector source implemented and locally verified above; managed-baseline and hosted recovery work remains open):**
- Keep manifest60f921316980558aa6adc2d61716107f95f2da76cd51678f557c53cf61e078ff and its6 files immutable. Do not relabel the supplemental5-stage diagnostic as full recovery.
- For future captures, explicitly exclude only storage.buckets_vectors and storage.vector_indexes in addition to the existing exact exclusions. Before exclusion, obtain native proof in the same held snapshot that both relations exist in the supported baseline and are empty. Missing relations, failed/partial observations, nonzero rows, stale or caller-only counts must fail closed; source/target zero verification remains required. Do not silently discard vector metadata or treat Storage objects0 as equivalent.
- Update the native capture, public/internal recipe arrays, operator/backup contracts and provenance/stage requirements together, with positive and missing/nonempty/unknown/snapshot-mismatch negative cases. Preserve four held-snapshot dumps, deadlines, TLS, immutable artifacts and current security gates. Source repair must be reviewed and published/bound before it supplies hosted operational authority.
- Managed-table DROP and Auth index CREATE are not portable user restoration steps. Require an independently observed destination platform baseline that satisfies the required schema/version/index/security state; restrict auth_storage_changes.sql to reviewed user-executable differences. Current supplemental baseline derives from source-shaped local platform-admin preparation and cannot establish what a newly provisioned hosted target supplies. Do not declare an empty user delta solely by deleting rejected statements or raise the restore role.
- After both boundaries are resolved, use a fresh artifact set and fresh isolated target to verify the unchanged six-stage order, full rows/history/Auth/dependencies/grants/RLS, and bridge/canonical replay. Hosted recovery readiness and final nine-stage GO evidence remain separate.
- Primary references: [Supabase backup/restore recipe](https://supabase.com/docs/guides/platform/migrating-within-supabase/backup-restore), [managed-schema restrictions](https://supabase.com/changelog/34270-restricting-access-on-auth-storage-and-realtime-schemas-on-april-21-2025), and [supautils settings and ownership boundary](https://github.com/supabase/supautils).


**Post-time-sync backup and actual restore checkpoint (supersedes the clock blocker):** 2026-09-08 latest phase4: user-authorized Windows resynchronization succeeded through the normal administrator prompt (w32tm /resync exit0); fresh native read-only DB metadata age is54ms, resolving the prior future-T0 rejection without weakening checks. Native Production backup then captured all6 files with4 same-snapshot dumps in10697ms and clean exporter close. Before/after50-table count/digests are identical. The protected external store and independent native artifact inspection verify all6 bytes/hashes and manifest60f921316980558aa6adc2d61716107f95f2da76cd51678f557c53cf61e078ff. Actual local restore as the non-superuser postgres profile restored roles.sql and schema.sql but failed auth_storage_changes.sql with42501 before data/history restoration. All3 newly owned containers were removed; no target reuse or privilege escalation was attempted. The earlier431-statement match used the local platform administrator to prepare a matching baseline and does not prove that the six-file recipe is executable by the restore role. Its local-only managed-schema delta is not accepted as a portable hosted recovery step. Production read-only checks confirm postgres is not a member of Auth/Storage admin roles; catalog SET privilege alone cannot resolve supautils runtime exceptions. The saved backup is byte-verified, not restore-accepted or ready for final Production apply. Next work is to resolve the managed-platform baseline versus user-executable Auth/Storage delta boundary, validate under the actual restore role, then complete data/history/security/bridge replay and recovery authority. Auth settings remain durably saved and hash/ACL verified. Source code/migrations and Production remain unchanged; Gate1 NO-GO.

**Latest phase4 progress / clock blocker:** 2026-09-08 engineering phase4: native Production Direct/verify-full schema-only capture succeeded (no row data). Auth77 and Storage68 managed migration records are bound; cached Storage61 was extended only in disposable containers with7 official SQL files whose sha1(filename+sql) matches Production history, pinned upstream b41d14fa15547284b351ea024f8c83a201cdc83a. Reviewed local-only delta creates4 Auth indexes, preserves4 Storage owner-ACL statements and drops2 verified-empty local-only Storage tables with RESTRICT. After actual local replay, all431 exported schema-statement token rows match Production with exact multiplicity; Windows dump CRLF decoding is independently checked against all21 native function-body MD5 values. This is rehearsal-baseline evidence, not authorization to run the local-only delta on hosted projects. Auth configuration including exact URL values is durably saved under the approved D:/Gate1Backups root, independently hash/ACL/reparse checked; plaintext values and transfer private keys were not emitted or persisted in logs. Native Production backup then stopped at SNAPSHOT_METADATA_INVALID before held-snapshot dumps/persistence. A separate read-only export/rollback confirms all metadata format/version/read-only checks pass, but the DB timestamp is1321ms ahead of the client at readback. Preserve the strict future-time rejection; do not relax deadlines or repeat capture unchanged. Exporter cleanup is confirmed; destination remains empty, with no six-file backup or restore acceptance.50 source-table aggregate count/digests were collected read-only. Next: obtain OS-scope permission to inspect Windows time synchronization and resynchronize if necessary, then recheck timing before a fresh backup and isolated full restore. No Production DDL/pause/provision/deploy/activation. Gate1 remains NO-GO.

**Engineering phase4 intake (2026-09-08, after phase3 acceptance):** Management read-only confirms two ACTIVE_HEALTHY projects in the same Free organization and `ap-northeast-1`, both PostgreSQL17. Local ToolProbe passes Supabase2.109.0, Docker and all four PostgreSQL17 clients. A bound Direct/verify-full native Production read-only transaction observes history22, Vault total/reserved0, Storage objects0 and Cron jobs/active0. Preserve these as intake observations, not durable backup or recovery proof. The existing approved capacity route is incident-source pause confirmed by the deadline, then a new recovery project; a permanent third free slot is not required and a healthy source is never paused to free capacity. The user confirmed the same Free organization/Tokyo recovery target, and the project quote returned monthly0. A quote does not establish incident-time provisioning/deadline readiness. Auth provider/redirect configuration, emergency cutover checklist, Auth/Storage diff and rehearsal are still to be established. The user completed login. Read-only Dashboard inspection confirms Email only enabled,24 other providers disabled, no custom or third-party providers, signup/email confirmation enabled, anonymous/manual linking disabled, secure email change enabled, minimum password length8, Email OTP3600s/8digits and site URL1/redirect URLs4. Other displayed password-change/protection switches are disabled. Restricted phase4-production-auth-observation.json retains sanitized settings and a complete inspected-configuration digest; exact URL values remain only in the live read-only session and are not a durable recovery artifact. No settings were saved. Auth/Storage diff remains unexecuted: inspected CLI2.109.0 pg-schema source discards TLSConfig when converting both connections to URLs and ignores connection-option callbacks; a process-wide PGSSLMODE=verify-full also reaches the local shadow. Do not execute that path with weakened TLS. A native verify-full schema capture followed by entirely local comparison is a candidate requiring exact baseline/version and dependency-equivalence validation before it can replace the approved diff evidence. The fixed CLI diff help and source were inspected only; no remote schema diff/shadow runtime, project pause/create, backup or restore was executed at this intake. No cost was incurred or production DDL attempted.

**Native backup observation and process evidence accepted locally (2026-09-08):** Parent extended the artifact store with native inspectBackupArtifacts and added backup-observe CLI/test. Existing root/Git checks were factored without broadening allowed paths.11 artifact-store tests and4 CLI groups PASS, including exact native request/no adapters, inert/default behavior, strict UTF8/duplicate JSON/64KiB limits, actual10s silent-input timeout, modified/missing/extra files, unknown manifest/order/size/authority, ACL/reparse/hardlink/handle replacement and zero writes. Syntax and parent source inspection pass. One actual public native inspector read the approved external roundtrip set against independently pinned manifest8561342d04adcb67597669714c6dbe60b67782325373a922c417c6e86d28d41a; all6 observed file hashes/sizes match and no SQL/raw-source-hash claims are returned. One actual repo-local CLI process then independently repeats that observation with a minimal Node environment: exit0/error-null/signal-null, complete1049-byte stdout and stderr0,18.346s elapsed. Parent binds entry9404c007064b1b1264c0b493e113ad5ac590e1c265d186f65e67d33dd63ba44d, librarycb7773cce0ab461d7a416f158b4253284769842f50ff76d31f1bb5fb30fda069 and strict-parser dependency hashes before/after; all unchanged. Observation SHA2567380c3079f4b5cf8df748b992308a44d3921f668c0583323bf5a9dff370c13ef. Restricted ignored artifacts local-native-backup-inspection.json, native-backup-observation.json and native-backup-observation-run.json preserve results/outer process measurements. These local records explicitly say sourceState UNCOMMITTED and authority UNESTABLISHED; they are not the committed R3 stage receipt/index or target/freshness/recovery proof. No backup rewrites/DB/agents/hosted/cost. Full R3 backup-stage semantics, other stage verifiers, immutable source/authority publication and actual Supabase/Production recovery remain mandatory; Gate1 NO-GO.
**Native observation process entry (2026-09-08):** Parent adds only backup-observe.mjs and backup-observe.test.mjs, registered in Task10. Inert imports/default unavailable; the only execution flag is --inspect. Read a single private strict UTF8 JSON request from stdin with 64KiB/10s bounds, exact keys schemaVersion1/directory/expectedManifestSha256, no adapters or arbitrary command arguments. Invoke inspectBackupArtifacts natively and emit exactly one sanitized JSON observation with actual startedAt/completedAt and the native readback result. Missing/unknown mode, malformed/duplicate/extra request, input limit/timeout and failed inspection produce fixed unavailable reasons and exit2; no raw input/path/SQL/error output. This repo-local producer gives the outer parent a real process exit/capture boundary for future R3 receipts. It does not fabricate a receipt, immutable source commit, target binding, restore result or stage authority. Parent runs one actual CLI observation against the already approved external directory with independently pinned manifest hash, minimal Node environment and independently measured complete output/stderr/exit, then verifies producer/library hashes unchanged. Preserve previous probe results; no DB or backup rewrite.
**Native persisted-backup observation boundary (2026-09-08):** Parent extends only backup-artifacts module/test, with plan/task bookkeeping. Add an inert public inspectBackupArtifacts({directory,expectedManifestSha256}) entry that constructs its own native store and accepts no injected fs/process/clock/results. It must independently verify the externally selected manifest SHA256, strict JSON/exact shape/order/fields, all six bounded SQL file lengths and actual hashes, exact seven-file inventory, protected ACL/reparse/hardlink/path/handle identity and before/after metadata. Reuse the existing native root/Git/ACL boundary; no arbitrary command/path files or manifest-supplied filenames. Return only six canonical names/observed sizes/SHA256 and fixed PERSISTED_BYTES_VERIFIED status, explicit acquisitionAuthority UNESTABLISHED, zero writes/remote calls. Original raw-dump SHA values in a manifest are not independently verified because the original raw bytes were not persisted; do not promote those claims. Missing/malformed expected digest rejects before I/O, and stored-data/manifest tampering, missing/extra files, link/ACL/capture failure all reject. Low-level mechanical tests may exercise the store method with fakes, but the public collector has no injectable adapter. Actual read-only run against the approved external roundtrip directory must match the parent's already observed manifest digest and six persisted bytes. This provides the bulk artifact observation required by later R3 backup-stage verification; it does not assert restore/Auth/configuration/RPO/source/provenance/freshness or GO.
**Actual persisted six-stage local roundtrip accepted (2026-09-08):** User explicitly approved D:/Gate1Backups; this approval persists for scoped backup storage there. Root did not exist, so parent created it and two unique child directories with protected current-user/SYSTEM/Administrators ACLs, preserving all pre-existing paths. First actual non-injected artifact-store run persisted six synthetic UTF8 files plus manifest; independent7-file ACL/reparse checks pass and a second save is rejected with all seven hashes unchanged. Then a fresh two-container pinned PG17 probe used the actual source capture and non-injected store: native roles plus four held-snapshot dumps/hashes completed in1746ms, clean exporter close, six actual SQL files persisted under the approved external root, and all6 restore transactions read those files from disk. Synthetic parent/child/history/Auth-row JSON, selected RLS policy/SELECT grant match; Storage0. Source4-module hashes captured before run remain unchanged; final six byte-count/hash comparisons and manifest hash independently pass. Manifest SHA2568561342d04adcb67597669714c6dbe60b67782325373a922c417c6e86d28d41a. Root+7-file ACL unexpected0/reparse0. Containers created2/removed2/remaining0 independently. Preserve external verification and roundtrip files; no cleanup is authorized by this result. Aggregate local-persisted-six-artifact-restore.json and original probe source remain in the restricted ignored evidence root; private directory records locate the external child directories. Actual local persistence uncertainty is closed. Status remains PERSISTED_AUTHORITY_UNESTABLISHED: supplied synthetic Auth/Storage bases and local adapter cannot prove hosted TLS, actual Supabase Auth configuration/dependency recovery, Production-source rehearsal, native trusted stage authority or zero-cost hosted recovery capacity. No hosted calls/cost/agents/deploy/activation. R3 complete stage verifiers/receipts and full Gate1 evidence remain mandatory; Gate1 NO-GO.
**Restricted persistence source verification (2026-09-08):** New backup-artifacts module/test passes two syntax checks and7 focused mechanical groups: exact ordered UTF8/fsync/readback/last manifest, zero-operation invalid inputs, ACL/nonempty rejection, reparse/hardlink no-byte rejection, partial failure retention and no overwrite. Parent inspected source. Actual extracted native ACL script initially rejected the known restricted directory because inherited PowerShell Core PSModulePath made Get-Acl unavailable in Windows PowerShell; no writes occurred. Fixed the native process environment to only SystemRoot/WINDIR/PATH/PSModulePath under Windows PowerShell home. Changed-state actual read-only probe accepts the protected ignored evidence root and rejects the ordinary repository root, both with empty stderr and writes0; this verifies the ACL probe, NOT permission to save backups inside either repository path. Native Git common-directory discovery also excludes the primary checkout; a seventh regression and actual main-checkout refusal pass with writes0. Actual six-file persistence/readback is still UNVERIFIED pending the user's explicit external directory selection (candidate D:/Gate1Backups). No directory/ACL was created or changed, no real SQL written, no DB/agents/hosted/cost. R3 acquisition authority remains UNESTABLISHED and Gate1 NO-GO; do not count the synthetic fs/process test as actual persistence or native stage provenance.
**Restricted artifact persistence boundary (2026-09-08):** Parent owns new backup-artifacts module/test registered in Task10. Windows-only library, inert import, no backup/restore/CLI. Persist exactly the six ordered capture artifacts into a caller-selected existing empty directory outside actual repository, its worktree parent, current workspace, drive/home roots. Reject relative/UNC/alternate-stream/reparse paths. Before writes, native fixed PowerShell validates protected root DACL, owner/current-user access and only current SID/SYSTEM/Administrators Allow entries; check every ancestor for reparse points. Do not modify ACLs or create the root implicitly. Validate all names/UTF8/size/hash fields before opening files. Exclusive creation, inherited ACL check before writing bytes, bounded 32MiB/file writes, fsync, exact readback and reparse/ACL recheck. Write a fixed JSON completion manifest last; report success only after its readback and final complete root inventory. Partial failure retains owned files, no overwrites/deletion/retry, and never reports persistence complete. Manifest proves byte persistence only, not native acquisition/approval/stage authority. Native operations expose only sanitized fixed status/count/hash results. Low-level fs/process seams support mechanical tests; actual positive filesystem check awaits a user-selected permitted external directory. Do not reinterpret the ignored synthetic aggregate root as permission to store real backup SQL in a repository.
**Native capture and six-stage local protocol accepted (2026-09-08):** Parent implemented backup-capture module/test; seven focused test groups and both syntax checks PASS. Actual source module through a fixed Docker adapter captured native roles plus four same-snapshot artifacts, hashed them before clean exporter close (1966ms), and produced all six ordered SQL artifacts. In two fresh pinned PG17 network-none/tmpfs/read-only/no-log containers, all six per-file transaction restores succeeded. Full synthetic parent/child/history/Auth-row JSON equals baseline; Storage objects0; selected RLS policy and SELECT grant agree. Auth/Storage base schemas are deliberately synthetic and the reviewed fixture ALTER is supplied separately; this is NOT a real Supabase Auth dependency/configuration rehearsal or Production backup. Created2/removed2; independent prefix inventory0. Aggregate local-six-artifact-capture-restore.json and probe six-artifact-capture-restore.mjs remain under the existing restricted ignored root; raw SQL stayed in memory. Initial generator failed because Python was unavailable before any container was started; the Node generator and one actual run then passed. No hosted calls/subagents/cost. Capture returns CAPTURED_NOT_PERSISTED; native input provenance, ACL-protected backup persistence, genuine Auth/Storage diff acquisition/review, complete Production-source rehearsal, full stage receipts/verifiers and no-cost hosted recovery capacity remain mandatory. No R3 authority or GO promotion follows from this probe.
**Native backup capture boundary (2026-09-08):** Parent owns the new backup-capture module/test registered in Task10. Continue the user's explicit no-subagent execution preference. Implement a non-CLI native read-only capture API reusing the existing binding/CA/minimal-env validator and held-snapshot transport. Copy the exact reviewed pinned schema/data filter vectors into source (no caller-supplied argv). Require separately reviewed Auth/Storage SQL and matching digest plus explicit zero Vault/Storage preconditions before any native process. Capture native PG17 roles outside snapshot, then four pg_dump outputs using one held snapshot; hash all four before clean commit by T0+5min. Native output must be complete strict UTF8 with 32MiB/file cap, bounded time/cancellation/kill-observation and stderr/exit failure; partial outputs cannot escape on failure. Return six ordered private SQL artifacts with raw/final hashes and byte counts; roles/schema use accepted transforms, data envelopes preserve native text exactly. Auth SQL is reviewed input, not claimed as acquired by this module. No arbitrary subprocesses, shell, filesystem writes, CLI linked discovery, restore/pause or Gate/stage authority. Subsequent restricted persistence and full local six-file restore are still required. Low-level spawn/fs/time seams support source tests and fixed local Docker adapter integration; synthetic binding never proves hosted TLS. Tests cover order/same-snapshot/hash, pre-spawn rejection, failures/cancellation/limits/cleanup and no success on partial capture.


**Native snapshot transport accepted locally (2026-09-08):** New source transport passes syntax and10 focused lifecycle/context/capture/timer test groups. Parent actual-source integration used the exact transport through the documented fixed local Docker adapter: server idle-in-transaction count1 while held, four same-snapshot dumps/hashes within2516ms after calendar-validation repair, clean commit and idle count0, then exact original2-parent/2-child/1-history restoration despite source updates. The accepted final aggregate is local-held-snapshot-transport-probe-v2.json (the earlier 2837ms aggregate is retained separately) under the restricted ignored root; raw SQL/snapshot remain memory-only. Created2/removed2 and independent remaining0. Adapter binding/CA are synthetic, so this proves native session behavior locally, not hosted TLS/binding or production backup. Parent inspected the real module; no CLI entry point, dump orchestration, stage-authority assertion or GO promotion was added. Production executor still needs artifact/receipt handling, all6 restore stages, recovery/rollback and full R3/R5 evidence.

**Actual snapshot-transport integration probe:** Adapt the existing ignored held-snapshot probe into a distinct ignored held-snapshot-transport-probe.mjs. Replace its handwritten exporter lifecycle with the new source transport; retain fresh pinned two-container isolation, four snapshot dumps, concurrent fixture mutation, exact baseline data/history restore and cleanup. The only low-level adapter maps fixed psql version/session calls to docker exec in the newly owned source container and appends fixed local postgres connection arguments. Synthetic binding/CA inputs are supplied only to exercise the normal validation path without a hosted connection; this cannot prove hosted TLS or production binding. Keep the real transport SQL, UTF8 decoder, timer/close logic and real native child streams unchanged. Observe the held transaction in local pg_stat_activity before dumping, use the source transport commit result, and verify no idle transaction remains after close. Save a separate aggregate artifact; preserve previous probe/results. No repetition of unaffected roles/schema suites or hosted operations.

**Native held-snapshot transport boundary:** Parent owns new scripts/lib/comment-translator-paid-core-v1-gate1-backup-snapshot.mjs and scripts/comment-translator-paid-core-v1-gate1-backup-snapshot.test.mjs, already registered in Task10 syntax/focused/final-path lists. A Node module is required to reuse existing target binding, CA and minimal-child-environment validation without duplicating it in the eventual PowerShell executor. Export createBackupSnapshotTransport with bounded low-level native dependency seams for tests; open accepts explicit target, raw binding JSON, independently supplied expected binding digest, environment and optional AbortSignal. Invalid/mismatched binding, CA, environment, cancellation or non17 native client rejects before opening a session. Launch only fixed psql arguments and fixed RR/RO export SQL; privately return serverT0/snapshot and a session with commit, abort, assertActive and closed-result promise. Decode native stdout strictly as UTF8, cap metadata at64KiB, reject any stderr/extra or malformed/duplicate-key JSON, exact-key mismatch, non17 server, non-RR/RO, invalid snapshot and expired/future T0. Startup10s, lifetime no later than serverT0+5minutes (plus server idle transaction timeout as a fallback), graceful close10s and kill-observation2s are bounded. Unexpected exit/error/abort/deadline kills the process and resolves sanitized failure; cleanup is confirmed only by the native close event, otherwise explicitly unconfirmed. No arbitrary SQL, restore/dump execution, DDL/pause, CLI entry point or default invocation. Session closure is not backup/stage completion; the future executor must still prove all4 dumps/hashes before commit. Test actual transport with controlled child streams/timers for lifecycle, context rejection with zero spawn, stream corruption, premature exit, cancellation and deadlines; do not execute hosted connections in this slice.

**Actual held snapshot/data roundtrip accepted (2026-09-08):** All four native command arrays now include one role=postgres/quote-all-identifiers/no-password flag; actual/public-array and24 missing/duplicate cases pass, with the full backup contract and operator contract PASS. The initial local protocol attempt reached restored-data comparison but failed; both owned containers were removed. A separate no-DB pipe probe reproduced Shift_JIS input/output defaults corrupting Unicode. Explicit strict UTF8 on Console.InputEncoding, Console.OutputEncoding and OutputEncoding before any read/write restores exact pipe identity. Every eventual native executor crossing PowerShell pipes must enforce this, not inherit console encoding. The changed local probe then passed with two fresh isolated PG17 containers: real RR/RO exporter remained open across all four same-snapshot dumps and hashes (server T0 elapsed3049ms), then closed cleanly. Concurrent synthetic source rows changed parents/children/history from2/2/1 to3/3/2, while restored target matched the entire original2/2/1 JSON including Unicode/multiline/COPY-like values and history arrays. Both containers removed; independent prefix count0. Failed aggregate local-held-snapshot-probe.json is retained; accepted aggregate local-held-snapshot-probe-v2.json and local-only probe source held-snapshot-probe.mjs remain in the restricted ignored evidence root. Initial inline syntax failure occurred before any process/DB creation; no output artifact was written by that parse failure. Raw dumps/snapshot stayed in memory. This proves the limited four-artifact protocol locally, not full six-file recovery, production executor, watchdog, native stage authority, recovery capacity or Gate1 GO.

**Bounded actual held-snapshot/data probe:** Use the same pinned local two-container isolation/ownership/cleanup contract as the accepted roles/schema probes, with fresh gate1-snapshot names. Fixed synthetic source contains parent/child FK tables, Unicode/multiline/COPY-like text and supabase_migrations.schema_migrations rows. Hold a real psql REPEATABLE READ READ ONLY exporter open; privately capture server T0 and snapshot, mutate synthetic source via a separate connection after export, then complete four native pg_dump schema/data/history-schema/history-data processes with the same snapshot and corrected producer flags. Keep exporter open until all four successful complete captures and hashes, within5minutes, then close cleanly. Restore schema through the actual schema helper and data/history data through the actual AST-extracted Add-DataRestoreEnvelope (no script entry point); history schema stays native. Compare restored rows/history with the pre-mutation baseline and prove source changed. This limited four-artifact protocol probe omits roles/Auth-Storage and cannot establish the full six-file recovery protocol or a production-ready executor. No hosted connections/credentials/DDL, watchdog activation, recovered-source pause claim or GO promotion. Raw SQL/snapshot stay in memory; aggregate ignored result only; cleanup finally and independent absence checks required.

**Snapshot command compatibility correction:** Before the held-snapshot probe, align all four internal and sanitized native pg_dump arrays with the tested native producer: require exactly one --role=postgres, --quote-all-identifiers and --no-password on schema/data/historySchema/historyData. The schema transform relies on quoted identifiers and the process must never prompt. Preserve snapshot, file destinations, separate reviewed filters and COPY defaults. Assert-CommandPlanShape rejects missing/duplicate required flags; actual-array and public-plan regressions must observe all four. Parent owns only the existing backup preflight/contract plus plan/task updates. This fixes plan compatibility, not actual executor or backup success.

**Schema source and actual local roundtrip accepted (2026-09-08):** Parent implemented transformRestoreSchema in the existing module; syntax and16 focused test groups PASS, including opaque function-body preservation, multiline exclusions, extension suffix changes, exact directive preservation and object-schema ACL selection. Mixed top-level ACL target lists reject rather than discard unrelated targets. Actual native schema-only dump in the bounded source container produced28 statements,1 omission and7 rewrites. Restore into the second new isolated PG17 target succeeded; source/target match4 relation kind/owner/ACL rows,2 pg_get_functiondef values and1 pg_get_triggerdef value. This selected comparison is not every schema catalog field or data/history restoration. Dump SHA2569e30788102ca295ce0effcbca1e3411b92671273090208c2849435ec23e49ac9; transformed SHA256638ff63fd798e374804c173caa3d2594a64f28ce8b9ad65e47651ef3c8a9be26. Created2/removed2/remaining0 with independent prefix postcheck0; aggregate result .tmp/gate1-evidence-20260908/local-schema-transform-probe.json, raw SQL memory-only. No hosted calls, dependency changes or subagents. Remaining native roles forms, held-snapshot executor, full schema/data/history restore, producer authority and recovery capacity remain mandatory; Gate1 NO-GO.

**Bounded actual schema probe:** Reuse the roles probe's pinned cached image and two newly owned network-none/tmpfs/read-only/no-logs container limits, readiness, major17 checks and owned-only finally cleanup. In the first create only a fixed synthetic gate1_probe schema with sequence/table/view, SQL and trigger functions, table trigger and table grant. Native pg_dump schema-only/quote-all-identifiers restricted to that schema remains in memory; transform and single-transaction restore into the second fresh container. Compare ordered native relation kinds/owners, pg_get_functiondef, trigger definitions and table ACLs. No hosted calls, data dump, exported-snapshot claim or Production rehearsal. Save only aggregate local results under the existing restricted ignored evidence root. This is source-compatible verification of the new schema helper, not full backup or R5 acceptance.

**Schema transformation boundary:** Parent adds pure transformRestoreSchema to the existing restore-sql module and focused tests only. Use parsed complete top-level statements, preserving all untouched spans, function bodies, strings, comments and matched PG17 directives. Implement pinned recipe CREATE IF NOT EXISTS (schema/table/sequence), CREATE OR REPLACE (view/function/trigger), reserved publication exclusions, event-trigger exclusion (including attached COMMENT), FDW owner and postgres grant-option exclusions, supabase_admin default-privilege exclusion, internal-schema ACL exclusion, pg_tle/pgsodium/pgmq extension suffix removal, extension-comment/cron-policy/cron-table/transaction_timeout exclusions. Bind the internal-schema set to the already reviewed schema recipe. Do not run sed or rewrite nested tokens. Recognized remaining dump statement families may pass unchanged; this transform does not authorize their SQL or replace native provenance/catalog checks. Unknown statement families or unsupported ACL shapes must stop. Return SQL plus aggregate edit counts; no processes/connections/files accessed by the helper. Schema restore compatibility still requires actual isolated verification after focused tests.

**Actual local roles roundtrip accepted (2026-09-08):** The bounded two-container probe completed successfully using cached image sha256:67f41722b7a8cbdb868a44a4995c846eddfdc2973bccb291ce937dce88ad5675; both servers independently report major17. Native pg_dumpall produced11 SQL statements; transform omitted2 reserved-role statements and removed4 exact attribute tokens. Restore under single-transaction/ON_ERROR_STOP succeeded in the second new isolated target. Two synthetic roles match source for name/inherit/create-role/create-db/login/connection-limit/config, and one membership matches with inherit/set true. This comparison does not claim every role catalog field or a complete Production restore. Native dump SHA256299c91dcac941010f047ae735dc70ab8a6ec618e56221c070b99eaa0f5a0ac76; transformed SHA256f58f853307f0a2477b664786389bc97c203fd978748d85627b91bec8b8a797be. Both owned containers removed; independent prefix postcheck0. Aggregate result saved only at .tmp/gate1-evidence-20260908/local-roles-transform-probe.json; raw dump stayed in memory. No hosted connections. This establishes the supported native roles roundtrip only; unsupported forms, full schema/data/history restore, trusted stage provenance, recovery capacity and R5/GO remain outstanding.

**Bounded actual local roles probe:** Parent may use exactly two newly owned, uniquely named containers from the already cached PostgreSQL17 image, pinned by local image ID. Both require network none, no published ports, read-only root, tmpfs-only data/runtime/tmp, disabled logs and bounded memory/PIDs/time/capture. Create only fixed synthetic roles/settings/membership in the first; native pg_dumpall roles-only/no-role-passwords/no-comments/quote-all-identifiers output stays in memory. Apply the pure roles transform and restore into the second with psql no-psqlrc/no-password/ON_ERROR_STOP/single-transaction; independently compare selected synthetic role attributes, setting and membership. Verify server major17 on both and actual container ownership before cleanup. Remove only these owned containers in finally and independently confirm absence. Failures stop the probe; do not reuse/reset targets. Save aggregate local evidence only in the existing restricted ignored evidence root. No hosted connection, credentials, recovery-capacity claim, migration/backup rehearsal or GO promotion. A native format incompatibility returns to the existing parser/roles transform boundary for an in-scope repair.

**2026-09-08 roles transformation source:** Existing restore-sql module now exports pure transformRestoreRoles returning SQL and aggregate counts. Recognize only fixed UTF8 PG17 preamble, RESET ALL, quoted CREATE ROLE, exact native seven-group ALTER ROLE attributes with optional limit/validity, literal role settings and membership grants. Omit exact CLI-reserved roles for CREATE/attribute ALTER and reserved membership recipients; allow only CLI-reviewed reserved setting names. Remove NOSUPERUSER/NOREPLICATION tokens from retained attribute statements without touching identifiers/literals, preserve grant options/grantor and PG17 restriction directives, append RESET ALL. Unknown forms including parameter ACLs/security labels stop before output; they require separately settled handling, not silent omission. This is a pure recipe transformation, not role-change authorization or executable provenance. PostgreSQL REL_17_11 pg_dumpall.c lines549-555 proves client_encoding appears before standard_conforming_strings; permit an ordinary literal in unknown mode only if it contains no backslash, for which both modes have identical boundaries. Exact source: https://raw.githubusercontent.com/postgres/postgres/REL_17_11/src/bin/pg_dump/pg_dumpall.c . Parent syntax check and all11 lexical/roles test groups PASS; no database or actual dump/restore ran. Full schema transformation, native acquisition/restore compatibility and all R5 evidence remain incomplete.

**Parser implementation boundary:** Parent owns new `scripts/lib/comment-translator-paid-core-v1-gate1-restore-sql.mjs` and `scripts/comment-translator-paid-core-v1-gate1-restore-sql.test.mjs`, with no subagents. Export a pure bounded `parseRestoreSql(source)` returning exact UTF-16 spans and lexical tokens for complete SQL statements, trivia and matched PG17 restriction directives. Preserve the full input by concatenating spans. Reject NUL, malformed Unicode, oversized input, unclosed lexical states, unsupported psql directives and COPY. Require an explicit standard_conforming_strings setting before backslash-bearing ordinary string literals; track on/off changes and reject unsupported resets/assignments. This API establishes lexical boundaries only, not SQL authorization, native provenance, transformation eligibility or restore safety. No I/O, process execution, dependency or existing parser change. Parent checks syntax and the new focused tests; full backup suites are unaffected until integration.

**2026-09-08 restore parser reuse decision:** An actual local synthetic probe establishes that catalog maskSql is unsuitable as the restore parser: unterminated single quote, dollar quote and block comment all return without rejection; a supplementary Unicode character before a dollar body exposes its inner CREATE token and changes UTF-16 length. It iterates code points while indexing the source in UTF-16 offsets. This is evidence against reuse, not a hosted catalog mismatch; leave the catalog helper unchanged in R5. The schema/roles transformer must scan exact source spans with consistent UTF-16 indexing, reject every unterminated lexical state, distinguish ordinary strings from E escapes, account for standard_conforming_strings, and split only at top-level semicolons. Preserve unchanged spans exactly. Validate matching PG17 restrict/unrestrict directives separately and reject other psql commands; reject COPY FROM STDIN in this schema/roles path. Apply reviewed transformations only to complete top-level statements, never line substitutions through function bodies or multiline role setting values. Test Unicode before quotes, embedded CREATE/GRANT tokens, doubled quotes, E escapes, nested comments, multiline statements and all unterminated states. Reserved-role/schema policies, native producer binding, bounded file handling, executor and actual restore remain mandatory and unimplemented. No external operation or GO evidence is established.

**2026-09-08 lossless data restore envelope:** Add a pure Add-DataRestoreEnvelope helper to the existing backup script. Reject empty/whitespace/NUL text; prepend the fixed SET session_replication_role = replica and append RESET ALL while preserving the entire original dump string, including COPY rows, Unicode, CRLF and PG17 restrict/unrestrict commands. Native PG17 clients are already required, so do not copy the CLI sed substitutions through possible data rows. This helper is not a SQL safety validator and does not prove trusted producer, restore privileges, snapshot identity or executable eligibility. No files/processes/connections are accessed by the helper; actual producer binding and streaming/size handling remain the executor responsibility. Cover positive exact-string preservation and invalid input with actual extracted PowerShell functions; no restored data or DB execution in this slice.

**2026-09-08 separate native schema/data filter inputs:** Existing reviewedSupabaseFilterArgs/Evidence are explicitly schema-only. Require independent reviewedSupabaseDataFilterArgs/Evidence for data, with the same exact reviewed flags equality, pinned CLI version and evidence validation; no fallback from schema input or implicit default. Resolve each separately and pass mandatory ReviewedDataFilterArguments through the real planner and sanitized projection. Native filter allowlist is --schema/--exclude-schema/--exclude-table; remove CLI-only --exclude. Keep shell-syntax rejection and represent schema alternatives using repeated native arguments rather than shell pipelines. History dump filters remain fixed and snapshot-bound. These inputs are reviewed plans, not proof of independent authority or correct complete exclusions; the actual recipe and transforms still require verification. Scope is the existing backup preflight/contract, with missing-data-input rejection and distinct actual/public-array regressions. No operational execution or subagents.

**2026-09-08 per-file restore transaction correction:** Every one of the six existing plain-SQL restore command arrays must include --single-transaction with --no-psqlrc and ON_ERROR_STOP=1. Apply this to actual internal arrays, sanitized arrays and their strict order validator. This is one transaction per file, not one transaction for the entire restore sequence; preserve roles/schema/reviewed Auth-Storage/data/history-schema/history-data order. Nontransactional statements and unreviewed transformations must still be rejected before actual restore. Regression checks both the actual six arrays and public plan; source-only acceptance never establishes restore success. No database execution is included in this correction.

**2026-09-08 native roles command correction:** In the existing command planner, roles means native pg_dumpall arguments: --roles-only, fixed postgres execution role, quote-all-identifiers, no-role-passwords, no-comments, no-password and the restricted roles.sql destination. Apply the same exact recipe to internal and sanitized arrays and validators; never include --snapshot or a CLI --linked flag in this native command. This matches the pinned CLI roles recipe while preventing password hash capture and prompts. Reserved-role transformations remain a separately required pre-restore step; command-plan validity never proves that transformation, execution, restore eligibility or GO. Scope remains the existing backup preflight/contract; no external operations.

**2026-09-08 roles-dump client readiness correction:** The required roles artifact uses native pg_dumpall, so ToolProbe must require that application at PostgreSQL major17 alongside psql/pg_dump/pg_restore. Missing or wrong-major pg_dumpall blocks aggregate tool readiness even when every other tool passes. Add only this required status and regression coverage to the existing backup preflight/contract; preserve dry-run defaults, approval states, no overrides, no operational subprocesses and all recovery conditions. Parent implements directly under the user instruction forbidding subagents.

**Authority:** active readiness document sections `実行順序と共通preflight` and `Backup / restore / watchdog`; existing preflight/backup contracts remain strict.

- [ ] Provision task-process inputs securely: exact target binding JSON/hash, Direct DB connection and pinned CA. Current process check on2026-09-07 found0/8 required binding/connection/TLS context fields, with neither password nor password-file input present; no external call was made. Never paste values into chat or place secrets in argv; verify-full/PG17 and target identity are mandatory.
- [ ] Prove exact no-cost recovery capacity and region/extensions/Auth/cutover/pause readiness. Do not pause a healthy production project to free a slot or buy a project/PITR/IPv4/add-on. If the free recovery condition cannot be met, report the concrete unmet condition and retain NO-GO; local restore success cannot replace hosted recovery capacity.
- [ ] After Preview passes, perform rehearsal backup and restore into a new isolated local target; prove history, counts, Auth/FK and migration rehearsal. Then obtain final same-snapshot artifacts/checksums and demonstrate watchdog readiness before arming Production DDL. Follow the exact restore order and5/10/20-minute limits in the authority, without inventing weaker substitutes.

### R6 — Execute the approved hosted sequence and audit full GO

**2026-09-09 outer native-process observation (local implementation):** New scripts/comment-translator-paid-core-v1-gate1-backup-acquire.mjs is an explicit --capture entry with strict UTF8/JSON stdin (1MiB/10seconds) and sanitized output only. New scripts/lib/comment-translator-paid-core-v1-gate1-backup-process.mjs launches that exact entry using process.execPath, fixed argv, minimal OS environment and private stdin; it does not accept caller command/argv or inherit NODE_OPTIONS/PG secrets through the child environment. It observes actual close status/signal, complete stdout/stderr byte counts and stdout SHA256. Child stdout is limited to64KiB; invalid JSON/UTF8, stderr, nonzero exit, cancellation, wall-clock reversal or monotonic timeout cannot return success. A300second child deadline uses one bounded Windows taskkill of its owned process tree, with2second close grace; an exited PID is never killed, and missing close remains cleanup-unconfirmed.

The outer orchestrator independently re-reads the source-bound acquisition record and six persisted artifacts, validates native process facts/source/run/target/dump ordering/snapshot-vector evidence/timestamps, and checks the immutable producer bytes again. A third distinct, non-nested, precreated protected directory holds only backup-process.json, exclusively created, flushed and hash-read back within T0+5minutes. The fixed acquisition producer list now has10 files (the prior8 plus this process module and child entry); historical8-file records are not silently upgraded. Input snapshotting prevents asynchronous caller redirection. The record is backup-native-process-observation and returns BACKUP_NATIVE_PROCESS_OBSERVATION_PERSISTED with stageAuthority=false/Gate1 NO-GO. It is not yet the exact R3 nine-stage receipt or stage observation: source-state captured under the same snapshot, complete rehearsal/state readback and the accepted stage adapter remain required. No live capture, source publication, arm, restore or hosted mutation was performed.

Local verification includes an actual harmless Node child/output capture, actual owned harmless Windows child-tree timeout/close, and the real entry's empty-input rejection before DB access, in addition to synthetic failure/identity/storage tests. These local process results do not prove a Production backup or hosted process lifetime. Final91 tests PASS, including the harmless native process probes and watchdog26; six changed-file syntax checks, operator and git diff --check PASS. Native source verification rejects the unpublished10-file set before acquisition; ESLint remains SETUP_BLOCKED without node_modules. Final logs and output SHA256 are in ignored backup-process-final-tests.txt / backup-process-final-acceptance.json; final source hashes and native guard are in backup-process-final-source.json. Next local boundary is same-snapshot source-state observation and the strict stage adapter; do not fabricate missing historical native receipts or promote the existing record to full authority.


**2026-09-09 restricted persistence/source-run join (local implementation):** The native-only captureAndPersistBackup API in scripts/lib/comment-translator-paid-core-v1-gate1-backup-acquisition.mjs pins eight fixed producer files to the requested immutable commit and their process-load bytes before acquisition, then checks them again after capture. It generates a fresh random run ID and snapshots caller inputs so asynchronous caller mutation cannot redirect the source, target binding or destinations. Both distinct, non-nested destination directories must already be empty, outside the repository and protected by the existing native ACL policy before capture. The capture factory now accepts a trusted persistence callback: all six transformed artifacts and the existing manifest are exclusively persisted, flushed and independently read/hash-checked while the exporter remains held. Only then may it commit. Persistence failure, hash mismatch, expired snapshot, invalid clock or uncertain native cleanup cannot return a successful capture. The original no-persistence API remains CAPTURED_NOT_PERSISTED; the held-persistence path returns CAPTURED_PERSISTED_NOT_AUTHORITY.

After native exporter close and a source recheck, a sanitized backup-acquisition.json record in the separate protected receipt directory binds source commit/eight file hashes/run ID/target digest/native T0/snapshot digest/five dump observations/verified manifest/six file hashes. Exclusive creation, fsync and pinned readback apply to this separate record too; no existing backup or record is overwritten, and partial artifacts are retained on failure. Both record creation and verification must remain within T0+5minutes. Returned status is SOURCE_BOUND_BACKUP_OBSERVATION_PERSISTED with stageAuthority=false and Gate1 NO-GO; acquisitionAuthority remains UNESTABLISHED. This record is not yet the outer native-process R3 receipt or a directly accepted nine-stage observation. No actual hosted capture, local restore, watchdog arm, executor success, commit/push or subagent ran. A deadline regression exposed that a snapshot assertion retained its pre-close cleanup uncertainty even after the exporter closed. The final exporter close now resolves only snapshot-origin uncertainty; an unconfirmed dump-process close remains unconfirmed. Current changed producer bytes are unpublished: the actual native entry was verified to stop at the Git source guard before destination access or a DB process. Next is the outer bounded native-process receipt/stage adapter and source publication acceptance, followed by separately authorized fresh rehearsal capture/restore. Final local acceptance: related55 tests PASS after cleanup repair, six syntax PASS, native unpublished-source guard PASS. Watchdog26 passed in the same-cycle combined run; its dependencies did not change in the capture-only repair. The initial combined run and its single deadline/cleanup failure are retained separately. ESLint remains SETUP_BLOCKED without node_modules. Final source/output hashes are retained in ignored acquisition-join-final-acceptance.json. Original lost receipts remain ORIGINAL_MISSING.


**2026-09-09 native capture observation join (local source slice):** The existing capture module now returns its own processObservations: exact native T0, verified target-binding digest, hashed snapshot identity, start/completion observations for all five dump processes (roles has no snapshot), successful close/capture/stderr/client-major facts, raw dump hashes and exporter-close observation time. No caller-supplied receipt is used to populate these fields. Invalid/nonfinite/reversed observation clocks reject, including a failure at a dump close while the exporter is held; cleanup remains required. Existing sensitive artifacts stay on the private return path and status stays CAPTURED_NOT_PERSISTED. This does not create persisted checksums/manifest authority, immutable producer/run binding, a watchdog arm receipt or an executor/readback receipt. Original missing acquisition receipts remain ORIGINAL_MISSING. The same native source must be published/accepted before final operational authority can rely on this changed producer. Next local join is restricted persistence plus the outer source/run receipt, before any fresh hosted rehearsal capture; actual backup/restore retains its action-specific authorization. Primary implementation only; no hosted calls, dependency changes or subagents. Verification result follows in task.md.

**2026-09-09 Auth comparison and ten-minute Cron observation:** Production's protected18-field Auth export matches the fresh authenticated Dashboard UI and its observed GET config response: zero changed fields, Email only/24 disabled/custom0/third-party0, exact Site URL and four redirect values. The password-length input rendered empty; inspection of the actual settings response confirmed PASSWORD_MIN_LENGTH=8, rather than interpreting blank as0. Fresh selected settings and UI observations are protected locally; no session tokens, credentials or complete unfiltered Auth response were exported. `auth-settings-comparison.json` SHA256 `20f968fae134e96236d74e6dc045e10aa17a14940678462cd831ad5432845894` was independently re-compared to the original18 fields. This revalidates the saved recovery configuration subset, not every possible Auth setting.

Native Direct verify-full/RR/RO observations at approximately 2026-09-08T16:46:48Z and16:57:13Z cover624578ms Production/624603ms Preview. Production remains0 jobs/0 runs. Preview retains the same single inactive job, cadence */5 * * * *, postgres user and the exact command `select private.ct_paid_invoke_maintenance_from_vault();`; run count27, maximum run ID and all selected job metadata are unchanged. Both baseline-relative run deltas are0. `cron-baseline.json`, `cron-final.json` and `cron-ten-minute-acceptance.json` preserve target-binding digests and output hashes. These are two endpoint observations plus retained execution logs, not continuous active-state surveillance, and they do not satisfy the future Production Cron configuration stage.

**Cloudflare fallback read-only check completed after user login (2026-09-09).** Authenticated Dashboard settings show no Cron triggers for either v-streamer-tools or v-streamer-tools-preview. Preview scheduler authority is supabase-cron; Production has no scheduler-authority name in the displayed settings. This establishes absent Cloudflare Cron triggers at observation time, not continuous surveillance or other invocation-path absence. Current 100% version displays: Production f45136b2; Preview 9e1a72be. Preview annotation git=306f5e5599abe0e2f440468b4c4343134f449308/tree=93dfdb7745ee5cc5a5330c7328766c923961facc is metadata only; ARTIFACT_IDENTITY=UNKNOWN remains. Sanitized private cloudflare-fallback-observation.json SHA256 7d595c196e0496766ffc77b8d1013cd8f4323909b458bc69efac2f8e03bd18c7. Prior AUTH_REQUIRED status is superseded. No settings change, DB mutation, backup, restore, deployment or activation. Gate1 NO-GO remains; native producer/backup/executor evidence binding and remaining Gate1 stages are separate.
**2026-09-09 Direct prerequisites recovered (steps1–2 authorized and completed):** Historical references led to existing target-specific connection/CA/credential files under the user's KEYS directory and the separately installed PostgreSQL17.11 client. Fresh authenticated project discovery independently matched each selected host/ref, region, status and major version. Strict field parsing, valid CA checks, existing binding/context validators and process-only child environment preserved verify-full/GSS-disabled/read-only options. One bounded native query per target passed TLS1.3, PG17, REPEATABLE READ/READ ONLY, postgres user/database and expected history counts22/56. No secret values were logged or copied into receipt/argv; no installation or persistent environment change. Private file references and binding digests are saved separately in the restricted evidence directory.

`direct-connection-verification.json` SHA256 `4ceac69321d19ae23e09f5fe533bde6b0c517e47dc12c85685931385e63fec8c` records both actual observations at 2026-09-08T16:17:32Z (2026-09-09 JST). This closes Direct connection readiness, not the full native source/capture/executor/nine-stage authority. The first helper invocation had a local import-path error before any connection; correcting the helper path preceded the two successful native queries.

**Original backup bytes survive:** `D:/Gate1Backups/production-portable-rehearsal-20260908-122048-83eb0180` contains the original six-file set. Existing native inspectBackupArtifacts returned PERSISTED_BYTES_VERIFIED with exactly6 artifacts and the independently retained manifest hash `288801029ad10e855bfa5edacaf49032902920e74642aae7b3693e18485cac1a`. No backup bytes were changed. This re-establishes original persisted-byte/ACL identity, not the lost producer receipt or a new restore run. Preserve this backup for approved rehearsal; never treat it as fresh final T0.

`D:/Gate1Backups/auth-settings-20260908-104426/auth-configuration.json` also survives: valid JSON object,706 bytes, current SHA256 `0291ae52f0feb4ecf1592f7431fb73bb69438278e00721fa492acf16f950df6b`, ACL has no allow rules outside current user/SYSTEM/Administrators. Historical hash comparison and equality to current hosted Auth settings remain unverified. Do not classify these external originals as missing merely because the old worktree evidence directory was lost. Recovered backup/auth inspections are stored as new observations under the existing restricted evidence directory; old missing receipts remain ORIGINAL_MISSING.

Next read-only evidence gaps are current Auth configuration comparison/export, fallback observation and ten-minute same-job run delta. Backup recapture and isolated restore/replay still need their concrete scope and separate authorization. No DB mutation, backup execution, restore, pause, deploy, activation or subagent in this step. Gate1 NO-GO.
**2026-09-09 fresh connector read-only baseline (authorized):** Both existing target projects are ACTIVE_HEALTHY in Tokyo on PostgreSQL17. The existing fixed POSTAPPLY_CATALOG_SQL ran with REPEATABLE READ/READ ONLY and bounded statement/lock timeouts, then ROLLBACK. Saved complete catalog/history JSON was independently parsed after persistence. Full version/name comparison with final56 has no unknown/duplicate history: Production22/pending34 exactly matches the saved pending34 identities; Preview56/pending0. Preview canonical33 tables/81 functions/1443 dependency rows. This observes current definitions but does not re-establish full equality to the lost independent expected catalog.

Operational read-only observations: Production Vault0, Cron jobs0/active0/runs0, Storage objects0, Auth77/Storage68 managed histories. Preview exact two reserved Vault rows/distinct names/encrypted nonnull, one inactive five-minute postgres Cron job, active0, runs27, Storage objects0, Auth77/Storage65 histories. A run total is a single observation, not a ten-minute run delta; the Preview Storage count is a new observation, not proof of compatibility with the Production restore baseline. No decrypted secret or application row payload was fetched.

Advisor baseline counts (findings, not category counts): Production security WARN3 and performance24 (INFO14/WARN10); Preview security35 (INFO33/WARN2) and performance55 (INFO45/WARN10). No prior raw baseline remains for a delta claim, and no new remediation or approval is inferred. Production security categories link to official remediation: [anon security-definer execution](https://supabase.com/docs/guides/database/database-linter?lint=0028_anon_security_definer_function_executable), [authenticated security-definer execution](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable), [leaked-password protection](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection).

Raw observation files remain in `.tmp/gate1-evidence-20260909/readonly-observations` with inheritance disabled and access limited to current user/SYSTEM/Administrators; original file bytes and hashes are listed in read-only-acceptance.json. Collector and inventory hashes, source commit, observation timestamps and transport classification are retained. These are connector observations, not native Direct verify-full evidence: GATE1 binding/digest, PG connection/CA/credential inputs and psql are unavailable in the current execution process. Auth configuration export, Cloudflare fallback, ten-minute same-job run delta, actual backup and any restore remain unexecuted. No DB mutation, hosted apply, dependency install or subagent. Continue only the already approved read-only scope; backup/restore retains separate approval. Gate1 NO-GO.
Acceptance SHA256: a7d3ecf3a57de72256ca3d6bb47bbb9ab143602128137aadac1c503c680075a4.


**2026-09-09 approved reacquisition policy:** Preserve Git implementation, procedures and historical result reports. Missing originals under the lost old evidence directory are classified as **原本欠落 / ORIGINAL_MISSING**; a reported hash or PASS cannot recreate their bytes. New tests carry actual new timestamps and hashes. This supersedes the earlier requirement to recover originals before continuing local work. Historical execution reports remain historical claims, not currently reinspectable raw evidence.

| Evidence | Current disposition | Minimum next acquisition and authorization |
| --- | --- | --- |
| Old phase5 acceptance/TAP, phase3/4 observations and bindings under the lost directory | 原本欠落; Git reports retained | Fresh source-only local tests authorized; retain new logs and source/output hashes separately |
| Six-file Production backup and protected Auth export outside the lost directory | Original location/bytes unverified; do not assume deletion solely from worktree loss | Identify approved protected storage and verify existing bytes/ACL first if available; otherwise request a fresh read-only capture scope |
| Production and Preview history/catalog/security observations | Historical report retained; missing raw observations cannot support a fresh stage | Request exact bound read-only collection for both existing projects: complete history/pending, canonical catalog/security, required aggregate data, Vault/Cron metadata and relevant Auth configuration; no mutation/provider calls |
| Rehearsal backup/restore/replay | Prior acceptance report retained; current source/target equality cannot be rechecked without artifacts | After fresh protected six-file backup and Auth configuration capture are approved and verified, request one fresh isolated local PG17 restore/34-migration replay with the existing R5 preparation and complete comparisons; no hosted restore |
| Final T0/Production apply/Vault/inactive Cron/GO | Future evidence, never replace with reconstructed history | Separate fresh final-backup/armed-execution and subsequent action-specific approvals; gate remains NO-GO |

Fresh local run: `.tmp/gate1-evidence-20260909/local-reacquisition.json` records actual UTC start/end, Node version, source commit, source SHA256/bytes, exact commands and output SHA256/bytes. Watchdog26 and binding/backup/restore-SQL/stage58 tests PASS; four watchdog syntax checks PASS, source drift0. These are synthetic/local-process tests, not DB observation or a restore rehearsal. Initial operator check failed because the local documentation edit removed a required fixed status marker; the marker was restored without changing the validator, and a separate fresh operator run passed (remote-calls0/mutations0). ESLint is SETUP_BLOCKED because node_modules is absent; no dependency installation is authorized by this record. This bounded refresh does not claim the entire historical regression inventory was rerun. New run UTC 2026-09-08T15:17:18.469Z to 2026-09-08T15:18:34.822Z, Node v22.22.2, source 31fd1693e7b6bde961af4458adb90cc6b2b8e95f. Watchdog output SHA256 c26cb6d19fd869bfff48af6f97d4167a70b3b2096a34d55bc979835ee11d63b5; related58-test output SHA256 53d0ffcba438f57e34b4de80febdf08f58ad79d6c5d92fde550f008df11b3d1b. Hashes retained here identify the new outputs; they do not replace the output originals.

**2026-09-09 current local audit:** PR #818 source publication/Preview integration was verified before workspace loss (exact source/merge/tree in task.md). Older UNPUBLISHED and missing-supervisor descriptions are historical. After recovery the same HEAD and existing branch are restored, but the referenced restricted phase5 local acceptance file is absent. Recover original private evidence before claiming hash continuity; do not recreate historical receipts or rerun hosted operations to fill this gap.

The next native join is at the existing runner boundary: `backup` inspects six persisted artifacts but receives identity/T0/completion fields from the caller; `success` forwards a caller receipt to the supervisor. Neither alone proves held-snapshot provenance or actual executor/readback execution.

| Join | Required evidence | Current limit |
| --- | --- | --- |
| Source → producer | Immutable commit and seven exact producer blobs | Publication is not trusted nine-stage authority |
| Producer → backup | Bound native connection, original T0/held snapshot, four dump lifetimes/exits, six-file manifest and persisted inspection, same source/commit/run | Caller identity plus bytes does not prove acquisition provenance |
| Backup → arm | Same source/run/T0/manifest, five-minute completion and durable arm ACK | Actual monitor host/lifetime remains unestablished |
| Executor → success | Actual pending-set execution and decisive full readback bound to source/run/arm, completion and receipt before ten minutes | Caller success alone is not execution evidence |
| Incident → recovery | One pause attempt, matching INACTIVE + exact DB TCP refusal + REST/Auth503 before twenty minutes | Hosted oracle support and emergency authority remain UNKNOWN |

Bind references for monitor host/lifetime, deployed app identity, recovery credential consumers, restrictions covering every writer, and incident/cutover authority. Record origin, target/run where applicable, observation time and verifier without secret values. Reuse the existing ten-entry recovery checklist and same-Free-organization/Tokyo incident-pause-then-provision policy. No third active project or healthy-source pause test is required. Fresh final backup/armed Production apply and subsequent Vault/Cron remain prerequisite- and approval-gated. Primary agent continues directly without subagents.


**2026-09-08 post-merge prerequisite audit:** PR #817 is merged at `5a110ae110fe24689132440dc470b1105b08aa8e`; its complete tree equals source `ae544a55443ed90daf71d8975d2fcb2e45393b57`. Fresh native source collection verifies56 migration blobs against the existing independently pinned corpus. All8 published source files and13 restricted evidence files match the publication binding. Restricted `phase4-postmerge-binding.json` extends that binding without overwriting historical observations; `phase5-preproduction-gap-audit.json` classifies the nine stages. This closes source publication/integration, not the final evidence authority.

**Final local checks:**26 focused tests PASS (including native PostgreSQL microsecond/offset T0 preservation and invalid-date rejection),4 Node syntax checks and4-file ESLint PASS, existing operator contract PASS, root actual diff review complete. The runner uses the same millisecond deadline calculation as the existing snapshot collector without rewriting its native T0 string. Test source and producer hashes are retained in restricted phase5 local acceptance evidence. No positive hosted execution or authority claim.

**2026-09-08 local watchdog result:** Parent directly implemented the four owned source/test files, preserving the no-subagent instruction. Source tests first failed on absent modules; the initial implementation then passed. A focused fault test found that an invalid clock threw before pause; repaired so a bounded pause is still attempted while recovery eligibility remains false. An accelerated100x separate-process timing test exposed that its real10ms margin was smaller than Windows timer scheduling; use20x for the actual process test, leaving production deadlines and its1000ms margin unchanged. The accepted process test blocks the parent31 seconds while the child requests pause before its virtual10-minute deadline; EOF and bad-sequence tests also reach pause. Native source publication, full authority binding, actual paused-hosted oracle behavior and emergency execution remain unverified. The active authority now contains the exact ten-entry recovery/cutover consumer checklist and its outstanding operational inputs.

**2026-09-08 approved watchdog implementation packet (local execution only):** The user approved implementing the previously presented5/10/20-minute watchdog, concrete recovery checklist and local failure verification. Reuse the settled design; no repeat approval, subagent, hosted call, commit or dependency change. Root owns this bounded implementation in the existing2d79 feature worktree. Add `scripts/lib/comment-translator-paid-core-v1-gate1-watchdog.mjs` for an asynchronous monotonic deadline supervisor, `scripts/lib/comment-translator-paid-core-v1-gate1-watchdog-transport.mjs` for fixed native Supabase pause/status and direct/public endpoint probes, `scripts/comment-translator-paid-core-v1-gate1-watchdog-runner.mjs` for a separate-process stdin protocol and sanitized durable receipt, and `scripts/comment-translator-paid-core-v1-gate1-watchdog.test.mjs` for focused controller/transport/process tests. Existing backup/state semantics and migrations stay unchanged. Documentation ownership is this plan, active readiness and task.md.

The supervisor owns arming and the irreversible pause latch. Backup completion must be source/target/run-bound and within5 minutes; no arm acknowledgement before native persisted-backup inspection and durable receipt. A separate process keeps monitoring when the caller's work stalls; lost input or malformed/duplicate/out-of-order protocol after arm triggers failure. Use monotonic elapsed time alongside wall time so clock rollback cannot extend deadlines. Success requires both migration and decisive readback completed after arm and before10 minutes; receipt arrival must also be before the deadline. Pause is attempted once on failure or timeout, with a one-second scheduling margin before10 minutes. Request failure/timeout is retained while independent confirmation continues; never retry a mutation automatically. Every call is bounded and abortable, late results cannot confirm or disarm, and20-minute expiry makes the backup ineligible. Record failure after arm cannot silently stop monitoring or authorize restore.

The native adapter admits only the existing Direct Production binding and a separately supplied exact execution authorization. Pin the Management API origin and paths; never accept arbitrary URLs, redirects, disabled TLS or raw-error output. Pre-arm verification requires exact active project identity and a successful read-only PG17 connection. Confirmation requires a fresh exact project INACTIVE result together with explicit bound database connection refusal and HTTP503 from both bound REST/Auth endpoints; DNS/TLS/timeout/401/403 and pending statuses remain UNKNOWN. This deliberately supported oracle is local-contract-tested; actual hosted behavior and emergency authority must still be verified before Production use. Secrets travel only via private stdin/context, never argv or journal. The runner never applies migrations, provisions, restores, cuts over, unpauses or deletes.

- [x] Write focused failing tests for invalid binding/approval, late backup/unarmed timeout, success, stalled work, immediate failure, pause rejection/timeout, wrong/unknown confirmation, late success, clock rollback, broken input and journal failure.
- [x] Implement supervisor, fixed native transport and bounded separate-process runner; preserve host-independent local tests and keep real transport unexecuted this cycle.
- [x] Run actual local child-process timing/EOF tests and transport contract cases, then inspect the final diff and run focused acceptance once.
- [x] Add concrete recovery/cutover consumer references and report local acceptance separately from hosted readiness and full9-stage GO.

**Immediate next slice: executable recovery readiness, before Production DDL.** The inspected backup preflight exposes Contract, ToolProbe, Plan and State; State calls the pure watchdog evaluator. No native pause/deadline supervisor was found in the Gate1 library or retained phase scripts. State-machine test success does not prove a running supervisor, authenticated exact-source pause request, or deadline-bounded inaccessibility confirmation. Prepare that execution path against the already approved5/10/20-minute protocol and verify it locally before requesting any hosted action. Preserve the pause latch and abort-before-DDL rules; never arm while the supervisor or emergency authority is missing.

The exact operator checklist must bind all ten recovery configuration entries: region, extensions, Auth providers, Auth redirects, endpoint cutover, secret rotation, client reauthentication, Free smoke, writes reopening and exact source pause. Reuse the protected Auth export and established same-Free-organization Tokyo selection. Identify concrete endpoint/credential consumers and verification actions before marking those entries ready; hashes or proposed steps alone are not operational evidence. Capacity remains confirmed-incident-source-pause-then-provision; neither a third active project nor pausing healthy Production is a prerequisite. Actual provisioning/configuration and emergency cutover retain their distinct approvals.

Retain accepted Preview and local rehearsal results without rerunning unchanged suites. Native authority still needs actual producer receipts, exact target bindings, independently trusted policy and a docs-only authority commit. Bind prior observations with their real timestamps/source identities; do not relabel them as newly acquired. Final backup, Production and canonical readback, Vault, inactive Cron and the successful watchdog timeline are future stages, not missing JSON fields to fill manually. Obtain fresh entry evidence immediately before any authorized mutation; an earlier rehearsal snapshot is never the final backup. Production apply and subsequent Vault/Cron actions remain blocked until these prerequisites and their action-specific authorization are established.


- [ ] After source integration, acquire fresh Preview preflight/pending rows; apply only the exact reviewed set and read back final56 plus full canonical/security state. Preserve its existing inactive scheduler and Vault values. Saved pending26 is not current authorization if the fresh full-row list differs.
- [ ] Only after R5 is proven, apply Production's exact pending set under the armed recovery protocol and fresh full history/catalog readback. Saved pending34 is a comparison baseline, not a substitute for fresh evidence. Prove archive3 zero-row tables/3 functions and unchanged source-era6 tables/7 functions, canonical owner/ACL/RLS/raw definitions/dependencies, extension availability and advisor deltas.
- [ ] After schema/security acceptance, atomically create the exact two Vault names without reading decrypted values; configure exactly one5-minute Cron job inactive before commit. Verify10 minutes of same-job baseline-relative run delta0, inactive state and absent/inactive Cloudflare fallback.
- [ ] Assemble actual nine-key evidence and audit every design GO item, including phase-correct rollback. Do not confuse local PASS, merged source, deployed application and hosted DB evidence. Normal deployment, Paid publication, Provider/Checkout activation and Cron activation remain outside Gate1 and unexecuted.
- [ ] Only after all mandatory evidence is current and directly verified, mark the new task's full Gate1 goal complete. Otherwise keep the precise unresolved item; do not change the objective to source-only readiness.

### Minimal next-task intake

Read this Chunk R, `task.md`'s current handoff, local `AGENTS.md`, and the active readiness document first. Read the relevant approved design/old plan section per slice, not the entire old conversation. Set the task goal to full Gate1 GO with zero added cost. Start R1 design equivalence and R2 scope reconciliation, then issue one bounded Luna packet at a time. No code writes by Astra, including review repairs; parent-only documentation edits remain allowed. Keep this exact worktree throughout.

## A3 inventory reconciliation and remaining scope

The approved forward migration `20260904000000_comment_translator_paid_gate1_a3_canonical_convergence` is the sole difference between final55 and final56. Preserve the 55-migration LF replay for pre-forward definition-drift diagnosis; final local convergence and deployment inventory require all 56. Pending26/pending34 are derived from saved history, not fresh remote observations. Compare complete sorted version/name arrays, not counts alone.

This checkpoint does not satisfy final all-task acceptance. The final allowlist below includes both the approved A3 harness paths and later operator deliverables; missing future files must remain unfinished rather than be removed from that list. Operator/preflight, backup/recovery/watchdog, Vault binding and their contracts, readiness documentation and runbook/task updates must be implemented and reviewed before the corresponding remote execution. Local PASS does not authorize remote apply, commit/push/PR, deploy or activation. Gate 1 remains NO-GO.

## File structure

**Create — migrations**

- `supabase/migrations/20260624040504_comment_translator_real_comments_feed_snapshots.sql`
- `supabase/migrations/20260624141142_account_display_timezone_preference.sql`
- `supabase/migrations/20260722000000_comment_translator_paid_entitlements.sql`
- `supabase/migrations/20260722001000_comment_translator_paid_usage_counters.sql`
- `supabase/migrations/20260722002000_comment_translator_obs_overlay_tokens.sql`
- `supabase/migrations/20260722003000_comment_translator_obs_overlay_browser_sessions.sql`
- `supabase/migrations/20260723000000_comment_translator_moderator_share_tokens.sql`
- `supabase/migrations/20260723001000_comment_translator_moderator_share_browser_sessions.sql`
- `supabase/migrations/20260723002000_comment_translator_custom_dictionary.sql`
- `supabase/migrations/20260723003000_comment_translator_creator_history.sql`
- `supabase/migrations/20260726111154_comment_translator_paid_entitlements.sql`
- `supabase/migrations/20260728135736_comment_translator_paid_usage_counters.sql`
- `supabase/migrations/20260728142220_comment_translator_obs_overlay_tokens.sql`
- `supabase/migrations/20260728145324_comment_translator_obs_overlay_browser_sessions.sql`
- `supabase/migrations/20260728160206_comment_translator_moderator_share_tokens.sql`
- `supabase/migrations/20260728164122_comment_translator_moderator_share_browser_sessions.sql`
- `supabase/migrations/20260728172220_comment_translator_custom_dictionary.sql`
- `supabase/migrations/20260729035621_comment_translator_creator_history.sql`
- `supabase/migrations/20260811000000_comment_translator_paid_v1_legacy_schema_bridge.sql`
- `supabase/migrations/20260814105000_comment_translator_paid_pg_net_extension.sql`

**Create — evidence and contracts**

- `scripts/fixtures/comment-translator-paid-core-v1-gate1-production-history.json` — exact 18-version metadata and replacement/retirement classification.
- `scripts/fixtures/comment-translator-paid-core-v1-gate1-paid-legacy-catalog.json` — complete sorted three-table/three-function/one-trigger catalog and dependency fingerprint.
- `scripts/fixtures/comment-translator-paid-core-v1-gate1-source-era-catalog.json` — exact read-only six-table/seven-function safety fingerprint; never used as a mutation allowlist.
- `scripts/fixtures/comment-translator-paid-core-v1-gate1-bridge-states.json` — version-2 exact legacy/full, canonical/structural, and clean state allowlists plus a separately validated canonical count observation bound to its source artifact; separately governed source-era names are an excluded observation set.
- `scripts/fixtures/comment-translator-paid-core-v1-gate1-canonical-function-manifest.json` — existing complete canonical `ct_paid_*` identity/return/owner/config/ACL/definition manifest; reuse this implementation path, do not create the formerly planned duplicate `canonical-rpc.json`.
- `scripts/fixtures/comment-translator-paid-core-v1-gate1-environment-inventories.json` — retained pre-forward final55 baseline and exact ordered final56, Preview-pending26, and production-pending34 version/name arrays plus sanitized target fingerprints.
- `scripts/fixtures/comment-translator-paid-core-v1-gate1-catalog-readonly.sql` — fixed SELECT-only catalog queries and canonical JSON projections used to acquire the five manifests.
- `scripts/comment-translator-paid-core-v1-gate1-catalog-acquire-readonly.ps1` — read-only `psql \copy` adapter that writes the four legacy statement arrays directly to a restricted local directory without returning SQL bodies.
- `scripts/comment-translator-paid-core-v1-gate1-catalog-contract.mjs` — fixture schemas, deterministic canonicalization, inventory, equivalence, and sensitive-value checks.
- `scripts/lib/comment-translator-paid-core-v1-gate1-catalog.mjs` — strict acquired/derived types and canonical structural projection; preserve existing catalog consumers.
- `scripts/comment-translator-paid-core-v1-gate1-catalog-contract.test.mjs` — focused schema, projection, observation and unchanged legacy-boundary tests.
- `scripts/comment-translator-paid-core-v1-gate1-catalog-readback-contract.mjs` — strict saved-artifact validation without remote reacquisition.
- `scripts/comment-translator-paid-core-v1-gate1-migration-contract.mjs` — marker, ordering, bridge, extension, and three-environment migration tests.
- `scripts/comment-translator-paid-core-v1-gate1-database-integration-contract.mjs` — disposable PostgreSQL-17 execution for legacy/canonical/clean/negative states and CLI rollback/history atomicity.
- `scripts/comment-translator-paid-core-v1-gate1-operator-contract.mjs` — approval/state-machine, backup/RPO, Preview, production, Vault, Cron, readback, and stop tests.
- `scripts/lib/comment-translator-paid-core-v1-gate1-evidence.mjs` — trusted committed authority and restricted artifact validation; no stage/GO promotion by itself.
- `scripts/comment-translator-paid-core-v1-gate1-evidence-contract.mjs` — common authority, native boundary and artifact tamper regressions.
- `scripts/lib/comment-translator-paid-core-v1-gate1-source-evidence.mjs` — native source/integration exact56 corpus observation and sourceCommit stage verifier.
- `scripts/comment-translator-paid-core-v1-gate1-source-evidence-contract.mjs` — isolated source-stage/native-boundary regressions.
- `scripts/lib/comment-translator-paid-core-v1-gate1-postapply-catalog.mjs` — pure post-apply catalog/full pg_depend row comparison; no stage or GO promotion.
- `scripts/comment-translator-paid-core-v1-gate1-postapply-catalog-contract.mjs` — typed comparison and drift regressions.
- `scripts/lib/comment-translator-paid-core-v1-gate1-postapply-acquire.mjs` — fixed post-apply catalog SQL and bounded native observation.
- `scripts/comment-translator-paid-core-v1-gate1-postapply-acquire-contract.mjs` — acquisition-boundary and fixed SQL contracts.
- `scripts/comment-translator-paid-core-v1-gate1-postapply-query.test.mjs` — bounded local query lifecycle and native capture tests.
- `scripts/comment-translator-paid-core-v1-gate1-preflight-readonly.mjs` — sanitized catalog/history/extension/Vault-name/Cron/advisor read-only evidence adapter; no function invocation or mutation.
- `scripts/comment-translator-paid-core-v1-gate1-preflight-readonly-contract.mjs` — source and fixture contract for the read-only adapter.
- `scripts/comment-translator-paid-core-v1-gate1-backup-recovery-preflight.ps1` — local tool/version checks and dry-run command construction only; it must not dump, pause, provision, restore, or cut over by default.
- `scripts/comment-translator-paid-core-v1-gate1-backup-recovery-contract.mjs` — exported-snapshot `T0`, 5/10/20-minute watchdog, restore ordering, and secret-safe reporting tests.
- `scripts/comment-translator-paid-core-v1-gate1-vault-write.ps1` — approval-gated PostgreSQL-17 `psql` extended-query runner for the exact two Vault names; inert without its exact mode/approval inputs.
- `scripts/comment-translator-paid-core-v1-gate1-vault-binding-integration-contract.mjs` — disposable local-Supabase proof of parameter binding, atomic two-row commit, and rollback without value disclosure.

**Create/modify — operator authority**

- Create `docs/active/COMMENT_TRANSLATOR_PAID_V1_GATE1_PRODUCTION_SUPABASE_READINESS.md` — exact approval packets and operator state transitions.
- Modify `docs/active/COMMENT_TRANSLATOR_PAID_V1_RUNBOOK.md` — link the Gate 1 operator authority and keep activation/deploy separate.
- Modify `task.md` — one concise current status line; no long session history.

No runtime application, UI, Stripe, Provider, Cloudflare deployment configuration, kill switch, package manifest, or lockfile is changed.

---

## Chunk 1: Migrations, manifests, and database contracts

### Amendment checkpoint: resume the existing implementation, do not restart it

This checkpoint takes precedence over Task 1's original clean-tree-only first-start requirement only for the recorded in-progress implementation. Worktree: `C:/Users/taka/.codex/worktrees/2d79/V_streamer_tools`; branch: `codex/comment-translator-paid-v1-gate1-source-readiness`; expected HEAD: `2775fba1f5ca687d23b25c88286ec5125d1663ec`. Inventory and preserve its existing dirty/untracked implementation. Unexpected changes still stop; never reset, discard, recreate completed markers, or silently switch to another checkout. Verify the existing Luna Max task is idle before the parent edits these documents, and hand source ownership back only after written approval. No second writer or new top-level task is implied.

**Later implementation ownership:** verified `gpt-5.6-luna / max` owns the existing catalog helper, catalog tests, acquisition/readback contracts, database integration contract, migration contract, derived bridge-state fixture and bridge SQL. Parent owns design/plan acceptance. `sol-reviewer` (`gpt-6-astra / medium`) reviews read-only; no model/role substitution. Source-era/legacy fixture content, acquired artifacts, recovered SQL, historical migrations, package manifests and lockfiles are not to be rewritten to satisfy the amendment.

- [ ] **A1: Verify immutable inputs and evidence limits.** Recheck Preview r13's supplied SHA-256 `489b36c13953b9fa1f508ea7e8bdb9a6ca5cb0aa0998de07cbbbe4189d174a16` and strict full-row validation, and the already validated Production legacy/source-era input. Record the source artifact binding without exposing its contents. Do not reacquire remotely. A missing Production canonical wrapper cannot be filled with empty values or accepted as full Production PASS.
- [ ] **A2: Write projection and observation RED tests.** Modify `scripts/comment-translator-paid-core-v1-gate1-catalog-contract.test.mjs`. Require two valid acquired canonical snapshots that differ only in table `rowCount` to have equal structural projections but distinct retained observations. Independently change each structural field/class (including ACL, raw definition digest and dependency counts) and require inequality. Missing/invalid counts, unknown fields, duplicate/extra/missing table identities, mismatched observation coverage/source digest, or missing FunctionRow fields must fail strict validation. Legacy `rowCount=1` remains a failing bridge precondition; source-era safety tests remain unchanged.
- [ ] **A3: Implement the typed projection and versioned derived fixture.** After RED, update `scripts/lib/comment-translator-paid-core-v1-gate1-catalog.mjs`, `scripts/comment-translator-paid-core-v1-gate1-catalog-readback-contract.mjs`, `scripts/comment-translator-paid-core-v1-gate1-migration-contract.mjs`, and the derived `scripts/fixtures/comment-translator-paid-core-v1-gate1-bridge-states.json` according to Task 1's version-2 types below. Keep the acquisition artifact's full schema and strict checks; do not make counts optional in `scripts/comment-translator-paid-core-v1-gate1-catalog-acquire-readonly.ps1`. Unknown or old derived fixture versions fail with a regeneration-required classification rather than automatic coercion. Retain legacy/source-era digests, complete FunctionRow requirements and raw function MD5 semantics.
- [ ] **A4: Resolve the local-baseline and raw-definition blockers before canonical PASS.** Diagnose the four ACL differences using exact grantee/privilege/grantable rows, not ACL lengths alone. Establish supported local Supabase roles/default privileges independently of expected Preview ACLs; no equality-driven privilege widening. Compare Git blob, working-tree and actual replay bytes for the five function differences without printing SQL bodies. Report LF-normalized equality only as diagnostic evidence. Do not change historical migration definitions, ACL expectations or digest algorithms under this amendment. An unexplained remaining difference stops canonical acceptance and requires a separately scoped proposal; independent harness work may continue within existing local authority.
- [ ] **A5: Update the bridge and prove canonical preservation.** Modify only the existing bridge plus its migration/integration tests to use complete canonical structural JSON equality. Retain exact full legacy/zero-row checks. Add both migration-only and small deterministic synthetic-data canonical no-op cases; the synthetic case is not a Preview count replica and must list its exact local table/column ownership before insertion. With no concurrent writer/scheduler, assert unchanged structural JSON, counts and lossless per-table row equality inside the disposable DB; do not print row values. Add an oracle self-test showing a same-count update fails preservation. This new local test scope requires written amendment approval; no remote row copy is permitted.
- [ ] **A6: Complete the independently constructed negative matrix and CLI history test.** Implement every case enumerated in Task 4 Step 5, reporting each as PASS/FAIL/UNVERIFIED, and execute the CLI-injected failure/success pair. A direct `psql` replay, a static guard, a single partial-state test or a hardcoded `UNVERIFIED` field cannot satisfy this gate. Keep all bridge state/digest comparisons and history assertions independent.
- [ ] **A7: Verify and return for review.** Run the focused commands below; then the complete local database suite only when its explicit local context and server are available. Record unchanged source-artifact hashes and unrelated-file/lockfile checks. Request read-only Sol review and parent actual-diff acceptance. Remain `NO-GO` for the overall Gate; do not commit, push or create a PR.

Focused sequence from the implementation root (A2 must first demonstrate the new tests fail for the intended reason; after A3/A5 the same tests must pass):

```powershell
node --test scripts/comment-translator-paid-core-v1-gate1-catalog-contract.test.mjs
node --check scripts/lib/comment-translator-paid-core-v1-gate1-catalog.mjs
node --check scripts/comment-translator-paid-core-v1-gate1-catalog-readback-contract.mjs
node scripts/comment-translator-paid-core-v1-gate1-catalog-contract.mjs
node scripts/comment-translator-paid-core-v1-gate1-migration-contract.mjs
git diff --check
```

Database acceptance uses Task 4 Step 5's command and explicit isolated local context. The runner must fail full acceptance if any required positive, preservation, negative or rollback/history case is not executed, including when canonical execution was not opted in. Missing Docker/server, local Supabase config or required existing image is `SETUP_BLOCKED`; do not start repair, pull/install, reuse/reset an existing database or delete a volume to bypass it. CLI initialization/config belongs only in a new restricted disposable work directory with a unique local project identity, verified through the pinned CLI's `--help`; never link a hosted project. These steps are future approved-local test execution, not commands to run during this draft-writing turn.

### Task 1: Freeze current source and sanitized evidence

**Files:**

- Verify: `docs/superpowers/specs/2026-08-31-comment-translator-paid-v1-gate1-production-supabase-design.md`
- Verify: `supabase/migrations/*.sql`
- Create later: the five JSON files, fixed SELECT-only SQL file, and read-only acquisition adapter listed above.

- [ ] **Step 1: Revalidate the implementation checkout**

Run:

```powershell
git status --short --branch
git rev-parse HEAD
.\node_modules\.bin\supabase.cmd --version
node --version
```

The Sol handoff must pin `GATE1_IMPLEMENTATION_ROOT`, `GATE1_IMPLEMENTATION_BRANCH`, and `GATE1_HANDOFF_HEAD` before Luna begins. The branch is either `codex/comment-translator-paid-v1-gate1-supabase-design` or one exact approved `codex/` successor created from it for the isolated implementation worktree; no wildcard branch is accepted. Require current root, branch, and pre-edit HEAD to equal those three values, and require `git merge-base --is-ancestor a275174dc6494a8d159f1fff30318c05fece0107 HEAD` to exit 0. Before implementation edits, the only permitted branch delta from that base is the approved design/plan path set under `docs/superpowers/specs` and `docs/superpowers/plans`; an approved successor commit may replace `HEAD`, but it must remain a descendant of the same base and contain the approved spec unchanged. Expected Supabase CLI is `2.109.0`; Node is 22.x. Stop on a dirty tree, wrong root/branch/base, unexpected pre-existing path, CLI drift, or missing dependency. Do not install or upgrade anything.

- [ ] **Step 2: Revalidate the migration inventory without linking or mutating a remote**

Create the catalog contract first with this exact ordered baseline and make it list `supabase/migrations/*.sql` directly:

```js
const baselineMigrations = [
  "20260527000000_account_preferences_foundation",
  "20260601000000_youtube_oauth_credentials",
  "20260615000000_comment_translator_sessions",
  "20260615001000_comment_translator_usage_ledger_events",
  "20260623000000_comment_translator_real_comments_feed_snapshots",
  "20260624000000_account_display_timezone_preference",
  "20260705000000_comment_translator_creator_waitlist_registrations",
  "20260706073204_supabase_default_privileges_guard",
  "20260812120000_comment_translator_paid_core_v1",
  "20260813130000_comment_translator_paid_task6_circuit_probe_claim",
  "20260813131500_comment_translator_paid_task6_openai_rate_retry",
  "20260813133000_comment_translator_paid_task6_owned_circuit_failure",
  "20260813134500_comment_translator_paid_task6_azure_billing_split",
  "20260813135500_comment_translator_paid_task6_azure_uncertain_retry_compatibility",
  "20260813140000_comment_translator_paid_task6_azure_uncertain_retry",
  "20260813141500_comment_translator_paid_task6_circuit_success_window",
  "20260813143000_comment_translator_paid_task6_openai_resume_status",
  "20260813144500_comment_translator_paid_task6_terminal_openai_partial",
  "20260813150000_comment_translator_paid_task6_openai_partial_receipt",
  "20260813151500_comment_translator_paid_task6_terminal_openai_partial_authority",
  "20260813153000_comment_translator_paid_task6_replay_circuit_authority",
  "20260813154500_comment_translator_paid_task6_dispatch_and_azure_partial_settlement",
  "20260814100000_comment_translator_paid_task7_runtime_authority",
  "20260814110000_comment_translator_paid_task9_retention_observability",
  "20260815090000_comment_translator_paid_cron_vault_transport",
  "20260818100000_comment_translator_paid_gate0a_poll_budget_rpc_repair",
  "20260819100000_comment_translator_paid_gate0a_lifecycle_read_repair",
  "20260819110000_comment_translator_paid_gate0a_capacity_read_repair",
  "20260823120000_comment_translator_paid_gate0a_unbound_hold_recovery",
  "20260823130000_comment_translator_paid_gate0a_schedule_function_privilege_repair",
  "20260826100000_comment_translator_paid_gate0a_checkout_recovery_window_floor",
  "20260826110000_comment_translator_paid_gate0a_recovery_floor_second_canonicalization",
  "20260827020609_comment_translator_paid_checkout_expiry_finalize_lease",
  "20260829100000_comment_translator_paid_task6_azure_uncertain_retry_guard_repair",
  "20260831100000_comment_translator_paid_task11_message_rate_runtime_repair"
];
```

Run `node scripts/comment-translator-paid-core-v1-gate1-catalog-contract.mjs --baseline-only`. Expected: `baseline-migrations=35 unique=35 ordered=pass additions=0` and exit 0. Any swapped, missing, duplicate, or extra file fails even when counts match.

- [ ] **Step 3: Freeze the exact sanitized production evidence into fixtures**

Before migration edits, create `comment-translator-paid-core-v1-gate1-catalog-readonly.sql`, `comment-translator-paid-core-v1-gate1-catalog-acquire-readonly.ps1`, and the catalog contract. The SQL file may contain only CTEs and SELECTs over `supabase_migrations`, `pg_catalog`, `information_schema`, `cron.job`, `cron.job_run_details`, `vault.secrets`, and extension/advisor-readable catalog surfaces. Acquire sanitized production and Preview catalog results through the Supabase connector's read-only `execute_sql` path after exact target-name selection; never use `apply_migration`. Feed returned sanitized JSON to the contract through a restricted temporary file outside the repository, then write only validated sanitized fixture JSON with `apply_patch`. Full SQL statement bodies are excluded from every connector query and result.

BridgeStates and environment-inventories use `schemaVersion: 2`; other fixture versions remain unchanged. Environment inventories contain final55 (pre-forward diagnostic baseline), final56, preview.pending26 and production.pending34; the old pending25/pending33 keys are rejected. No fixture contains a timestamp or project/organization ID. Acquired artifacts remain unchanged. All derived fixtures use this deterministic digest rule. The digest input is always the complete top-level object with `aggregateSha256` omitted; the stored `aggregateSha256` is then added after hashing that projection:

```js
function sortKeysDeep(value) {
  if (Array.isArray(value)) return value.map(sortKeysDeep);
  if (!value || typeof value !== "object") return value;
  return Object.fromEntries(Object.keys(value).sort().map((key) => [key, sortKeysDeep(value[key])]));
}
const { aggregateSha256: _excluded, ...digestProjection } = value;
const canonicalJson = JSON.stringify(sortKeysDeep(digestProjection));
const sha256 = crypto.createHash("sha256").update(canonicalJson, "utf8").digest("hex");
```

Catalog row normalization is fixed: identifiers are lowercase server-returned names; arrays are sorted by schema/name/identity or ordinal; PostgreSQL types use `format_type`; defaults/constraints/indexes/triggers use server `pg_get_*def` output with CRLF→LF and outer whitespace trimmed only; ACLs use `aclexplode` sorted by grantee role, privilege, and grantable; function bodies are not committed and only `md5(pg_get_functiondef(oid))` is stored. Dependency counts are separate exact integers for inbound FK, views, materialized views, rules, policies, user triggers, event triggers, publications, outside function-source references, and unexpected `pg_depend` edges.

The exact nested row schemas are:

```text
HistoryRow = {version,name,statementCount,sqlBytes,statementsMd5,classification,replacementVersion}
ColumnRow = {schema,table,ordinal,name,type,notNull,defaultDefinition}
ConstraintRow = {schema,table,name,type,definition}
IndexRow = {schema,table,name,definition}
PolicyRow = {schema,table,name,command,permissive,roles,usingDefinition,checkDefinition}
AclRow = {schema,objectKind,objectIdentity,grantee,privilege,grantable}
TableRow = {schema,name,owner,rlsEnabled,rowCount,columns,constraints,indexes,policies,acls}
FunctionRow = {schema,name,identityArguments,resultType,owner,securityDefiner,config,acls,definitionMd5}
TriggerRow = {tableSchema,tableName,name,enabled,functionIdentity,definition}
DependencyCounts = {inboundForeignKeys,views,materializedViews,rules,policies,userTriggers,eventTriggers,publications,outsideFunctionSourceReferences,unexpectedPgDependEdges}
CatalogState = {tables,functions,triggers,dependencyCounts}
CanonicalTableStructureRow = {schema,name,owner,rlsEnabled,columns,constraints,indexes,policies,acls}
CanonicalStructuralState = {tables:CanonicalTableStructureRow[],functions:FunctionRow[],triggers:TriggerRow[],dependencyCounts:DependencyCounts}
CanonicalCountRow = {schema,name,rowCount}
CanonicalObservation = {sourceArtifactSha256,rows:CanonicalCountRow[],aggregateSha256}
BridgeStates = {schemaVersion:2,legacy:CatalogState,canonical:CanonicalStructuralState,clean:CatalogState,canonicalObservation:CanonicalObservation,sourceEraExcludedObservation:{tables:{schema,name}[],functions:{schema,name,identityArguments}[]},aggregateSha256}
```

`CatalogState.tables` still contains full `TableRow` values for raw readback, legacy and source-era evidence. Strictly validate a full canonical readback before deriving `CanonicalStructuralState`: remove exactly `tables[*].rowCount`, with no other omission or new normalization. Desired `CanonicalStructuralState` is generated from the repository migration corpus plus the approved safe security contract and is the local authority; it must equal the locally regenerated desired state, not a Preview copy. The saved Preview projection remains immutable observed-drift evidence and is compared separately for classification only. `CanonicalObservation.rows` retains all 33 current canonical table identities and their observed nonnegative integer counts in exact schema/name order, with no missing, duplicate or extra entries. Its source SHA-256 must equal the independently verified input artifact hash, not an arbitrary well-formed hash; each count must equal that source's full row. Its nested `aggregateSha256` uses the same omission/hash rule; the top-level fixture hash includes the complete observation with its nested hash. Both hashes are independently verified. The observation is integrity/provenance evidence only, never embedded in the canonical bridge-state equality predicate. Full FunctionRow values and their raw MD5 contract are unchanged. A later valid count-only observation may change observation/top-level integrity digests without changing canonical structure; do not silently update the reviewed fixture or bypass the fresh-evidence gate for a later remote operation. Only a fresh readback after a separately approved hosted apply can prove deployed convergence.

Every nested array uses the catalog sort order already defined; `roles` and `config` are sorted strings. The history fixture is `{schemaVersion,rows:HistoryRow[],highConfidenceSecretMatches,rollbackStatementCount,aggregateSha256}`. Paid-legacy and source-era fixtures are `{schemaVersion,tables:TableRow[],functions:FunctionRow[],triggers:TriggerRow[],dependencyCounts:DependencyCounts,aggregateSha256}`. The saved HEAD RPC fixture is an identity-only fixture `{schemaVersion,expectedCount,functions:IdentityFunctionRow[],aggregateSha256}` with 81 direct identities and aggregate SHA-256 `7f833ff897e57d1d418bcf954c3367cd3b07612b09f4e182d94bbc5fee1680d9`; it is not a new remote readback and its SHA-256 must not be reused as an MD5. Separately, the implementation must generate a complete canonical RPC manifest `{schemaVersion,functions:FunctionRow[],aggregateMd5,aggregateSha256}`. Its `aggregateMd5` is computed only after full FunctionRow normalization from the UTF-8 bytes of `functions.map(sortKeysDeep).map(JSON.stringify).join("\n")` in exact identity order; it must be independently recalculated and compared row-by-row, and is not inherited from the superseded 82-identity value `d975b161bf115fe6ecd80ad68c55134e`. Bridge state digests use the top-level SHA-256 projection rule and never reuse the canonical RPC aggregate MD5.

The Paid-legacy function identities are pinned as:

```text
apply_comment_translator_paid_entitlement_evidence(p_billing_user_reference_id text, p_stripe_customer_reference_id text, p_stripe_subscription_reference_id text, p_subscription_status text, p_billing_state text, p_current_period_end timestamp with time zone, p_evidence_event_reference_id text, p_evidence_created_at timestamp with time zone, p_evidence_recorded_at timestamp with time zone) -> boolean
apply_comment_translator_paid_usage(p_billing_user_reference_id text, p_expected_period_end timestamp with time zone, p_usage_event_reference_id text, p_occurred_at timestamp with time zone, p_translated_message_count bigint, p_provider_input_character_count bigint, p_estimated_cost_micros bigint) -> text
sync_comment_translator_paid_usage_from_entitlement() -> trigger
```

All three are owner `postgres`, `SECURITY DEFINER`, `search_path=public`, with definition MD5s from the approved spec/current read-only evidence.

The source-era fixture must independently record all six tables and seven functions with exact zero rows, RLS flags, owners, ACLs, identities, and definition digests. It is compared before/after every later readback and is prohibited from appearing in bridge mutation lists.

The four Paid-legacy bodies required only for disposable integration are versions `20260722000000`, `20260722001000`, `20260726111154`, and `20260728135736`, with exact `<version>_<name>.sql` filenames from the production-history fixture. During the separately approved read-only acquisition, run the PowerShell adapter with connection material supplied only through process environment. It must require PostgreSQL `psql` major 17, set `PGOPTIONS=-c default_transaction_read_only=on`, begin `REPEATABLE READ READ ONLY`, verify the exact production target fingerprint through a sanitized scalar query, and use client-side `\copy` to write one ordered JSONL staging file directly under a newly created restricted directory outside repository/workspace/home roots. The fixed query selects only `{version,name,statements}` for the four exact versions ordered by version; `psql` stdout/stderr are captured and filtered to fixed status labels, so statement text never enters terminal/tool output. No URL/password/SQL body may appear in arguments or logs.

The adapter parses the JSONL locally and requires each `statements` value to be an ordered JSON string array. For each row it computes `statementsMd5 = MD5(UTF8(JSON.stringify(statements)))`, then computes `serializedSql = statements.join("\n\n") + "\n"` with no newline normalization, no BOM, no added terminator, and writes its exact UTF-8 bytes to `<version>_<name>.sql`. `sqlBytes` is `Buffer.byteLength(serializedSql, "utf8")`. Those two values are the only definitions used when generating and validating the production-history fixture. Delete the JSONL staging file after successful conversion, bind the directory path to `GATE1_LEGACY_SQL_DIR` without logging it, and leave exactly four regular non-symlink files. Before any migration edit, the catalog contract maps each filename to one history row, verifies exact count/version/name/byte length/MD5, runs the high-confidence scan, rejects rollback/history-repair/credential material, and rejects any fifth file or symlink. The files remain untracked temporary evidence and are never copied into source, fixtures, reports, commits, or PRs.

Stage acquisition to avoid circular authority: production history/Paid-legacy/source-era and Preview canonical/RPC evidence are acquired first; migrations and the safe security contract are then implemented from the approved authority. Disposable local replay regenerates and validates the desired canonical structural manifest from repository LF source bytes; the complete local desired state must equal that repository-derived authority, not the Preview projection. Compare the local desired state with the immutable Preview projection only to classify observed drift, never to copy values or make Preview equality a pass criterion. Keep source-bound canonical count observations separate. For this amendment's local work, reuse the hash-verified r13 input; no new remote readback is implied. Fresh evidence immediately before a future remote operation remains a separate gate requiring its own authority, and only a fresh post-apply readback can establish hosted convergence. Any structural or safety evidence mismatch stops acceptance and returns for diagnosis/design review; count-only differences across the two environments are observations, not structural mismatches.

- [ ] **Step 4: Run changed-fixture secret and identifier scans**

Run:

```powershell
$gate1Files = Get-ChildItem scripts/fixtures/comment-translator-paid-core-v1-gate1-* -File | ForEach-Object FullName
rg -n -i --pcre2 '(?:postgres(?:ql)?://[^\s]+|sb_(?:secret|publishable)_[A-Za-z0-9_-]{20,}|eyJ[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}|Authorization\s*:\s*\S+|Bearer\s+[A-Za-z0-9_.-]{20,}|(?:service_role|owner(?:_id| id)|project(?:_id| id))\s*[:=]\s*["''][^"'']+["''])' -- $gate1Files
if ($LASTEXITCODE -eq 1) { 'HIGH_CONFIDENCE_SECRET_MATCHES=0'; exit 0 }
exit $LASTEXITCODE
```

Expected: `HIGH_CONFIDENCE_SECRET_MATCHES=0`. Any `rg` execution error is not converted to pass.

- [ ] **Step 5: Prove the two timestamp variants are schema-equivalent**

The production versions `20260624040504` and `20260624141142` must be compared respectively with local replacements `20260623000000` and `20260624000000`. Acquire each original production statement only through the approved read-only path into a restricted temporary file outside the repository; never print or commit it. Run the high-confidence scan before use and delete the temporary file only after the comparison has completed.

On disposable PostgreSQL 17 databases, apply each production variant and its local replacement to separate clean databases. Query the affected tables, columns, defaults, indexes, constraints, triggers, RLS policies, ACLs, and functions through the fixed catalog SQL; normalize with the exact `sortKeysDeep` and server-definition rules above; and require byte-identical canonical JSON plus equal SHA-256 digests for each pair. The catalog contract exposes this as:

```powershell
node scripts/comment-translator-paid-core-v1-gate1-catalog-contract.mjs --timestamp-equivalence
```

Expected: `timestamp-equivalence=2/2 production-sql-committed=0`. If PostgreSQL 17/Docker or the read-only evidence path is unavailable, report `SETUP_BLOCKED`; do not install tools and do not claim local integration verification.

### Task 2: Write the migration contract RED tests

**Files:**

- Create: `scripts/comment-translator-paid-core-v1-gate1-migration-contract.mjs`
- Read: `scripts/comment-translator-paid-core-v1-migration-parser-contract.mjs`
- Read: `scripts/comment-translator-paid-core-v1-schema-contract.mjs`

- [ ] **Step 1: Add exact fixture and migration inventory assertions**

Use `node:assert/strict`, `node:crypto`, `node:fs`, and `node:path`. Load all five catalog JSON fixtures as independent expected evidence; the sixth environment-inventory fixture is created and governed separately in Task 5. Validate every top-level/nested field against the exact Task 1 schemas, recompute every aggregate digest from its pre-digest projection, reject additional fields, and assert every expected row and sort order. Assert:

```js
assert.equal(history.rows.length, 18);
assert.equal(new Set(history.rows.map(({ version }) => version)).size, 18);
assert.equal(history.highConfidenceSecretMatches, 0);
assert.equal(history.rollbackStatementCount, 0);
assert.equal(legacy.tables.length, 3);
assert.equal(legacy.functions.length, 3);
assert.equal(legacy.triggers.length, 1);
assert.equal(sourceEra.tables.length, 6);
assert.equal(sourceEra.functions.length, 7);
assert.deepEqual(bridgeStates.sourceEraExcludedObservation.tables, sourceEra.tables.map(({ schema, name }) => ({ schema, name })));
assert.deepEqual(bridgeStates.sourceEraExcludedObservation.functions, sourceEra.functions.map(({ schema, name, identityArguments }) => ({ schema, name, identityArguments })));
assert.deepEqual(bridgeStates.legacy, {
  tables: legacy.tables,
  functions: legacy.functions,
  triggers: legacy.triggers,
  dependencyCounts: legacy.dependencyCounts
});
assert.equal(canonicalRpc.functions.length, 81);
assert.equal(canonicalRpc.expectedCount, 81);
assert.equal(canonicalRpc.aggregateSha256, "7f833ff897e57d1d418bcf954c3367cd3b07612b09f4e182d94bbc5fee1680d9");
```

The saved `canonicalRpc` fixture above is identity-only evidence and must not be accepted as the complete FunctionRow manifest. The complete manifest must be generated separately from normalized rows, with `owner`, ACL, return type, `securityDefiner`, `config`, and `definitionMd5`; its MD5 is independently computed from those complete rows and cannot be copied from the identity fixture's SHA-256 or from the superseded `d975b161bf115fe6ecd80ad68c55134e` value.

The A3 authority fixture records `browserExecuteRoles=[PUBLIC,anon,authenticated]` as a zero-grant security boundary and `serviceRolePolicy=explicit-minimum-only`; Preview ACL rows outside that contract are immutable observed drift. It records `definitionDriftExpectedTrueCount=2` together with `definitionDriftObservation=UNVERIFIED_UNTIL_LOCAL_REPLAY`. Source-file LF equivalence is diagnostic only, not a database definition-digest result. The exact three LF-only identities and exact two true-drift identities are selected only after the 55-migration Git/deployment-equivalent LF replay; no forward migration may be guessed before that classification. The complete FunctionRow manifest and bridge state are regenerated from local replay and compared row-by-row with the reviewed authority; hand-editing either fixture cannot establish PASS.

Also assert exact per-version classification: the two timestamp variants map to normalized local replacements; the four entitlement/usage versions map to the Paid-legacy archive + canonical base; the remaining twelve source-era versions map to `retained-production-only-outside-a2`. Assert source-era names occur only in the source-era fixture and `sourceEraExcludedObservation`, never in the legacy/canonical/clean mutation allowlists.

- [ ] **Step 2: Add marker safety assertions**

For each of 18 expected marker files, assert exact filename, one `DO` statement, `begin null; end`, matching sanitized comment metadata, and no recovered SQL body. Reject `create table`, `alter table`, `create function`, grants, DML, history repair, IDs, URLs, secrets, or source-era object mutation.

- [ ] **Step 3: Add bridge state and mutation assertions**

Assert the bridge is ordered after `20260729035621` and before `20260812120000`; contains exact statements `set local lock_timeout = '5s'`, `set local statement_timeout = '60s'`, and `perform pg_catalog.pg_advisory_xact_lock(pg_catalog.hashtextextended('comment-translator-paid-v1-gate1-legacy-bridge', 0))`; recognizes only `exact-legacy`, `exact-canonical`, or `exact-clean`; and compares complete embedded expected/actual `jsonb` structurally using `IS NOT DISTINCT FROM`. Canonical uses the complete `CanonicalStructuralState`; legacy uses full `CatalogState` including its zero-row checks. Neither canonical count observations nor the top-level fixture hash may select the canonical branch. Reject `jsonb::text`, Node serialization constants, and cross-runtime byte/digest comparison in the bridge.

Before any DDL, require the archive schema to be absent, the exact Paid Cron job count to be zero, all canonical Paid identities to be absent in legacy state, and the executor to own every object that could be moved. Exact legacy additionally requires all three table row counts to be zero, RLS enabled, the one expected user trigger present and enabled, all three internal FK definitions exact, and each external dependency class independently zero: inbound FKs, views, materialized views, rules, policies, outside user triggers, event triggers, publications, outside function-source references, and unexpected `pg_depend` edges. Assert only the three manifest-listed Paid-legacy tables and the three exact function identities from Task 1 can move; disable the moved trigger; revoke archive access from `PUBLIC`, `anon`, `authenticated`, and `service_role`; and post-verify freed canonical names plus inaccessible archive objects. Reject source-era names in every `ALTER ... SET SCHEMA`, grant/revoke target, or dynamic mutation statement.

- [ ] **Step 4: Add `pg_net` and environment convergence assertions**

Require `20260814105000_comment_translator_paid_pg_net_extension.sql` to contain only:

```sql
create extension if not exists pg_net with schema extensions;
```

Require exact inventories: repository 56; saved production history 22 with exact pending set 34; saved Preview history 30 with exact pending set 26; clean baseline pending 56. Count-only matches are insufficient; compare ordered version/name arrays.

- [ ] **Step 5: Run RED**

Run:

```powershell
node --check scripts/comment-translator-paid-core-v1-gate1-migration-contract.mjs
node scripts/comment-translator-paid-core-v1-gate1-migration-contract.mjs
```

Expected: syntax PASS, execution FAIL on missing marker/bridge/extension sources, not on fixture parsing.

### Task 3: Create the 18 inert history markers

**Files:** the 18 marker migration paths listed in File structure.

- [ ] **Step 1: Create every migration through the pinned CLI**

For each approved migration name, run:

```powershell
.\node_modules\.bin\supabase.cmd migration new <approved_name>
```

Then use a bounded PowerShell `Move-Item -LiteralPath` to rename only the newly generated file to its exact approved production version. Verify source and destination absolute paths remain under `supabase/migrations`. Never overwrite an existing file.

- [ ] **Step 2: Populate the exact inert marker template**

Each file contains only its own sanitized values:

```sql
-- Comment Translator Paid Core v1 Gate 1 migration-history marker.
-- Original production version: <14-digit version>
-- Original production name: <name>
-- Original statement count: <count>
-- Original SQL bytes: <bytes>
-- Original statements MD5: <md5>
-- Schema effect: <normalized-local-replacement | paid-legacy-archive-and-canonical-replacement | retained-production-only-outside-a2>
-- The original SQL is deliberately not replayed by this marker.

do $comment_translator_paid_gate1_history_marker$
begin
  null;
end
$comment_translator_paid_gate1_history_marker$;
```

- [ ] **Step 3: Run the focused contract**

Run:

```powershell
node scripts/comment-translator-paid-core-v1-gate1-migration-contract.mjs --phase markers
```

Expected exit 0 only when all 18 markers pass, with `markers=18 bridge=expected-next-phase pg_net=expected-next-phase`. A malformed/missing marker remains a hard failure; the phase flag must not convert unrelated parser or fixture failures into success.

### Task 4: Implement the exact legacy bridge and `pg_net` migration

**Files:**

- Create: `supabase/migrations/20260811000000_comment_translator_paid_v1_legacy_schema_bridge.sql`
- Create: `supabase/migrations/20260814105000_comment_translator_paid_pg_net_extension.sql`

- [ ] **Step 1: Create both files with the pinned CLI and exact-path rename**

Use `supabase migration new` first, then the same checked rename procedure. Do not hand-create timestamped migration paths.

- [ ] **Step 2: Implement one transactional bridge block**

Use one `DO $bridge$ ... $bridge$` block. Execute exact `set local lock_timeout = '5s'` and `set local statement_timeout = '60s'`, then take `pg_catalog.pg_advisory_xact_lock(pg_catalog.hashtextextended('comment-translator-paid-v1-gate1-legacy-bridge', 0))`. Build sorted `jsonb_agg(... order by ...)` catalog rows for the A2-reserved Paid object set with the same null handling, array ordering, and server-definition trimming as Task 1. Embed expected values as `jsonb` constants and compare actual/expected structurally with `IS NOT DISTINCT FROM`; do not compare Node `JSON.stringify` bytes with PostgreSQL `jsonb::text`, and do not compute a cross-runtime digest inside the bridge. Node-side SHA-256 remains fixture-integrity/reporting evidence only.

State selection must be equivalent to:

```plpgsql
if v_legacy_fingerprint = v_expected_legacy then
  v_state := 'exact-legacy';
elsif v_canonical_structure is not distinct from v_expected_canonical_structure
  and v_legacy_reserved_count = 0 then
  v_state := 'exact-canonical';
elsif v_a2_reserved_object_count = 0 then
  v_state := 'exact-clean';
else
  raise exception 'Gate 1 bridge state is partial, mixed, or unknown';
end if;
```

For `exact-canonical` and `exact-clean`, return without DDL, DML, sequence changes, grants/revokes, function invocation or scheduler/Vault mutation after exact negative/positive checks. Runner history bookkeeping is separate. Build `v_canonical_structure` only from the exact structural type; do not compare current canonical counts with the saved observation. For `exact-legacy`, after the lock require archive absence, zero matching Paid Cron jobs, canonical identity absence, executor ownership, all three zero-row counts, RLS, the exact enabled trigger, exact internal FKs/ACLs/functions, and every named external dependency class at zero. Create `comment_translator_paid_legacy_archive`; move only the three manifest-listed tables and three exact function identities; disable the moved user trigger; revoke archive schema/object privileges from application roles; and post-verify exact archive/public state. Do not interpolate arbitrary catalog names and do not reference the six source-era tables or seven source-era functions in mutation SQL.

- [ ] **Step 3: Add the exact `pg_net` statement**

Do not pin an extension version; current Supabase changelog deprecates explicit extension version clauses. Do not add grants, function calls, or scheduler behavior.

- [ ] **Step 4: Run GREEN and inspect the diff**

Run:

```powershell
node --check scripts/comment-translator-paid-core-v1-gate1-migration-contract.mjs
node scripts/comment-translator-paid-core-v1-gate1-migration-contract.mjs
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
git diff --check
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
$gate1Changed = @(
  Get-ChildItem -LiteralPath scripts/fixtures -Filter 'comment-translator-paid-core-v1-gate1-*' -File
  Get-ChildItem -LiteralPath scripts -Filter 'comment-translator-paid-core-v1-gate1-*-contract.mjs' -File
  Get-Item -LiteralPath scripts/comment-translator-paid-core-v1-gate1-catalog-contract.mjs
  Get-Item -LiteralPath scripts/comment-translator-paid-core-v1-gate1-catalog-acquire-readonly.ps1
  Get-ChildItem -LiteralPath supabase/migrations -Filter '*.sql' -File | Where-Object Name -Match '^(20260624040504|20260624141142|2026072|20260811000000|20260814105000)'
) | Sort-Object FullName -Unique
rg -n -i --pcre2 '(?:postgres(?:ql)?://[^\s]+|sb_(?:secret|publishable)_[A-Za-z0-9_-]{20,}|eyJ[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}|Authorization\s*:\s*\S+|Bearer\s+[A-Za-z0-9_.-]{20,})' -- ($gate1Changed | ForEach-Object FullName)
if ($LASTEXITCODE -eq 1) { 'HIGH_CONFIDENCE_SECRET_MATCHES=0'; exit 0 }
exit $LASTEXITCODE
```

Expected after the A3 amendment checkpoint: marker/fixture validation and `git diff --check` pass, while the full migration contract remains nonzero with `A3_FORWARD_CONVERGENCE_MIGRATION_UNVERIFIED` until Docker-backed LF replay determines the exact two identities and the forward-only migration is reviewed. A missing forward migration must remain `UNVERIFIED`/blocked, not be converted to a static PASS; the secret scan must print `HIGH_CONFIDENCE_SECRET_MATCHES=0`.

- [ ] **Step 5: Execute the database integration contract**

Implement and run `scripts/comment-translator-paid-core-v1-gate1-database-integration-contract.mjs` against disposable PostgreSQL 17/local Supabase only. Never link a remote project. Task 1 supplies the four recovered Paid-legacy production SQL bodies through a restricted external directory named by `GATE1_LEGACY_SQL_DIR`; the contract validates exact approved metadata/digests before reading them, never prints them, and fails closed when they are absent. The contract must create isolated databases and cover:

1. `clean`: execute bridge as a no-op, then replay the complete 56-migration chain.
2. `canonical`: construct the desired canonical structural state through the supported local baseline migration replay using Git/deployment-equivalent LF bytes plus the approved safe security contract and any separately verified forward-only convergence migration. Compare the generated local state byte-for-byte with the desired canonical authority. Compare the immutable Preview structural projection separately as observed drift; never seed rows, ACLs, or definitions from Preview and never use Preview equality as the canonical PASS criterion. Run the bridge as a no-op in both migration-only and bounded synthetic-data variants; counts need not equal Preview. Verify unchanged structural JSON/digest, per-table counts, and lossless duplicate-sensitive equality of every table's row values inside the isolated local DB. A same-count-update oracle self-test must fail preservation; output only booleans/counts. No concurrent application writer, scheduler or outbound RPC is permitted in this local test. Ordinary remote counts are only snapshot observations; do not claim remote preservation from this local proof or from count equality alone.
3. `exact-legacy`: copy the repository to a restricted temporary work directory; replace only the four Paid-legacy inert marker bodies with the four digest-validated recovered bodies so the CLI both constructs legacy state and records their production history labels; retain the remaining markers unchanged; run `supabase db reset --local --no-seed`; verify the exact four history rows, bridge archive/public post-state, and canonical continuation.
4. Negative cases, one independently constructed database per precondition: partial objects; mixed objects; non-empty Paid-legacy table; wrong owner; RLS drift; ACL drift; function identity/result/config/security/definition drift; trigger absent; trigger disabled; internal-FK drift; pre-existing archive schema; canonical overload/extra canonical identity present in legacy state; malformed/extra/duplicate legacy objects; Paid Cron present; and each external dependency class. Every case must raise before DDL and retain byte-identical before/after normalized catalog JSON and equal SHA-256 digests. Non-empty canonical belongs to the positive preservation case, not this legacy rejection. Enumerate every case's status independently; a shared guard or one partial-state failure is not evidence for the rest.
5. CLI transaction/history atomicity: in another restricted temporary copied work directory, replace the same four markers with digest-validated legacy bodies and inject `raise exception 'gate1-atomicity-test'` immediately after the first move in the copied bridge. From that copied directory run `supabase db reset --local --no-seed` and require the command to fail. Query the local database with the contract's non-printed local connection context and assert legacy objects remain in `public`, archive movement is absent, and version `20260811000000` is absent from `supabase_migrations.schema_migrations`. Restore the unmodified copied bridge, rerun the exact reset command, require exit 0, and assert archive movement plus the history row are both present. Finally run `supabase migration list --local` and compare the exact 56-version list. All spawned CLI argument arrays must include `--local` where supported and must reject `--linked`, `--db-url`, `db push`, and remote project identifiers.

Run:

```powershell
node --check scripts/comment-translator-paid-core-v1-gate1-database-integration-contract.mjs
node scripts/comment-translator-paid-core-v1-gate1-database-integration-contract.mjs
```

Expected: `postgres=17 clean=pass canonical-structure=pass canonical-preservation=pass legacy=pass negatives=all-pass rollback-history-atomic=pass`. Canonical/negative/history opt-out or an unexecuted case prevents full acceptance and must yield nonzero exit for the full suite. Missing PostgreSQL 17/Docker/local Supabase is `SETUP_BLOCKED`, not a pass; do not install or upgrade tooling. Do not treat this local result as Preview-deployed or production-GO evidence.

---

## Chunk 2: Fail-closed operator tooling and recovery contracts

### Task 5: Write operator state-machine RED tests

**Files:**

- Create: `scripts/comment-translator-paid-core-v1-gate1-operator-contract.mjs`
- Create: `scripts/comment-translator-paid-core-v1-gate1-backup-recovery-contract.mjs`
- Create: `scripts/fixtures/comment-translator-paid-core-v1-gate1-environment-inventories.json`

- [ ] **Step 1: Encode separate approval gates**

Model immutable states for source implementation, Preview apply, tooling setup, recovery cost/capacity, recovery-project provisioning, recovery region/extensions/Auth configuration, rehearsal backup/restore, final backup, bounded-RPO/watchdog acceptance, production migration apply, Vault write, inactive Cron configuration, incident source pause, pause confirmation, recovery restore, endpoint/credential cutover, client reauthentication, reopening writes, later source unpause/deletion, and activation. An approval for one state must never satisfy another.

- [ ] **Step 2: Encode exact Preview and production inventories**

Create and validate `comment-translator-paid-core-v1-gate1-environment-inventories.json` with schema `{schemaVersion:2,final55:[{version,name}],final56:[{version,name}],preview:{targetFingerprint,pending26:[{version,name}]},production:{targetFingerprint,pending34:[{version,name}]},aggregateSha256}`. Its SHA-256 input is the complete top-level object with `aggregateSha256` omitted, canonicalized by Task 1 `sortKeysDeep`; the contract recomputes it and rejects extra fields. Bootstrap inputs come from the already approved Task 1 read-only acquisition: its exact target selection emits only sanitized target fingerprint plus ordered migration version/name rows into the validated restricted temporary input, never through the new preflight adapter. Combine those rows with repository `final56` to generate this fixture once. Later preflight runs treat the fixture as authority and must equal it; they never generate or update it. Contracts require exact row-by-row equality, uniqueness, `pending26.length === 26`, `pending34.length === 34`, and both pending arrays to be exact subsets of `final56`. No count-only transition exists.

Preview may transition only after exact fingerprint selection, exact fixture `pending26` dry-run, explicit Preview approval, apply, exact `final56` readback, canonical/RPC manifest equality, archive absence, installed `pg_net`, retired source-era public objects absent, existing scheduler job ID/name/cadence/command/database/username unchanged and inactive, scheduler mutation delta 0, scoped run delta 0, and exact two-name Vault count/name/record-ID digest unchanged without value readback.

Production may transition only after exact fingerprint selection, exact fixture `pending34` dry-run, all recovery/backup gates, explicit production approval, apply, and all of these contracted readbacks: exact `final56`; installed `pg_net`, `pg_cron`, and `supabase_vault`; canonical relation/table/column/constraint digests; archive exactly three zero-row RLS tables plus three functions with disabled trigger and application-role denial; exact canonical RPC/ACL manifest; reconciler default/hard limit 50 and 120-second lease; stale-token rejection; retention default/hard limit 500 and range rejection; sanitized scheduler evidence including retry alert threshold 5; zero `PUBLIC`/`anon`/`authenticated` Paid-RPC execute privileges; exact `service_role` ACLs; HTTP/private Vault maintenance transport denial to browser roles and allowance only to `service_role`; and scoped security/performance advisor baseline/delta with no new in-scope or high/critical finding. No maintenance RPC is invoked.

- [ ] **Step 3: Encode the backup/watchdog state machine**

Require these exact states and transitions:

```js
const states = [
  "PRE_DDL_UNARMED",
  "ABORTED_NO_DDL_NO_PAUSE",
  "ARMED_BEFORE_FIRST_DDL",
  "SUCCESS_DISARMED",
  "PAUSE_REQUESTED",
  "PAUSE_CONFIRMED"
];
```

`T0` must come from `transaction_timestamp()` in the same held repeatable-read transaction that exports the snapshot used by schema/data/history dumps. Backup/checksum missing `T0+5m` transitions to `ABORTED_NO_DDL_NO_PAUSE`. Only a completed backup can arm immediately before first DDL. Failure starts pause immediately; no success by `T0+10m` starts pause; only confirmed project inaccessibility by `T0+20m` permits restore. Pending/late pause forbids restore and leaves `NO-GO`.

Test two distinct backup stages. Rehearsal is unrestricted read-only and must restore into an isolated PostgreSQL-17 target with no production endpoint. Final backup must keep the exporter transaction open until every snapshot-bound schema/data/history dump exits 0, pass the exact exported snapshot using `--snapshot` to each such `pg_dump`, capture roles separately without claiming snapshot membership, complete all SHA-256 checks by `T0+5m`, close the exporter only after dumps finish, and arm only in the last pre-DDL check immediately before the first production DDL.

Before either stage, require total Vault rows = 0, both reserved-name rows = 0, and Storage objects = 0. A nonempty Auth/Storage diff requires a separately reviewed `auth_storage_changes.sql`; absent review is a hard stop. Rehearsal verification requires exact history count/digests, aggregate table row counts, Auth schema/dependencies/user count, zero Storage objects, grants/RLS, bridge plus canonical replay, and proof that every connection target is local/disposable.

- [ ] **Step 4: Encode Vault and Cron contracts**

Vault constants are names `comment_translator_paid_maintenance_url` and `comment_translator_paid_cron_token` and advisory lock `pg_catalog.hashtextextended('comment-translator-paid-v1-gate1-vault-binding', 0)`. The owned runner is `scripts/comment-translator-paid-core-v1-gate1-vault-write.ps1`; values enter only through inherited `GATE1_VAULT_MAINTENANCE_URL` and `GATE1_VAULT_CRON_TOKEN`, while libpq connection context uses standard process environment. It invokes PostgreSQL-17 `psql --no-psqlrc --quiet --set=ON_ERROR_STOP=1` with no secret argv and feeds this fixed script on stdin with echo disabled:

```text
BEGIN;
SELECT pg_catalog.pg_advisory_xact_lock(pg_catalog.hashtextextended('comment-translator-paid-v1-gate1-vault-binding', 0));
SELECT (count(*) = 0) AS gate1_absent FROM vault.secrets WHERE name IN ('comment_translator_paid_maintenance_url','comment_translator_paid_cron_token') \gset
\if :gate1_absent
\else
  \quit 4
\endif
\getenv gate1_url GATE1_VAULT_MAINTENANCE_URL
\getenv gate1_token GATE1_VAULT_CRON_TOKEN
SELECT vault.create_secret($1,'comment_translator_paid_maintenance_url'), vault.create_secret($2,'comment_translator_paid_cron_token');
\bind :'gate1_url' :'gate1_token'
\g
SELECT (count(*) = 2 AND count(DISTINCT name) = 2 AND count(id) = 2) AS gate1_created FROM vault.secrets WHERE name IN ('comment_translator_paid_maintenance_url','comment_translator_paid_cron_token') \gset
\if :gate1_created
  COMMIT;
\else
  \quit 5
\endif
```

Connection close on either `\quit` rolls back the open transaction. The runner captures output to fixed count/status fields, never interpolates values into SQL, and never enables statement/value logging. `comment-translator-paid-core-v1-gate1-vault-binding-integration-contract.mjs` runs the exact runner against disposable PostgreSQL-17 local Supabase with Vault installed: quote/newline/secret-shaped values commit exactly two rows in the success case; pre-existing one-name and forced post-create assertion failures commit zero new rows; argv/stdin-source logs/stdout/stderr/evidence contain no value. Missing local Supabase/Vault is `SETUP_BLOCKED`, not pass.

Cron constants are name `comment-translator-paid-maintenance`, cadence `*/5 * * * *`, command `select private.ct_paid_invoke_maintenance_from_vault();`, command SHA-256 `af78ff4c62c6f72af5902c867b254193a844764ad97154195b213a74a95e3d8c`, database `postgres`, and username `postgres`. Require scheduler process count 1 and Cloudflare fallback absent/inactive; record exact new job ID and pre-transaction maximum run-detail ID/time; immediately set `active=false` before commit; and query only that job ID after the baseline. Require exact one-row readback, `active=false` at both ends of the fixed 10-minute/two-cadence observation, and run delta 0. Direct writes to `cron.job` are forbidden; use only supported `cron.schedule`, `cron.alter_job`, and readback APIs.

- [ ] **Step 5: Run RED**

Run:

```powershell
node --check scripts/comment-translator-paid-core-v1-gate1-operator-contract.mjs
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
node --check scripts/comment-translator-paid-core-v1-gate1-backup-recovery-contract.mjs
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
$operatorRed = @(& node scripts/comment-translator-paid-core-v1-gate1-operator-contract.mjs --phase red 2>&1); $operatorCode = $LASTEXITCODE
if ($operatorCode -ne 3 -or $operatorRed.Count -ne 1 -or $operatorRed[0] -ne 'RED_EXPECTED_MISSING_IMPLEMENTATION') { $operatorRed; exit 1 }
$backupRed = @(& node scripts/comment-translator-paid-core-v1-gate1-backup-recovery-contract.mjs --phase red 2>&1); $backupCode = $LASTEXITCODE
if ($backupCode -ne 3 -or $backupRed.Count -ne 1 -or $backupRed[0] -ne 'RED_EXPECTED_MISSING_IMPLEMENTATION') { $backupRed; exit 1 }
'RED_CONTRACTS=EXPECTED_FAILURE'
```

Expected: both syntax checks exit 0 and wrapper prints only `RED_CONTRACTS=EXPECTED_FAILURE`. Fixture/schema/parser/secret-safety errors make the wrapper fail.

### Task 6: Implement the sanitized read-only preflight adapter

**Target binding authority (2026-09-05):** Apply the design's Target binding amendment in full. Its separate TargetBindingV1 and GATE1_TARGET_BINDING_SHA256 supersede the old instruction to compare a SHA-256 with environment-inventories.targetFingerprint. Preserve that legacy MD5 as state evidence and preserve acquired artifacts. Implement Direct-only private binding validation, validated minimal libpq environment, provider-CA and verify-full enforcement, zero-transport rejection tests and explicit missing-evidence blocking. No real remote execution or binding enrollment is authorized. Operator/backup/Vault evidence not yet implemented remains UNVERIFIED; do not synthesize a full-Gate PASS.

**Bounded source ownership:** luna-implementer owns only operator-contract.mjs, preflight-readonly.mjs and preflight-readonly-contract.mjs with the existing comment-translator-paid-core-v1-gate1- prefix in scripts/. Parent owns these documents and task.md. Test binding parsing and transport injection through these files; proposed additional paths return to the parent first. Parent acceptance requires the focused contract and unconfigured CLI exit-2 check; full DB integration is not repeated for this slice.

**Files:**

- Create: `scripts/comment-translator-paid-core-v1-gate1-preflight-readonly.mjs`
- Create: `scripts/comment-translator-paid-core-v1-gate1-preflight-readonly-contract.mjs`

- [ ] **Step 1: Implement target-explicit, SELECT-only execution**

Follow the existing read-only triage runner pattern: require target kind `preview|production`, match a caller-supplied expected SHA-256 target fingerprint to the named environment-inventory fixture, and accept connection context only through process environment. Start `BEGIN ISOLATION LEVEL REPEATABLE READ READ ONLY`, set `statement_timeout='30s'` and `lock_timeout='5s'`, execute only fixed SELECT/catalog/advisor commands, cap stdout and stderr independently at 1 MiB and every result set at 10,000 rows, and roll back. Print exactly one compact JSON object with schema `{schemaVersion,target,status,historyCount,historyDigest,pendingDigest,legacyDigest,sourceEraDigest,canonicalRelationsDigest,canonicalRpcAclDigest,archiveDigest,limitsDigest,schedulerEvidenceDigest,maintenanceTransportPrivilegeDigest,extensions:{pgNet,pgCron,vault},storageObjectCount,grantsRlsDigest,vault:{total,reservedNames,recordIdDigest},cron:{matching,active,jobIdDigest,commandDigest,runDelta},schedulerProcessCount,advisor:{baselineDigest,currentDigest,inScopeNew,highCriticalNew},mutationCounts:{ddl,dml,rpc,remote,total},blockedReason}`. Never print project ID, URL, DB URL, SQL body, Vault value, raw error, row value, private identifier, or token. Missing tools/context returns `status:"blocked-context-absent"`, a fixed reason enum, and all mutation counters 0.

- [ ] **Step 2: Cover all design evidence**

Read migration history; three Paid-legacy fingerprints/row counts/dependencies; six-table/seven-function source-era read-only safety manifest; canonical bridge/RPC manifest; extension status; Vault total/name counts without `decrypted_secrets`; Cron exact row/active/run counts; scheduler process count; current grants/RLS; and scoped security/performance advisor counts. Do not invoke any RPC or maintenance function.

- [ ] **Step 3: Contract the SQL allowlist and sensitive-output boundary**

Parse the runner source and reject DDL, DML, `vault.decrypted_secrets`, RPC/maintenance invocation, `cron.schedule`, `cron.alter_job`, history repair, `db push`, `apply_migration`, project pause, project creation, or deploy strings in executable paths. The complete callable SQL allowlist is `format_type`, `pg_get_expr`, `pg_get_constraintdef`, `pg_get_indexdef`, `pg_get_triggerdef`, `pg_get_functiondef`, `pg_get_userbyid`, `aclexplode`, `has_schema_privilege`, `has_table_privilege`, `has_function_privilege`, `md5`, `digest`, `encode`, `coalesce`, `lower`, `count`, `max`, `cardinality`, `octet_length`, `array_agg`, `jsonb_agg`, `jsonb_build_object`, `to_jsonb`, `unnest`, `current_setting`, `transaction_timestamp`, and `version`; every other callable name fails source validation. Schema-qualified application/private RPC calls are rejected. Fixture-run the parser against success, wrong target fingerprint, missing, duplicate, drifted, raw-error, oversized, over-row-limit, and secret-shaped outputs.

- [ ] **Step 4: Run GREEN**

Run:

```powershell
node --check scripts/comment-translator-paid-core-v1-gate1-preflight-readonly.mjs
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
node --check scripts/comment-translator-paid-core-v1-gate1-preflight-readonly-contract.mjs
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
node scripts/comment-translator-paid-core-v1-gate1-preflight-readonly-contract.mjs
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
$preflightRaw = @(& node scripts/comment-translator-paid-core-v1-gate1-preflight-readonly.mjs --target production 2>&1); $preflightCode = $LASTEXITCODE
if ($preflightCode -ne 2 -or $preflightRaw.Count -ne 1) { $preflightRaw; exit 1 }
try { $preflight = $preflightRaw[0] | ConvertFrom-Json -ErrorAction Stop } catch { exit 1 }
if ($preflight.status -ne 'blocked-context-absent' -or $preflight.mutationCounts.total -ne 0) { exit 1 }
if ($preflightRaw[0] -match '(?i)(postgres(?:ql)?://|Bearer\s|sb_secret_|eyJ[A-Za-z0-9_-]{20,}\.)') { exit 1 }
'UNCONFIGURED_PREFLIGHT=EXPECTED_BLOCK'
```

Expected: both syntax checks and the contract exit 0; wrapper prints `UNCONFIGURED_PREFLIGHT=EXPECTED_BLOCK` only after proving the unconfigured run exits 2 with the exact blocked status, zero mutations, and no secret/private identifier.

### Task 7: Implement backup/recovery preflight without executing backup or pause

**Files:**

- Create: `scripts/comment-translator-paid-core-v1-gate1-backup-recovery-preflight.ps1`
- Test: `scripts/comment-translator-paid-core-v1-gate1-backup-recovery-contract.mjs`

- [ ] **Step 1: Add local tool checks**

Resolve exact local Supabase CLI path and require `2.109.0`; require Docker availability; require PostgreSQL `psql`/`pg_dump` major 17; and record exact versions/container digest labels only. Do not install tools.

- [ ] **Step 2: Add dry-run command construction**

Construct but do not run the approved sequence. First parse and snapshot the exact CLI `db dump --help` and `db diff --help` output for version `2.109.0`. Build these argument arrays, with resolved artifact paths and connection material supplied only through environment:

```text
supabase = ["db","dump","--linked","--dry-run"]
authStorageDiff = ["db","diff","--linked","--schema","auth,storage","--file",auth_storage_changes.sql]
roles = ["db","dump","--linked","--role-only","--file",roles.sql]
schema = ["--schema-only","--snapshot",snapshot,"--file",schema.sql,...reviewedSupabaseFilterArgs]
data = ["--data-only","--snapshot",snapshot,"--file",data.sql,...reviewedSupabaseFilterArgs]
historySchema = ["--schema-only","--schema=supabase_migrations","--snapshot",snapshot,"--file",history_schema.sql]
historyData = ["--data-only","--schema=supabase_migrations","--snapshot",snapshot,"--file",history_data.sql]
restoreRoles = ["--no-psqlrc","--set=ON_ERROR_STOP=1","--file",roles.sql]
restoreSchema = ["--no-psqlrc","--set=ON_ERROR_STOP=1","--file",schema.sql]
restoreAuthStorage = ["--no-psqlrc","--set=ON_ERROR_STOP=1","--file",auth_storage_changes.sql]
restoreData = ["--no-psqlrc","--set=ON_ERROR_STOP=1","--file",data.sql]
restoreHistorySchema = ["--no-psqlrc","--set=ON_ERROR_STOP=1","--file",history_schema.sql]
restoreHistoryData = ["--no-psqlrc","--set=ON_ERROR_STOP=1","--file",history_data.sql]
```

The held `psql --no-psqlrc --set=ON_ERROR_STOP=1` session executes `BEGIN ISOLATION LEVEL REPEATABLE READ READ ONLY; SELECT transaction_timestamp(), pg_export_snapshot();`, returns `T0` and snapshot through a non-logged pipe, remains open while `schema`, `data`, `historySchema`, and `historyData` run under PostgreSQL-17 `pg_dump`, then commits only after all four exit 0. `roles` is explicitly outside the snapshot. Plain SQL restore uses the six `psql` arrays in listed order; `pg_restore --version` is checked but `pg_restore` is not used for plain-format files. Reject `--db-url`, inline URL/password, `--password`, shell interpolation, missing `--snapshot`, reordered restore, and any unsupported flag relative to pinned `--help`. The two `--linked` discovery arrays are emitted but never spawned in source-only mode; later execution requires its named read-only backup approval.

Use a caller-supplied restricted artifact directory, resolve it, reject repository/workspace/home roots and existing nonempty directories, and never echo connection material. Require preflight Vault total/reserved counts = 0 and Storage object count = 0. A nonempty Auth/Storage diff requires a present, separately reviewed `auth_storage_changes.sql` digest before any restore plan can pass. Generate SHA-256 checks and post-restore assertions for exact history digests, aggregate row counts, Auth dependencies/user count, zero Storage objects, grants/RLS, bridge/canonical replay, and local-only endpoint evidence.

- [ ] **Step 3: Add watchdog dry-run states**

Default output must be `PRE_DDL_UNARMED` and `mutation_status=not-run`. Model deadlines and exact project status checks without calling pause/provision/cutover. Distinct approval tokens are required for recovery cost/capacity, provisioning, region/extensions/Auth configuration, source pause request, confirmed source inaccessibility, restore, endpoint/credential cutover, client reauthentication, reopening writes, and later unpause/deletion. Restore state is unreachable unless the exact source fingerprint is confirmed inaccessible by `T0+20m`; requested/pending/wrong/late status remains `NO-GO`. The script must have no implicit retry, no sleep over 60 seconds, no cleanup, and no destructive path.

- [ ] **Step 4: Run the contract**

Run:

```powershell
$tokens = $null; $errors = $null
[System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path scripts/comment-translator-paid-core-v1-gate1-backup-recovery-preflight.ps1), [ref]$tokens, [ref]$errors) | Out-Null
if ($errors.Count -ne 0) { $errors; exit 1 }
node --check scripts/comment-translator-paid-core-v1-gate1-backup-recovery-contract.mjs
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
node scripts/comment-translator-paid-core-v1-gate1-backup-recovery-contract.mjs
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
pwsh -NoProfile -File scripts/comment-translator-paid-core-v1-gate1-backup-recovery-preflight.ps1 -Mode DryRun
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
```

Expected: parser and contract exit 0 with `backup-contract=pass restore-order=6 snapshot-dumps=4`; default preflight exits 0 with only sanitized `state=PRE_DDL_UNARMED mutation_status=not-run mutation_count=0`. Tool-missing fixtures pass as blocked states, unsafe artifact paths fail, and no pause/provision/restore process is spawned.

**Parent tool-specific correction (2026-09-06):** installed PostgreSQL 17.11 `pg_dump --help` confirms COPY is the default data format and has no `--use-copy` option. That flag belongs to Supabase CLI `db dump` only. Native data/history argument arrays above omit it; reject `--inserts`, `--column-inserts`, and `--rows-per-insert` in native dump/filter arguments to retain COPY. Update the matching backup command-plan producer and regression tests before executing backup; existing source-only passes are not proof of executable native arguments. This corrects tool syntax, not snapshot consistency, filtering, restore order, or recovery requirements.

### Task 8: Write the exact operator authority

**Parent execution clarification (2026-09-06):** finish the required actual-local Vault transaction test separately from synthetic contracts. The explicit integration phase must create a uniquely named, owned, network-disabled disposable container from the already cached Supabase PostgreSQL 17 image pinned by RepoDigest (`--pull=never`), using temporary storage and no existing DB/volume. Reuse the PowerShell runner's exact transaction generator in memory through a local Docker/psql test adapter; do not substitute a second SQL implementation. This proves the transaction on real Vault, not the hosted entry point, Direct/TLS binding, or hosted readiness. Default execution must remain source-only and must not start Docker. Validate actual extension availability and PostgreSQL major, success with two distinct encrypted records, second-create failure with independent zero-row readback, pre-existing/partial-name rejection and record preservation, duplicate/re-run rejection, and secret-safe bound inputs. Never read decrypted values. Test-only fault injection is restricted to the new disposable database. Missing tools/image/Vault fail as SETUP_BLOCKED. Report sanitized counts/booleans, versions and image digest only; clean only verified resources generated by that invocation and confirm residue zero. Keep remote Write blocked until hosted prerequisite verification exists. Synthetic, actual-local, and hosted results remain separate evidence states. This clarification does not relax any Vault/security/GO requirement.

**Files:**

- Create: `docs/active/COMMENT_TRANSLATOR_PAID_V1_GATE1_PRODUCTION_SUPABASE_READINESS.md`
- Create: `scripts/comment-translator-paid-core-v1-gate1-vault-write.ps1`
- Create: `scripts/comment-translator-paid-core-v1-gate1-vault-binding-integration-contract.mjs`
- Test: `scripts/comment-translator-paid-core-v1-gate1-operator-contract.mjs`

**Parent local bootstrap clarification (2026-09-06):** the cached isolated image's `postgres` role can create the fresh case database and Vault schema but is not a superuser; `supabase_vault` is untrusted and requires a superuser for installation. In the newly owned local target only, retain schema scaffolding and normal verification/runner transactions as `postgres`. Install the extension only through the image's existing `supabase_admin`, after checking role existence, login/superuser capability and container ownership. Do not grant privileges, alter roles/owners, or use the bootstrap identity for secret transactions/readback. Return immediately to `postgres` for strict extension/function metadata and actual tests. Missing bootstrap capability is SETUP_BLOCKED, not permission to widen privileges. Keep setup-session NOTICE suppression, strict exit/stderr checks, pinned image/context, network-none/tmpfs/log-none, and owned-only cleanup. Hosted runners and hosted approvals are not affected.

- [ ] **Step 1: Record current evidence and source-only status**

Separate `repository-implemented`, `locally-verified`, `Preview-not-applied`, `production-not-applied`, `backup-not-run`, `Vault-not-written`, `Cron-not-configured`, `activation-closed`, and artifact identity states. Record source artifact identity as literal `UNCOMMITTED/UNKNOWN` until an actual reviewed commit exists; after commit, replace that literal with the exact commit and tree rather than retaining placeholder syntax. Never invent remote evidence.

- [ ] **Step 2: Add copy-ready approval packets**

Provide separate packets for: Preview exact `pending26` apply; Docker/PostgreSQL-17 tooling setup; recovery-project cost/capacity; recovery-project provisioning; region/extensions/Auth configuration; rehearsal backup/restore; final read-only backup; `RPO <= 20 minutes` and watchdog acceptance; production exact `pending34` apply; two-name Vault write; one inactive Cron configuration; incident source pause; source-inaccessibility confirmation; recovery restore; endpoint/credential cutover; client reauthentication; reopening writes; later source unpause/deletion; commit/push/PR; and later activation. Every packet names exact scope and exclusions, and no packet text can satisfy another state.

- [ ] **Step 3: Add exact run/readback/stop procedures**

Document local CLI `2.109.0` commands from `--help`, exact migration fixture arrays, exported-snapshot backup/restore order, rehearsal assertions, watchdog states, exact source fingerprint pause confirmation by `T0+20m`, separately approved recovery provisioning/configuration/restore/cutover/reauth/reopen sequence, manifest comparisons, scoped advisor baseline/delta, exact Vault transaction, exact Cron transaction and 10-minute observation, source-era non-mutation, rollback boundaries, and fail-closed stop conditions. Original-project unpause/deletion remains a later separate decision. Do not include credential values or commands that embed secrets in command-line arguments.

- [ ] **Step 4: Run operator contracts GREEN**

Run:

```powershell
node --check scripts/comment-translator-paid-core-v1-gate1-operator-contract.mjs
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
node scripts/comment-translator-paid-core-v1-gate1-operator-contract.mjs
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
node scripts/comment-translator-paid-core-v1-gate1-backup-recovery-contract.mjs
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
node --check scripts/comment-translator-paid-core-v1-gate1-vault-binding-integration-contract.mjs
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
node scripts/comment-translator-paid-core-v1-gate1-vault-binding-integration-contract.mjs
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
```

Expected: all commands exit 0 with `operator-contract=pass approval-states=isolated remote-calls=0 mutations=0`, `backup-contract=pass`, and `vault-binding-integration=pass success-rows=2 rollback-rows=0 secret-output=0`; no source-only path performs a remote call or mutation. A missing local Supabase/Vault integration environment is reported only as `SETUP_BLOCKED` and prevents a full local-integration PASS.

---

## Chunk 3: Documentation, root verification, and implementation handoff

### Task 9: Update active authority without broad history churn

**Files:**

- Modify: `docs/active/COMMENT_TRANSLATOR_PAID_V1_RUNBOOK.md`
- Modify: `task.md`

- [x] **Step 1: Link the new Gate 1 authority from the runbook**

Add only the minimum section necessary to route operators to the new document. Preserve Task 11 closed evidence and state explicitly that source readiness does not prove Preview/production apply, backup, Vault, Cron, deploy, or activation.

- [x] **Step 2: Add one concise task status entry**

Do not write the final `task.md` status yet. Prepare the exact one-line shape with branch, source scope, `verification=pending-review`, and remaining approval gates only for the documentation contract fixture; Task 11 writes the real line after root verification and independent review succeed. This prevents a premature or stale `locally-verified` claim.

- [x] **Step 3: Run documentation contracts**

The operator contract must assert the runbook/task markers and reject stale claims such as `GO`, `production-applied`, `backup-pass`, `Vault-configured`, or `Cron-configured` before external evidence exists.

Task9 source-only acceptance (2026-09-06): parent contract/syntax/diff checks passed at operator SHA-256 `9d5544032aaabfe459629b203489850cc54786381d8d5932a80918eea7bf0891`; independent re-review accepted the bounded Markdown P2 correction with21 added cases. The pending task-status line remains a test fixture, not final Task11 acceptance. No hosted GO, apply, backup, Vault, Cron or activation evidence is created by this completion.

### Task 10: Root-owned verification

**Engineering phase2 verification/review (2026-09-08):**29 Node syntax checks and68 source checks passed; the single default Vault probe is SETUP_BLOCKED by design, not an actual-local failure or a converted PASS. Earlier accepted actual-local Vault and R4 execution evidence remains separate; their execution paths are unchanged in this phase (current Vault harness7ee8b314fb6ba965fc84a1c4ef6327981f4b033080dfb2ef97216857b028e24b, DB harnessfedaf12b3ca90f48e2ba53e971ad1e3a70305b57d0dc554b56d042578d7988e7). No unchanged actual matrix was repeated. Final persistence deadline regression demonstrated RED and now passes: final11 stage groups plus CLI diagnostics (12 test-runner results) PASS; risk-acceptance and migration-inventory checks and changed-file lint PASS. TypeScript, repository lint(0 errors/2 existing warnings), Next and Cloudflare production builds PASS; three PowerShell parser checks/four actual no-mutation entry guards PASS. Root reviewed the current source/approved specification and found no unresolved material issue after the recorded repairs; no new independent review or subagents under the user's instruction. Source publication is authorized, while merge/hosted apply/deploy/activation remain outside this phase. The saved-artifact diagnostic still exits1 for the known Production canonical-wrapper omission; both saved hashes match and Preview is valid. It remains a hosted evidence blocker and is not counted as a source test pass.

Final changed code hashes: stage library893d3f4092f636424735a66424db14c5e999e1013722506d5e94420e9c89c3ed; stage test7366c5a8d54237e8941e84a383f95685bed9b2dc81d49a7ba636b385531d6e20; CLI diagnostics7a3315252e5ff5fc3ebd667ed29ac9a3f23a7a0e25d2d215cb6a7e4cfc86a77d; migration inventory399788bf956072e0df060c1f5c7d19975d27b4bc66228b5018add300d020faed; risk-acceptance59bfba8e5974d1e2df874ecd250709d7f9e6ec5c194c66103a8135d0bf329345. These are working-file verification hashes; immutable Git artifact/producer authority must still be established separately after publication.


**2026-09-08 publication scope reconciliation:** Current HEAD and fetched Preview integration are both2775fba1f5ca687d23b25c88286ec5125d1663ec. Use this independently verified publication base for the final PR delta; do not infer an unknown historical handoff value. The two original catalog/SQL files are already present unchanged in this base via a778b7f/PR813, so remove only their zero-delta entries from the final changed-path list. Include the approved gate0a2 history-contract adaptation required by final56. Preserve AGENTS.md and .codex/config.toml against their captured exact hashes, exclude them from the77-path publication list and leave them unstaged. No reset or discard. This supersedes historical clean-worktree assumptions for current publication, while ancestry to the approved design base remains required. The current user authorizes commit/push/PR after verification, not merge/deploy/activation.

**Final review repairs:** Parent review found the final-backup stage checked raw checksum completion but omitted the persistence completion deadline. A regression first demonstrated late persistence incorrectly accepted; the stage now also requires its authority-bound completedAt <= server T0+5 minutes. Final11 stage groups and the CLI diagnostics test pass. Rename the diagnostics VM variable from module to runnerModule to satisfy the existing Next lint rule without altering behavior. The obsolete acquisition -Mode DryRun recipe is replaced by its real mandatory-parameter, noninteractive fail-before-body check. No source collector interface or remote authority is expanded. The secret scanner identified one fixed synthetic Authorization canary; it is now assembled at runtime with identical value, preserving its rejection test and avoiding a source false positive. Final SQL scope verification now uses the existing lexical parser to require exactly the two original LF function definitions plus four exact revokes in A3, with additional-DDL/body-drift/missing-revoke negatives. This replaces the obsolete text-wide DML grep that incorrectly treated preserved function bodies as executed migration DML; bridge/marker/extension checks remain unchanged.


**2026-09-08 inventory reconciliation:** Current read-only enumeration finds58 matched contract files: the prior53 regression identities plus the four approved R3 evidence/source-evidence/postapply-catalog/postapply-acquire contracts and one saved-artifact diagnostic. The explicit regression list is now57, with56 source-only plus1 separately executed local database integration; no prior check was removed. The restore-sql module/test are explicitly added to syntax, focused tests and final changed-path allowlist. Their .test.mjs naming is outside the Step2 contract matcher, so Step1 explicitly runs that test. This supersedes the historical53/54 counts below. Unknown additions, missing files and duplicates still fail before execution. Enumeration validates the inventory only, not unexecuted checks, source readiness or GO.

**Files:** verify every changed file; do not modify unrelated files.

- [ ] **Step 1: Run focused syntax and contracts**

```powershell
$gate1Node = @(
  'scripts/lib/comment-translator-paid-core-v1-gate1-stage-evidence.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-stage-evidence.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-observe.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-backup-artifacts.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-backup-capture.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-backup-snapshot.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-observe.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-artifacts.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-capture.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-snapshot.test.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-restore-sql.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-restore-sql.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-catalog-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-migration-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-database-integration-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-operator-contract.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-evidence.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-source-evidence.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-postapply-catalog.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-postapply-acquire.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-evidence-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-source-evidence-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-postapply-catalog-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-postapply-acquire-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-postapply-query.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-preflight-readonly.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-preflight-readonly-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-recovery-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-vault-binding-integration-contract.mjs'
)
foreach ($file in $gate1Node) { node --check $file; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }

$gate1Contracts = @(
  'scripts/comment-translator-paid-core-v1-gate1-stage-evidence.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-observe.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-artifacts.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-capture.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-snapshot.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-restore-sql.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-catalog-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-migration-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-preflight-readonly-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-recovery-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-evidence-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-source-evidence-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-postapply-catalog-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-postapply-acquire-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-postapply-query.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-operator-contract.mjs'
)
foreach ($file in $gate1Contracts) { node $file; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }

foreach ($file in @('scripts/comment-translator-paid-core-v1-gate1-vault-binding-integration-contract.mjs')) {
  $result = @(& node $file 2>&1); $code = $LASTEXITCODE
  if ($code -eq 0) { continue }
  if ($code -eq 2 -and $result.Count -eq 1 -and $result[0] -match '^SETUP_BLOCKED:') { $result; continue }
  $result; exit 1
}

$gate1PowerShell = @(
  'scripts/comment-translator-paid-core-v1-gate1-catalog-acquire-readonly.ps1',
  'scripts/comment-translator-paid-core-v1-gate1-backup-recovery-preflight.ps1',
  'scripts/comment-translator-paid-core-v1-gate1-vault-write.ps1'
)
foreach ($file in $gate1PowerShell) {
  $tokens=$null; $errors=$null
  [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path $file),[ref]$tokens,[ref]$errors) | Out-Null
  if ($errors.Count -ne 0) { $errors; exit 1 }
}

# The acquisition script has mandatory target/destination parameters and no DryRun mode.
# NonInteractive binding must reject before any script body, prompt, or native query.
$acquireRaw = @(& pwsh -NoProfile -NonInteractive -File scripts/comment-translator-paid-core-v1-gate1-catalog-acquire-readonly.ps1 2>&1); $acquireCode=$LASTEXITCODE
if ($acquireCode -ne 1 -or (($acquireRaw -join " ") -notmatch 'Environment.*DestinationRoot')) { exit 1 }

$backupRaw = @(& pwsh -NoProfile -File scripts/comment-translator-paid-core-v1-gate1-backup-recovery-preflight.ps1 -Mode DryRun 2>&1); $backupCode=$LASTEXITCODE
if ($backupCode -ne 0 -or $backupRaw.Count -ne 1 -or $backupRaw[0] -ne 'state=PRE_DDL_UNARMED mutation_status=not-run mutation_count=0') { $backupRaw; exit 1 }

$vaultRaw = @(& pwsh -NoProfile -File scripts/comment-translator-paid-core-v1-gate1-vault-write.ps1 -Mode Preflight 2>&1); $vaultCode=$LASTEXITCODE
if ($vaultCode -ne 2 -or $vaultRaw.Count -ne 1) { $vaultRaw; exit 1 }
$vault = $vaultRaw[0] | ConvertFrom-Json
if (Compare-Object @('mutationCount','status') @($vault.PSObject.Properties.Name | Sort-Object)) { exit 1 }
if ($vault.status -ne 'blocked-approval-absent' -or $vault.mutationCount -ne 0) { exit 1 }

$preflightRaw = @(& node scripts/comment-translator-paid-core-v1-gate1-preflight-readonly.mjs --target production 2>&1); $preflightCode=$LASTEXITCODE
if ($preflightCode -ne 2 -or $preflightRaw.Count -ne 1) { $preflightRaw; exit 1 }
$preflight = $preflightRaw[0] | ConvertFrom-Json
$preflightKeys = @('advisor','archiveDigest','blockedReason','canonicalRelationsDigest','canonicalRpcAclDigest','cron','extensions','grantsRlsDigest','historyCount','historyDigest','legacyDigest','limitsDigest','maintenanceTransportPrivilegeDigest','mutationCounts','pendingDigest','schedulerEvidenceDigest','schedulerProcessCount','schemaVersion','sourceEraDigest','status','storageObjectCount','target','vault') | Sort-Object
if (Compare-Object $preflightKeys @($preflight.PSObject.Properties.Name | Sort-Object)) { exit 1 }
if (Compare-Object @('pgCron','pgNet','vault') @($preflight.extensions.PSObject.Properties.Name | Sort-Object)) { exit 1 }
if (Compare-Object @('recordIdDigest','reservedNames','total') @($preflight.vault.PSObject.Properties.Name | Sort-Object)) { exit 1 }
if (Compare-Object @('active','commandDigest','jobIdDigest','matching','runDelta') @($preflight.cron.PSObject.Properties.Name | Sort-Object)) { exit 1 }
if (Compare-Object @('baselineDigest','currentDigest','highCriticalNew','inScopeNew') @($preflight.advisor.PSObject.Properties.Name | Sort-Object)) { exit 1 }
if (Compare-Object @('ddl','dml','remote','rpc','total') @($preflight.mutationCounts.PSObject.Properties.Name | Sort-Object)) { exit 1 }
if ($preflight.status -ne 'blocked-context-absent' -or $preflight.mutationCounts.total -ne 0) { exit 1 }
$allEntryOutput = ($acquireRaw + $backupRaw + $vaultRaw + $preflightRaw) -join "`n"
if ($allEntryOutput -match '(?i)(postgres(?:ql)?://[^\s]+|Authorization\s*:\s*\S+|Bearer\s+[A-Za-z0-9_.-]{20,}|sb_(?:secret|publishable)_[A-Za-z0-9_-]{20,}|eyJ[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,})') { exit 1 }
```

Expected: all source/fixture contracts PASS; acquisition rejects missing mandatory target/destination before its body, and backup dry run returns sanitized `not-run`; Vault and read-only entry points return the exact blocked statuses with zero mutation; integration checks either PASS or emit one sanitized `SETUP_BLOCKED:` line and exit 2. Any setup block prevents claiming full PostgreSQL integration coverage and is reported precisely rather than converted to PASS.

Parent entrypoint safety correction (2026-09-06): the database-integration contract is not a source-only default probe. Missing legacy/artifact inputs produce exit1 FAIL, and valid inputs permit Docker/DB execution without an extra flag. Run it separately, once, only after pinning the approved local inputs, image/tool readiness and fresh disposable ownership; it remains mandatory actual-local evidence. Do not launch it from the source-only loops or relabel its failures SETUP_BLOCKED. The Vault contract's default source-only behavior is independently tested and remains separate from its explicit actual-local phase.

- [ ] **Step 2: Run existing Paid and Supabase regression bundles**

Use this exact ordered 57-file inventory; missing, duplicate, or additional matched files fail before execution:

Parent inventory classification (2026-09-06): current matching files are the53 source-regression entries below plus exactly one saved-artifact diagnostic, `scripts/comment-translator-paid-core-v1-gate1-catalog-readback-contract.mjs`. That diagnostic requires explicit saved target directories and intentionally reports incomplete Production wrapper evidence; it is not a no-input synthetic regression. Validate the exact54-file union before running the unchanged53-file regression list. Do not broadly exclude `readback` filenames or accept unknown additions. Run/report the artifact diagnostic separately with approved immutable inputs; missing/FAIL evidence is never converted to PASS and still prevents full hosted/Gate1 acceptance. This separates evidence classes rather than waiving readback completeness.

```powershell
$expectedRegression = @(
  'scripts/comment-translator-paid-core-v1-gate1-evidence-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-postapply-acquire-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-postapply-catalog-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-source-evidence-contract.mjs',
  'scripts/comment-translator-paid-core-v1-concurrency-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate0a-billing-lifecycle-entitlement-rpc-behavior-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate0a-capacity-read-repair-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate0a-checkout-expiry-finalize-lease-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate0a-checkout-recovery-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate0a-function-privilege-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate0a-legacy-expiry-compatibility-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate0a-lifecycle-read-repair-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate0a-poll-budget-repair-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate0a-recovery-floor-second-canonicalization-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate0a-recovery-window-floor-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate0a-stripe-failure-diagnostic-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate0a-tax-location-minimization-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate0a-unbound-hold-recovery-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate0a2-migration-history-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-recovery-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-catalog-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-database-integration-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-migration-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-operator-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-preflight-readonly-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-vault-binding-integration-contract.mjs',
  'scripts/comment-translator-paid-core-v1-migration-parser-contract.mjs',
  'scripts/comment-translator-paid-core-v1-schema-contract.mjs',
  'scripts/comment-translator-paid-core-v1-store-contract.mjs',
  'scripts/comment-translator-paid-core-v1-stripe-checkout-customer-selector-contract.mjs',
  'scripts/comment-translator-paid-core-v1-task10-legal-security-contract.mjs',
  'scripts/comment-translator-paid-core-v1-task11-local-load-fixture-contract.mjs',
  'scripts/comment-translator-paid-core-v1-task11-postgres-runtime-repair-contract.mjs',
  'scripts/comment-translator-paid-core-v1-task11-preview-load-harness-contract.mjs',
  'scripts/comment-translator-paid-core-v1-task11-tax-checkout-policy-contract.mjs',
  'scripts/comment-translator-paid-core-v1-task4-behavioral-contract.mjs',
  'scripts/comment-translator-paid-core-v1-task4-capacity-consent-fixtures-contract.mjs',
  'scripts/comment-translator-paid-core-v1-task4-checkout-policy-contract.mjs',
  'scripts/comment-translator-paid-core-v1-task5-usage-cost-contract.mjs',
  'scripts/comment-translator-paid-core-v1-task6-provider-contract.mjs',
  'scripts/comment-translator-paid-core-v1-task7-session-contract.mjs',
  'scripts/comment-translator-paid-core-v1-task8-ui-contract.mjs',
  'scripts/comment-translator-paid-core-v1-task9-retention-contract.mjs',
  'scripts/comment-translator-paid-core-v1-webhook-contract.mjs',
  'scripts/comment-translator-supabase-current-grant-drift-readonly-triage-contract.mjs',
  'scripts/comment-translator-supabase-current-grant-remediation-apply-preflight-contract.mjs',
  'scripts/comment-translator-supabase-current-grant-remediation-approval-preflight-contract.mjs',
  'scripts/comment-translator-supabase-default-privileges-guard-contract.mjs',
  'scripts/comment-translator-supabase-default-privileges-managed-owner-blocker-contract.mjs',
  'scripts/comment-translator-supabase-default-privileges-owner-specific-remediation-preflight-contract.mjs',
  'scripts/comment-translator-supabase-default-privileges-permission-capable-apply-contract.mjs',
  'scripts/comment-translator-supabase-default-privileges-privileged-apply-path-preflight-contract.mjs',
  'scripts/comment-translator-supabase-default-privileges-remediation-apply-contract.mjs',
  'scripts/comment-translator-supabase-default-privileges-remediation-execution-preflight-contract.mjs',
  'scripts/comment-translator-supabase-default-privileges-remote-remediation-approval-contract.mjs',
  'scripts/comment-translator-supabase-default-privileges-risk-acceptance-contract.mjs',
  'scripts/comment-translator-supabase-default-privileges-support-pending-contract.mjs'
)
$expectedReadbackDiagnostics = @('scripts/comment-translator-paid-core-v1-gate1-catalog-readback-contract.mjs')
$expectedMatchedContracts = @($expectedRegression + $expectedReadbackDiagnostics | Sort-Object -Unique)
$actualMatchedContracts = @(rg --files scripts | rg 'comment-translator-paid-core-v1-.*-contract\.mjs$|comment-translator-paid.*migration.*contract\.mjs$|default.*privilege.*contract\.mjs$|grant.*contract\.mjs$' | ForEach-Object { $_ -replace '\\','/' } | Sort-Object -Unique)
if ($expectedRegression.Count -ne 57 -or @($expectedRegression | Sort-Object -Unique).Count -ne 57 -or $expectedMatchedContracts.Count -ne 58 -or (Compare-Object $expectedMatchedContracts $actualMatchedContracts)) { exit 1 }
$actualRegression = @($actualMatchedContracts | Where-Object { $_ -notin $expectedReadbackDiagnostics })
if (Compare-Object $expectedRegression $actualRegression) { exit 1 }
$explicitLocalIntegration = @('scripts/comment-translator-paid-core-v1-gate1-database-integration-contract.mjs')
$sourceOnlyRegression = @($expectedRegression | Where-Object { $_ -notin $explicitLocalIntegration })
if ($sourceOnlyRegression.Count -ne 56 -or $explicitLocalIntegration.Count -ne 1) { exit 1 }
# Apply to this verification process only; never inherit legacy Preview opt-ins.
$task11PreviewKeys = @([Environment]::GetEnvironmentVariables('Process').Keys | Where-Object { $_ -like 'COMMENT_TRANSLATOR_PAID_TASK11_*' })
foreach ($key in $task11PreviewKeys) { [Environment]::SetEnvironmentVariable([string]$key, $null, 'Process') }
foreach ($file in $sourceOnlyRegression) {
  $result = @(& node $file 2>&1); $code = $LASTEXITCODE
  if ($code -eq 0) { continue }
  if ($code -eq 2 -and $result.Count -eq 1 -and $result[0] -match '^SETUP_BLOCKED:') { $result; continue }
  $result; exit 1
}
```

Expected: exact count 57 and every runnable contract PASS. A single sanitized `SETUP_BLOCKED:` result is recorded precisely and is not called a pass; any other nonzero exit fails immediately.

Execution partition is exact:56 source-only entries plus1 separately gated local DB integration, together57 regression identities. The additional saved-artifact diagnostic makes58 matched files overall. The old Task11 Preview load contract spawns a confirmed-target preflight while inheriting environment; clear its entire named process-only prefix before the source-only child invocations, so linked metadata cannot silently cause a remote migration-list call. Missing/unexecuted local integration remains incomplete, never an omitted PASS. Capture child stdout/stderr privately and report only fixed classifications, counts and exit codes; do not print raw failure payloads from the illustrative loops above.

Report the separate saved-artifact diagnostic's actual exit/status, per-target SHA match and missing-field classifications without raw catalog output. Its existing Production canonical-wrapper omission remains incomplete evidence. A source-regression PASS does not override that diagnostic or establish a fresh remote snapshot.

- [ ] **Step 3: Run repository checks**

```powershell
.\node_modules\.bin\tsc.cmd --noEmit --pretty false
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
npm run lint
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
npm run build
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
npm run build:cloudflare
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
git diff --check
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
```

The approved specification requires production build evidence because executable operator scripts are changed, even though application runtime imports are untouched. Expected: TypeScript PASS, lint 0 errors with no new warnings, Next production build PASS, Cloudflare build PASS, and diff check PASS. Node and PowerShell syntax checks are already exhaustive in Step 1.

- [ ] **Step 4: Run security/scope checks**

The final all-task path check below compares the complete implementation delta (including approved design/plan changes and preserved earlier dirty work) with the verified current Preview integration base `GATE1_PUBLICATION_BASE`. It is an end-of-plan acceptance check, not a requirement that every future operator file already exist at amendment checkpoint A1. The two amendment documents are parent-owned; listing them permits their preserved diff, not Luna edits without authority. The existing complete function-manifest filename replaces the originally planned `canonical-rpc.json`; do not create duplicates or rename away existing evidence.

At amendment resume, separately capture a sorted path/SHA-256 inventory of every modified/untracked file before writing. Compare after the amendment slice: changes are limited to A2-A5's explicit implementation paths, with unrelated existing files byte-identical and no deletion/rename. Report that slice check separately from the final all-task exact-path check; an early missing future operator file is not a reason to discard or rebuild the dirty baseline. A preserved file is not automatically accepted for the final PR: it still undergoes the full scope/review checks below.

Run at final all-task acceptance against the exact implementation delta from the current Preview integration commit recorded in `GATE1_PUBLICATION_BASE`:

```powershell
$env:GATE1_PUBLICATION_BASE = '2775fba1f5ca687d23b25c88286ec5125d1663ec'
$preserved = @{
  'AGENTS.md' = 'c5aeb93c9b7dc3c7f7245d27d01b991dac25cf980e841d6cd52fcc47d54d3402'
  '.codex/config.toml' = '872fa089b07468b1f528ca935c761759db49103951368d5527d5f996dfbe8312'
}
foreach ($file in $preserved.Keys) {
  if ((Get-FileHash -LiteralPath $file -Algorithm SHA256).Hash.ToLowerInvariant() -ne $preserved[$file]) { exit 1 }
}
$expectedChanged = @(
  'scripts/comment-translator-paid-core-v1-gate0a2-migration-history-contract.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-stage-evidence.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-stage-evidence.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-observe.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-backup-artifacts.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-backup-capture.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-backup-snapshot.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-observe.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-artifacts.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-capture.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-snapshot.test.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-restore-sql.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-restore-sql.test.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-evidence.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-source-evidence.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-postapply-catalog.mjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-postapply-acquire.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-evidence-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-source-evidence-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-postapply-catalog-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-postapply-acquire-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-postapply-query.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-cli-atomicity-order.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-cli-diagnostics.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-isolated-bridge-replay.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-runtime-cleanup.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-migration-inventory.test.mjs',
  'scripts/fixtures/comment-translator-paid-core-v1-gate1-canonical-authority.json',
  'scripts/lib/comment-translator-paid-core-v1-gate1-cli-runner.cjs',
  'scripts/lib/comment-translator-paid-core-v1-gate1-runtime-cleanup.mjs',
  'supabase/migrations/20260904000000_comment_translator_paid_gate1_a3_canonical_convergence.sql',
  'docs/superpowers/specs/2026-08-31-comment-translator-paid-v1-gate1-production-supabase-design.md',
  'docs/superpowers/plans/2026-09-01-comment-translator-paid-v1-gate1-production-supabase-readiness.md',
  'docs/active/COMMENT_TRANSLATOR_PAID_V1_GATE1_PRODUCTION_SUPABASE_READINESS.md',
  'docs/active/COMMENT_TRANSLATOR_PAID_V1_RUNBOOK.md',
  'task.md',
  'scripts/fixtures/comment-translator-paid-core-v1-gate1-production-history.json',
  'scripts/fixtures/comment-translator-paid-core-v1-gate1-paid-legacy-catalog.json',
  'scripts/fixtures/comment-translator-paid-core-v1-gate1-source-era-catalog.json',
  'scripts/fixtures/comment-translator-paid-core-v1-gate1-bridge-states.json',
  'scripts/fixtures/comment-translator-paid-core-v1-gate1-canonical-function-manifest.json',
  'scripts/fixtures/comment-translator-paid-core-v1-gate1-environment-inventories.json',
  'scripts/comment-translator-paid-core-v1-gate1-catalog-acquire-readonly.ps1',
  'scripts/lib/comment-translator-paid-core-v1-gate1-catalog.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-catalog-contract.test.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-catalog-readback-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-migration-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-database-integration-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-operator-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-preflight-readonly.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-preflight-readonly-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-backup-recovery-preflight.ps1',
  'scripts/comment-translator-paid-core-v1-gate1-backup-recovery-contract.mjs',
  'scripts/comment-translator-paid-core-v1-gate1-vault-write.ps1',
  'scripts/comment-translator-paid-core-v1-gate1-vault-binding-integration-contract.mjs',
  'scripts/comment-translator-supabase-default-privileges-guard-contract.mjs',
  'scripts/comment-translator-supabase-default-privileges-risk-acceptance-contract.mjs',
  'supabase/migrations/20260624040504_comment_translator_real_comments_feed_snapshots.sql',
  'supabase/migrations/20260624141142_account_display_timezone_preference.sql',
  'supabase/migrations/20260722000000_comment_translator_paid_entitlements.sql',
  'supabase/migrations/20260722001000_comment_translator_paid_usage_counters.sql',
  'supabase/migrations/20260722002000_comment_translator_obs_overlay_tokens.sql',
  'supabase/migrations/20260722003000_comment_translator_obs_overlay_browser_sessions.sql',
  'supabase/migrations/20260723000000_comment_translator_moderator_share_tokens.sql',
  'supabase/migrations/20260723001000_comment_translator_moderator_share_browser_sessions.sql',
  'supabase/migrations/20260723002000_comment_translator_custom_dictionary.sql',
  'supabase/migrations/20260723003000_comment_translator_creator_history.sql',
  'supabase/migrations/20260726111154_comment_translator_paid_entitlements.sql',
  'supabase/migrations/20260728135736_comment_translator_paid_usage_counters.sql',
  'supabase/migrations/20260728142220_comment_translator_obs_overlay_tokens.sql',
  'supabase/migrations/20260728145324_comment_translator_obs_overlay_browser_sessions.sql',
  'supabase/migrations/20260728160206_comment_translator_moderator_share_tokens.sql',
  'supabase/migrations/20260728164122_comment_translator_moderator_share_browser_sessions.sql',
  'supabase/migrations/20260728172220_comment_translator_custom_dictionary.sql',
  'supabase/migrations/20260729035621_comment_translator_creator_history.sql',
  'supabase/migrations/20260811000000_comment_translator_paid_v1_legacy_schema_bridge.sql',
  'supabase/migrations/20260814105000_comment_translator_paid_pg_net_extension.sql'
)
$nameStatus = @(git diff --name-status $env:GATE1_PUBLICATION_BASE --)
if ($nameStatus | Where-Object { $_ -match '^(?:D\s|R\d+\s|C\d+\s)' }) { $nameStatus; exit 1 }
$changedFiles = @(
  git diff --name-only $env:GATE1_PUBLICATION_BASE --
  git ls-files --others --exclude-standard
) | ForEach-Object { $_ -replace '\\','/' } | Where-Object { $_ -notin $preserved.Keys } | Sort-Object -Unique
if (Compare-Object ($expectedChanged | Sort-Object) $changedFiles) { Compare-Object ($expectedChanged | Sort-Object) $changedFiles; exit 1 }
if ($changedFiles -match '(^|/)(package(-lock)?\.json|pnpm-lock\.yaml|yarn\.lock)$') { exit 1 }

$absoluteChanged = @($changedFiles | ForEach-Object { (Resolve-Path -LiteralPath $_).Path })
rg -n -i --pcre2 '(?:postgres(?:ql)?://[^\s]+|sb_(?:secret|publishable)_[A-Za-z0-9_-]{20,}|eyJ[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}|Authorization\s*:\s*\S+|Bearer\s+[A-Za-z0-9_.-]{20,})' -- $absoluteChanged
if ($LASTEXITCODE -eq 0) { exit 1 } elseif ($LASTEXITCODE -ne 1) { exit $LASTEXITCODE }

# Validate A3's complete six-statement allowlist against the original LF RPC
# definitions. Do not mistake their non-executed bodies for migration-time DML.
node scripts/comment-translator-paid-core-v1-gate1-migration-inventory.test.mjs
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

node scripts/comment-translator-paid-core-v1-gate1-migration-contract.mjs --phase security-scope
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
node scripts/comment-translator-paid-core-v1-gate1-operator-contract.mjs --phase source-only-scope
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
```

Expected: changed-path allowlist exact; zero secret/DB URL/token hits; zero forbidden destructive DML/DDL or direct `cron.job` writes. Intentional bridge operations are permitted only when the migration contract prints `security-scope=pass destructive=0 direct-cron-writes=0 source-era-mutations=0 bridge-allowlist=pass`; the operator contract prints `remote-calls=0 mutations=0`. Exactly 56 unique migrations is asserted by the migration contract, and package/lock changes are zero.

- [ ] **Step 5: Inspect actual diff**

Before inspection, fail closed on workspace drift:

```powershell
$root = (git rev-parse --show-toplevel) -replace '\\','/'
$branch = git branch --show-current
$head = git rev-parse HEAD
if ($root -ne (($env:GATE1_IMPLEMENTATION_ROOT) -replace '\\','/')) { exit 1 }
if ($branch -ne $env:GATE1_IMPLEMENTATION_BRANCH) { exit 1 }
git merge-base --is-ancestor a275174dc6494a8d159f1fff30318c05fece0107 $head
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
git merge-base --is-ancestor $env:GATE1_PUBLICATION_BASE $head
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
git status --short --branch
git diff --stat $env:GATE1_PUBLICATION_BASE --
git diff --name-status $env:GATE1_PUBLICATION_BASE --
git diff --check $env:GATE1_PUBLICATION_BASE --
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
```

Root owner must inspect the substantive SQL/runner/document diffs and rerun the Step 4 changed-path allowlist against this same publication base. Expected dirty state consists only of the exact allowed implementation paths; delegated reports are claims, not acceptance evidence.

### Task 11: Independent implementation review and bounded repair

**Files:** review-only unless a bounded repair is explicitly listed.

- [ ] **Step 1: Run `sol-reviewer / medium` read-only review**

Review against the approved spec and this plan. Require findings by severity for migration semantics, state fingerprints, RLS/ACL/security-definer posture, source-era non-mutation, approval isolation, backup/watchdog correctness, secret handling, and verification coverage.

- [ ] **Step 2: Route only listed corrections to `sol-repairer / medium`**

The repairer may edit only files named by the reviewer and may not redefine the spec or perform external actions. After repair, root reruns focused and affected regression checks and re-inspects the diff. Then the same read-only `sol-reviewer / medium` must re-review the repaired diff against every listed finding; repeat bounded repair/re-review only for explicit residual findings, and prohibit acceptance while any material finding remains.

- [ ] **Step 3: Record final acceptance status**

Only after root verification and the final independent review report no material finding, update `task.md` with the prepared one-line status using the exact branch, `repository-implemented`, and remaining approval gates. If any required PostgreSQL-17/database/Vault integration is `SETUP_BLOCKED`, record the exact blocker but do not write `locally-verified`, do not accept source readiness, and do not complete this plan. Rerun the documentation contract after this edit, then mandatorily rerun Task 10 Steps 4 and 5 against the final post-repair/post-status diff immediately before Task 12 Step 1 reporting. Source completion may be reported only when all required integration checks PASS and the final state is `locally-verified`. Gate 1 remains `NO-GO` until later Preview, backup/recovery, production, Vault, and inactive Cron approvals execute and pass.

### Task 12: Stop at the Git publication approval boundary

- [ ] **Step 1: Present the verified source diff and checks to the user**

Only after the mandatory final Task 10 Steps 4 and 5 rerun passes on the post-review/post-`task.md` state, report exact branch/worktree, changed files, checks, known warnings, and remaining external gates. Any integration `SETUP_BLOCKED` is reported as incomplete work rather than source completion. Do not commit implementation changes, push, create a PR, merge, deploy, or clean the worktree without the matching user approval. The existing design commits remain local history and do not authorize publication of implementation changes.

- [ ] **Step 2: After explicit commit/push/PR approval only**

Immediately before publication, rerun the Task 10 Step 5 root/branch/base/handoff ancestry checks, the Task 10 Step 4 changed-path allowlist, `git diff --check`, and `git status --short --branch`; stop on any drift or new path. Then, and only under explicit commit/push/PR approval, commit the reviewed implementation with a scoped message, verify the new commit remains on `GATE1_IMPLEMENTATION_BRANCH` and descends from both approved base and `GATE1_PUBLICATION_BASE`, push that exact `codex/` feature branch, create a PR targeting `codex/comment-translator-paid-v1-preview`, and report CI/mergeability without merging. Production and Preview database operations remain separately blocked.

---

## Completion boundary for this plan

This implementation plan is complete only when the source migrations, manifests, contracts, preflight tooling, and operator authority are independently reviewed and all required local PostgreSQL-17/database/Vault integration checks PASS with no `SETUP_BLOCKED` scope. It does **not** make Gate 1 item 1 `GO`. The real goal remains active until the separately approved Preview apply, backup/recovery readiness, production migration, Vault write, inactive Cron configuration, and exact readback all pass with no contradictory evidence.

**2026-09-09 same-snapshot state and finalBackup adapter — LOCAL_VERIFIED / UNPUBLISHED:** The fixed aggregate SQL runs in the same guarded RR/read-only exporter as snapshot export. The normal capture path requires it; caller state and opt-out fields cannot replace native output. Shared pinned dump exclusions select table data plus separately captured history; ONLY counts avoid inherited/foreign partition traversal and double counting. Source state includes full migration-row digest, sorted hashed table identities/counts, Auth-user count and FK definitions, schema/relation/column/function/default ACL plus policy/RLS fingerprints, and zero legacy/Vault/Storage/vector counts. Raw rows, names, policies and ACLs do not leave the SQL as payload; only aggregate counts/digests do. The fixed producer list includes the new state module (11 files).

Acquisition and outer native-process validation retain this state. The outer record now includes backupObservation with the exact finalBackup observation shape, five filename-mapped dump outcomes, exporter closure, manifest/files and Auth-file digest for independent policy comparison. This field does not establish Auth review approval or outer nine-stage authority. It has restore:null and cannot be accepted as rehearsalBackup. The stage timestamp parser now preserves valid native PostgreSQL microsecond/offset timestamps and rejects invalid calendar dates. Existing old records without sourceState are rejected, never upgraded.

Root verification:56 related tests PASS, including the actual stage validator against the generated observation and refusal of rehearsal reuse. An isolated existing postgres:17 image (network none, no ports, tmpfs, synthetic data only) executed the fixed SQL, verified stable readbacks, detected RLS/function/sequence/default privilege changes, and proved an open RR snapshot stays unchanged across another connection's insert while a fresh read sees the increment. Only the uniquely owned temporary container was removed; independent remaining count0. No image pull/install, hosted connection, backup/restore, source publication or subagents. The initial strict-JSON null-prototype test comparison failure was corrected before final acceptance. Logs: ignored source-state-stage-tests.txt and source-state-native-probe.json. ESLint remains setup-blocked without node_modules. Full rehearsal restore/readback, nine-stage outer receipt integration and independently selected execution policy remain pending; Gate1 NO-GO.

**2026-09-09 synthetic six-file restore — LOCAL_VERIFIED / CLI_SETUP_BLOCKED:** Added a bounded restore orchestrator and explicit --run-synthetic local probe. The probe accepts no caller target, credentials, SQL or paths. It uses the fixed local Docker named pipe, existing postgres:17 resolved image ID with pull=never, two uniquely labelled owned containers, no network/ports/binds and tmpfs only. Native roles capture plus four identical-snapshot pg_dump outputs feed the existing roles/schema transformers and data replica envelopes. Six individual psql --single-transaction --file=- / ON_ERROR_STOP restores ran in the required order; complete source-state equality (history22/full digest, row counts, Auth FK/users, ACL/RLS, zero Vault/Storage/vector) passed. A deliberate failing transaction left no created table, proving rollback. Container creation with uncertain CLI return is recovered only by exact planned name/run label/image; cleanup independently confirmed remaining0. Raw SQL remains in memory, no private production backup was used.

The orchestrator snapshots validated input before async work, checks exact six-file names/bytes/SHA256, rejects malformed native results/stderr/deadline/clock rollback, stops subsequent restores on failure, and returns RESTORE_STATE_MATCH_OBSERVED with stageAuthority=false. The native probe returns targetKind=isolated-local-postgres, explicitly not isolated-local-supabase. It must not fill CLI version or bridge/canonical replay fields from expectations, so no complete rehearsalBackup/9-stage acceptance is emitted. Root22 related tests and3 syntax checks PASS; native final result is ignored rehearsal-native-final-v2.json, tests rehearsal-final-tests.txt. Earlier native runs are retained.

Remaining tool prerequisite: neither node_modules/supabase/dist/supabase.js nor node_modules/@supabase/cli-windows-x64/bin/supabase-go.exe exists, and supabase is absent on PATH. package-lock.json pins both to2.109.0 and pins the platform tarball integrity. A minimal next setup can extract only that locked platform package into an ignored tools directory and verify package integrity plus the existing expected executable SHA256; no manifest/lockfile change or npm lifecycle scripts are needed. Existing no-install authorization boundary remains closed pending the user's approval. Actual CLI/Supabase local migration replay, real rehearsal evidence and full nine-stage binding remain pending. No hosted connections/mutations, dependency installation, publication or subagents; Gate1 NO-GO.
2026-09-09 CLI限定setup完了: 承認済み範囲でlock固定platform packageのSHA512とGo exe SHA256を照合し、ignored .tmp/tools/supabase-2.109.0へ配置。native --version=2.109.0 PASS、必要Docker image8種は既存cacheに存在。manifest/lock/node_modules変更・npm lifecycle・image pullなし。既存runnerはnode_modules entry前提のため未接続。旧履歴replayに必要な元SQL4件が未配置で、migration replay/完全rehearsalは未実施。hosted操作なし、Gate1 NO-GO。

2026-09-09 pinned CLI runner接続 LOCAL_VERIFIED: --local-cli=pinned-goの明示選択で固定Go経路を利用し、npm配置がなければ承認済みignored tools配置へ解決。毎回のSHA256/symlink拒否、local transport、bounded runner、既存npm既定を維持。CLI存在preflightをprofile対応。実runner経由native2.109.0 PASS、runtime-cleanup/cli-atomicity-order/postapply-queryの3 tests PASS。元SQL4件のGit履歴8revisionを照合したがbyte/MD5一致0。元SQL・過去catalog証跡不足のためreplay未実施。次は外部保存backupのhistory_dataから4件のみのローカル抽出と既存fixture照合が必要（プロジェクト外読取の範囲確認後）。hosted操作0、Gate1 NO-GO。

2026-09-09 legacy SQL recovery LOCAL_VERIFIED: 明示承認された保存backupのhistory_data.sqlのみread-onlyで解析。22history rowsから対象4件を抽出し、statements.join("\n")/末尾改行追加なしで既存fixtureのbytes/MD5全件一致。既存validateLegacyInputs PASS、parsed statement数17/25/17/25。合計32988 bytesをprivate ACLのignored recovered-legacy-sqlへ保存しreadback照合、secret scan PASS。元backup変更なし、SQL実行/hosted接続なし。CLIと元SQLの準備は解消したが、過去catalog証跡・隔離migration replay/完全rehearsal受理は未解決。Gate1 NO-GO。

2026-09-09 isolated local migration replay LOCAL_VERIFIED: --phase=local-replay --local-cli=pinned-goで既存postapply経路を実行。過去hosted catalogは読まず、fixture一致の旧SQL4件と現在migrationで検証。固定CLI2.109.0/PG17・既存cacheを使用し、privateなproject内scratchで起動。実SQL適用、履歴56件、canonical構造、archive3 tables/3 functionsがPASS。source-era0のためProduction shapeはsource-era-mismatchで拒否されることを確認し、hostedEvidence=falseを維持。関連3 tests/2 syntax PASS。終了後独立read-only inventoryでGate1 container/network/volume各0・scratch directory0。full6file backup restoreとmigration replayの一体実行/9-stage受理は未実施。hosted接続/変更0、Gate1 NO-GO。

2026-09-09 six-file restore + migration replay LOCAL_VERIFIED: --phase=local-restore-replay --local-cli=pinned-goを固定PG17/CLI2.109.0の所有隔離環境で完遂。ローカル26historyから同snapshot4dumpとrolesを取得し、6fileをhash確認してsingle-transaction/ON_ERROR_STOPで復元。復元前後source-state SHA256完全一致、その後history56/canonical構造/archive3tables・3functions PASS。空targetではhistory schema自体がない場合を受理。native診断でpublic12relationのtarget既定ACL混入を検出し、ローカルschema artifactに同snapshot取得のglobal/public既定table ACLの一時解除・正確復元を含めて修正。比較条件は維持し、Production validatorは22固定、local26専用validatorを分離。関連40 tests・8 syntax PASS、ESLintは依存未配置でsetup-blocked。独立inventoryでGate1 container/network/volume各0・scratch0。失敗attempt1–4も保持。これは本番22history/実データ/全source-eraの再現ではなく、Production6fileの完全rehearsal・9-stage受理・source公開は未完了。hosted接続/変更0、Gate1 NO-GO。

2026-09-09 Production rehearsal preparation / default ACL capture LOCAL_VERIFIED・UNPUBLISHED: 検証済みdefault-table-ACL SQL/生成器をbackup-default-acl.mjsへ共通化。Production captureのRR/RO exporterにtableDefaultsを必須追加し、不足/不正/重複/許可外scope-role-privilegeを拒否、freezeしてschema artifactへ結合。対象はpostgresのglobal/public table defaultsとanon/authenticated/service_role。26件ローカル検証と同じ処理を使うがProduction source-stateは22固定を維持。source producer12filesに追加し未公開HEADを拒否することを確認。関連46 tests・5 syntax PASS、共通化後native6file復元state完全一致→history56/canonical/archive PASS、独立cleanup全0。ESLint setup-blocked。旧backupには新しい同snapshot default ACL/正式producer証跡がないため最終T0証拠には昇格させない。順序は公開前レビュー/必要チェック→明示承認によるcommit/push/PRとsource確定→別途承認された最新backup等の取得→本番22history/実データの隔離rehearsal→9-stage判定。本番変更/deployはその後の別承認。今回hosted接続/再取得/変更0、Gate1 NO-GO。

2026-09-09 pre-publication review / lint LOCAL_REVIEWED・UNPUBLISHED: primary agentが実diffと新規moduleを直接レビューし、重大な指摘なし。既存lock固定のnpm ci --ignore-scripts --no-audit --no-fundを作業worktree内cacheで実行（691 packages）、manifest/lockfile SHA256不変。変更23 MJSのESLint --max-warnings=0 PASS、コード修正なし。直前の46 tests/native6file復元＋history56 replayの対象source/log SHA256不変を照合し、受理済み実行は反復しない。過去entryのESLint SETUP_BLOCKEDは今回解消。commit/push/PR・新本番backup・本番rehearsal・9-stage受理は未実施、Gate1 NO-GO。次は明示承認されたsource公開。

2026-09-09 Auth/Storage delta review BLOCKED_ON_BASELINE: merged source1509dabの12producer照合PASS後、承認済みProduction Directをnative PG17.11/verify-full/read-onlyで確認。history22、legacy/Vault/Storage/vector0。Auth/Storage schema-onlyをrestricted artifactへ取得し、既存固定CLI2.109.0/PG17.6の所有ローカル隔離環境と比較。Auth migration77/latest20260625000000は一致、StorageはProduction68/latest67とlocal61/latest60で不一致。初期SQL token集計は本文保持不足のため無効（後続native catalog比較で置換）。これは管理baseline差を含み、review済みuser deltaや空auth_storage_changes.sqlの根拠にしない。schema dumpとversion queryは別snapshot。native CLIの既知TLS問題を回避するためhosted CLI diffは未実行。local cleanup2actions PASS、独立container/network/volume/scratch各0。本番書込み0、fresh6file capture未実施。次は復元先自身の管理baseline/依存関係を満たす経路を確定し、user-executable差分を再レビューする。Gate1 NO-GO。

2026-09-09 Storage baseline release identified: native verify-full/read-onlyでProduction storage.migrationsの68件を取得。公式storage v1.72.11 commit518862dff5b2e4f3386ad51e4a172e91d0ce2ad2のtenant SQL1-67は全件Git blob整合・本番name/hash一致。bootstrap0もpostgres-migrations v5.3.0公式SQLのname/SHA1(filename+SQL)一致、合計68/68。固定local storage v1.61.7は0060までで、差分0061-0067は公式platform更新と確認。v1.72.11は比較候補であり、本番稼働imageや復元先catalog適合の証明ではない。新image取得/新version起動/管理SQL適用は未実施。次は承認済みの使い捨てlocalサービスとして候補版を起動し、完全Auth/Storage catalog・ACL・依存関係を再比較する。本番書込み0、user delta未確定、Gate1 NO-GO。

2026-09-09 Storage v1.72.11 local candidate comparison: 承認済みimageを取得しsha256:d189b8072865b5d8289af9435dee18eb6eef82f465e1d7d092fa9665bbd4ce54へ固定。既存所有local CLI環境内のStorageのみ候補serviceへ切替、local DB alias/network一致を確認しhosted credentialは渡さず実行。Auth77/Storage68とStorage name/hash全68一致。しかしnative RR/RO catalogはsource595/candidate621 objects、dependencies795/802で不一致。共通function21の定義は完全一致、source-only Auth index4、candidate-only Iceberg2tablesと関連18columns/5constraints/5indexes、共通storage.buckets/objectsのowner grant-option表記2差が残る。Auth4indexは公式Auth v2.192.0 indexworkerの定義に一致し、Storage0038/0047/0048はstorage.multitenantでIceberg table作成を分岐する。これは初期化設定差の説明であり、本番service設定の直接観測ではない。既存SQL token比較は文字列/dollar本文を十分保持していなかったため旧14/23・8/20件は判定根拠から除外。生本文を保持するとCRLF/LF由来の関数差も含むため、最終判断はnative catalog比較を使用する。2回目は不足していたnative定義/ACL/依存関係取得のため実施。両runの候補container cleanupとCLI cleanup2actions PASS、独立CLI container/network/volume・候補container・scratch各0。次は管理サービスの正式な初期化設定（Auth indexworkerとStorage multitenant分岐）を空の隔離環境で再現する設計確認。管理tableの手動DROP、role権限拡大、本番書込み、6file本番backupは未実施。user delta未確定、Gate1 NO-GO。

- 2026-09-09 Goal 2 started / WAITING_FOR_LOCAL_CLOCK_SYNC: 最新Production6file取得＋隔離full restore/replay22→56をユーザー承認で開始。公開済source12producer照合・Goal1 baseline前確認PASS。保存先ACL初期設定は子Windows PowerShellのmodule path競合で停止し、空directoryを保護した後に再開。native captureはSNAPSHOT_METADATA_INVALIDで停止。固定RR/RO診断はPG17/read-only/isolation/sourceState22/defaultACL12/metadata形式が正常、DB T0が受信時369ms未来。別clock観測でもserver ahead516–899msを確認。時計検証を緩和せずWindows同期待ち。新規6file/取得record/process receiptは各0、残存idle transaction0。本番書込み0、復元/replay未実施、Goal2未完了、Gate1 NO-GO。
