# /tools ページ確認メモ 2026-04-26

## 結論

`/tools` ページのベース実装はできている。  
ただし、現在の状態は「一覧ページの骨組みができた段階」であり、設計で合意した `HOME = スイート一覧` との連携までは未完了。

## 良かった点

- `/tools/` 自体は安定して表示されている
- 左サイドバーの `Tools` がアクティブになっている
- ツールカード一覧は表示されている
- `Schedule Calendar` の導線 href は `/tools/schedule-calendar/` で入っている
- 見た目の方向性は HOME より一覧探索寄りになっている

## 修正優先度

### P1

1. `suite` クエリの初期適用
   - `?suite=stream-workflow` のような URL で開いた時に、対応スイートで初期絞り込みされるようにする
   - 現状は URL だけ残り、表示は全件のまま

2. スイート文脈の可視化
   - 現在どのスイートで見ているかを画面上で分かるようにする
   - 例: `配信ワークフローを表示中`

### P2

3. `/tools` ページタイトルと短説明の追加
   - 現状はフィルタ行から始まっており、一覧ページとしての文脈が弱い
   - `Tools` または `ツール一覧` の見出しと、1文説明を追加する

4. スイートフィルタUIの追加
   - 現状は `カテゴリ` と `実装状態` のみ
   - 設計では `スイート / カテゴリ / 実装状態` の3系統を想定している

### P3

5. フィルタ適用時の表示件数や空状態の見直し
   - 今後、該当ツール数の表示や空状態の補助文言を追加してもよい

6. フィルタ行の見た目調整
   - 幅の詰まり方や配置バランスは、上記のP1/P2修正後に再調整する

## 直近でやるべきこと

- `suite` クエリを読む
- suite に応じた初期絞り込みを適用する
- 現在の suite 状態を画面上に表示する
- `/tools` 見出しと短説明を追加する

## 参照

- `docs/design-tools-index.md`
- `docs/PORTAL_INFORMATION_ARCHITECTURE.md`
- `docs/mockups/tools-index-final.png`
