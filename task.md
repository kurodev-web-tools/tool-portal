# task.md

## 現在の前提

- このリポジトリは `V Streamer Tools` の初回MVPを段階実装する
- 初回MVPは `認証なしポータル + スケジュールカレンダー1本`
- 公開前提は `Cloudflare Pages`
- Phase 1 ではポータル基盤と共通UIを先に確定する
- Phase 2 でスケジュールカレンダー本体を実装する
- 初回のデータ保存は Phase 2 で `localStorage` を使う

## 今回の意思決定

- 先にポータルを作って共通部分を固める
- その後にスケジュールカレンダーの実装計画を詳細化する
- 左サイドバーは初期段階では `固定項目 + 実装済みツール`
- 実装済みツールとして表示するのは `Schedule Calendar` のみ
- 他ツールは準備中表示に留める
- 将来的に左サイドバーは `個人用ランチャー` に拡張する

## Phase 1 実装状況

- [x] Next.js + React + TypeScript + Tailwind CSS の土台を作成する
- [x] Cloudflare Pages 向けの静的出力設定を追加する
- [x] ライト / ダーク切替を含む共通テーマを作る
- [x] ポータル共通レイアウトを作る
  - Header
  - Left Sidebar
  - Main Content
- [x] ツールカードと状態表示を実装する
- [x] ポータルTOPを正式モックに寄せて組む
- [x] `/tools` をポータル一覧として用意する
- [x] `/tools/schedule-calendar` に Phase 1 用の入口ページを用意する
- [x] HOME をスイート一覧の入口ページへ改修する
- [x] HOME から個別ツールフィルタとツールカード一覧を撤去する
- [x] `/tools` を個別ツール一覧ページとして分離する
- [x] スイートカードから `/tools?suite=<key>` に遷移できるようにする
- [x] `/tools` で `suite` クエリの初期絞り込みを適用する
- [x] `/tools` にページタイトル、短説明、現在のスイート文脈を表示する
- [x] `/tools` のフィルタをスイート / カテゴリ / 実装状態の3系統に整理する

## ポータルで固めた共通部分

- 配色トークン
- タイポグラフィ
- 余白ルール
- 8px基準の角丸
- フラット + 薄い境界線の面設計
- Header
- Left Sidebar
- Suite Card
- Filter Chips
- Status Badge
- Tool Card

## 次にやること

- `/tools` 側で検索、並び替え、スイートフィルタ解除時のURL同期を検討する
- Phase 2 としてスケジュールカレンダーの初回MVPを実装する
  - [x] 週表示の情報量
  - [x] 右サイドパネルのタブ構成
  - [x] `localStorage` データモデル
  - [x] 投稿補助UI
- [x] `/tools/schedule-calendar` を作業画面として実装する
- Phase 2 実装後の確認と改善候補を整理する
  - 月表示 / 日表示の情報密度調整
  - モバイル時の右パネル配置の追加調整
  - 将来の外部カレンダー連携に向けたデータ移行方針
- 将来のログイン導入後のURL構成を整理する
  - `/`
  - `/tools/...`
  - `/app/...`

## 次スプリント（Phase 2 不足実装）

- [x] `/tools/schedule-calendar` の設定タブを実用化する
  - 初期表示ビュー（月 / 週 / 日）
  - 週開始曜日
  - 既定開始時刻 / 既定予定時間
  - データ管理（JSONエクスポート / インポート / 全データ初期化）
  - モバイル設定タブを今後の項目追加に耐えるアコーディオン構成へ整理
- [x] 投稿補助テンプレート設定を追加する
  - テンプレ本文の CRUD
  - 既定テンプレート選択
- [x] 投稿補助を設定連動化する
  - 固定テンプレ配列ではなくユーザー設定テンプレ優先で反映する
  - 初回はデフォルトテンプレを自動投入する
- [x] 予定一覧タブを強化する
  - 直近順ソート（本日以降優先）
  - フィルタ（カテゴリ / プラットフォーム / 期間）
  - 検索（タイトル / メモ部分一致）
- [x] `localStorage` を versioned payload 化する
  - `version`, `events`, `settings`, `postTemplates` の一括保存
  - `load -> normalize -> migrate(version)` 導線の整備
  - 既存データからの移行処理

## 参照ドキュメント

- `docs/design.md`
- `docs/design-portal.md`
- `docs/design-schedule-calendar.md`
- `docs/INITIAL_MVP_PLAN.md`
- `docs/INITIAL_MVP_DESIGN_AND_IMPLEMENTATION_PLAN.md`
- `docs/PORTAL_INFORMATION_ARCHITECTURE.md`

## この Phase 1 ではやらないこと

- ログイン実装
- 課金実装
- 個人設定
- Favorites / Recent / Pinned の保存
- Google Calendar連携
- スケジュールカレンダー本体実装
- 外部API連携

## Phase 2 実装状況

- [x] `/tools/schedule-calendar` の本体レイアウトを実装する
- [x] カレンダーツールバーを実装する
  - 期間表示
  - 前後移動
  - 月 / 週 / 日 切替
- [x] 週表示カレンダーを実装する
  - 時間軸
  - 日付列
  - 予定ブロック
  - 選択日 / 選択予定の反映
- [x] 月表示と日表示を初回MVPとして実装する
- [x] 右サイドパネルを実装する
  - 予定管理
  - 投稿補助
- [x] 予定管理の基本操作を実装する
  - 予定一覧
  - 新規追加
  - 編集
  - 削除
- [x] `localStorage` 保存と復元を実装する
- [x] 投稿補助タブの初回MVPを実装する
  - テンプレ選択
  - 投稿文プレビュー
  - コピー
  - X 投稿画面への遷移

## Phase 2 品質向上状況

- [x] 初期表示 / リロード時の選択日を今日に統一する
- [x] 週 / 日表示の24時間・30分グリッドを調整する
- [x] 月表示の過密セルをタイトル優先 + 残件数表示に整理する
- [x] 日表示の空状態と件数表示を週表示のトーンに寄せる
- [x] モバイル時に右パネルを下段化し、保存 / 削除 / コピー / Xで開くを到達しやすくする
- [x] 投稿文コピー失敗時の手動コピー用フォールバックを追加する
- [x] タブレット幅で左サイドバーをアイコン rail 化し、週カレンダー + 右補助パネルの横並びを維持する
- [x] タブレット幅で右パネルを圧縮し、予定管理 / 投稿補助タブと主要操作を到達可能にする
- [x] モバイル最適化本体を実施する
  - スマホ幅で左サイドバー常設を使わず、上部ヘッダー + 日付ストリップ + 日タイムライン + 下段操作パネルに再配置
  - `予定管理 / 投稿補助` タブと主要アクションを下段パネル内の到達しやすい位置へ整理
  - モバイルでも保存 / 削除 / コピー / Xで開く、コピー失敗時の手動コピー案内を維持
  - 月 / 週 / 日のモバイル表示を分け、月は俯瞰グリッド、週は週ストリップ + 日タイムライン、日は時間軸主役に整理
  - FAB と時間セルタップから文脈付き新規作成を開始できるようにする
  - モバイルの予定管理 / 投稿補助は常設せず、FAB・月の日付セル・週/日の時間セル操作時のみボトムシート表示にする
- [x] モバイル導線の追加改善を実施する
  - 共通ヘッダーにハンバーガーメニューと現在画面タイトルを追加
  - `/tools/schedule-calendar` のモバイルヘッダー表示を「スケジュールカレンダー」に変更
  - Schedule Calendar にモバイル下部タブ（カレンダー / 予定一覧 / 設定）を追加
  - 予定一覧は既存 localStorage データを使った簡易一覧として実装し、選択時に編集シートへ接続
  - カレンダー内の重複タイトルを削除し、予定一覧 / 設定ではカレンダー操作バーを非表示化
  - モバイルドロワーの背景を強め、ライト / ダーク切替をドロワー内へ移動
  - モバイルドロワーのレイヤーをヘッダーより前面に整理し、メニュー面を不透明化してカレンダー面と分離
  - モバイルのハンバーガーボタンとドロワーを右側へ寄せ、右手操作時の開閉距離を短縮
  - モバイルドロワーをヘッダー外の兄弟要素へ移し、背景面がヘッダー下で途切れる問題を修正
  - モバイルドロワー内のナビ外枠を外し、表示テーマ切替を下部へ移動
  - モバイルボトムシートの固定タブ領域を不透明ヘッダー面化し、スクロール本文が背後に見えないよう整理
- [x] `/tools/schedule-calendar` のレスポンシブ境界を整理する
  - `~1023px` はモバイル導線として扱い、768〜1023px でも FAB / ボトムシート / 下部タブを維持
  - `1024〜1279px` は既存タブレット 2 ペイン、`1280px~` は既存 PC 2 ペインとして明示
  - モバイルボトムシート最大高さをタブレット縦向けに `74vh` へ調整
- [x] `/tools/schedule-calendar` の 1024px 以上向け右パネルを 4 タブ化する
  - 予定管理 / 投稿補助 / 予定一覧 / 設定を 2 行 2 列のタブとして配置
  - 予定一覧に検索、カテゴリ / プラットフォーム / 期間フィルタ、並び順、件数付きリストを追加
  - 設定に表示設定、既定値、投稿補助設定、データ管理を追加
  - 1023px 以下のモバイル統合 UI は既存導線を維持

## Phase 2 ではまだやらないこと

- ログイン
- サーバー保存
- 複数端末同期
- Google Calendar連携
- SNS API直接投稿
- AI生成機能

## `/tools/schedule-calendar` 凍結前の完了条件

### 予定一覧タブ

- 検索対象は `title` と `memo` の部分一致とする。
- フィルタ項目は `category`、`platform`、`period` の3種類とする。
- `period` は `全期間`、`今日以降`、`7日以内`、`30日以内` を選べる。
- 並び順は `直近順`、`日付昇順`、`日付降順` を選べる。
- `直近順` は本日以降の予定を先に表示し、未来予定は昇順、過去予定は降順で表示する。
- 一覧の予定を選択すると、選択日、選択予定、編集フォームが同じ予定に切り替わる。
- 予定が0件のときは `予定はまだありません。` を表示する。
- 検索またはフィルタ結果が0件のときは `条件に一致する予定はありません。` を表示する。

### 設定タブ

- 初期表示ビューは `月`、`週`、`日` のいずれかを選べ、変更直後に現在表示へ反映する。
- 週開始曜日は `日曜` または `月曜` を選べ、変更直後に月表示と週表示へ反映する。
- 既定開始時刻と既定所要時間は、次に作成する新規予定の初期値として反映する。
- 設定、投稿補助テンプレート、予定データは変更後に `localStorage` の versioned payload として保存される。
- JSONエクスポートは `version`、`events`、`settings`、`postTemplates` を含むJSONを出力する。
- JSONインポートは旧形式の予定配列と versioned payload を受け付け、成功時のみ予定、設定、投稿補助テンプレートを置き換える。
- JSONインポートに失敗した場合は既存データを変更せず、`JSONをインポートできませんでした。形式を確認してください。既存データは変更していません。` を表示する。
- 全データ削除は予定を0件にし、設定と投稿補助テンプレートを既定値へ戻す。

## `/tools/schedule-calendar` 運用安定化状況

- [x] 空状態とエラー文言を統一する
  - 予定0件
  - 検索0件
  - 保存失敗
  - インポート失敗
  - コピー失敗時の手動コピー案内
- [x] 壊れた localStorage JSON 読み込み時に、初回 hydration 直後の自動保存で既存値を上書きしない
- [x] インポート失敗時に既存データを変更しないガードを明示する
- [x] 回帰確認チェックリストを `docs/SCHEDULE_CALENDAR_STABILITY_CHECK_2026-04-28.md` に作成する
- [x] 幅別回帰確認を実施して結果を記録する
  - 起動済みの `http://localhost:3001/tools/schedule-calendar/` で 390 / 820 / 1024 / 1180 / 1366 を確認済み
  - `~1023px` はモバイル統合UI、`1024~1279px` はタブレット2ペイン、`1280px~` はPC表示として確認済み
- [x] localStorage 移行確認を実施して結果を記録する
  - 壊れたJSON、Import失敗、Exportの保護条件はブラウザ実行とコードパスで確認済み
  - 旧形式データから versioned payload への移行をブラウザ上の実データで確認済み
- [x] PR前レビューで見つかった軽微な回帰を修正する
  - 既定開始時刻と既定所要時間の組み合わせで終了時刻が `24:00` になるケースを `23:59` に丸め、時刻入力が空表示になる問題を修正
- [x] PR前の最小リファクタ確認を実施する
  - 重複していた予定一覧の空状態判定、既定所要時間 options、既定設定からの新規 draft 生成、保存 payload 生成を小さなヘルパーへ整理
  - UI仕様、保存仕様、localStorage の既存データ保護ロジックは変更なし
- [x] 削除後の undo toast を追加する
  - 削除前確認モーダルは入れず、全表示で `予定を削除しました。` + `元に戻す` に統一
  - undo 対象は直近1件のみ、8秒で自動消滅し、localStorage に undo 専用履歴は保存しない

## 次フェーズ候補

- 候補A: ポータルのタブレット / モバイル最適化
  - `/tools/schedule-calendar` は凍結前提にし、共通ポータル側のレスポンシブ、一覧性、導線を整える。
- 候補B: ログイン導入前の情報設計整理
  - 認証追加前に `/`、`/tools/...`、将来の `/app/...`、保存データの責務を整理する。

## ポータル レスポンシブ最適化状況

- [x] HOME / TOOLS のタブレット・モバイル最適化を実施する
  - `~767px` はハンバーガー + ドロワー、1カラム基調、HOMEサマリーは縦型に再配置
  - `768~1023px` はモバイル同等ナビを維持し、TOOLSフィルタは横スクロールチップを維持
  - `1024~1279px` は左サイドを簡略 rail 表示、HOME のスイートカードは2カラム基調
  - `1280px~` は現行PC表示を維持
- [ ] 幅別回帰確認を実施する
  - 対象幅: 390 / 820 / 1024 / 1280 / 1366
  - 対象ページ: `/` / `/tools`
  - 観点: HOME/TOOLS間遷移、Schedule Calendar導線、テーマ切替、フィルタ操作性、カード可読性
  - Codex環境では Node 起動時の `ncrypto::CSPRNG` エラーにより lint / dev server / ブラウザ幅確認は未実施
- [x] HOME スイートカードのレビュー指摘を修正する
  - 1024px付近と1280〜1320px付近でカード下部ボタンがはみ出ないよう、カード内の固定右余白と下部行の横並び強制を解除
  - 390pxモバイルでカード右側に不自然な空白カラムが残らないよう、スマホ時のアイコンとボタン幅を調整
  - ブラウザ計測で 390 / 1024 / 1280 / 1320 / 1366 の HOME スイートカード内リンクがカード幅内に収まることを確認
- [x] HOME スイートカードの余白と状態表示重複を追加整理する
  - ステータスは右上バッジに集約し、下部の `● 利用可能 / 準備中` 表示を削除
  - 説明文、タグ、ツール数行はカード幅を自然に使えるよう、本文側の固定右余白を解除
  - 下部の `ツール数` とボタンはスマホのみ縦積み、`sm` 以上は横並びに戻して一覧性を維持
- [x] PR前レビューで見つかった軽微なナビゲーション整合を修正する
  - モバイルドロワーで `/tools/schedule-calendar` 表示時に `Tools` と `Schedule Calendar` が同時 active になる状態を解消
  - 左サイドバー rail でも `/tools/schedule-calendar` 表示時に `Tools` と `SC` が同時 active になる状態を解消
  - `/tools` のスイートフィルタ変更時に `suite` query を同期し、`すべて` 選択時は `suite` query を削除する
