# ポータル確認レビュー 2026-04-26

## 結論

- `HOME = スイート入口`、`/tools = ツール一覧` の責務分離は成立している。
- `HOME -> /tools?suite=...` の導線と、`/tools` 側の初期絞り込み表示は確認できた。
- 現時点でスケジュールカレンダー実装へ進んで問題ない。

## 確認内容

- `http://localhost:3001/`
  - スイート一覧中心の HOME になっていること
  - 左サイドバーの固定ナビ、実装済みツール、将来枠の構成
  - HOME から `/tools` へ進む導線があること
- `http://localhost:3001/tools/?suite=stream-workflow`
  - `/tools` にページタイトルと説明があること
  - `suite` クエリに応じて「配信ワークフローを表示中」が出ること
  - `スイート / カテゴリ / 実装状態` の3系統フィルタがあること
  - 実装済み `Schedule Calendar` への導線があること

## 現状評価

### 問題なし

- HOME の役割が明確になり、以前の「HOME と /tools の重複」は解消されている。
- `/tools` は探索ページとして必要な情報を持っている。
- スイートカードからの遷移先が URL と表示文脈の両方でつながっている。
- 左サイドバーの情報設計は、今後の `Favorites / Recent / Pinned` 拡張にもそのまま乗せやすい。

### 軽微な調整候補

- HOME のスイートカード CTA 文言は、`開く` と `詳細` の意図を今後もう一段そろえてもよい。
- `/tools` のカテゴリフィルタは、画面幅によっては折り返しや密度をもう少し整えられる余地がある。
- HOME 右側のサマリー領域は、将来スケジュールカレンダー実装後に実データ連携前提で再調整してもよい。

## 次に進んでよいか

- はい。ポータル Phase 1 としては十分まとまっている。
- 以降は、ポータルを大きく作り直すより、`Schedule Calendar` 実装を進めたうえで必要な共通UIだけ戻って磨く方が効率がよい。

## 次段階の推奨

1. `Schedule Calendar` の情報設計と実装範囲を `task.md` ベースで再確認
2. `/tools/schedule-calendar` のレイアウト実装
3. `localStorage` 前提の予定追加・編集・削除
4. 投稿補助タブと `X` 遷移・コピー導線の追加
